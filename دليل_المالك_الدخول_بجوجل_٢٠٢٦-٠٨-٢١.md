# 🔑 دليل المالك — الدخول بحساب Google لكل أنواع الحسابات

**التاريخ: ٢١ أغسطس ٢٠٢٦** · *(العمل اكتمل ورُفع إلى `main`)*

مرحباً د. محمد،

نُفِّذ قرارك **س١٠ — الدخول بجوجل**. وأصبح حساب Google يعمل تماماً كحساب البريد الإلكتروني **لأنواع الحسابات التسعة**: فرد، محامٍ، شركة محاماة، شركة، منشأة صغيرة، جهة حكومية، جمعية، ومقدّم خدمة بأنواعه الثلاثة.

| الفحص | النتيجة |
|---|---|
| فحص الأنواع (TypeScript) | نظيف |
| الاختبارات الآلية | **٢١٥ ناجح · ٠ فاشل** (كانت ١٤٥) |
| البناء للإنتاج | نجح |
| عدد الحِزَم | ١٢ |

> ⚠️ **لكن الزر لا يعمل بعد، ولن يعمل حتى تنفّذ أنت إعداد Google.** هذا ليس عيباً في الكود: تسجيل الدخول بجوجل يحتاج حساباً لدى Google وربطاً في Supabase، وكلاهما بيدك أنت لا بيد المبرمج — تماماً مثل `N8N_WEBHOOK_SECRET` في الجولة الماضية. الخطوات في القسم ٤، والتفصيل الكامل في ملف `docs/GOOGLE_OAUTH_SETUP.md`.

---

## ١. أهم ما في هذه الجولة: الخطة الأولى كانت ستُنتج نتيجة خاطئة

كانت هناك خطة جاهزة للدخول بجوجل مكتوبة من قبل. **رفضتُها وأعدت كتابتها**، والسبب يستحق أن تعرفه:

تلك الخطة كانت ستجعل **كل من يدخل بجوجل «عميلاً فرداً»** — بغضّ النظر عن كونه محامياً أو شركة. ولم يكن ذلك خطأً عابراً فيها: كان مكتوباً في دليل التحقق الخاص بها **بوصفه النتيجة المتوقّعة الصحيحة**.

وطلبك كان واضحاً: أن يعمل جوجل «حتى لو محامي أو عميل أو شركة وهكذا». فلو نُفِّذت تلك الخطة لكان محامٍ يسجّل بجوجل يجد نفسه عميلاً فرداً، بلا لوحة محاماة، بلا طريق للرجوع.

**السبب التقني في جملة واحدة:** ثمانية مواضع في النظام كانت تقرأ نوع الحساب من مكان **لا يكتبه جوجل إطلاقاً** (`user_metadata`). فكان النظام يسأل سؤالاً لا جواب له، ويفترض «فرد» عند الصمت.

✅ **الحل:** صار **جدول `profiles` في قاعدة البيانات هو المرجع الوحيد** لنوع الحساب — وهو نفس المكان الذي كانت صلاحيات الخادم تقرأ منه أصلاً. أي أن المتصفح والخادم صارا يقولان الشيء نفسه عن هوية المستخدم.

### وثلاثة عيوب أخرى كانت ستظهر بعد النشر

| العيب | ما كان سيحدث |
|---|---|
| **المتصفح كان يخالف الخادم** | الخادم يقول «محامٍ» والواجهة تقول «عميل» — قائمة جانبية خاطئة، ولوحة المحاماة تمنعه من الدخول إلى لوحته هو |
| **فحص الصلاحيات كان يُتخطّى بالكامل** | أي حساب بلا نوع مسجّل — وهذا حال كل حساب جوجل — كان يفتح صفحات **أي لوحة**: المحامي، المكتب، أي قطاع |
| **قائمة اختيار نوع الحساب كانت تحمل قيمة ترفضها قاعدة البيانات** | من يختار «شركة / مؤسسة» كان حفظه سيُرفض من قاعدة البيانات فوراً |

---

## ٢. ⚠️ اكتشاف مهم: تسجيل مقدّمي الخدمات كان معطّلاً بالكامل منذ يونيو

هذا لم يكن من طلبك ولا علاقة له بجوجل، لكنه ظهر أثناء العمل ويجب أن تعرفه.

**الموثّق والمعقّب والمحكّم لم يكن بإمكانهم إنشاء حساب إطلاقاً** — لا بجوجل ولا بالبريد. كل محاولة تسجيل كانت تفشل داخل قاعدة البيانات.

**السبب، بالتفصيل:** جدول `provider_profiles` يشترط حقل التخصص (`sub_role`) إلزامياً بلا قيمة افتراضية. والدالة التي تُنشئ الحساب عند التسجيل كانت **لا ترسل ذلك الحقل**، فتفشل العملية وتُلغى معها عملية إنشاء الحساب كلها.

وهذا **تراجع** حدث في تحديث ٢٠٢٦-٠٦-١٦: التحديث الذي سبقه كان يضع قيمة افتراضية لهذا الحقل تحديداً لهذا السبب، فحُذفت.

**والدليل من قاعدة بياناتك:** من أصل ١٦ حساباً مسجّلاً، عدد حسابات مقدّمي الخدمات **صفر**.

✅ **أُصلح، وقد نفّذت أنت أمر SQL بالفعل** — فالمسار بالبريد يعمل الآن. والمسار عبر جوجل لم يكن محتاجاً لذلك الأمر أصلاً، لأنه يمرّ بطريق مختلف داخل التطبيق لا بقاعدة البيانات.

---

## ٣. ما يستطيع كل نوع فعله الآن

| نوع الحساب | بجوجل | بالبريد | ملاحظة |
|---|---|---|---|
| فرد | ✅ | ✅ | |
| محامٍ | ✅ | ✅ | يدخل لوحته فوراً؛ **الاعتماد يبقى بيدك** |
| شركة محاماة | ✅ | ✅ | كذلك |
| شركة / مؤسسة | ✅ | ✅ | |
| منشأة صغيرة | ✅ | ✅ | |
| جهة حكومية | ✅ | ✅ | |
| جمعية / منظمة | ✅ | ✅ | |
| **موثّق · معقّب · محكّم** | ✅ | ✅ | كانا معطّلين تماماً قبل هذه الجولة |
| مسؤول (admin) | ❌ | ❌ | **عمداً**، ولا يجوز تغييره |

### ثلاث ملاحظات على الجدول

**١) المسؤول لا يمكن اختياره — وهذا مقصود.** لا يظهر في أي قائمة، ويرفضه الكود صراحةً، وثلاثة اختبارات آلية تثبّت ذلك. الطريق الوحيد إليه هو قاعدة البيانات مباشرة.

**٢) اختيار نوع الحساب يحدث مرة واحدة.** بعدها لا يستطيع المستخدم تغييره بنفسه — تمنعه قاعدة البيانات. وقد كانت الرسالة تقول له «تواصل مع الدعم»، **والدعم هو أنت، ولم تكن تملك شاشة تفعل ذلك**. ✅ صار لديك الآن زر «تغيير نوع الحساب» في لوحة الإدارة ← المستخدمون ← الحساب.

> ⚠️ **لكن انتبه:** تغيير النوع لا يُنشئ ملف النوع الجديد تلقائياً. الشاشة تطبع لك الجدول والحقول المطلوبة بالضبط، وتقول لك: **أنشئ الملف الجديد أولاً، ثم احذف القديم** — لأن الحذف أولاً يتلف بيانات الترخيص وحالة التوثيق قبل أن يوجد بديل.

**٣) المحامي بجوجل يصل إلى لوحته فوراً، لكن اعتماده يبقى بيدك.** حالة التوثيق تبدأ «قيد المراجعة» تماماً كالمحامي المسجَّل بالبريد. لا امتياز إضافي في مسار جوجل.

---

## ٤. ✅ ما عليك أنت — بالترتيب

### الجزء الأول: حساب Google Cloud

**١)** ادخل <https://console.cloud.google.com> وأنشئ مشروعاً جديداً باسم `Nzamy`.

**٢)** من قائمة **☰** اختر **Google Auth platform**.

> ⚠️ **غيّرت Google ترتيب هذه الشاشات مؤخراً.** ما كان اسمه «OAuth consent screen» صار أربع صفحات منفصلة، **بلا معالج متسلسل وبلا أزرار «Save and Continue»**. كل صفحة تُفتح وتُحفظ وحدها. الدليل المفصّل مكتوب على أساس الشكل الجديد.

| الصفحة | ما تضعه فيها |
|---|---|
| **Branding** | اسم التطبيق `نظامي` · الشعار · بريد الدعم |
| **Audience** | النوع **External**، ثم اضغط **PUBLISH APP** |
| **Data Access** | Add or Remove Scopes ← اختر `email` و`profile` و`openid` فقط |
| **Clients** | Create client ← النوع **Web application** |

> ⚠️ **إن لم تضغط PUBLISH APP** فلن يستطيع الدخول إلا حسابات تضيفها يدوياً كـ Test users.

### الجزء الثاني: 🔴 الفخ الأكبر — عنوان إعادة التوجيه

داخل صفحة **Clients**، حقل **Authorized redirect URI** يأخذ **عنوان Supabase لا عنوان موقعك**:

```
https://gdqfqfcxnwrwgaphtfhu.supabase.co/auth/v1/callback
```

**لو وضعت `nezamy.sa` هنا فستظهر رسالة `redirect_uri_mismatch` ولن يعمل شيء.** هذا أكثر خطأ يقع فيه الناس.

**٣)** احفظ **Client ID** و**Client Secret** الظاهرين بعد الإنشاء.

### الجزء الثالث: Supabase

**٤)** **Authentication → Providers → Google** ← فعّله ← ألصق الـ Client ID والـ Secret ← Save.

**٥)** **Authentication → URL Configuration**:
- Site URL: `https://nezamy.sa`
- Redirect URLs — أضف السطرين معاً:
  ```
  https://nezamy.sa/auth/callback
  http://localhost:3000/auth/callback
  ```

### الجزء الرابع: النشر

```bash
cd /www/wwwroot/nzamy/latest-nzamy-full && git pull origin main && NODE_OPTIONS="--max-old-space-size=4096" npm run build && pm2 reload nzamy
```

```bash
rm -rf /www/server/nginx/proxy_cache_dir/*
```

> ✅ **أمر SQL نفّذته بالفعل** — فلا يوجد أي أمر قاعدة بيانات إضافي في هذه الجولة.

### الجزء الخامس: التجربة

**٦)** نافذة تصفح خاص ← بريد Google **لم تستخدمه على الموقع من قبل** ← اضغط «تسجيل الدخول بـ Google».

**المتوقع:** شاشة موافقة Google ← ثم صفحة **تهيئة الحساب** ← تختار نوع الحساب وتُدخل رقم الجوال ← ثم تصل إلى لوحة النوع الذي اخترته.

**٧)** للتأكد من قاعدة البيانات:

```sql
select email, user_type, phone, onboarding_completed
from public.profiles
order by created_at desc
limit 3;
```

المتوقع: صف جديد بالنوع الذي اخترته على الشاشة، ورقم جوال بصيغة `+9665XXXXXXXX`.

---

## ٥. تنبيه واحد قبل النشر — يخصّ حساباتك الحالية

رقم الجوال صار **إلزامياً**، لأنه القناة الوحيدة لإشعارات الواتساب.

وقياساً على قاعدة بياناتك اليوم: **جميع الحسابات الـ١٦ بلا رقم جوال**. وعند النشر:

| الحسابات | ما سيحدث |
|---|---|
| **٩ حسابات أفراد** | ستُحوَّل إلى شاشة التهيئة **مرة واحدة** لتُدخل رقم الجوال، ثم تكمل طبيعياً |
| **٦ حسابات محامين** | لا يتأثرون — مستثنون |
| **حساب المسؤول (حسابك)** | لا يتأثر — مستثنى عمداً، لأن شاشة التهيئة لا تحتوي خياراً للمسؤول أصلاً، وإكمالها كان سيغيّر نوع حسابك أنت |

**هذا سلوك مقصود لا عطل.** لكن أخبر من يستخدم النظام حتى لا يظنّها مشكلة.

---

## ٦. سؤال مفتوح — جوابه ليس عندنا، ويحتاج فحصك

أثناء كتابة الدليل كنا سنكتب لك: «المحامي يظهر في دليل المحامين العام بعد اعتماده». **فحصنا ذلك على قاعدة بياناتك الحيّة قبل كتابته — ولم نستطع إثباته.**

قراءة الجداول بصلاحية زائر غير مسجّل تُرجع **صفر صفوف** من `profiles` ومن `lawyer_profiles` معاً، ولا توجد في أي تحديث صلاحية قراءة عامة لجدول `profiles`.

**ما يعنيه هذا:** قد لا يكون دليل المحامين العام يعرض شيئاً لأي زائر — **لسبب لا علاقة له بالاعتماد ولا بجوجل**، وهو أقدم من هذه الجولة كلها.

**لذلك:** الدليل لا يَعِد بظهور المحامي للعامة. ما هو مؤكد: **الاعتماد مطلوب وهو بيدك أنت.** أمّا الظهور بعد الاعتماد فيحتاج فحصاً على النظام الحيّ — والاستعلام موجود في `docs/GOOGLE_OAUTH_SETUP.md` القسم الخاص به.

**لم نصلحه لأنه خارج نطاق هذه الجولة تماماً**، وإصلاح شيء في الصلاحيات دون فهم سبب وضعه هكذا قد يفتح ما هو أخطر. يستحق مهمة مستقلة متى أردت.

---

## ٧. الملف المرجعي

**`docs/GOOGLE_OAUTH_SETUP.md`** — الدليل الكامل بالعربية: كل شاشة باسمها، وجدول الأخطاء الشائعة وأسبابها، واستعلامات التحقق، **وقائمة من سبعة عشر بنداً مرتّبة** بكل ما عليك أنت.

اقرأ هذا الملف مرة، ثم اعمل من تلك القائمة.

---

**الخلاصة:** الكود جاهز ومُختبَر ومرفوع. أنواع الحسابات التسعة كلها تعمل بجوجل. ويبقى عليك إعداد Google Cloud وSupabase — نحو عشرين دقيقة — ثم النشر. ولا يوجد أي أمر قاعدة بيانات متبقٍّ.
