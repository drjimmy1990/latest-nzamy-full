# خطة معالجة ملاحظات المالك — مجلد `lasttest`

**التاريخ:** ٢ سبتمبر ٢٠٢٦ · **المصدر:** ثلاثة ملفات و٣٤ لقطة أرسلها د. محمد
**طريقة الفحص:** ٣٨ وكيلاً على فرع `owner-edits` — الفرع الذي فُحص فعلاً — ثم جولة نقض معاكسة على كل حكم «مغلق»
**ملحق الأحكام الكامل:** [`docs/audits/2026-09-02-owner-lasttest-verdicts.md`](docs/audits/2026-09-02-owner-lasttest-verdicts.md)

---

## ١ · قبل أي شيء: التقرير فحص فرعاً غير الفرع الذي نعمل عليه

هذه ليست تفصيلة إدارية، هي الشيء الذي كان سيُفسد المعالجة كلها لو لم نمسكه.

```
نسخة المالك        ٥١ ترحيلاً
main               ٤١ ترحيلاً
owner-edits        ٥١ ترحيلاً   ← merge-base(main, owner-edits) = رأس main بالضبط
                                   git log owner-edits..main = فارغ
```

**`owner-edits` يحتوي `main` كاملاً وزيادة ٧٦ ملفاً و+١١٬٢١٩ سطراً** — الأكاديمية كلها، و`src/lib/rateLimit.ts`،
وإعادة بناء `laws/[slug]`. كل `ملف:سطر` في تقريره يُحَل هناك.

جرّبنا أول ملاحظة في تقريره على `main` فرجعت فارغة: `20260820_academy_schema.sql` **غير موجود على `main` أصلاً**.
لو شغّلنا التحقق على نسخة العمل عندنا لخرجنا بحكم «الملف غير موجود ← الملاحظة باطلة» على **صنف كامل من
الملاحظات الصحيحة**. فَتحنا نسخة عمل منفصلة على `owner-edits` وقرأ كل وكيل منها.

> وفائدة جانبية مؤكِّدة: البند ١٠١ يستشهد بـ`SharedSidebar.tsx:189-196`، والعيب على `owner-edits` في ١٨٩ و١٩٤ بالضبط،
> وعلى `main` في ١٨٦ و١٩١. تطابق رقم السطر يُثبت أي نسخة كانت أمامه.

---

## ٢ · النتيجة على البنود الـ١٩٥

| الحكم | العدد |
|---|:--:|
| 🔴 مفتوح | **١٢٤** |
| 🟡 منفَّذ جزئياً | **٤٩** |
| 🟠 مغلق في الشاشة المفحوصة فقط — وحيٌّ في غيرها | **١٠** |
| ✅ مغلق كلياً | **٣** |
| ⚪ مؤجَّل بقرارك | ٧ |
| 🔵 قرار عمل لا واقعة برمجية | ١ |
| ⚠️ ادعاء التقرير غير صحيح | ١ |

**ثلاثة بنود فقط من ١٩٥ نجت من جولة النقض بحكم «مغلق كلياً».** وهذا مقصود: وجّهنا وكلاء النقض إلى
مهاجمة الأحكام **المغلقة** لا المفتوحة، لأن «مغلق» كاذبة تشحن عيباً حياً بصمت، بينما «مفتوح» كاذبة
تكلّف تحقيقاً ضائعاً فقط.

وبتقاطع البنود مع ما نتتبعه أصلاً: **١١٥ بنداً مسجّل عندنا من قبل، و٨٠ بنداً جديد فعلاً.**
فالسجل ليس قائمة رابعة مكرّرة — هو ٥٩٪ إعادة صياغة و٤١٪ عمل جديد، والجديد مركَّز في
معمار البيانات والامتثال وخط منتج «درع نظامي» الذي لا يذكره أي ملف عندنا.

**٩٩ بنداً يحتاج ترحيل قاعدة بيانات · ٢٩ بنداً يحتاج قرارك أولاً.**

---

## ٣ · النمط الذي تكرّر ١٠ مرات: إصلاح الشاشة ≠ إغلاق الملاحظة

هذا هو الدرس نفسه الذي كتبناه في تدقيق العميل والشركة، وقد تكرر هنا بنفس الشكل:

**«نسبة الفوز ٧٤٪»** — حُذفت من ملف المحامي المهني كما طلبت. وهي حيّة الآن في:

```
src/app/dashboard/firm/team/[id]/page.tsx:153  و :230
src/app/dashboard/firm/team/workload/page.tsx:219      ← ٨٥ / ٧٨ / ٩٠ ثابتة
src/app/dashboard/firm/analytics/page.tsx:14           ← «معدل الفوز ٧٢٪» حرفياً
src/app/dashboard/lawyer/_data/analytics.ts:50         ← export const WIN_RATE_PCT = 74 (ميت، بلا مستورد)
```

ونصف القرار الآخر — توليد رابط مشاركة حقيقي — **لم يُبنَ على أي شاشة**: النافذة حُذفت بدل أن تُصلَّح،
فلا يوجد زر مشاركة على ملف المحامي إطلاقاً.

ونسبة فوز مُختلَقة ليست تجميلاً: **القاعدة ٣٨ من قواعد السلوك المهني (قرار وزير العدل ٦٧٦ — ١٩/٤/١٤٤٦هـ)**
تمنع نِسَب الفوز غير الموثقة في إعلان المحامي.

ومثل ثانٍ: **«الجراف الوهمي — عقد ١٢٣»** حُذف من شاشة المحامي، ونسخة ثانية مستقلة منه حيّة على
`src/app/dashboard/firm/cases/[id]/LegalCanvas.tsx:12` — «عقد المقاولة الأساسي»، «مطالبة مالية (١٫٢ مليون)»،
«سلمان العتيبي (محامي)» — ومركَّبة فعلاً في `page.tsx:257`.

---

## ٤ · بند واحد ادعاؤه غير صحيح — والإصلاح المقترح له ما كان سيعالج اللقطة

**البند ٥٩ · وميض انهيار التنسيق وانهيار النافبار (`17_fouc...png`)**

يقول الباب إن `skip-to-content` «غير معرّف نهائياً في `globals.css`». **هو معرَّف**، في
`globals.css:228-244`، قاعدة مكتوبة بخط اليد (فلا يمسّها تنقيح Tailwind)، وموجودة منذ أول commit
(`5a6a724`)، وعلى الفرعين معاً.

والأهم من تصحيح المعلومة: **الإصلاح المقترح — إخفاء الرابط بـ`sr-only` — ما كان سيعالج اللقطة.**
اللقطة تُظهر نافبار بلا تنسيق **و**رابطاً أزرق خاماً في وقت واحد، أي أن ملف الأنماط **لم يُحمَّل أصلاً**.
و`sr-only` يعيش داخل نفس الملف الذي فشل — فيسقط معه.

**السبب الحقيقي تسليم الـCSS**: `ChunkLoadError` أو كاش Nginx قديم. وهذا **إجراء تشغيلي، لا تعديل كود**،
وهو ثالث ما يوصي به الباب نفسه. يبقى صحيحاً وقائماً أن `layout.tsx:126` لا يستخدم الـutilities المطلوبة،
وأن شقّي القرار لم يُنفَّذا — لكنهما لا يعالجان ما في الصورة.

---

## ٥ · المرحلة صفر — النشر. وهي التي تفسّر أكثر من ثلث الملاحظات

**١٢ لقطة من الـ٣٤ يظهر في شريط عنوانها `nezamy.sa`** — أي أنك اختبرت **الموقع المنشور**، والموقع المنشور
متأخر عن الكود بأربعة إلى تسعة commits. ومن البنود الـ١٩٥ هناك **١٠ بنود مُصلَحة في الكود وتنتظر النشر فقط**
(١٢، ١٣، ١٤، ١٥، ١٦، ٤٥، ١٦٣، ١٦٤، ١٦٧، ١٦٨)، و**٣٩ بنداً** مسجّلة عندنا مغلقة من جولات سابقة.

فالردّ الصحيح على هذا الجزء **نَشر ثم إعادة اختبار** — لا إعادة بناء، ولا «التقرير مخطئ».

```bash
cd /path/to/nzamy-website && git pull && npm run build && pm2 reload nzamy
```

```bash
rm -rf /www/server/nginx/proxy_cache_dir/*
```

ثم ترحيل `20260827_signup_contact_fields.sql` في محرر SQL. **ولا يُطبَّق بعده أي ملف يعيد تعريف
`handle_new_user()`** — و`20260826_corporate_identity_persisted.sql` يعيد تعريفها، فترتيبها قبله لا بعده.

> **خبر مطمئن حسمناه:** لا واحد من ترحيلاتك العشرة يعرّف `handle_new_user()`، ولا يحوي أياً منها
> `create function` أو `security definer` أو `public.profiles` أو أي trigger على `auth.users`.
> فترتيبها لا يمسّ التقاط الجوال والمدينة عند التسجيل مهما كان.

---

## ٦ · الموجة الأولى بعد النشر: ٢٤ بنداً رخيصاً وخطره نظامي أو «وعد كاذب»

كلها بلا ترحيل، وحجمها «بسيط» أو «صغير»، وكل واحد منها يزيل ادعاءً غير صحيح أمام المستخدم.

| # | البند | لماذا الآن |
|:--:|---|---|
| ١٧١ | **ادعاء AES-256** في الخصوصية والأمان والأسئلة الشائعة | لا سطر تشفير تطبيقي واحد في الكود. والمسح وجد أوسع مما ذكر التقرير: ادعاء تشفير طرف‑لطرف في `security:37`، و«256-bit SSL» في `Hero.tsx:252` و`FAQ.tsx:23,50` — وهذه صياغة تُوهم بتشفير **المخزَّن** بينما SSL نقلٌ فقط — و`ai/corp/privacy:15` يعلّم التشفير `done:true`، و`SignatureTab.tsx:29` يعد بحفظ الختم «مشفراً» في تبويب لا يحفظ شيئاً |
| ٢١ | استبدال «بشرياً» بـ«بواسطة محامٍ مرخص ومتمرس» | العبارة المطلوبة **صفر ظهور** في المستودع — الاستبدال لم يحدث. والممنوع حيّ في `clientServiceCatalog.ts:108` ويُعرض عنواناً فرعياً في كتالوج العميل |
| ٤ | حذف «نسبة الفوز» من الشاشات الأربع الناجية | القاعدة ٣٨ |
| ١٣٤ + ١٣٥ | حذف صفحة العروض الترويجية والبند من السايدبار | القاعدة ٣٨؛ الصفحة قالب «قريباً» من ١٢ سطراً وشارتها «جديد» في `navigation.sidebars.legal.ts:121` |
| ١٦٦ | لا وقائع نزاع في رابط `wa.me` + إخلاء مسؤولية الوكالة | تسريب بيانات في رابط، ولم يسجّله أحد قبلاً |
| ٨٤ | حذف `MOCK_RESULT` من المجيب السريع | `quick-answer/page.tsx:129` يعرض نتيجة مختلَقة كأنها جواب |
| ١٨٧ | حذف `NafathTab.tsx` و`canUseNafath` و`seatPolicy` الثابت | التبويب ما زال يحاكي الربط بـ`setCode("74")` ثم ٤ ثوانٍ ثم «نفاذ مربوط بنجاح»؛ و`seatPolicy` يعرض «٢ من ٣ مقاعد» مختلَقة |
| ١٠٦ | حذف شريط المستجدات التشريعية الثابت | يعرض «مستجدات» ليست مستجدات |
| ٥١ | `/media` — شارة «قريباً» أو ربط CMS | مصفوفات ريلز وبودكاست مختلَقة |
| ٧٧ | حذف «٩٨٪» و«٥٠٠ شركة» من كرت قطاع الأعمال | أرقام لا سند لها |
| ١٠١ + ١٠٢ | الشعار الرسمي بدل حرف «ن» | `public/logo.png` موجود على الفرعين — لا عائق أصلاً، فقط `<Image>` بدل `<div>` |
| ١١٨ + ١١٩ + ١٢٦ + ١٢٧ + ٩٩ + ١٠٠ + ١٤٦ + ١٤٨ + ٦ + ٥٢ | حذف الأرشيف والشبكة و`/ai/orders` وتنظيف السايدبار | كلها روابط تقود إلى قوالب «قريباً» |

**التحوّل الواحد المطلوب في التنفيذ:** لا يُغلق أي بند بإصلاح شاشة واحدة. كل بند يُبحث عن **شكل العيب**
— النص العربي، الرقم المختلَق، الخاصية `disabled`، المصفوفة الوهمية — عبر كل الواجهات التسع
(`client · lawyer · firm · business · micro · provider · government · ngo · admin`) والصفحات العامة.

---

## ٧ · ما يجب أن يسبق كل بناء: العطل الجذري الذي يحجب ٤ بنود أخرى

**البند ٧٩ · «تعذّر حفظ الموكل».** السبب الذي أثبته تقريرك قائم حرفاً بحرف:

```
route.ts:527   requester_user_id: null          ← ما يُدرَج
20260518:152   with check (requester_user_id = auth.uid())   ← السياسة الوحيدة للإدراج
route.ts:555   → 42501 → 500 «تعذّر حفظ الموكّل»
```

فتّشنا الـ٥١ ترحيلاً كلها: **لا سياسة INSERT أخرى على `service_requests`**، و`assertRole` تمنع صراحةً
الالتفاف بـ`service_role`. وجدول `lawyer_clients` **غير موجود إطلاقاً** — لا في ترحيل ولا في كود.

**والبند ١٨١ أكبر منه وهو أصل نصف اللوحة.** الجداول `hearings` و`tasks` و`task_steps` و`case_stages`
و`activity_events` و`time_entries` — **لا وجود لأيٍّ منها** في الـ٥١ ترحيلاً. لوحة المحامي كلها تعيش
في `service_requests.metadata` بأعلام JSON، و`AddHearingModal.tsx:117` ما زال **يكتب** بالشكل الذي
أمر الباب ٣٥ بهجره — فالبيانات الجديدة تصل بنفس الشكل المعطوب.

> وفي `lawyer/tasks/route.ts:53` تعليق مكتوب بخطنا يقول إن سقف الـ`.limit(50)` الصامت **«ما زال غير مُصلَح»**.
> محامٍ عنده ٦٠ بنداً يرى لوحة تبدو كاملة. هكذا تُفوَّت جلسة.

**١٨١ يحجب البنود ٦٦ و١٠٧ و١٨٠ وأكثر الكتلة المالية.** بناء أي منها قبله بناء فوق JSON حر.

---

## ٨ · ثغرة الأكاديمية: صحيحة كما كتبتها، لكن الاستغلال غير مُثبَت — واقرأ هذا قبل كتابة أي ترحيل

`20260820_academy_schema.sql:105` صحيح تماماً كما رصدته: `for all using (true)` بلا `to service_role`.
وسياسة بلا `TO` تعني `TO PUBLIC` — أي `anon` و`authenticated`. و`FOR ALL` بـ`USING` بلا `WITH CHECK`
تعيد استخدام نفس الشرط للكتابة. والسطر ١٠٤ يكشف كل صف رغم أن التعليق فوقه يدّعي التقييد بـ`verify_id`.

**لكن مسار الاستغلال غير قائم من المستودع:** الجدول في schema اسمه `academy` لا `public`، و PostgREST
لا يخدم إلا الـschemas المعلنة في إعدادات المشروع. ولا يوجد `supabase/config.toml` في الشجرة، فالتعرّض
لا يُقرأ من الكود.

**والأرجح أن الترحيل لم يُشغَّل أصلاً:** صفر استعلام runtime على جداول الأكاديمية في كل `src`؛ ومسارات
`api/v1/academy/*` الأربعة تقرأ JSON ثابتاً و`fs` فقط ولن تُخطئ ٥٠٠ بدون الجداول؛ وصفحة الشهادات
مصفوفة ثابتة في `certificates/page.tsx:11`؛ والبذّارة `academy-seed.mjs` تدّعي في توثيقها أنها تكتب في
Supabase وهي تكتب ملفَّي JSON فقط؛ و`IMPLEMENTATION_STATUS.md:30` و`REMAINING_WORK.md:250` يسجّلان
الأكاديمية **غير مبنية**.

**الحسم استعلامان للقراءة فقط على الإنتاج — لا ترحيل:**

```sql
select to_regclass('academy.certificates');
select polname, polcmd, polroles::regrole[] from pg_policy
where polrelid = to_regclass('academy.certificates');
```

**وإن ظهر الجدول موجوداً، الترحيل التصحيحي يحتاج خمسة احترازات** وإلا فشل أو ترك الباب مفتوحاً:
يُلَف كامل الجسم في `do $$ ... if to_regclass('academy.certificates') is not null then ... end if; end $$;`
(لأن `to_regclass` تُرجع NULL ولا ترفع خطأ)؛ و`drop policy if exists` دائماً؛ و`with check` يُكتب صراحةً
لا اتكالاً على `using`؛ و`revoke all ... from anon, authenticated` لأن RLS تأتي بعد GRANT؛ والتحقق العام
لا يصلح بسياسة صف — الشكل الصحيح `for select to service_role` + دالة `security definer` بـ`search_path` مثبَّت.

> **وثغرة ثانية في نفس الملف لم يرصدها التقرير:** `public.academy_questions` و`academy_quizzes` في
> السطرين ١٠٨–١٠٩ views بلا `security_invoker`، فتعمل بصلاحية مالكها و**تتجاوز RLS الجداول تحتها** —
> أي تُبطِل سياستَي `is_active` و`is_published`. تُصلَح في نفس الترحيل.

**وفخ ثالث في `20260823_fts_number_search.sql`:** يستخدم `to_tsvector('library.arabic', …)` حرفياً،
و`main` ينشئ هذا الإعداد **شرطياً** فقط إن لم يوجد `arabic` — و Postgres يشحن `pg_catalog.arabic` جاهزاً،
فقد لا يُنشأ `library.arabic` أبداً ويفشل الترحيل. **تحقق قبل لا بعد:** `select to_regconfig('library.arabic');`

---

## ٩ · دليل بريد الدوائر: بيانات جديدة كلياً — وفيه عطل لم يذكره التقرير

الملف ٣٬١٨٤ سطراً، ١١٢ صفحة، **٢٬١٩٢ صفاً** (عددناها فطابقت رقمه)، كلها على نطاق `moj.gov.sa`، بلا صف
واحد ناقص `@`.

**وهو جديد بالكامل — لا نظير له في المستودع.** ما يُشحن اليوم:
`business/circuits-emails` مصفوفة **٤ صفوف** وبحثها بلا `onChange` (زينة)، و`firm/circuits-emails`
**١٦ قسماً مختلَقاً** بأرقام هواتف مخترعة، و`ai/procedures/_data.tsx` **١٤ دائرة مخترعة** بأكواد ناجز مخترعة.
**تقاطع العناوين الـ٢٩ المختلَقة مع الـ٢٬١٩٢ الحقيقية = صفر.**

**والـ٣٦ خلية المعطوبة سببها واحد وقابل للاختبار:** كل واحدة منها فيها رقم هندي‑عربي (`١` أو `٢`) داخل
جزء لاتيني. هذه الأرقام صنفها الاتجاهي AN فتقطع المقطع اللاتيني، فيُعاد ترتيب ما بعدها حسب اتجاه الفقرة.
القاعدة الفاصلة دقيقة:

> يقع الانقلاب **إذا وفقط إذا** جاء بعد الرقم الهندي‑العربي مباشرةً `-` أو `@`.

٣١ من ٣١ تنطبق عليها، و٠ من ٢٬١٦١ الباقية. ولا توجد محارف تحكّم اتجاهي في الملف إطلاقاً — الانقلاب
مطبوع في الـPDF نفسه، فلا يُستردّ بتنظيف المحارف.

**وثلاثة أشياء لا تغطيها الـ٣٦:**

1. **١٦ خلية أخرى غير مؤشَّرة** فيها أرقام هندية‑عربية نجت من الانقلاب (الرقم لم يتبعه `-` ولا `@`) فلم
   ينتبه لها أحد: `ktabh١D@` و`KIT٢E-RYD@` وأخواتها. **لا عميل بريد يستطيع الإرسال إليها كما هي.**
2. **التصحيحات الـ٣٦ نفسها غير متسقة:** الحالتان ١ و٢ تحوّلان `٢` إلى `2`، والـ٢٩ الباقية تُبقي الرقم
   الهندي. **فبعد تطبيق الـ٣٦ كاملةً يبقى ٤٥ عنواناً غير قابل للإرسال.**
3. عمود المنطقة موجود لـ٥٦١ صفاً فقط من ٢٬١٩٢، وقيمه الخام **١٦ لا ١٣** (`منطقة مكة المكرمة` بجانب
   `منطقة مكة`، و`المنطقة الشمالية` بجانب `منطقة الحدود الشمالية`).

**قراران لك قبل أي إدخال:**
- **أ)** هل نحوّل `١`→`1` و`٢`→`2` في كل العناوين الـ٤٥؟ (الأرجح أن العناوين الحقيقية لاتينية والخط
  في الـPDF استبدل الرسم — والتحويل خريطة من محرفين اثنين.)
- **ب)** الـ٣٦ المصحَّحة: نشحن المطبوع أم المقترح أم الاثنين بشارة «غير مُتحقَّق»؟ الوثيقة تقول صراحةً إن
  المقترح استنتاج نمطي مؤيَّد بموقع غير رسمي. ونشر ٣٦ عنواناً حكومياً مُخمَّناً بلا شارة — والبندان ٣٦ و٧٥
  يريدان الدليل عاماً ومفهرساً — مسؤولية.
- **ج)** الدليل عام (البند ٣٦) أم خلف اشتراك؟ الشاشتان اليوم خلف `SubscriptionGuard` — أي عكس المطلوب.

> **وملاحظة صدق:** بيانات ملف الوزارة تعود لـ٢٠١٥ حسب بياناته الوصفية. دليل اتصال عمره ١١ سنة سيحوي
> عناوين ميتة مهما أُتقِن نقله. هذه مخاطرة على البيانات لا عيب في الملف.

---

## ١٠ · ما نحذفه من قوائمنا — لا نبنيه

نحو ١٥ بنداً في سجلك **يناقض** ما نتتبعه: `PRODUCT_COMPLETENESS_BACKLOG` يسجّلها «تُبنى» وأنت تقول «تُحذف».

| البنود | قائمتنا تقول | قرارك |
|---|---|---|
| ١٢٦ + ١٢٧ | الشبكة المهنية تحتاج نموذج علاقات | **حذف** + تحويل ٣٠١ للسوق |
| ١٣٤ + ١٣٥ | العروض تحتاج كياناً وميزانية وAPI | **حذف** — القاعدة ٣٨ |
| ١٣٦–١٤١ | الخزنة تُبنى | **مؤجَّلة لملف الأفكار** |
| ١٠٣–١٠٥ | السكرتير يُبنى | **مؤجَّل** |
| ٩٩ + ١٠٠ · ١١٨ + ١١٩ · ١٨٧ | — | **حذف** |

القرار هنا **حذف صفوف من قائمتنا**، لا إعادة صياغتها. وسبب ١٣٤ معلومة جديدة لا تفضيلاً: القاعدة ٣٨
تجعل حملات خصم المحامي مسألة نظامية — وهي **صفر ظهور** في المستودع كله.

---

## ١١ · ٢٦١ عيباً في اللقطات لا يذكرها أي بند

الوكلاء قرأوا الـ٣٤ لقطة ووجدوا **٢٦١ عيباً مرئياً خارج السجل**. أمثلة تتكرر:

- **عدّادات لا تتفق مع نفسها:** «جميع القضايا ٩» وشرائح الحالة تجمع ٧؛ وفي لقطة ثانية ٤ مقابل ٣.
  ليست صدفة — تتكرر عند حجمين مختلفين.
- **خلط الأرقام العربية والهندية في السطر نفسه:** «انتظار ٧» بجوار «انتظار: 7» لنفس العدد.
- **نص الحالة الفارغة يلوم البحث والفلاتر** بينما لا بحث مكتوب ولا فلتر مفعّل.
- **زرّان لنفس الفعل باسمين:** «إضافة قضية» في الحالة الفارغة و«قضية جديدة» في الترويسة.

هذه أرخص ما في القائمة وأكثره ظهوراً للمستخدم. مقترحنا: موجة «اتساق الواجهة» بعد الموجة الأولى.

---

## ١٢ · الترتيب المقترح

| المرحلة | المحتوى | يعتمد على |
|:--:|---|---|
| **٠** | النشر + مسح كاش Nginx + ترحيل التسجيل + استعلاما الأكاديمية للقراءة | — |
| **٠٫٥** | إعادة اختبار الـ١٢ لقطة الإنتاجية على البناء الجديد قبل أي كود | ٠ |
| **١** | الـ٢٤ بنداً الرخيصة (§٦) — كلها بلا ترحيل | ٠٫٥ |
| **٢** | اتساق الواجهة: عيّنة من الـ٢٦١ (§١١) | ١ |
| **٣** | **١٨١** — الجداول الحقيقية + سكربت ترحيل لمرة واحدة | قرارك |
| **٤** | **٧٩** — `lawyer_clients` + سياسات الإدراج للاستشارات والقضايا والعقود (١٧٠) | ٣ |
| **٥** | الكتلة المالية (١٨٢، ١٩٤، ١٢٠–١٢٥) ورادار المهل (١٨٠) | ٣ + ٤ |
| **٦** | «درع نظامي» (١٨، ١٩، ٢٠، ٤٧، ٥٧، ٦١–٦٣، ٩٥) — خط منتج جديد | قرارك + مزود دفع |

**المراحل ٠ و٠٫٥ و١ و٢ تنفَّذ فوراً بلا انتظار قرار.** والمراحل ٣ فما فوق تحتاج قرارك.

> **وقد تحققنا من هذا لا افترضناه:** البنود الـ٢٤ في المرحلة ١ كلها ملفاتها **موجودة على الفرعين معاً**،
> فقرار الفرع (§١٣‑١) لا يعطّل منها شيئاً. البندان الوحيدان اللذان ملفاهما على `owner-edits` وحده —
> ١٦٩ (`20260820_academy_schema.sql`) و١٧٢ (`src/lib/rateLimit.ts`) — **ليسا في المرحلة ١** أصلاً:
> الأول ينتظر استعلامَي القراءة في §٨، والثاني حجمه كبير ومكانه المرحلة ٥.

---

## ١٣ · القرارات المطلوبة منك (٢٩ بنداً — هذه أهمها)

1. **الفرع.** `owner-edits` يسبق `main` بـ٧٦ ملفاً. نضمّه إلى `main` فيصير الفرعان واحداً ونعمل على أساس واحد؟
   أم نُبقيهما منفصلين ونصلح كل بند على الفرع الذي يملك ملفه؟ (بندان فقط — ١٦٩ و١٧٢ — ملفاهما على `owner-edits` وحده.)
2. **ترحيلاتك العشرة.** حسمنا أنها آمنة تجاه `handle_new_user`. هل نطبّقها؟ وثلاثة منها تعلن عن نفسها
   «غير مطبَّقة»، و`20260820_academy_schema` **لا يعلن شيئاً** — وهذا هو الخطر.
3. **الأكاديمية:** هل الجدول موجود على الإنتاج؟ (الاستعلامان في §٨.)
4. **دليل البريد:** القرارات الثلاثة في §٩.
5. **الشروط والاسترجاع** — ما زالت تذكر Escrow، وصياغتها البديلة تنتظر اعتمادك منذ ٢٥ أغسطس.
6. **مكافأة الإحالة** — الصفحة أُصلحت، والرقم لم يُحسم.
7. **مزود الدفع** — يحجب المرحلة ٦ كاملة.
8. **الأقسام الـ١٦ المخفية في حساب الشركة** — تُبنى أم تُحذف؟
9. **استضافة البيانات وPDPL** (١٧٦) — Supabase بضوابط النقل عبر الحدود أم داخل المملكة؟ هذا القرار
   **لا يظهر في أي وثيقة عندنا**، وهو يسبق برنامج التشفير كله (١٧٥).

---

## ١٤ · ما لم نفعله في هذه الجولة

- **لم نصلح شيئاً بعد.** هذه الجولة تحقّق وتخطيط. البند ١٧١ (ادعاء AES-256) والبند ٢١ («بشرياً») جاهزان
  للتنفيذ فوراً بمجرد كلمتك، وكلاهما بلا ترحيل.
- **لم نشغّل أي ترحيل** — لا لنا ولا لك.
- **لم نلمس قاعدة الإنتاج** بأي كتابة.
- **لم نُنشئ حساب اختبار** على auth الإنتاج.
- **٣٤ لقطة، ١٢ منها فقط يمكن الجزم بأنها من الإنتاج**؛ الـ٢٢ الباقية لا يظهر فيها شريط العنوان،
  فلا نستطيع الجزم إن كانت من الموقع المنشور أم من نسخة محلية — وهذا يغيّر الحكم عليها.
