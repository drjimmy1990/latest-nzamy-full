# خطة المقالات — تفريغ المدونة ورفع العينة وإصلاح العرض
**التاريخ:** ٢٣ أغسطس ٢٠٢٦ · **المصدر:** `C:\Users\LOQ\Downloads\عقل_المقالات_نسخة_20260820_194919`
**الحالة:** ✅ نُفِّذ — المدونة فُرِّغت، والعينة (١١ مقالاً) مرفوعة، و٦ أعطال عرض مُصلَحة ومُتحقَّق منها في المتصفح.

---

## ١. ما نُفِّذ فعلياً

| # | الإجراء | النتيجة |
|:--:|---|---|
| ١ | **نسخة احتياطية كاملة** قبل أي حذف | ٦١٤ صفاً → `blog-toolkit/_articles-backup-20260823.json` (١٢.٦ ميجابايت، جميعها بالمتن والغلاف وأزواج AEO) |
| ٢ | **تفريغ جدول `articles`** | `✔ deleted 614 rows` |
| ٣ | **تجهيز العينة** | `عينة_مقالات_للرفع/` → `blog-toolkit/blog_final/` — والكوربس القديم حُفظ في `blog_final_july_old/` ولم يُحذف |
| ٤ | **زرع العينة** | `Done. Upserted 11, failed 0.` |
| ٥ | **إصلاح ٦ أعطال في العرض والزرع** | تفاصيلها في القسم ٣ |

**التراجع عند الحاجة:**
```bash
node blog-toolkit/blog-backup.mjs --restore blog-toolkit/_articles-backup-20260823.json --dry
node blog-toolkit/blog-backup.mjs --restore blog-toolkit/_articles-backup-20260823.json
```

## ٢. حالة المقالات الـ ١١ الآن

| القسم | العدد | الغلاف |
|---|:--:|---|
| `sec_00_procedural` | ١ | ✅ يعمل |
| `sec_03_civil` | ١ | ✅ يعمل |
| `sec_04_commercial` | ١ | ❌ لا صورة (مقال جديد) |
| `sec_06_labor` | ٣ | ✅ ٢ · ❌ ١ (مقال جديد) |
| `sec_07_real_estate_construction` | ٥ | ✅ تعمل |

٩ من ١١ كانت موجودة أصلاً بنفس الـ slug فتحدّثت في مكانها وأغلفتها القديمة ما زالت في المخزن. المقالان الجديدان بلا صورة:
`company-incorporation-saudi-arabia-2026` و`saudization-administrative-support-professions-2026`.

**تم فتح المقالين للتأكد:** الصفحة تعمل (200) والمحتوى كامل، والأثر الوحيد صورة غلاف مكسورة و`400` في سجل الخادم عند طلبها من المخزن. لا يوجد أي عطل آخر.

---

## ٣. الأعطال التي أُصلحت (كلها مُتحقَّق منها في المتصفح)

| # | العطل | الملف | الأثر قبل / بعد |
|:--:|---|---|---|
| ١ | **خريطة الأقسام قديمة** | `blog-toolkit/seed-blog.mjs` | استُبدلت بالكامل بالأقسام الثلاثين والتسميات الرسمية من دليل المطور. قبل: `sec_00_procedural` كان يأخذ «الإجراءات والمرافعات». بعد: «أنظمة المرافعات والإجراءات القضائية والتنفيذ» |
| ٢ | **علم `--sections` / `--keep` يُتجاهَل بصيغة المسافة** | `seed-blog.mjs`, `seed-blog-images.mjs`, `blog-clear.mjs` | كان `--keep a,b` يعني **حذف كل شيء**. الآن الصيغتان تعملان — تم التحقق: `--sections sec_06_labor` حصر النتيجة في ٣ ملفات بدل ١١ |
| ٣ | **القوائم الفرعية تُعرض بنجمة خام** | `ArticleView.tsx` | ٤ فقرات على الأقل بكل مقال كانت تظهر `*   المادة (400): …` بلا تنقيط ولا إزاحة. الآن ٣ مستويات تنقيط (`•` `◦` `▪`) بإزاحة متدرجة. **صفر نجوم خام** على الصفحة |
| ٤ | **عناوين `###` غير مرئية في الوضع الداكن** | `ArticleView.tsx` | `globals.css` يحوّل `--color-gray-100` إلى `#1c2128` (لون سطح داكن)، فكان العنوان أسود على أسود — لونه المحسوب `rgb(28,33,40)`. صار `rgb(248,250,252)`. يمس كل عناوين «أولاً/ثانياً/ثالثاً» في السند القانوني |
| ٥ | **الاقتباس النظامي متعدد الأسطر يظهر بنجوم** | `ArticleView.tsx` | نص المادة ملفوف بـ `*"…"*` عبر سطرين، وكان يُعرض سطراً سطراً فتظهر النجمة منفردة. الآن تُجمَّع أسطر الاقتباس في كتلة واحدة وتُزال أقواس المائل |
| ٦ | **`LegalArticle` يُخفَّض إلى `Article`** | `ArticleJsonLd.tsx` | السكيما الآن `LegalArticle` كما تفرض المواصفة |
| ٧ | **«6 قراءة» بدل «٦ دقائق»** | `blog/page.tsx`, `blog/[slug]/page.tsx` | صارت «6 دقائق قراءة» |
| ٨ | **روابط جدول المحتويات المكسورة** | `ArticleView.tsx` | مولّد المحتوى يحذف التشكيل من المرساة («بمضي 60 يوماً» ← «…-60-يوما») بينما الكود يبقيه. أُضيفت مرساة بديلة بلا تشكيل — **إضافة فقط، بلا أي كسر للروابط العاملة**. النتيجة: ٧٩/٨٠ رابط يعمل بدل ٧٧/٨٠ |
| ٩ | **رابط مقال محذوف في خريطة الموقع** | `src/app/sitemap.ts` | `/blog/wrongful-termination-rights` كان مكتوباً يدوياً، وقد حُذف ضمن الـ ٦١٤ فصار **404 معلَناً في `sitemap.xml` مباشرة**. أُزيل — تم التحقق من `sitemap.xml` الحي |
| ١٠ | **`--keep` بلا قيمة كان يحذف كل شيء** | `blog-clear.mjs` | إن ابتلعت الطرفية القيمة أو أُخطئ في كتابتها، كانت القائمة تصير فارغة فيُحذف كل شيء. الآن يرفض السكربت التنفيذ ويخرج بخطأ. تم التحقق: `--keep` بلا قيمة → رفض · `--keep <slug>` → أبقى ١ وحذف ١٠ |

**التحقق:** `npm run type-check` → صفر أخطاء · `eslint` → صفر أخطاء (٧ تحذيرات سابقة غير متعلقة) · فحص بصري وبرمجي على ٣ مقالات في `localhost:3000`.

---

## ٤. عطلان من جهة المحتوى — يحتاجان قراراً قبل وصول الـ ٦٥٥

### ٤-١ 🔴 `related_articles` تحوي أسماء ملفات لا سلاجات — الربط الداخلي المُنسَّق معطّل بالكامل

الفرونتماتر يكتبها هكذا:
```yaml
related_articles:
  - "001_تعديلات_نظام_العمل_السعودي_2025"
```
بينما `getRelated()` في `blog/[slug]/page.tsx` يبحث بها في عمود `slug` (وقيمه إنجليزية kebab-case مثل `best-labor-lawyer-saudi-2026`). **النتيجة: صفر تطابق في كل مقال، دائماً**، فيسقط الكود بصمت إلى بديل «مقالات من نفس القسم».

**لهذا لم يلاحظ أحد:** الشريط الجانبي يعرض مقالات فعلاً — لكنها ليست المقالات التي اختارها الكاتب. تم التحقق على مقال التوطين: عرض مقالين عماليين آخرين، لا الثلاثة المُعلنة في الفرونتماتر.

ودليل المطور نفسه ينص على أنها سلاجات: `related_articles: string[]; // مصفوفة سلاجات المقالات المرتبطة`.

**القرار المطلوب (أحد اثنين):**
1. **إصلاح المحتوى:** يعدّل فريق المحتوى المولّد ليكتب السلاجات بدل أسماء الملفات — الأنظف ومطابق للمواصفة.
2. **إصلاح الزرع:** يبني السكربت فهرس «اسم ملف ← slug» أثناء المسح ويترجم، مع تسجيل ما لا يُحل في `_blog-seed-rejects.log` (لا إسقاط صامت).

### ٤-٢ 🟡 مرساة مكسورة بمسافة

في `001_إجراءات_نقل_ملكية_عقار_في_السعودية.md`:
```
السند-القانوني والنظامي     ← مسافة بدل شرطة في منتصف المرساة
```
يجب أن تكون `السند-القانوني-والنظامي`. **يُبلَّغ فريق المحتوى ليصحح المولّد**، لأن الخطأ سيتكرر في الـ ٦٥٥ مقالاً.

---

## ٥. ما زال مطلوباً من المالك

1. مجلد `blog_final/` كاملاً بالأقسام الثلاثين و٦٥٥ مقالاً — **لم يصل**، المُسلَّم عينة ١١ فقط.
2. `images_delivered/` بصور WebP بمقاس 1200×630 (٦٥٥ صورة).
3. `blog_images_registry.json` مطابقاً للسلاجات الجديدة.

---

## ٦. ما تبقى من العمل البرمجي

### 🔴 قبل رفع الكوربس الكامل
| # | البند | الملف |
|:--:|---|---|
| ١ | **Migration جدول `blog_sections`** — ما زال ٣١ صفاً بالأكواد القديمة، وكل `category_code` جديد يتيم بلا صف مقابل | `supabase/migrations/` |
| ٢ | **صفحة `/blog` تعرض ٦ مقالات وهمية عند فراغ القاعدة**، والنقر عليها يعطي 404 | `src/app/blog/page.tsx` |
| ٣ | **قرار `related_articles`** (القسم ٤-١) — يُحسم قبل زرع الـ ٦٥٥ | محتوى أو `seed-blog.mjs` |

> البند ٢ لا يظهر الآن لأن القاعدة ليست فارغة (١١ مقالاً)، لكنه سيظهر في أي تفريغ قادم.

### 🟠 مخالفات «ممنوع اختراع القيم» (القاعدة الثانية في دليل المالك)
| # | البند | الملف |
|:--:|---|---|
| ٤ | «النقاط الرئيسية» تعرض قيماً مُلفَّقة عن مكافأة نهاية الخدمة عند غياب `aeo_pairs` | `ArticleView.tsx` |
| ٥ | اسم كاتب مُلفَّق («أ. أحمد الغامدي» / «أ. سارة العتيبي») عند غياب `author_name` | `blog/page.tsx` |
| ٦ | تقييم ٥ نجوم وتخصص «قانوني» ثابتان لكل كاتب | `blog/[slug]/page.tsx` |

### 🟡 مطابقة مواصفة — مؤجَّل بقرار
مسارات `/blog/category/<slug>` الثلاثين · صندوق الاقتباس الثلاثي (Pattern 2b) · جدول محتويات لاصق بـ IntersectionObserver · إدراج المقالات في خريطة الموقع · دعم جداول الماركداون (صفر جداول في العينة، خطر كامن في الـ ٦٥٥) · بطاقات السند القانوني الثلاث · ألوان تنبيهات GFM حسب المواصفة.

---

## ٧. أوامر التشغيل

```bash
npm run blog:seed -- --dry                        # معاينة دائماً أولاً
npm run blog:seed -- --sections sec_06_labor      # ✅ الصيغتان تعملان الآن
npm run blog:seed -- --sections=sec_06_labor      # ✅

node blog-toolkit/blog-backup.mjs                 # نسخة احتياطية قبل أي تفريغ
node blog-toolkit/blog-backup.mjs --restore <file> --dry
```

قاعدة الإنتاج: `gdqfqfcxnwrwgaphtfhu.supabase.co` (من `.env.vps`).

> ⚠️ الكوربس القديم (٦١٤ مقالاً + ٦١٤ صورة) محفوظ في `blog-toolkit/blog_final_july_old/` ولم يُمس.
