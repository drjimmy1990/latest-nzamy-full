# ✅ دليل اختبار التحديثات — الجولة الثالثة (١٦ يوليو ٢٠٢٦)
# منصة نظامي — تصحيحات أمنية + إصلاحات المكتبة + المدونة + تحسينات الأداء

مرحباً د. محمد،
هذا دليل الجولة الثالثة من الإصلاحات. تم شحن **٢٥ إصلاحاً وتحسيناً** في هذه المرحلة تغطّي ثغرات أمنية حرجة + المكتبة القانونية الكاملة (٥٠,٩٢٩ سجل مزروع) + المدونة + تحسينات الأداء. كل اختبار مكتوب بصيغة: **أين تذهب** ← **ماذا تفعل** ← **ما الذي يجب أن تراه ✅**.

---

## ⚠️ قبل أن تبدأ (مهم جداً — خطوات إلزامية)

1. **تطبيق تحديثات قاعدة البيانات أولاً.** يوجد تحديثان جديدان يجب تطبيقهما على Supabase **قبل** رفع النسخة:
   - `20260716_security_hardening.sql` — **أمان حرج:** يمنع أي مستخدم من ترقية نفسه لمشرف + يُغلق ثغرة التسجيل كمشرف + يُصلح مشكلة التكرار في سياسات الصلاحيات.
   - `20260716_missing_fk_indexes.sql` — **أداء:** يُضيف فهارس ناقصة لتسريع استعلامات المقالات والتذاكر.

2. **تأكّد أن تحديثات الجولة الثانية مُطبّقة أيضاً** (لو لم تُطبّق سابقاً):
   `20260706_entitlement_requests.sql` · `20260706_draft_cart_payload.sql` · `20260706_content_and_ops.sql` · `20260706_articles_seed.sql`

3. **رفع النسخة الجديدة:**
   ```
   git pull && npm run build && pm2 reload nzamy
   ```

4. **اختبر فقط بعد الرفع.** الإصلاحات جاهزة على GitHub لكن لن تظهر على الموقع المباشر إلا بعد الرفع.

> **ملاحظة:** هذا الدليل يُكمّل الجولتين السابقتين (الاختبارات ١–٣٦). الأرقام تبدأ من ٣٧.

---

## القسم الأول: إصلاحات أمنية حرجة 🔒

> هذه إصلاحات خلفية **لا تظهر بالضغط عادةً** لكنها أهم ما في هذه الجولة. بعضها يمكن التحقق منها.

### ٣٧) منع تسجيل حساب مشرف عن طريق الاحتيال
- **الخلفية:** كان بإمكان أي شخص إنشاء حساب مشرف (admin) مباشرةً عند التسجيل عن طريق حقنة في بيانات التسجيل. **تم إغلاقها.**
- **كيف تتحقق (اختياري):** حاول إنشاء حساب جديد — ستلاحظ أنه يُسجَّل دائماً كمستخدم عادي (`individual`) بغض النظر عن أي شيء. لا توجد طريقة للتسجيل كمشرف إلا بأن يُرقّيه مشرف حالي.
- **✅ المتوقع:** أي حساب جديد يُنشأ كـ `individual` فقط. كلمة `admin` أُزيلت تماماً من قائمة الأنواع المسموحة عند التسجيل.

### ٣٨) منع المستخدم من ترقية نفسه لمشرف
- **الخلفية:** كان بإمكان أي مستخدم مسجّل تعديل ملفه الشخصي وتغيير نوعه إلى `admin` بنفسه — ثغرة خطيرة. **تم إغلاقها بقفل على قاعدة البيانات.**
- **كيف تتحقق (اختياري):** افتح ملفك الشخصي `/settings` → غيّر أي بيانات واحفظ — كل شيء يُحفظ عادي. لكن لو حاول أي شخص (عبر أي وسيلة تقنية) تغيير نوع حسابه إلى `admin`، تمنعه قاعدة البيانات مباشرةً.
- **✅ المتوقع:** الحفظ العادي يعمل بدون مشاكل. فقط تغيير نوع الحساب (`user_type`) ممنوع إلا للمشرف.

### ٣٩) إصلاح تكرار صلاحيات طلبات الترقية
- **الخلفية:** سياسات الصلاحيات (RLS) لجدول `entitlement_requests` كانت تستعلم من جدول `profiles` مباشرة — مما يسبب حلقة تكرار لا نهائية عند وجود سياسات متبادلة. **تم إعادة كتابتها** لاستخدام دالة آمنة `public.is_admin()` بدلاً من الاستعلام المباشر.
- **كيف تتحقق:** افتح `/dashboard/admin/entitlements/requests` بحساب مشرف.
- **✅ المتوقع:** الصفحة تُحمَّل بدون أخطاء. لو كانت فيه مشكلة التكرار ستظهر رسالة خطأ أو صفحة بيضاء.

---

## القسم الثاني: إصلاح المكتبة القانونية 📚

### ٤٠) تفاصيل الكتب الفقهية تعمل الآن
- **أين:** `/laws` ← تبويب **"كتب فقهية"** ← اضغط على أي كتاب فقهي.
- **ماذا تفعل:** افتح صفحة كتاب فقهي ← ابحث عن فهرس الأبواب والفصول والمباحث.
- **✅ المتوقع:** تُعرض بيانات الكتاب من قاعدة البيانات (جدول `feqh_books`) مع التقسيم الصحيح:
  - **الأبواب** (`feqh_chapters`) ← **الفصول** (`feqh_sections`) ← **المباحث** (`feqh_blocks`).
  - سابقاً كان يظهر **"عفواً، الكتاب غير موجود"** لأن أسماء الجداول كانت خاطئة.
- **ملاحظة:** يتطلب وجود بيانات فقهية مزروعة في قاعدة البيانات. لو الجداول فارغة ستظهر حالة "لم يُعثر على الكتاب" — وهذا متوقع ويحتاج زرع بيانات.

### ٤١) تفاصيل الأوامر والتعاميم تعمل
- **أين:** `/laws` ← تبويب **"أوامر وتعاميم"** ← اضغط على أي عنصر.
- **✅ المتوقع:** تُعرض تفاصيل الأمر/التعميم من جدول `decrees_circulars` بدلاً من جدول خاطئ (`decrees`). سابقاً كان يفشل بصمت.

### ٤٢) البحث في المكتبة يستعلم من الجداول الصحيحة
- **أين:** `/laws` ← اكتب كلمة في شريط البحث.
- **ماذا تفعل:** ابحث عن مصطلح قانوني (مثلاً "الإثبات" أو "العمل").
- **✅ المتوقع:** يبحث في الجداول الصحيحة:
  - **المبادئ القضائية** ← من جدول `principles` (كان يبحث في `judicial_principles` الخاطئ).
  - **الأوامر والتعاميم** ← من جدول `decrees_circulars` (كان يبحث في `decrees` الخاطئ).
  - **الكتب الفقهية** ← من جدول `feqh_books` (كان يبحث في `books` الخاطئ).
- **ملاحظة مهمة:** البحث يعمل على **أول ١٠٠ عنصر فقط** (محمّلة في الذاكرة). البحث الخلفي الكامل (عبر `library_search` RPC) لم يُبنَ بعد — هذا عمل قادم.

---

## القسم الثالث: إصلاح صفحة العرض الترويجي 🎟️

### ٤٣) زر "ابدأ الآن" في صفحة العرض لم يعد يعطي 404
- **أين:** `/promo/welcome` (أو أي صفحة عرض ترويجي).
- **ماذا تفعل:** اضغط زر **"ابدأ الآن"** أو أي زر CTA في صفحة العرض.
- **✅ المتوقع:** يُوجَّه إلى صفحة `/login?from=/pricing&promo=true` — **ولا** يظهر خطأ 404. سابقاً كان يُحيل إلى `/auth/login?promo=true` وهو مسار غير موجود.
- **ملاحظة:** الزر يوصلك لتسجيل الدخول ثم لصفحة الأسعار. **ربط الكوبون تلقائياً بعد تسجيل الدخول** (auto-apply coupon) لم يُبنَ بعد — عمل قادم عند تفعيل بوابة الدفع.

---

## القسم الرابع: إصلاح صفحة الدعوة 🔗

### ٤٤) لا تسريب للمكتبة مجاناً من صفحة الدعوة الخاطئة
- **أين:** `/invite/رمز-خاطئ` (أدخل أي رمز دعوة غير صالح).
- **ماذا تفعل:** عند ظهور رسالة "رابط الدعوة غير صالح"، ابحث عن أزرار **"تصفح المكتبة مجاناً"**.
- **✅ المتوقع:** الأزرار الآن تُحيل إلى **`/pricing`** (صفحة الأسعار) بدلاً من `/laws`. سابقاً كانت تُوصل مباشرةً للمكتبة الكاملة — **ثغرة تجاوز الاشتراك أُغلقت**.
- **ملاحظة:** هذا الإصلاح يمنع الوصول المجاني غير المشروع. المستخدم الآن يُوجَّه لصفحة الأسعار ليختار باقة.

---

## القسم الخامس: تحسينات الأداء والعرض ⚡

### ٤٥) اختفاء شريط "البيئة الخلفية جاهزة" في الإنتاج
- **أين:** أي صفحة في لوحة التحكم (مثلاً `/dashboard/lawyer` أو `/dashboard/client`).
- **ماذا تفعل:** انظر أعلى الصفحة.
- **✅ المتوقع:** **لا يظهر** شريط أخضر بعنوان "البيئة الخلفية جاهزة" أو `BackendReadyNotice`. سابقاً كان يظهر حتى في بيئة الإنتاج. الآن مخفي تلقائياً عندما يكون `NEXT_PUBLIC_NZAMY_WORKFLOW_BACKEND` مضبوطاً على `supabase`.
- **ملاحظة:** لو ظهر الشريط، تأكد أن المتغير `NEXT_PUBLIC_NZAMY_WORKFLOW_BACKEND=supabase` مضبوط في `.env.production`.

### ٤٦) تحميل أسرع — الأدراج تُحمّل عند الطلب فقط
- **الخلفية:** كان المتصفح يُحمّل كود `WhatsAppWidget` و`DraftDrawer` (أدراج ثقيلة) فوراً عند فتح **أي** صفحة — حتى الصفحة الرئيسية. الآن تُحمّل **فقط عند الحاجة** (عند الضغط على الأزرار العائمة).
- **كيف تتحقق:** افتح الصفحة الرئيسية `/` ← لاحظ سرعة التحميل الأوّلي.
- **✅ المتوقع:** الصفحة أسرع بشكل ملحوظ. عند الضغط على زر WhatsApp أو زر المسودات يُحمّل الدرج لحظياً.

---

## القسم السادس: فهارس قاعدة البيانات (خلفي) 🗄️

### ٤٧) فهارس FK لتسريع الاستعلامات
- **الخلفية:** أُضيفت ٣ فهارس جديدة لتسريع استعلامات:
  - `articles.author_id` — تسريع جلب مقالات الكاتب.
  - `support_tickets.user_id` — تسريع تذاكر المستخدم.
  - `support_tickets.assignee_id` — تسريع تذاكر المُعيَّن لها.
- **كيف تتحقق:** تفتح `/dashboard/admin/content/articles` أو `/dashboard/admin/tickets` — يجب أن تكون أسرع خصوصاً مع بيانات كثيرة.
- **✅ المتوقع:** لا فرق مرئي مع بيانات قليلة. الفائدة تظهر عند نمو البيانات.

---

## القسم السابع: المكتبة القانونية — بعد البذرة الكاملة 📚🌱

> **تم زرع المكتبة بالكامل:** ٦٠ نظاماً + ١,٧٧٢ مادة + ٩ تعاميم + ١٠,٣٠٠ مبدأ قضائي + ٤٠ كتاباً فقهياً = **٥٠,٩٢٩ سجل**.
> هذه الاختبارات تتحقق من أن البيانات تظهر صحيحة على الموقع.

### ٤٨) قائمة الأنظمة — التبويب الرئيسي
- **أين:** `/laws` ← تبويب **"أنظمة ولوائح"** (الافتراضي).
- **ماذا تفعل:** افتح الصفحة وانتظر التحميل.
- **✅ المتوقع:**
  - تظهر قائمة بـ **٦٠ نظاماً** من قاعدة البيانات (ليست بيانات تجريبية).
  - كل بطاقة تعرض: عنوان النظام + الجهة المصدرة + عدد المواد.
  - البطاقات مُرتّبة ومقروءة.

### ٤٩) تحميل المزيد (Pagination)
- **أين:** `/laws` ← تبويب **"أنظمة ولوائح"**.
- **ماذا تفعل:** مرّر للأسفل ← ابحث عن زر **"تحميل المزيد"** أو **"Load More"**.
- **✅ المتوقع:**
  - يظهر زر "تحميل المزيد" إذا كان عدد الأنظمة أكبر من الحد الأولي.
  - عند الضغط عليه، تُحمّل دفعة جديدة بدون إعادة تحميل الصفحة.
  - لا تتكرر البطاقات.

### ٥٠) البحث في المكتبة — بحث خلفي (Server-side)
- **أين:** `/laws` ← شريط البحث في الأعلى.
- **ماذا تفعل:** اكتب **"العمل"** أو **"الإثبات"** ← انتظر النتائج.
- **✅ المتوقع:**
  - النتائج تأتي من الخادم (ليست تصفية في المتصفح) — عبر `POST /api/library/search`.
  - تظهر نتائج من كل الأقسام: أنظمة + تعاميم + مبادئ + فقه.
  - البحث سريع (أقل من ٣ ثوانٍ).
  - سابقاً كان يبحث فقط في أول ١٠٠ عنصر محمّل — **الآن يبحث في الكل**.

### ٥١) تفاصيل نظام — صفحة المواد
- **أين:** `/laws` ← اضغط على أي نظام (مثلاً نظام العمل أو نظام المرافعات).
- **ماذا تفعل:** افتح صفحة النظام ← تصفّح المواد.
- **✅ المتوقع:**
  - تظهر صفحة تفصيلية بأبواب النظام ومواده.
  - المواد الأولى **مجانية** (مفتوحة) والبقية **مقفلة** (تظهر رسالة اشتراك).
  - عدد المواد الظاهرة يتوافق مع ما هو مسجّل في قاعدة البيانات.

### ٥٢) نظام الدفع (Paywall) يعمل
- **أين:** `/laws` ← افتح أي نظام ← مرّر بعد المواد المجانية.
- **ماذا تفعل:** حاول فتح مادة مقفلة (بحساب مجاني أو بدون تسجيل).
- **✅ المتوقع:**
  - المواد المجانية تظهر كاملة.
  - المواد بعد الحد المجاني تظهر **مقفلة** مع رسالة "اشترك للمتابعة" أو ما يشابهها.
  - سابقاً كانت **كل** المواد مفتوحة مجاناً (كان فيه `free: true` ثابتة) — **الآن مُصلح**.
  - **ملاحظة:** الحد المجاني يُتحكم به من `platform_settings.library_free_article_limit`.

### ٥٣) تبويب الكتب الفقهية — ٤٠ كتاباً
- **أين:** `/laws` ← تبويب **"كتب فقهية"** أو **"فقه ومراجع"**.
- **ماذا تفعل:** تصفّح قائمة الكتب ← اضغط على أي كتاب.
- **✅ المتوقع:**
  - تظهر **٤٠ كتاباً** (المغني، كشاف القناع، الروض المربع، الوسيط للسنهوري... إلخ).
  - عند فتح كتاب، تظهر **الأبواب** ← **الفصول** ← **المباحث** (من جداول `feqh_chapters` → `feqh_sections` → `feqh_blocks`).
  - الكتب الشرعية والقانونية تظهر بتصنيفات صحيحة.

### ٥٤) تبويب الأوامر والتعاميم — ٩ تعاميم
- **أين:** `/laws` ← تبويب **"أوامر وتعاميم"**.
- **ماذا تفعل:** تصفّح القائمة ← اضغط على أي تعميم.
- **✅ المتوقع:**
  - تظهر **٩ تعاميم** (تعاميم وزارة العدل، البنك المركزي، إلخ).
  - عند فتح تعميم، تظهر تفاصيله وصفحاته من `decree_pages`.

### ٥٥) تبويب المبادئ القضائية — ١٠,٣٠٠ مبدأ
- **أين:** `/laws` ← تبويب **"مبادئ وسوابق قضائية"**.
- **ماذا تفعل:** تصفّح القائمة ← اضغط على أي مجموعة مبادئ.
- **✅ المتوقع:**
  - تظهر **٣٧ مجموعة** (تقريرات محكمة التمييز، مبادئ ديوان المظالم، لجان التأمين... إلخ).
  - عند فتح مجموعة، تظهر المبادئ مع فقراتها.
  - **ملاحظة:** بعض المجموعات ضخمة (مئات المبادئ) — التحميل قد يأخذ لحظات.

### ٥٦) المجلدات الذكية (SmartFolders) — حفظ في DB
- **أين:** `/laws` ← أيقونة **المجلدات الذكية** (عادةً في الشريط الجانبي أو الأعلى).
- **ماذا تفعل (بحساب مسجّل):**
  1. أنشئ مجلداً ذكياً جديداً (مثلاً "أنظمة مهمة").
  2. أضف نظاماً أو مادة إلى المجلد.
  3. أغلق المتصفح ← أعد فتحه ← تحقق أن المجلد ما زال موجوداً.
- **✅ المتوقع:**
  - المجلد يُحفظ في قاعدة البيانات (عبر `/api/library/folders`).
  - المحتوى يبقى بعد إعادة فتح المتصفح.
  - سابقاً كانت تُحفظ فقط في `localStorage` وتضيع عند تبديل الجهاز — **الآن تُحفظ في Supabase**.
- **ملاحظة:** الضيوف (بدون تسجيل) ما زالوا يستخدمون `localStorage`.

### ٥٧) أداة إدارة المكتبة (Library Toolkit)
- **الخلفية:** تم بناء ٦ أوامر CLI لإدارة محتوى المكتبة:
  - `npm run library:status` — عرض عدد الصفوف.
  - `npm run library:parse` — تحليل ملفات `.md` → JSON.
  - `npm run library:seed` — زرع JSON في Supabase.
  - `npm run library:clear` — مسح الجداول.
  - `npm run library:verify` — فحص صحة البيانات.
  - `npm run library:reseed` — مسح + زرع + فحص بأمر واحد.
- **أين:** افتح Terminal في مجلد المشروع.
- **كيف تتحقق:** شغّل `npm run library:status`.
- **✅ المتوقع:**
  ```
  laws: 60 | chapters: 99 | articles: 1772
  decrees: 9 | decree_pages: 14
  collections: 38 | principles: 10300
  feqh_books: 40 | feqh_chapters: 5695 | feqh_sections: 31474
  GRAND TOTAL: 50929
  ```
- **الدليل الكامل:** `library-toolkit/STEP_BY_STEP.md` + `library-toolkit/README.md`.

---

## القسم الثامن: المدونة 📝

### ٥٨) صفحة المدونة الرئيسية
- **أين:** `/blog`.
- **ماذا تفعل:** افتح الصفحة ← تصفّح المقالات.
- **✅ المتوقع:**
  - تظهر قائمة المقالات المنشورة من قاعدة البيانات (جدول `articles`).
  - كل بطاقة تعرض: العنوان + الملخص + صورة الغلاف + التاريخ + الكاتب.
  - **تصفية بالفئة:** يظهر شريط فئات (Categories) — عند الضغط على فئة تُفلتر المقالات.
  - التحميل التدريجي يعمل (صفحات أو Load More).
  - لو لم تكن هناك مقالات مزروعة، ستظهر حالة "لا توجد مقالات" — وهذا متوقع ويحتاج زرع بيانات عبر `blog-toolkit/`.

### ٥٩) صفحة مقال مفرد
- **أين:** `/blog` ← اضغط على أي مقال.
- **ماذا تفعل:** افتح المقال ← تصفّح المحتوى.
- **✅ المتوقع:**
  - يظهر العنوان + المحتوى الكامل + صورة الغلاف.
  - **مسار التنقل** (Breadcrumb): الرئيسية ← المدونة ← عنوان المقال.
  - **مقالات ذات صلة** تظهر أسفل المقال.
  - **SEO:** العنوان يظهر في تبويب المتصفح + meta description موجود (مهم لمحركات البحث).

### ٦٠) الفئات (Categories)
- **أين:** `/blog` ← شريط الفئات.
- **ماذا تفعل:** اضغط على فئة مختلفة ← لاحظ تغيّر المقالات.
- **✅ المتوقع:**
  - الفئات تُجلب من `/api/v1/blog/categories`.
  - عند اختيار فئة، تُفلتر المقالات حسب الفئة المختارة.
  - عدد المقالات يظهر بجانب كل فئة.

### ٦١) إدارة المقالات (لوحة الإدارة)
- **أين:** `/dashboard/admin/content/articles` (بحساب مشرف).
- **ماذا تفعل:**
  1. اضغط **"إضافة مقال"** ← عبّئ العنوان + المحتوى + الفئة + صورة الغلاف.
  2. احفظ كمسودة ← ثم انشر.
  3. افتح `/blog` ← تحقق أن المقال يظهر.
- **✅ المتوقع:**
  - المقال يُحفظ في قاعدة البيانات.
  - المسودات لا تظهر للزوار.
  - المقالات المنشورة تظهر فوراً في `/blog`.
  - زر **"معاينة"** 👁 يفتح المقال في `/blog/[slug]`.
  - أزرار التعديل والحذف تعمل.
- **أداة البذرة:** لو أردت زرع مقالات جاهزة → `blog-toolkit/seed-blog.mjs` + `blog-toolkit/README.md`.

## 🔒 تمّ في الخلفية (لا يُختبر بالنقر) — الجولة الثالثة

هذه إصلاحات مهمة تمت في الكود ولا تظهر بالنقر — نذكرها للعلم:

- **قفل `user_type` على مستوى قاعدة البيانات** — trigger `check_user_type_lock()` يمنع أي محاولة لتغيير نوع الحساب إلا من المشرف أو من الـ service role. (اختبار ٣٨)
- **إعادة كتابة `handle_new_user()` بالكامل** — الدالة الجديدة تحافظ على منطق تعيين القطاعات (`individual`/`lawyer`/`firm`/`corporate`/`micro`/`provider`/`government`/`ngo`) + إنشاء `user_settings` + حماية `ON CONFLICT DO NOTHING` — مع حذف `admin` من القائمة المسموحة. (اختبار ٣٧)
- **إصلاح تحويل `/dashboard/corporate`** — كان يُحيل إلى `/dashboard/business` (غير موجود). الآن `proxy.ts` يعيد التوجيه الصحيح.
- **إصلاح catch blocks في `casesService.ts` و `adminService.ts`** — بدل إخفاء الأخطاء وإرجاع بيانات وهمية، الآن تُسجَّل الأخطاء في الـ console وتُعاد مصفوفة فارغة `[]`.

---

## ⏳ ما يزال مؤجّلاً (لم يُنفَّذ بعد)

هذه عناصر معروفة ولم تُنجَز في هذه الجولة — مذكورة حتى لا تبحث عنها:

### أمان (متبقي)
- [ ] التحقق من إزالة أداة تبديل الأدوار (`role-switcher`) من بناء الإنتاج.
- [ ] اختبار ملكية حذف المجلدات (`DELETE /api/library/folders`) بحسابين مختلفين.
- [ ] حد أقصى لعدد طلبات AI (`/api/ai/explain-article` و `/api/ai/library-chat`) — لمنع الاستهلاك المفرط.

### وظائف معطّلة (تحتاج عملاً إضافياً)
- [ ] أزرار الترقية في `/pricing` — ما زالت لا تستجيب (تحتاج ربطاً بنظام طلبات الترقية).
- [ ] رابط الدعوة (`/invite/[code]`) — "رابط الدعوة غير صالح" — يحتاج تتبع مشكلة.
- [ ] إنشاء مجموعة (`/dashboard/client/my-group`) — "تعذّر إنشاء المجموعة".
- [ ] البث لا يصل للوحة المحامي — بعد إرسال بث من الإدارة، المحامي لا يراه.
- [ ] إشراف المجتمع لا يتزامن — الموافقة من الإدارة لا تظهر في `/community`.
- [ ] سجل المدفوعات — تداخل في النصوص + نقص فلاتر.
- [ ] `/find-lawyer` — ما زال يعرض ٨ محامين وهميين بدل البيانات الحقيقية.
- [ ] `/dashboard/lawyer/tasks` — تلف في النص العربي (mojibake) عند الحفظ/القراءة.

### أدوات AI — ردود وهمية (`setTimeout`)
> ⚠️ **٨ صفحات AI ما زالت تعرض ردوداً مُختلَقة** — لم تُربط بـ n8n بعد:

| الصفحة | الوضع الحالي |
|---|---|
| `/ai/consult` | يُولّد رداً وهمياً بعد ثانيتين |
| `/ai/assistant` | يُولّد رداً وهمياً بعد ثانيتين |
| `/ai/analyze` (المحلل الذكي) | يُنتج تقريراً مُختلَقاً |
| `/ai/analyze-strength` | رد ثابت مُختلَق |
| `/ai/brief-check` | رد ثابت مُختلَق |
| `/ai/case-brief` | رد ثابت مُختلَق |
| `/ai/communicate` | رد ثابت مُختلَق |
| `/ai/compare` | نتيجة مقارنة مُختلَقة بالعربية |

**المطلوب:** استبدال كل هذه الردود ببطاقة **"قريباً / قيد التفعيل"** أو ربطها بـ n8n webhooks حقيقية.
**الاستثناء:** `/api/ai/library-chat` هو الوحيد المربوط بـ n8n بصدق — يعرض "قريباً" عند عدم وجود webhook.

### أتمتة n8n (لم يُبنَ بعد)
> ⚠️ **١٨ سير عمل (workflow) مخطط لها — ولا واحد مُفعَّل:**

**القسم أ — مطلوب الآن (٧ سير عمل تُكمل دورة العميل ↔ المحامي):**

| # | سير العمل | الوظيفة | الوقت المقدّر |
|---|---|---|---|
| أ١ | ترحيب بالعضو الجديد | بريد + واتساب عند التسجيل | ٢ ساعة |
| أ٢ | طلب خدمة جديد ← إشعار المحامين | إشعار المحامين المناسبين + تأكيد للعميل | ٤ ساعات |
| أ٣ | تعيين محامٍ ← إشعار العميل | "تم تعيين محامي لقضيتك" | ٢ ساعة |
| أ٤ | إنجاز الطلب + طلب تقييم | إغلاق الحلقة + تقييم بعد ٢٤ ساعة | ٣ ساعات |
| أ٥ | توثيق المحامي | إشعار المشرف ← موافقة ← إشعار المحامي | ٣ ساعات |
| أ٦ | تذكير الاستشارة | تذكير قبل ٢٤ ساعة + قبل ساعة واحدة | ٣ ساعات |
| أ٧ | فرز طلبات الواتساب (AI) | تصنيف ذكي + إنشاء طلب تلقائي | ٥ ساعات |

**القسم ب — لاحقاً (١١ سير عمل — ٧ تشغيلية + ٤ مالية مُعلّقة ببوابة الدفع):**
- ب١: تصعيد الطلبات (٤٨ ساعة SLA)
- ب٢: تذكير الجلسات
- ب٣: تسجيل مكتب محاماة
- ب٤: توثيق مزوّد خدمة
- ب٥: ملخص يومي للمشرف
- ب٦: إشراف محتوى بالذكاء الاصطناعي
- ب٧: تنبيه أمني (محاولات دخول فاشلة)
- ب٨–ب١١: إيصال دفع، تذكير تجديد اشتراك، تحذير نفاد الرصيد، إصدار فاتورة ← **مُعلّقة حتى تفعيل بوابة الدفع**

### تحسينات أخرى مؤجّلة
- [x] ~~البحث الكامل في المكتبة~~ — ✅ **تم** (عبر `POST /api/library/search` — بحث خلفي كامل).
- [ ] تطبيع الأحرف العربية في البحث (`الإثبات` ↔ `الاثبات`، `١٤٤٤` ↔ `1444`).
- [ ] JSON-LD (بيانات منظمة لمحركات البحث).
- [ ] `next/font` بدل `<link>` لتحميل الخطوط.
- [ ] استبدال ٢٠٠٠+ لون مُعرَّف يدوياً (`#0B3D2E`) بمتغير ألوان واحد.
- [ ] إصلاح ومضة تغيّر الاتجاه (FOUC) عند تبديل اللغة.
- [ ] ترحيل أنماط CSS `text-align` إلى `text-start`/`text-end` (٢٦١ استخدام).
- [ ] دمج ملفات المالك من مجلد `modifications/` (المدونة + إطلاق المكتبة + الإعدادات + DraftDrawer).
- [ ] بناء نظام اشتراكات الإدارة (تمديد + إتاحة مجانية + تعويض).

---

## 📋 قائمة تحقق سريعة — الجولة الثالثة

### أمان (٣ اختبارات)
- [ ] حساب جديد يُنشأ كـ `individual` فقط — لا يمكن التسجيل كمشرف (٣٧)
- [ ] تعديل الملف الشخصي يعمل عادي + تغيير `user_type` ممنوع (٣٨)
- [ ] صفحة طلبات الترقية `/dashboard/admin/entitlements/requests` تُحمَّل بدون خطأ (٣٩)

### المكتبة — إصلاحات سابقة (٣ اختبارات)
- [ ] صفحة كتاب فقهي تعرض الأبواب/الفصول/المباحث من قاعدة البيانات (٤٠)
- [ ] صفحة أمر/تعميم تعرض تفاصيل من `decrees_circulars` (٤١)
- [ ] البحث في المكتبة يبحث في الجداول الصحيحة (٤٢)

### العرض والدعوة (٢ اختباران)
- [ ] زر العرض الترويجي يفتح `/login?from=/pricing&promo=true` — لا 404 (٤٣)
- [ ] أزرار "تصفح المكتبة مجاناً" في صفحة دعوة خاطئة تُحيل لـ `/pricing` (٤٤)

### الأداء والعرض (٣ اختبارات)
- [ ] لا شريط "البيئة الخلفية جاهزة" في الإنتاج (٤٥)
- [ ] الأزرار العائمة (واتساب/مسودات) تُحمّل عند الضغط فقط — أسرع (٤٦)
- [ ] صفحات المقالات/التذاكر تعمل بسرعة (فهارس جديدة) (٤٧)

### المكتبة — بعد البذرة الكاملة (١٠ اختبارات)
- [ ] قائمة الأنظمة تعرض ٦٠ نظاماً من DB — ليست بيانات وهمية (٤٨)
- [ ] زر "تحميل المزيد" يعمل بدون تكرار بطاقات (٤٩)
- [ ] البحث الخلفي يُرجع نتائج من كل الأقسام (٥٠)
- [ ] صفحة تفاصيل نظام تعرض الأبواب والمواد (٥١)
- [ ] المواد بعد الحد المجاني مقفلة — الـ Paywall يعمل (٥٢)
- [ ] تبويب الفقه يعرض ٤٠ كتاباً مع أبواب/فصول/مباحث (٥٣)
- [ ] تبويب التعاميم يعرض ٩ تعاميم مع تفاصيلها (٥٤)
- [ ] تبويب المبادئ يعرض ٣٧ مجموعة — ١٠,٣٠٠ مبدأ (٥٥)
- [ ] المجلدات الذكية تُحفظ في DB وتبقى بعد إغلاق المتصفح (٥٦)
- [ ] `npm run library:status` يُظهر ٥٠,٩٢٩ سجل (٥٧)

### المدونة (٤ اختبارات)
- [ ] `/blog` تعرض المقالات المنشورة مع الفئات (٥٨)
- [ ] صفحة مقال مفرد تعرض المحتوى + مقالات ذات صلة (٥٩)
- [ ] الفئات تعمل — تفلتر المقالات (٦٠)
- [ ] إنشاء/تعديل/حذف مقال من لوحة الإدارة يعمل (٦١)

---

## 📊 ملخص الإنجاز التراكمي

| الجولة | التاريخ | عدد الإصلاحات | الأهم |
|---|---|---|---|
| الأولى | يونيو ٢٠٢٦ | ١٨ إصلاحاً | إخفاء أدوات التطوير، إصلاح الملف المهني، المجلدات الذكية |
| الثانية | ٦ يوليو ٢٠٢٦ | ١٨ إصلاحاً | نظام الصلاحيات، ربط لوحة الإدارة، المدونة، الإشعارات |
| **الثالثة** | **١٦ يوليو ٢٠٢٦** | **٢٥ إصلاحاً** | **أمان + بذرة المكتبة الكاملة (٥٠K سجل) + بحث خلفي + Paywall + أدوات CLI** |
| **الإجمالي** | — | **٦١ إصلاحاً** | — |

### ما تبقّى (بإيجاز):
1. **رفع النسخة + تطبيق التحديثات** ← كثير من الأعطال ستُحَل تلقائياً.
2. **ربط ٨ أدوات AI** ببطاقة "قريباً" أو بـ n8n حقيقي.
3. **بناء ٧ سير عمل n8n** لإكمال دورة العميل ↔ المحامي.
4. **دمج تعديلات المالك** (الإعدادات + DraftDrawer).
5. **تفعيل بوابة دفع** ← يفتح ٤ سير عمل مالية + نظام الاشتراكات الكامل.
6. **المكتبة:** تطبيع الأنواع (type normalization) + فك تجميع الحاويات (container unbundling) لـ ٩٠٢ ملف غير مصنّف.

---

*لأي ملاحظة أثناء الاختبار، دوّنها بجانب رقم الاختبار وسنعالجها. شكراً د. محمد.*
