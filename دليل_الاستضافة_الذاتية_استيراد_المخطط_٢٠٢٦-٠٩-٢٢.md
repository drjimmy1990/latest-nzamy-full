# دليل الاستضافة الذاتية — استيراد المخطط والبيانات من Supabase المستضاف

> **التاريخ:** ٢٢ سبتمبر ٢٠٢٦ · **الجمهور:** المطوّر المنفِّذ (ليس دليل مالك).
> **الحالة عند كتابة هذا الملف:** قاعدة الإنتاج ما زالت على Supabase المستضاف. ترحيلات `20260922_01/02/03` **نُفِّذت ✓ (٢٢ سبتمبر)**، ونشر الكود `ca0b22b` يتبعها اليوم. هذا الدليل يصف النقل إلى استضافة ذاتية **عندما يُقرَّر موعده** — ولا يُنفَّذ اليوم.
> **المراجع التي بُني عليها:** `scripts/selfhost/README-BACKUP.md` و`scripts/selfhost/RESTORE-SELFHOST.md` (نسخة حرفية من عُدّة النسخ الاحتياطي)، و`_fix-delivery-2026-09-20/backup/REVIEW-2026-09-21.md`، و`supabase/migrations/_verify.sql`، و`DEPLOY_AND_SMOKETEST_RUNBOOK.md`، و`deployment_guide.md`، والتوثيق الرسمي لـ Supabase (Self-Hosting with Docker).
> كل أمر في هذا الدليل مأخوذ من واحد من هذه المصادر أو من التوثيق الرسمي. وما لا يمكن إثباته من ملف مكتوب مُعلَّم صراحةً بـ **«يُتحقق عند التنفيذ»**.
> **لا يحتوي هذا الملف أي مفتاح أو كلمة مرور أو سلسلة اتصال.** كل قيمة سرّية مكتوبة كعنصر نائب بين قوسين زاويين مثل `<POSTGRES_PASSWORD>`.

---

## ٠. ما هذا الترحيل، وما ليس هو

### ٠‏.١ ما ينتقل فعلاً

| ما ينتقل | بأي وسيلة | مصدر الإثبات |
|---|---|---|
| المخطط الكامل لمُخطَّطَي `public` و`library` (الجداول، الأعمدة، الدوال، قيود التحقق، الفهارس، الـ triggers، سياسات RLS، الـ grants، الـ sequences) | `selfhost/01-schema.sql` | `backup-supabase.sh:152-154` و`:191-205` |
| كل صفوف `public` و`library` + جدول تاريخ الترحيلات `supabase_migrations.schema_migrations` | `selfhost/03-data.sql` | `backup-supabase.sh:157-159` |
| مستخدمو المنصّة: `auth.users` و`auth.identities` و`auth.instances` و`auth.mfa_factors` و`auth.sso_*` و`auth.saml_providers` — بكلمات المرور المُعمّاة كما هي | `selfhost/02-auth-data.sql` | `backup-supabase.sh:164-167` |
| المُشغِّل `on_auth_user_created` على `auth.users`، وحاويات التخزين (buckets)، وسياسات `storage.objects`، وأعضاء نشر الـ realtime | قسم `02-extras.sql` المضمَّن داخل `selfhost/01-schema.sql` | `sql/02-export-extras.sql` (الأقسام A/B/C/D) |
| ملفات التخزين نفسها وصفوف `storage.objects` | `copy-storage.mjs` | ترويسة `copy-storage.mjs:1-20` |

أرقام النسخة المرجعية `run2` (٢١ سبتمبر ١٢:٤٥ UTC): PostgreSQL **17.6**، حجم قاعدة البيانات **792 MB**، **81** مستخدماً و**81** هوية، `library.articles` **13,436** مادة، و**626** ملفاً في التخزين (`00-server-info.txt` و`MANIFEST.txt` و`REVIEW-2026-09-21.md`).

### ٠‏.٢ ما **لا** ينتقل — ويجب أن يكون معلوماً قبل تحديد الموعد

1. **الجلسات النشطة لا تنتقل إطلاقاً.** جداول `auth` المؤقتة — `sessions` و`refresh_tokens` و`flow_state` و`one_time_tokens` و`mfa_challenges` و`mfa_amr_claims` و`saml_relay_states` و`audit_log_entries` و`schema_migrations` — **مستبعَدة عمداً** من الـ dump (`backup-supabase.sh:161-163`). النتيجة: **كل مستخدم يسجّل الدخول من جديد بعد النقل، سواء غيّرنا مفاتيح JWT أو لم نغيّرها.** وما يغيّره قرار المفاتيح أمران فقط: (أ) هل يبقى رمز الوصول الموجود في متصفّح المستخدم صالحاً حتى انتهاء مدّته `JWT_EXPIRY`، (ب) هل نُضطر لتحديث `ANON_KEY`/`SERVICE_ROLE_KEY` في `.env` وفي n8n.
2. **إعدادات المصادقة والتخزين ليست في قاعدة البيانات.** عناوين إعادة التوجيه، وSMTP وقوالب الرسائل، ومزوّدو الدخول (Google)، وحدود المعدّل، ومدّة الـ JWT، وسقف حجم الملف — كلها إعدادات لوحة تحكّم تُعاد كتابتها في `.env` الخاص بالاستضافة الذاتية. الجدول الكامل في `scripts/selfhost/RESTORE-SELFHOST.md` القسم ٧.
3. **لا Edge Functions ولا مهام pg_cron ولا Database Webhooks على المشروع.** أثبتته المراجعة (`REVIEW-2026-09-21.md`: «لا webhooks، لا pg_cron، لا edge functions»)، ويؤكّده القسمان `[E]` و`[F]` في `02-extras.sql` عند كل تشغيل.
4. **التطبيق نفسه لا يتصل بقاعدة البيانات مباشرة.** تحقّقنا: لا وجود لـ`SUPABASE_DB_URL` ولا لأي سلسلة `postgresql://` داخل `src/`، ولا حزمة عميل Postgres في `package.json`. المنفذ الوحيد هو `src/lib/supabase/server.ts` بمتغيّرَي `NEXT_PUBLIC_SUPABASE_URL` و`NEXT_PUBLIC_SUPABASE_ANON_KEY` (وللمسارات الإدارية `SUPABASE_SERVICE_ROLE_KEY`)، و`src/lib/supabaseLibrary.ts` يستدعي نفس العميل. **لذلك تحويل التطبيق = تغيير متغيّرات بيئة وإعادة بناء، لا أكثر** (مع استثناء واحد في القسم ٥‏.١).

---

## ١. المتطلبات المسبقة

### ١‏.١ استضافة ذاتية من `supabase/docker` الرسمي

لا تستخدم صورة Postgres عادية: المخطط يعتمد على مُخطَّط `extensions` وعلى أدوار `anon`/`authenticated`/`service_role` التي تأتي مع صور Supabase (`RESTORE-SELFHOST.md` — جدول الأعطال: `schema "extensions" does not exist`).

```bash
git clone --depth 1 --branch self-hosted/v0.8.1 https://github.com/supabase/supabase
```

```bash
mkdir supabase-project && cp -rf supabase/docker/. supabase-project
```

```bash
cd supabase-project && cp .env.example .env
```

وسجّل الإصدار الأساس حتى يستطيع `update.sh` ترقية هذا التثبيت لاحقاً (سطر منصوص عليه في التوثيق الرسمي):

```bash
printf 'ref=self-hosted/v0.8.1\n' > .supabase-version
```

```bash
docker compose pull
```

(المصدر: التوثيق الرسمي — Self-Hosting with Docker.)

### ١‏.٢ تطابق إصدار Postgres

المصدر **PostgreSQL 17.6**. والتوثيق الرسمي ينصّ على أن الاستضافة الذاتية **تعمل على Postgres 17 افتراضياً** للنشر الجديد بلا أي تجاوز (`sh run.sh start`) — أي أن التطابق يتحقق تلقائياً.

**وإن — ولهذا وحده — ثبّتّ الاستضافة الذاتية على Postgres 15** (الأمر الرسمي للرجوع: `sh run.sh config add pg15`)، فانتبه إلى صلاحية `MAINTAIN`:

* `MAINTAIN` صلاحية استحدثها PostgreSQL 17، وتظهر في أسطر `GRANT` بعد أن ضيّقنا `GRANT ALL` بـ`REVOKE` على `profiles` و`subscriptions` و`credit_transactions`. وخادم 15/16 **يرفضها** فيتوقف الاستيراد تحت `ON_ERROR_STOP`.
* السكربت حُدِّث ليحذفها من نسخ `selfhost/` (`backup-supabase.sh:186-187` و`backup-supabase.ps1:188-189`).
* **لكن نسخة `run2/selfhost/01-schema.sql` الموجودة عندنا وُلِّدت قبل هذا التحديث** — وتحقّقنا للتوّ: ما زالت تحتوي **5** مواضع `MAINTAIN`. لذلك **لا تُستعمل `run2` للنقل**؛ الخطوة (أ) تُولّد `run3` بالسكربت المحدَّث.

### ١‏.٣ المساحة والأدوات

| المطلوب | القيمة المُثبتة | المصدر |
|---|---|---|
| مساحة لمجلد النسخة الاحتياطية | `run2` شغلت **756 MB** — احجز **2 GB** على الأقل لـ`run3` | `run2/backup.log` («756M in /out/run2») |
| أكبر ملف داخل النسخة | `20-data.sql` و`selfhost/03-data.sql` ≈ **395.5 مليون بايت** لكلٍّ منهما | `run2/MANIFEST.txt` |
| مساحة قرص الاستضافة الذاتية | ≥ حجم قاعدة البيانات **792 MB** + ملفات التخزين (626 ملفاً؛ حجمها الإجمالي **يُتحقق عند التنفيذ**) + هامش لـ WAL والفهارس | `00-server-info.txt` |
| أدوات العميل | `pg_dump` و`psql` بإصدار رئيسي **≥ 17** | `backup-supabase.sh:15-17` |
| Node | **≥ 20.6** — فالاستدعاء يمرّ بـ`--env-file` (القسم ٤‏.٦)، وهو أول إصدار يدعمه؛ والمشروع نفسه يشترط `>=22`. و`copy-storage.mjs` يُشغّل **من جذر الريبو** لأنه يستعمل `@supabase/supabase-js` الخاص بالمشروع | ترويسة `copy-storage.mjs:6` و`package.json:5-7` |

### ١‏.٤ ملف `.env` الخاص بالاستضافة الذاتية

الحد الأدنى المطلوب قبل البدء (من `RESTORE-SELFHOST.md` القسم ٠، ومن التوثيق الرسمي): `POSTGRES_PASSWORD` و`JWT_SECRET` و`ANON_KEY` و`SERVICE_ROLE_KEY` و`SITE_URL` و`API_EXTERNAL_URL` و`SUPABASE_PUBLIC_URL` و`SMTP_*`.

والتوثيق الرسمي يوفّر مولّد المفاتيح داخل مجلد المشروع:

```bash
sh utils/generate-keys.sh
```

> ⚠️ `ANON_KEY` و`SERVICE_ROLE_KEY` مشتقّان من `JWT_SECRET`. وتغيير `JWT_SECRET` يُبطل المفتاحين القديمين — وهذا مقصود في الخيار (أ) أدناه.

### ١‏.٥ قرار مفاتيح JWT — محسوم

السؤال رقم **8** في تقرير المالك `docs/audits/2026-09-21-owner-report-profiles.md:51` كان: «هل نُبقي نفس مفاتيح JWT (الجلسات تستمر) أم نولّد جديدة (الجميع يعيد الدخول — الأأمن)؟» والافتراض المعتمد عند سكوت المالك مُدوَّن في نفس الصفّ: **«بعد الإغلاق؛ مفاتيح جديدة»**.

فالمسار المعتمد هو **الخيار (أ)**: `JWT_SECRET` جديد ومفاتيح `ANON_KEY`/`SERVICE_ROLE_KEY` جديدة. ويترتّب عليه:

* تحديث `NEXT_PUBLIC_SUPABASE_ANON_KEY` و`SUPABASE_SERVICE_ROLE_KEY` في `.env.local` على الخادم (القسم ٥).
* تحديث أي مفتاح Supabase داخل n8n.
* **دخول جديد للجميع** — وهو حاصل على أي حال لأن الجلسات لا تنتقل (القسم ٠‏.٢).

(الخيار (ب) — نسخ نفس `JWT_SECRET` والمفاتيح من المشروع المستضاف — موثّق في `RESTORE-SELFHOST.md` القسم ٠، ويفرض تدوير المفاتيح بعد إيقاف المشروع المستضاف نهائياً. ولا نأخذ به.)

### ١‏.٦ تشغيل المنصّة لأول مرة — بعد اكتمال `.env` لا قبله

بعد أن صار `.env` نهائياً بمفاتيح القسم ١‏.٥ — وليس قبل ذلك — شغّل المنصّة:

```bash
sh run.sh start
```

(المصدر: التوثيق الرسمي — Self-Hosting with Docker.)

> ⚠️ **الترتيب هنا ليس تجميلياً.** `POSTGRES_PASSWORD` و`JWT_SECRET` يُثبَّتان داخل الـvolume عند **أول** إقلاع للقاعدة، فلا يغيّرهما تعديل `.env` بعده. ومَن شغّل المنصّة قبل ضبط `.env` وقبل `utils/generate-keys.sh` عليه إعادة العمل من الصفر: `docker compose down -v` (يمسح الـvolumes) ثم التشغيل من جديد — وهو نفس الدواء المذكور في القسم ٦‏.٣.

وهذه الخطوة هي التي تُنشئ الحاوية `supabase-db` التي تناديها كل أوامر `docker exec` في القسم ٤ (وحالة الخدمات تُقرأ بـ`docker compose ps`). فإن ردّ أمر لاحق بـ`No such container: supabase-db` فالسبب أن المنصّة لم تُشغّل أصلاً، لا أن الاستيراد أخفق.

---

## ٢. الخطوة (أ) — نسخة احتياطية جديدة `run3` **بعد** ترحيلات ٢٢ سبتمبر

### ٢‏.١ لماذا نسخة جديدة ولا نكتفي بـ`run2`

سببان، كلاهما قاطع:

1. `run2` أُخذت في **٢١ سبتمبر ١٢:٤٥ UTC** (`run2/backup.log`)، أي **قبل** ترحيلات `20260922_01_library_grants` و`20260922_02_members_accept_own_invitation` و`20260922_03_lawyer_provider_column_grants` التي **نُفِّذت ✓ (٢٢ سبتمبر)**. واستيراد `run2` يعني قاعدة ينقصها منح `library` وذراع قبول الدعوة وضبط أعمدة الكتابة — وهي بالضبط ما يفحصه `_verify.sql` في نهاية الاستيراد، فسيفشل.
2. `run2/selfhost/01-schema.sql` وُلِّدت قبل تحديث حذف `MAINTAIN` (القسم ١‏.٢).

### ٢‏.٢ الأمر (Windows — وهو الأمر المُوثَّق في العُدّة)

`README-BACKUP.md` يوثّق استدعاء PowerShell؛ وهذا هو نفسه بعد تحويله إلى المسار الجديد `scripts/selfhost/` وإلى مجلد `run3`:

```powershell
cd "D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website"
```

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\selfhost\backup-supabase.ps1 -OutDir "$env:USERPROFILE\nzamy-backups\run3"
```

وإن لم تكن أدوات PostgreSQL على `PATH`:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\selfhost\backup-supabase.ps1 -OutDir "$env:USERPROFILE\nzamy-backups\run3" -PgBin "C:\pgsql\bin"
```

السكربت يقرأ `SUPABASE_DB_URL` من `.env.local` (وهو ملف git-ignored) ولا يطبعه أبداً — يطبع نسخة مُقنَّعة فقط (`backup-supabase.ps1:79` و`:112`). وهو **قراءة فقط**: `pg_dump`/`psql` لا يغيّران شيئاً في المصدر.

### ٢‏.٣ الأمر عبر Docker (كما وُلِّدت `run2`) — **يُتحقق عند التنفيذ**

> **تنبيه صدق:** `README-BACKUP.md` **لا يحتوي** أمر Docker؛ المُوثَّق فيه هو استدعاء PowerShell أعلاه فقط. لكن `run2/backup.log` يثبت أنها وُلِّدت داخل حاوية (`pg_dump 17.11 (Debian 17.11-1.pgdg13+2)` ومخرجات إلى `/out/run2`)، وقالب `docker run` المستعمل في هذا المشروع موجود في `_fix-delivery-2026-09-20/sql-apply-order/README-ORDER.md:28`. والأمر التالي مُركَّب من هذين الأثرين ولم يُنفَّذ بعد بهذه الصياغة — **تحقق منه عند التنفيذ**.

```powershell
docker run --rm --env-file .env.local -v "${PWD}\scripts\selfhost:/kit" -v "$env:USERPROFILE\nzamy-backups:/out" postgres:17 bash /kit/backup-supabase.sh -o /out/run3
```

> **لا تُصلح نهايات الأسطر داخل `/kit`.** المجلد مربوط مباشرة بـ`scripts/selfhost/` في الريبو، فأي `sed -i` عليه يعدّل النسخة الحرفية المحفوظة في المستودع. والنسخة الحالية مطابقة بايت ببايت للملف الذي أنتج `run2` داخل حاوية `postgres:17` بنجاح، أي أن نهاياته LF أصلاً. وإن احتجت الإصلاح يوماً — عند نسخ الملف بـ`scp` إلى VPS كما في `README-BACKUP.md` تحت «من الـ VPS بدل Windows» — فاعمله على نسخة، لا على الأصل:

```bash
cp /kit/backup-supabase.sh /tmp/backup-supabase.sh && sed -i 's/\r$//' /tmp/backup-supabase.sh
```

### ٢‏.٤ كيف تقرأ ناتج النسخة قبل أن تبني عليها

المجلد `run3` يجب أن يحتوي: `00-server-info.txt` · `01-row-counts.csv` · `02-extras.sql` · `10-schema.sql` · `20-data.sql` · `21-auth-data.sql` · `selfhost/01-schema.sql` · `selfhost/02-auth-data.sql` · `selfhost/03-data.sql` · `MANIFEST.txt` · `backup.log`.

**`MANIFEST.txt`** — افحص فيه أربعة أشياء:

1. سطر `server:` — يجب أن يقول `PostgreSQL 17.x`، وأن إصدار العميل ≥ إصدار الخادم.
2. سطر `user schemas:` — يجب أن يقول `library public` (لا `academy` ولا `library_precedent_private`؛ ترحيلاتهما لم تُطبَّق عمداً — الدفعة C في `README-ORDER.md`).
3. جدول `bytes / sha256 / file` — **لا يجوز أن يكون أي ملف بحجم صفر**. وقارن `selfhost/01-schema.sql` بحجم `run2` (503,067 بايت)؛ يُتوقّع أن يكبر قليلاً بترحيلات ٢٢ سبتمبر.
4. سطرا `SAFE TO SHARE` و`PRIVATE — never send, never commit`. **الملفات التي في اسمها `data` لا تُرسَل ولا تُرفَع على أي مستودع.**

**`01-row-counts.csv`** — هو المرجع الذي ستُقارَن به القاعدة الجديدة في الخطوة ٤‏.٥. احتفظ به. وأعداد `run2` للمقارنة السريعة: `auth.users` = 81، `auth.identities` = 81، `library.articles` = 13,436، `library.feqh_blocks` = 119,392.

**`backup.log`** — تحذيرات `circular foreign-key constraints` على `service_requests`/`consultations` و`contracts`/`contract_versions` و`community_posts`/`community_answers` و`chat_messages` **طبيعية ومتوقّعة**؛ ظهرت في `run2` أيضاً، وهي بالضبط سبب استيراد البيانات في وضع `session_replication_role = replica` في الخطوة ٤‏.٤.

---

## ٣. الخطوة (ب) — ماذا يحتوي `selfhost/` وما المعالجة التي طُبِّقت عليه

المجلد `selfhost/` داخل `run3` هو **الوحيد** الذي يُستورَد على الاستضافة الذاتية. وملفاته الثلاثة:

| الملف | ماذا يحتوي |
|---|---|
| `01-schema.sql` | ضمان الامتدادات (`pgcrypto` و`uuid-ossp`) + `10-schema.sql` بعد المعالجة + `02-extras.sql` كاملاً |
| `02-auth-data.sql` | بيانات `auth` (المستخدمون والهويات) بعد المعالجة |
| `03-data.sql` | بيانات `public` و`library` + تاريخ الترحيلات، بعد المعالجة |

### ٣‏.١ المعالجات الخمس (من `README-BACKUP.md` و`backup-supabase.sh:172-189`)

1. حذف أسطر `\restrict` و`\unrestrict` — أوامر psql التعريفية التي يُصدرها pg_dump الحديث وتكسر ثنائي psql الأقدم داخل صورة `supabase/postgres`.
2. تعليق `SET transaction_timeout = 0;` — المعامل لا يوجد قبل PostgreSQL 17، وتحت `ON_ERROR_STOP` يوقف الاستيراد.
3. `CREATE SCHEMA "x";` ← `CREATE SCHEMA IF NOT EXISTS "x";` — فـ`public` موجود أصلاً على أي هدف.
4. تعليق `ALTER DEFAULT PRIVILEGES FOR ROLE "supabase_admin"` — يتطلب superuser، والاستضافة الذاتية تحمل الافتراضات نفسها.
5. **حذف `MAINTAIN` من قوائم `GRANT`** — صلاحية PostgreSQL 17، يرفضها خادم 15/16، ولا يحتاجها أي دور من أدوار التطبيق. (هذه المعالجة أُضيفت **بعد** توليد `run2` — القسم ١‏.٢.)

### ٣‏.٢ ما يفعله `01-schema.sql` بالترتيب

ضمان الامتدادات ← المخطط كاملاً (جداول، دوال، سياسات RLS، triggers، grants، sequences) ← المُشغِّل `on_auth_user_created` على `auth.users` ← حاويات التخزين ← سياسات `storage.objects` ← أعضاء نشر الـ realtime (`RESTORE-SELFHOST.md` القسم ٣).

> **نتيجة مهمة:** سياسات bucket `documents` **تأتي داخل `01-schema.sql`** عبر القسم `[C]` من `02-extras.sql`، لأن `02-extras.sql` يُصدّر السياسات **الحيّة لحظة النسخ** — أياً كانت وأياً كانت أسماؤها. أي أن الخطوة ٤‏.٧ **شرطية**، لا روتينية.
>
> **ولا تتوقّع أربع سياسات بأربعة أسماء نظيفة.** ففي `run2` (٢١ سبتمبر ١٢:٤٥ UTC، أي **بعد** إغلاق الثغرة) يحمل القسم `[C]` **ستّ** سياسات، ما زالت كلها بأسماء الداشبورد `documents … flreew_0`/`flreew_1`، وكلها **مقصورة على المالك** (كل واحدة تشترط `bucket_id = 'documents'` **و**`auth.uid()::text = (storage.foldername(name))[1]`): واحدة DELETE، وواحدة INSERT، وواحدة UPDATE، و**ثلاث** SELECT. وتقرير المالك يقول صراحةً إن الستّ «اتصلّحت كلها لتقييد الـ bucket + مجلد المالك» (`docs/audits/2026-09-21-owner-report-profiles.md:18`) — أي صُحّحت التعابير في مكانها وبقيت الأسماء وبقي التكرار. والأسماء تدلّ على أن ذلك جرى من واجهة الداشبورد (الطريقة ٢ في `README-ORDER.md:32-46`) لا بملف `storage_policies_documents.sql`، فهو يُنشئ أسماء أخرى صريحة.
>
> ولهذا: **لا تستنتج فشل الاستيراد لأنك بحثت عن الأسماء الأربعة الموثّقة فلم تجدها.** فبوّابة `_verify.sql` تَعُدّ **العمليات المتمايزة** لا عدد السياسات — `count(DISTINCT polcmd)` على السياسات المقصورة على المالك (`_verify.sql:280-281` و`:308`)، وهي تُصنّف **بالمضمون لا بالاسم** (`:247-253`) — فالستّ تُعطي **4**، وعدّاد «الأخرى التي قد تطابق `documents`» يُعطي **0** — وهي بالضبط القراءة المطلوبة في ٤‏.٨.
>
> وتبقى القاعدة العامة: القسم `[C]` يحمل ما هو حيّ لحظة نسخ `run3`، فافتحه واقرأه بنفسك قبل الاستيراد.

### ٣‏.٣ ما بقي خارج نسخة `selfhost/` عمداً

جداول `auth` المؤقتة (القسم ٠‏.٢)، وصفوف `storage.objects` — فالملفات نفسها هي ما يُنشئ تلك الصفوف عند رفعها بـ`copy-storage.mjs`.

---

## ٤. الخطوة (ج) — ترتيب الاستيراد على الاستضافة الذاتية

> **قبل يوم النقل الحقيقي:** نفّذ هذا القسم كاملاً على استضافة ذاتية **تجريبية** أولاً. وهذه ليست توصية بل شرط منصوص عليه في `RESTORE-SELFHOST.md` وفي خطة يوم النقل (القسم ٩ منه).

### ٤‏.١ نقل الملفات إلى الخادم

```powershell
scp -r "$env:USERPROFILE\nzamy-backups\run3\selfhost" <user>@<vps>:/srv/nzamy-restore/
```

```powershell
scp -r ".\scripts\selfhost\sql" <user>@<vps>:/srv/nzamy-restore/
```

```powershell
scp "$env:USERPROFILE\nzamy-backups\run3\01-row-counts.csv" <user>@<vps>:/srv/nzamy-restore/
```

ملفات البيانات تُنقل بـ`scp`/`sftp` فقط، ولا تُرفع إلى أي مكان عام.

### ٤‏.٢ فحوص ما قبل الاستيراد (حارس الأدوار والامتدادات)

اتصل بالقاعدة داخل الحاوية:

```bash
docker exec -it supabase-db psql -U postgres -d postgres
```

ثم نفّذ الفحوص الأربعة (من `RESTORE-SELFHOST.md` القسم ٢). **وكلها يجب أن تمرّ قبل أي استيراد:**

```sql
select version();
```

```sql
select extname from pg_extension where extname in ('pgcrypto','uuid-ossp');
```

```sql
select rolname from pg_roles where rolname in ('anon','authenticated','service_role','postgres','supabase_admin');
```

```sql
select count(*) from pg_tables where schemaname = 'public';
```

| الفحص | المتوقَّع | إن خالف |
|---|---|---|
| `version()` | 17.x (أو 15.x مع تحذير `MAINTAIN` في القسم ١‏.٢) | راجع ١‏.٢ |
| الامتدادات | الاثنان موجودان (وإلا فـ`01-schema.sql` يُنشئهما بنفسه في أول سطرين) | لا شيء |
| الأدوار | **5 صفوف** | أنشئ الناقص قبل المتابعة |
| جداول `public` | **0** (قاعدة فارغة) | `docker compose down -v` ثم `up -d`، وابدأ من جديد |

وافتح القسم `[H]` في `run3/02-extras.sql` — **على جهازك**، فهذا الملف لم يُنقل إلى الخادم في ٤‏.١ (المنقول هو مجلد `selfhost/` الذي يحمل محتواه مدمجاً). إن ذُكر فيه أي دور مخصّص فأنشئه الآن على الهدف (`create role <name>;`) قبل الخطوة التالية. وفي `run2` كانت النتيجة: لا أدوار مخصّصة مرجعيّة — فالأدوار التي تتلقّى grants هي `anon` و`authenticated` و`postgres` و`service_role` فقط (`REVIEW-2026-09-21.md`).

### ٤‏.٣ استيراد المخطط

```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 --single-transaction < /srv/nzamy-restore/selfhost/01-schema.sql
```

**المتوقَّع:** صفر `ERROR`. ورسائل `NOTICE: ... does not exist, skipping` طبيعية.

### ٤‏.٤ استيراد البيانات — `auth` أولاً ثم بياناتنا، في وضع `replica`

```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 --single-transaction -c "set session_replication_role = replica" -f - < /srv/nzamy-restore/selfhost/02-auth-data.sql
```

```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 --single-transaction -c "set session_replication_role = replica" -f - < /srv/nzamy-restore/selfhost/03-data.sql
```

**لماذا `replica`:** يعطّل الـ triggers وفحوص المفاتيح الأجنبية أثناء التحميل. وبدونه سيحاول `on_auth_user_created` إنشاء ملف تعريف لكل مستخدم يُحمَّل فيتعارض مع صفوف `profiles` القادمة في `03-data.sql` — وهو أيضاً ما يعالج تحذيرات الـ circular FK التي رأيتها في `backup.log`.

**والترتيب إلزامي:** `02-auth-data.sql` قبل `03-data.sql`، لأن `profiles` وأخواتها تشير إلى `auth.users`.

وإن ظهر `permission denied to set parameter "session_replication_role"`، فأعد الأمرين نفسيهما بـ`-U supabase_admin` (بيانات فقط؛ وملكية الجداول لا تتأثر).

### ٤‏.٥ مطابقة أعداد الصفوف

```bash
docker exec -i supabase-db psql -U postgres -d postgres -X -q -v ON_ERROR_STOP=1 --csv -f - < /srv/nzamy-restore/sql/01-row-counts.sql > /srv/nzamy-restore/counts-selfhost.csv
```

```bash
diff <(grep -vE '^(realtime|net|storage|auth,(sessions|refresh_tokens|audit_log_entries|flow_state|one_time_tokens|mfa_challenges|mfa_amr_claims|saml_relay_states|schema_migrations))' /srv/nzamy-restore/01-row-counts.csv) <(grep -vE '^(realtime|net|storage|auth,(sessions|refresh_tokens|audit_log_entries|flow_state|one_time_tokens|mfa_challenges|mfa_amr_claims|saml_relay_states|schema_migrations))' /srv/nzamy-restore/counts-selfhost.csv) && echo "COUNTS IDENTICAL"
```

`storage.objects` سيختلف حتى تنتهي الخطوة ٤‏.٦؛ وجداول `auth` المؤقتة و`realtime`/`net` مستثناة عمداً.

وأربعة فحوص بنيوية:

```sql
select tgname from pg_trigger where tgrelid = 'auth.users'::regclass and not tgisinternal;
```

```sql
select id, public from storage.buckets;
```

```sql
select schemaname, tablename from pg_publication_tables where pubname = 'supabase_realtime';
```

```sql
select version, name from supabase_migrations.schema_migrations order by version;
```

المتوقَّع: `on_auth_user_created` · الحاويتان `documents` (خاصة) و`blog-covers` (عامة) · و`notifications` و`chat_messages` في نشر الـ realtime.

### ٤‏.٦ حاويات التخزين وملفاتها

الحاويتان تأتيان مع `01-schema.sql` (القسم `[B]`)، لكن **الملفات** لا. أنشئ على جذر الريبو ملف `.env.storage-copy` (أضفه إلى `.git/info/exclude` — ولا يُرفع أبداً) بأربعة أسطر:

```
OLD_SUPABASE_URL=<https://المشروع-المستضاف.supabase.co>
OLD_SERVICE_ROLE_KEY=<مفتاح service_role للمشروع المستضاف>
NEW_SUPABASE_URL=<https://عنوان-بوابة-الـAPI-للاستضافة-الذاتية>
NEW_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY من .env الخاص بالاستضافة الذاتية>
```

(الأسماء الأربعة من ترويسة `copy-storage.mjs:13-16`. و`NEW_SUPABASE_URL` هو عنوان **بوابة الـAPI** للاستضافة الذاتية — وهي في `self-hosted/v0.8.1` **Envoy** افتراضاً، وKong متاح كتجاوز اختياري بـ`sh run.sh config add kong` (التوثيق الرسمي — `docker/README.md`)؛ وترويسة `copy-storage.mjs:15` و`RESTORE-SELFHOST.md` القسم ٧ تسمّيانها Kong لأنهما كُتبا على إصدار أقدم — والعنوان هو نفسه في الحالتين: `SUPABASE_PUBLIC_URL`/`API_EXTERNAL_URL`. ويجب أن يبدأ بـ`https://` — فالسكربت يرفض غير ذلك إلا على `localhost` أو `127.0.0.1` وحدهما: `copy-storage.mjs:42-44`.)

ثم، **من جذر الريبو**، بترتيب: عرض ← نقل ← مقارنة.

```powershell
node --env-file=.env.storage-copy .\scripts\selfhost\copy-storage.mjs --dry-run
```

```powershell
node --env-file=.env.storage-copy .\scripts\selfhost\copy-storage.mjs
```

```powershell
node --env-file=.env.storage-copy .\scripts\selfhost\copy-storage.mjs --verify
```

`--verify` يجب أن ينتهي بـ`VERIFY OK — every OLD object exists on NEW`. والإخفاقات تُكتب في `copy-storage-failures.json` بجوار السكربت؛ أعد التشغيل بعد إصلاح السبب — والملفات المنقولة تُتخطّى تلقائياً. والعدد المرجعي: **626** ملفاً.

> السكربت يستخدم مفتاح `service_role` على الطرفين **عن قصد**: فهو سكربت نقل إداري، وليس كود تطبيق.

### ٤‏.٧ سياسات bucket `documents` — **خطوة شرطية**

سياسات `storage.objects` جاءت مع `01-schema.sql` (القسم ٣‏.٢). **فلا تُعِد تطبيقها بلا سبب.** نفّذ `_verify.sql` أولاً (الخطوة ٤‏.٨)، وانظر صفَّي التقرير:

* `documents owner-only storage policies (expect 4 …)` ← يجب أن يقرأ **4**
* `other storage.objects policies that can match bucket documents (expect 0)` ← يجب أن يقرأ **0**
* والصفّ الثالث `storage.objects policies (for the record)` سيسرد بعد الاستيراد **ستّ** سياسات بأسماء `flreew_` كلها موسومة `owner-only` — وهذا هو المتوقّع (القسم ٣‏.٢)، لا خلل.

وإن لم يقرأ الصفّان الأولان `4` و`0`، فعندئذ **فقط** طبّق `supabase/storage_policies_documents.sql`. وهو ملف **مُتَّسق التكرار (idempotent)** يبني قائمة الحذف **بالمضمون لا بالاسم**: يمسح كل سياسة على `storage.objects` ليست إحدى الأربع المسمّاة فيه ويَرِد في تعبيرها `documents` أو لا يَرِد فيها أي `bucket_id` إطلاقاً، ويترك ما هو مقصور على حاوية أخرى (`storage_policies_documents.sql:79-107`)، ثم يُنشئ الأربع بأسمائها الصريحة (`:114-140`). فتشغيله على قاعدة مستورَدة يستبدل بالستّ `flreew_` أربعاً بأسماء نظيفة — وهي نتيجة سليمة لا عطل. و`storage.objects` مملوك للدور `supabase_storage_admin`، فلا يكفي `postgres` (الخطأ المتوقّع `42501: must be owner of table objects` — موثّق في ترويسة الملف نفسه وفي `supabase/APPLY_MIGRATIONS_GUIDE.md`).

الملف موجود على الخادم داخل الريبو نفسه بعد `git pull` (لم يُنقل في ٤‏.١ ولا يحتاج نقلاً)، وإعادة التوجيه `<` تُقرأ على المضيف لا داخل الحاوية:

```bash
docker exec -i supabase-db psql -U supabase_storage_admin -d postgres -v ON_ERROR_STOP=1 < /www/wwwroot/nzamy/latest-nzamy-full/supabase/storage_policies_documents.sql
```

> **يُتحقق عند التنفيذ:** هذه الصياغة **مُركَّبة**، لا منقولة من العُدّة. `RESTORE-SELFHOST.md` لا يحتوي أمراً بدور `supabase_storage_admin`، و`README-ORDER.md:28` يصل إلى هذا الدور عبر pooler المشروع المستضاف بكلمة مرور، ويحذّر صراحةً من `password authentication failed for user "supabase_storage_admin"`. وهل ينجح الاتصال المحلي بهذا الدور داخل الحاوية يتوقّف على `pg_hba` الخاص بالصورة — وهو ما لا يمكن إثباته من هنا.
>
> **البديل المضمون إن فشل الأمر:** واجهة Studio الخاصة بالاستضافة الذاتية → **Storage** → **Policies** → bucket `documents`: امسح كل سياسة وَسَمَها صفّ `for the record` بـ`OTHER` (أي كل ما ليس مقصوراً على المالك)، وإن أردت البدء من صفحة نظيفة فامسح معها الستّ `flreew_` أيضاً، ثم **New policy → For full customization** أربع مرات، سياسة لكل عملية (SELECT / INSERT / UPDATE / DELETE) بنفس التعبير `bucket_id = 'documents' AND auth.uid()::text = (storage.foldername(name))[1]` (الخطوات بالتفصيل في `README-ORDER.md:32-46`). ثم أعد `_verify.sql` — لا بد أن يقرأ `4` و`0`.

ونموذج السياسة: كل ملف يُخزّن تحت مجلد اسمه معرّف مالكه، فـ`auth.uid()::text = (storage.foldername(name))[1]` يحصر كل مستخدم في ملفاته. وعند الإنشاء اليدوي: سياسة واحدة لكل عملية (SELECT/INSERT/UPDATE/DELETE)، فتعليم عمليتين معاً في واجهة الداشبورد يولّد سياستين بلاحقتي `_0`/`_1` بنفس التعبير، وهو ما ولّد `flreew_1` (`README-ORDER.md:44`). **وانتبه:** الثغرة القديمة لم تكن في التكرار نفسه بل في التعبير — إذ كانت `flreew_0`/`flreew_1` تشترطان `bucket_id = 'documents'` وحده بلا شرط المالك، فيقرأ أي مستخدم مسجّل ملفات غيره ويمسحها (`REVIEW-2026-09-21.md:16-18`). أمّا الستّ المكرّرة التي تصل مع الاستيراد اليوم فتحمل شرط المالك، ولذلك تمرّ من البوّابة بلا ضرر.

### ٤‏.٨ البوّابة النهائية — `_verify.sql`

من نسخة الريبو على الخادم بعد `git pull`، وبالمسار المطلق كما في ٤‏.٧ حتى لا يعتمد الأمر على مجلدك الحالي:

```bash
PGOPTIONS="-c nzamy.env=production" docker exec -i -e PGOPTIONS supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /www/wwwroot/nzamy/latest-nzamy-full/supabase/migrations/_verify.sql
```

**ما يجب أن يظهر:**

1. **لا `EXCEPTION` على الإطلاق.** الملف لا يكتب شيئاً دائماً في القاعدة (يُنشئ `TEMP VIEW` ثم يحذفه — `_verify.sql:254` و`:319`)، وكل `RAISE EXCEPTION` فيه يعني أن شيئاً في الاستيراد سقط. وهذا هو المعيار الحاسم.
2. الصفّان أعلاه عن `documents` يقرآن **4** و**0** (`README-ORDER.md:48`).
3. `platform_settings.payments_gateway status` يقرأ `disabled` أو `live` — لا `test` ولا `stub` (ومع `nzamy.env=production` يصير هذا الشرط **قاتلاً** وليس تحذيراً).
4. صفوف منح `library`: `library.article_regulations readable by anon` و`… writable by service_role` و`schema library default privileges set` — وهي ما أضافه ترحيل `20260922_01`، وبها نتأكد أن `run3` التُقطت بعد ترحيلات ٢٢ سبتمبر.

> **تصحيح:** `RESTORE-SELFHOST.md` يقول «المتوقع: 23 صف تقرير». وهذا الرقم **قديم**: فالملف عُدِّل بعده مرتين (`8727d2d` ثم `ca0b22b`)، وفيه **27** صفّ `AS check` عند الإصدار `ca0b22b` (والعدد يتغيّر مع كل ترحيل يضيف فحصاً) بالإضافة إلى صفوف `SELECT * FROM classified` المتغيّرة العدد. **فلا تستعمل عدد الصفوف كبوّابة** — استعمل «لا `EXCEPTION`» + الصفوف الأربعة أعلاه.

---

## ٥. الخطوة (د) — تحويل التطبيق إلى القاعدة الجديدة

### ٥‏.١ المتغيّرات التي تتغيّر على الـ VPS

الملف الذي يقرأه التطبيق الحيّ هو **`/www/wwwroot/nzamy/latest-nzamy-full/.env.local`** — لا `.env.vps` (فهو قالب على جهاز المطوّر، و`.gitignore` يستبعد `.env.*` فلا يصل مع `git pull`). وهذا منصوص عليه في `DEPLOY_AND_SMOKETEST_RUNBOOK.md` القسم A2.

| المتغيّر | يتغيّر؟ | القيمة الجديدة |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | **نعم** | `<https://عنوان-بوابة-الـAPI-للاستضافة-الذاتية>` — وهي Envoy افتراضاً في `v0.8.1`، أو Kong |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | **نعم** | `<ANON_KEY>` الجديد |
| `SUPABASE_SERVICE_ROLE_KEY` | **نعم** | `<SERVICE_ROLE_KEY>` الجديد |
| `SUPABASE_URL` | **نعم — إن كان مضبوطاً** | نفس عنوان بوابة الـAPI. ⚠️ مسار `src/app/api/client-pricing/route.ts:8` يقرأ `process.env.SUPABASE_URL ?? process.env.NEXT_PUBLIC_SUPABASE_URL` — فقيمة قديمة باقية هنا تُبقي هذا المسار وحده يقرأ من المشروع القديم بصمت |
| `SUPABASE_DB_URL` | **لا يستخدمه التطبيق** | لا وجود له في `src/` إطلاقاً؛ وهو متغيّر أدوات النسخ الاحتياطي في `.env.local` على جهاز المطوّر. حدّثه هناك فقط إن أردت نسخ الاستضافة الذاتية لاحقاً |
| `CRON_SECRET` | **لا يتغيّر** | كما هو — لا علاقة له بـSupabase |
| `N8N_WEBHOOK_SECRET` و`N8N_WEBHOOK_BASE_URL` | لا تتغيّران | كما هما |

وفي n8n: حدِّث أي عقدة تحمل مفتاح Supabase أو عنواناً على `*.supabase.co`.

### ٥‏.٢ إعادة البناء والنشر

```bash
cd /www/wwwroot/nzamy/latest-nzamy-full && git pull origin main
```

```bash
nano .env.local
```

```bash
npm run build
```

```bash
pm2 reload nzamy
```

```bash
rm -rf /www/server/nginx/proxy_cache_dir/*
```

```bash
pm2 logs nzamy --lines 20
```

**المتوقَّع:** `✓ Ready in ...` بلا أخطاء إقلاع. (أوامر البناء وإعادة التحميل من `DEPLOY_AND_SMOKETEST_RUNBOOK.md` القسم A؛ ومسار ذاكرة nginx المؤقتة من `deployment_guide.md:711`.)

> **لا تستعمل `npx supabase db push`.** فخطوة `db push` في `deploy.sh` تنكسر على تكرار البادئة الرقمية في أسماء الترحيلات (`20260906_…` مرتين، و`20260921_01..04`) فتصطدم بـ`duplicate key … schema_migrations_pkey`. والترحيلات تُطبَّق يدوياً بترتيب موثّق — `RESTORE-SELFHOST.md` القسم ٨ و`README-ORDER.md`.

### ٥‏.٣ فحوص الدخان (بالترتيب، وكلها إلزامية)

| # | الفحص | المتوقَّع |
|---|---|---|
| 1 | تسجيل دخول بحساب حقيقي | ينجح، ولوحة التحكم تفتح ببيانات حقيقية (وكل الجلسات القديمة منتهية — وهذا متوقَّع) |
| 2 | فتح صفحة نظام: `/laws/<slug>` | **تظهر مواد النظام**، لا صفحة فارغة. وهذا هو الفحص الذي يكشف سقوط منح `library` (`20260922_01`) |
| 3 | رفع مستند من لوحة التحكم ثم تنزيله | ينجح للمالك؛ وحساب آخر لا يصل إليه |
| 4 | مسار الدعوات بلا جلسة | `401` |
| 5 | نبضة الـcron | `200` بالسرّ الصحيح، و`401` بدونه |

```bash
curl -i -s "https://<النطاق>/api/v1/me/invitations" | head -1
```

يجب أن يعيد `401`. تحقّقنا من المصدر: `src/app/api/v1/me/invitations/route.ts:95` يستدعي `assertRole()`، و`src/lib/auth/assertRole.ts:38` يردّ `401` للمتصل غير المُصادَق.

```bash
curl -X POST -H "x-cron-secret: <CRON_SECRET>" https://<النطاق>/api/v1/cron/deadlines
```

(الصيغة من ترويسة `src/app/api/v1/cron/deadlines/route.ts:44-50`. والسرّ الخاطئ أو الغائب = `401`، فالمسار يفشل مغلقاً.)

```bash
curl -s "https://<النطاق>/api/v1/lawyers" | head -c 600
```

**المتوقَّع:** لا `phone` ولا `email` في الرد (فحص تسرّب PII — `DEPLOY_AND_SMOKETEST_RUNBOOK.md` القسم E).

---

## ٦. الخطوة (هـ) — التراجع

القاعدة الحاكمة: **لا يُمَسّ المشروع المستضاف حتى تنجح كل فحوص القسم ٥‏.٣.** فهو طريق الرجوع الوحيد، ولا شيء في هذا الدليل يكتب فيه حرفاً (فالنسخ قراءة فقط، والنقل في اتجاه واحد).

1. **أثناء النقل:** أوقف الكتابة على القاعدة القديمة (`pm2 stop nzamy` + تعطيل مسارات n8n الكاتبة) قبل نسخة `run3` النهائية، حتى لا تُكتب صفوف بعد لحظة النسخ فتضيع.
2. **إن فشل أي فحص:** أعِد المتغيّرات الأربعة في `.env.local` إلى قيم المشروع المستضاف، ثم:

```bash
npm run build && pm2 reload nzamy && rm -rf /www/server/nginx/proxy_cache_dir/*
```

والرجوع فوري وكامل لأن القاعدة القديمة لم تتغيّر ولم تتوقف. والأثر الوحيد: من سجّل دخوله على النظام الجديد يسجّل دخوله مرة أخرى.

3. **إن أردت إعادة المحاولة على الاستضافة الذاتية:** `docker compose down -v` (يمسح الـ volumes) ثم `up -d`، وابدأ من ٤‏.٢. فاستيراد على قاعدة غير فارغة ينتج `relation "…" already exists`.
4. **بعد النجاح:** اترك مشروع Supabase المستضاف **Paused وليس Deleted** لمدة **أسبوعين** كطريق رجوع، ثم احذفه (`RESTORE-SELFHOST.md` القسم ٩‏.٣).

---

## ٧. الخطوة (و) — جدول التحقق

| # | البند | البوّابة | ✔ |
|---|---|---|---|
| 1 | استضافة ذاتية من `supabase/docker` الرسمي وبأحدث الصور | `docker compose pull` نجح | ☐ |
| 2 | تطابق إصدار Postgres (17 افتراضياً) — أو معالجة `MAINTAIN` إن كان 15/16 | `select version()` | ☐ |
| 3 | `.env` للاستضافة الذاتية مكتمل بمفاتيح **جديدة** (قرار السؤال 8) | `sh run.sh start` بعد اكتمال `.env` — والخدمات قائمة | ☐ |
| 4 | نسخة `run3` بعد ترحيلات ٢٢ سبتمبر — **لا تُستعمل `run2`** | `MANIFEST.txt` + لا ملف بحجم صفر | ☐ |
| 5 | الأدوار الخمسة موجودة و`public` فارغ | فحوص ٤‏.٢ | ☐ |
| 6 | `selfhost/01-schema.sql` | صفر `ERROR` | ☐ |
| 7 | `selfhost/02-auth-data.sql` ثم `03-data.sql` في وضع `replica` | صفر `ERROR` | ☐ |
| 8 | مطابقة أعداد الصفوف | `COUNTS IDENTICAL` | ☐ |
| 9 | المُشغِّل والحاويتان ونشر الـrealtime | `on_auth_user_created` · `documents`+`blog-covers` · `notifications`+`chat_messages` | ☐ |
| 10 | ملفات التخزين | `VERIFY OK` (626 ملفاً) | ☐ |
| 11 | `_verify.sql` بـ`nzamy.env=production` | **لا `EXCEPTION`**، وصفّا `documents` = 4 و0 | ☐ |
| 12 | متغيّرات `.env.local` على الـVPS (٤ متغيّرات؛ ولا تنسَ `SUPABASE_URL`) | `grep` يؤكّد القيم الجديدة | ☐ |
| 13 | `npm run build` + `pm2 reload` + مسح ذاكرة nginx | `✓ Ready` | ☐ |
| 14 | فحوص الدخان الخمسة | كلها كما في ٥‏.٣ | ☐ |
| 15 | المشروع المستضاف **Paused** لا Deleted، لأسبوعين | — | ☐ |

---

## ٨. أعطال متوقّعة ومعانيها

| الرسالة | السبب | الحل |
|---|---|---|
| `invalid command \restrict` | psql قديم على الهدف مع dump حديث | استعمل ملفات `selfhost/` (فالأسطر محذوفة منها) |
| `unrecognized configuration parameter "transaction_timeout"` | عميل 17 ← خادم 15/16 | نفس الحل (معلّق في `selfhost/`) |
| `syntax error at or near "MAINTAIN"` | نسخة `selfhost/` وُلِّدت قبل تحديث السكربت (حالة `run2`) | ولّد `run3` بالسكربت الحالي — القسم ١‏.٢ |
| `column "…" of relation "users" does not exist` عند ٤‏.٤ | GoTrue على الاستضافة الذاتية أقدم من المستضاف | `docker compose pull && docker compose up -d` ثم أعد ٤‏.٤ |
| `permission denied to set parameter "session_replication_role"` | `postgres` ليس superuser على الهدف | أعد ٤‏.٤ بـ`-U supabase_admin` |
| `role "…" does not exist` | دور مخصّص من القسم `[H]` | `create role …;` قبل ٤‏.٣ |
| `must be member of role "supabase_admin"` | سطر `ALTER DEFAULT PRIVILEGES` الأصلي | استعمل `selfhost/01-schema.sql` (معلّق فيه) |
| `relation "…" already exists` | الهدف غير فارغ | `docker compose down -v` وابدأ من ٤‏.٢ |
| `schema "extensions" does not exist` | الصورة ليست `supabase/postgres` | استعمل `supabase/docker` الرسمي |
| `42501: must be owner of table objects` | محاولة تطبيق سياسات التخزين بدور `postgres` | `-U supabase_storage_admin` — القسم ٤‏.٧ |
| `duplicate key … schema_migrations_pkey` | `supabase db push` مع بادئات ترحيل مكرّرة | لا تستعمل `db push` — القسم ٥‏.٢ |

(الجدول أعلاه موسَّع من جدول الأعطال في `scripts/selfhost/RESTORE-SELFHOST.md`.)

---

## ٩. ملفات هذا الدليل

| الملف | الغرض |
|---|---|
| `scripts/selfhost/backup-supabase.ps1` | النسخة الاحتياطية من Windows |
| `scripts/selfhost/backup-supabase.sh` | النسخة الاحتياطية من Linux أو الـVPS أو حاوية |
| `scripts/selfhost/sql/00-server-info.sql` | حقائق المصدر (قراءة فقط) |
| `scripts/selfhost/sql/01-row-counts.sql` | أعداد الصفوف — مرجع المطابقة |
| `scripts/selfhost/sql/02-export-extras.sql` | ما يعيش خارج مخطَّطاتنا (triggers و buckets و policies و realtime) |
| `scripts/selfhost/copy-storage.mjs` | نقل ملفات التخزين |
| `scripts/selfhost/README-BACKUP.md` | ملاحظات العُدّة الأصلية عن النسخ |
| `scripts/selfhost/RESTORE-SELFHOST.md` | ملاحظات العُدّة الأصلية عن الاسترجاع (المرجع التفصيلي) |
| `supabase/migrations/_verify.sql` | البوّابة بعد الاستيراد |
| `supabase/storage_policies_documents.sql` | إعادة تأكيد سياسات `documents` — خطوة شرطية |
