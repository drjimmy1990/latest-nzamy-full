# تقرير الفحص الشامل وتجهيز الجوال — 11 أغسطس 2026

> فحص كامل للمشروع (التخطيط: Fable 5 — التنفيذ: وكلاء Sonnet 5)
> النتيجة النهائية: `tsc` صفر أخطاء · `next build` ناجح · نسخة سطح المكتب جاهزة بمشروع أندرويد مولّد

---

## 🔴 أولاً: أخطر ما وُجد — وتم إصلاحه

### 1) الكود كان لا يُبنى إطلاقاً
ملف عرض مواد الأنظمة `src/app/laws/[slug]/_article-components.tsx` كان فيه أسطر مكررة من merge خاطئ (السطور 580-582) تكسر الملف بالكامل — أي محاولة build كانت ستفشل. **أُصلح وتحقق منه ببناء إنتاجي كامل ناجح.**

### 2) المفاتيح السرية كانت داخل git ⚠️ (يتطلب إجراء منك)
ملفا `env.local` و`env.vps` — وفيهما مفتاح **Supabase Service Role** (يتخطى كل الصلاحيات) ومفتاح WhatsApp/Evolution — كانا مرفوعين داخل git في commit «wip» (`7319732`).

**ما تم:** أزيلا من التتبع (باقيان على جهازك — لن يتعطل شيء)، وأضيفا لـ `.gitignore`.
**الخبر الجيد:** هذا الـ commit **محلي فقط ولم يُرفع** إلى GitHub.
**المطلوب منك (مهم):**
1. الأفضل تدوير المفاتيح احتياطاً: لوحة Supabase → Settings → API → **Rotate service_role key**، ثم حدّث `.env.local` هنا وعلى السيرفر.
2. **token جيت هاب مكشوف**: ملف `.git/config` يحتوي توكن `ghp_…` بشكل مكشوف. ادخل GitHub → Settings → Developer settings → Tokens → أنشئ توكن جديد واحذف القديم.
3. قبل أي `git push` لهذا الفرع: التاريخ المحلي ما زال يحوي المفاتيح — نظّفه أولاً (بأداة `git filter-repo`) أو اطلب تنفيذ ذلك في جلسة قادمة.

---

## 🧠 ثانياً: حقيقة «عقل المشروع» (منطقة الذكاء الاصطناعي)

فحصنا كل أدوات `src/app/ai/**` (43 أداة، ~22,000 سطر) سطراً سطراً:

- **كل الأدوات واجهات عرض تجريبية**: المخرجات ثابتة (`MOCK_` + مؤقّت `setTimeout`) ولا توجد أي أداة تتصل بنموذج ذكاء اصطناعي حقيقي. لا توجد أي مفاتيح LLM في المشروع (وهذا صحيح أمنياً — التصميم يضعها في n8n).
- **الاتصال الحقيقي الوحيد**: مسارا المكتبة `/api/ai/library-chat` و`/api/ai/explain-article` عبر n8n — **وn8n نفسه غير مستضاف بعد**، فيظهران رسالة «قريباً» الصادقة.
- **بوابة المراجعة التجريبية** (`BETA_REVIEW_MODE = true`) تخفي المخرجات المفبركة عن المستخدمين الحقيقيين — قرار صحيح.
- الأدوات الحقيقية فعلاً (غير AI): حاسبة الأتعاب، حاسبة المواعيد، مولّد إنذارات التحصيل، منظّف ناجز، محرك تقييم المخاطر — تعمل بمنطق حقيقي على مدخلات المستخدم.

**ما أُصلح في هذه الجلسة:**
| المشكلة | الإصلاح |
|---|---|
| زر «أرسل للمراجعة» كان وهمياً (يَعِد بمراجعة بشرية ولا يرسل شيئاً) | أصبح يرسل طلباً حقيقياً إلى جدول جديد `ai_review_requests` (migration جاهز) مع حالة تحميل وخطأ صادق عند الفشل |
| شارة «بحث حي» في نظامي عالمي (البحث ليس حياً) | أصبحت «نسخة تجريبية» بصياغة صادقة |
| أسماء نماذج مزيفة معلنة (GPT-4·Gemini·Claude / Whisper) | استُبدلت بـ«قيد التفعيل» |
| لا حد لمعدل الطلبات على مساري AI (خطر تكلفة/إساءة) | حد 20 طلب/دقيقة لكل IP + سقف أطوال المدخلات + رد 429 عربي |

**قرارك المطلوب لاحقاً (لم نغيّره):** ربط الأدوات الـ43 فعلياً يتطلب استضافة n8n وربط مزوّد LLM — هذا مشروع البنية التحتية القادم، والكود جاهز له (18 متغير webhook محجوز في `.env.example`).

---

## ✅ ثالثاً: المشاكل القائمة التي أُصلحت (من قائمة يوليو + مكتشفات جديدة)

| # | المشكلة | الحالة |
|---|---|---|
| 1 | إنشاء مجموعة يفشل دائماً («تعذّر إنشاء المجموعة») — إدخال NULL في عمود لا يقبله | ✅ أُصلح |
| 2 | رابط الدعوة «غير صالح» دائماً — الأكواد لم تكن تُسجل في قاعدة البيانات إطلاقاً | ✅ أُصلح (مسار مزامنة جديد + تحقق + صلاحية سنة) |
| 3 | قرارات الإشراف المجتمعي لا تنعكس على `/community` (الحذف/الإخفاء بلا أثر) | ✅ أُصلح (فلترة الحالة في المسار العام) |
| 4 | البث الإداري لا يصل لأحد + لا يوجد خيار «الأفراد/العملاء» | ✅ أُصلح (توزيع حقيقي على جرس الإشعارات + الخيار الجديد) |
| 5 | سجل مدفوعات الأدمن: نصوص متداخلة + بيانات وهمية تظهر كأنها حقيقية | ✅ أُصلح (قصّ النصوص + إزالة الوهمي + حالات فارغة صادقة) |
| 6 | «ابحث عن محامي» لقطاع micro: قائمة وهمية 100% | ✅ رُبط بقاعدة البيانات الحقيقية |
| 7 | **مكتشف جديد**: بيانات المحامين الحقيقية كانت ستظهر فارغة في كل صفحات البحث والحجز (عدم تطابق شكل البيانات بين الخادم والواجهة) | ✅ أُصلح من الجذر في `lawyerService` بتوثيق من مخطط قاعدة البيانات — بدون تقييمات مفبركة |
| 8 | صورة المشاركة `og-image.png` كانت JPEG بامتداد PNG + أبعاد معلنة خاطئة | ✅ حُوّلت لـ PNG حقيقي + صُححت الأبعاد في موضعين |

**كان مُصلحاً من قبل (تحققنا):** صلاحيات حذف المجلدات، حماية عناصر المجلدات الذكية، إزالة مبدّل الأدوار من الإنتاج، أزرار الترقية في `/pricing`.

---

## 📱 رابعاً: تجهيز الجوال

### في المنصة نفسها (تعمل الآن كـ PWA أيضاً)
- `manifest.json` عربي RTL + طقم أيقونات كامل (192→512 + maskable + Apple 180)
- Service Worker آمن (لا يخزّن API أو صفحات — شبكة أولاً) + صفحة «لا يوجد اتصال» عربية
- مساحات الأمان للنوتش (الناف بار + الأزرار العائمة)
- زر الإدخال الصوتي يختفي تلقائياً حيث لا يوجد دعم (بدل تنبيه مزعج)
- **تم التحقق حياً**: الـ SW سجّل وتفعّل، الملفات كلها ترد 200

### نسخة سطح المكتب — `C:\Users\Judge\Desktop\NZAMY-Mobile`
نسخة كاملة (بكل تاريخ git وكل الإصلاحات) + فوقها:
- إعداد Capacitor كامل (`sa.nezamy.app` / «نظامي») بوضع server.url → أي تحديث تنشره على الموقع يظهر في التطبيق فوراً بدون تحديث متجر
- **مشروع أندرويد مولّد وجاهز** (`android/`) + 100 أيقونة وشاشة افتتاحية أصلية مولّدة
- **`دليل_تطبيق_الجوال.md`**: خطوات النشر كاملة بالعربي (Android Studio → AAB → Google Play، وiOS على Mac → Xcode → App Store، مع تنبيه سياسة Apple 4.2 وكيفية تقويتها بالإشعارات)
- `BACKUP_INFO.md`: توثيق النسخة وطريقة الاستعادة

**تقييم الجاهزية المعماري:** المصادقة بالكوكيز والروابط النسبية — كلها سليمة داخل WebView بدون أي تغيير. نقاط تُختبر على جهاز حقيقي: أزرار الطباعة/PDF وتنزيل الملفات (مذكورة في الدليل).

---

## 📋 خامساً: قائمة أعمالك المتبقية (بالأولوية)

1. **الأمن (الآن):** تدوير مفتاح Supabase + توكن GitHub (الخطوات أعلاه).
2. **النشر:** تطبيق الـ migrations المعلقة على Supabase — ومنها الجديد `20260811_ai_review_requests.sql` — ثم نشر الفرع على الخادم. *(ملاحظة: المكتبة تظهر فارغة حالياً للزوار — تحقق من إعداد `library_status` وتطبيق migrations يوليو إن لم تكن طُبقت.)*
3. **قرارات منتج معلقة:** استضافة n8n لتفعيل الذكاء الاصطناعي الحقيقي، اختيار بوابة الدفع، حلقة تحويل الإحالات (`/join?ref=` لا يُقرأ حالياً).
4. **جوال:** اتبع `دليل_تطبيق_الجوال.md` — أندرويد جاهز من هذا الجهاز؛ iOS يحتاج Mac.
5. **مؤجل موثّق:** إصلاح مسودة «الصائغ القانوني» (تتجاهل مدخلات المعالج)، أزرار «طبّق التعديل» الوهمية، توحيد ألوان الثيم (2000+ لون صريح)، fالترجمة الاحتياطية الفرنسية المضللة — كلها في تقارير المراجعة داخل هذه الجلسة.

---

## 🚀 الجولة الثانية (نفس اليوم): إكمال كل المعلق بخلاف الباك اند

بناءً على توجيهك، نُفذت 6 حزم إضافية بوكلاء Sonnet 5 (كلها متحقق منها بـ tsc وبناء إنتاجي):

### صفحة الإشعارات أصبحت حقيقية
- `/notifications` كانت بيانات وهمية بالكامل رغم وجود نظام إشعارات حقيقي. الآن: بيانات حقيقية بتجميع زمني عربي، تعليم مقروء فردي وجماعي، بوابة دخول، وحالات صادقة — **ورسائل البث الإدارية تظهر فيها فعلاً**. (زر الحذف أُخفي لأن الخادم لا يدعم الحذف أصلاً — كان زراً كاذباً).

### الصائغ القانوني توقف عن تجاهل مدخلاتك
- المسودة تُبنى الآن من بياناتك الفعلية (الأطراف، بيانات الحكم، الدفوع المؤكدة، النصوص المخصصة) — والفراغ يظهر «[أكمل هذا الجزء]» بدل وقائع مخترعة، مع سطر صراحة: «مسودة أولية مبنية على مدخلاتك».
- زر «طبّق التعديل» صار يُلحق تعديلك مرئياً بالبند (مع تراجع فعلي) بدل الاختفاء بلا أثر — في الصائغ ومحترف العقود معاً.
- مترجم العقود توقف عن عرض نص فرنسي تحت اسم أي لغة تطلبها — لغة غير مدعومة = رسالة صادقة «قيد التفعيل».
- إعدادات نظامي AI تُحفظ وتُستعاد فعلياً.

### حزمة التوجيه والإظهار (11 بنداً من قائمتك)
- روابط «ترقية الباقة» و«اشترك الآن» للعميل والمحامي تذهب لوجهاتها الصحيحة (شملت 3 أزرار إضافية مكتشفة توجه للمحفظة خطأً).
- «سؤال قانوني سريع» → المساعد الصحيح، والمحامون يُحوَّلون تلقائياً بعيداً عن أداة العملاء.
- بطاقة التصعيد للأفراد فقط · واتساب مخفي للأدمن · بانر البيتا أُزيل · LegalMail مخفي للمحامي المنفرد (وكان يتسرب من كارت ترويجي في السكرتير، ليس من القائمة) · المكتبة أُزيلت من قائمة العميل · ازدواجية «الإعدادات» حُلّت · شبكة فحص العقود: 4 + «أخرى» + «عرض المزيد».

### التسعير حسب نوع المستخدم
- `/pricing` تختار تلقائياً باقات نوعك عند تسجيل الدخول (الأنواع الثمانية كلها — الجهات الحكومية والجمعيات كانت مكسورة)، مع بقاء حرية التصفح. وأثناء العمل اكتُشف وأُصلح **انهيار hydration** كامن من محاولة قديمة.

### واجهة المكتبة (بقياس DOM حي)
- تداخل قائمة الجوال على `/laws`: فراغ علوي ناقص تحت الشريط الثابت — وُحّد مع بقية الصفحات.
- انضغاط المجلدات الذكية: صندوق تمرير 225px كان يبتلع النموذج — وُسّع ونموذج الإنشاء أصبح مرئياً دائماً.
- 12 موضع نص خافت بالوضع الليلي رُفع تباينها.

### SEO وأداء
- بيانات `LegalService` المهيكلة على الرئيسية (تحسين ظهور جوجل).
- خريطة الموقع تقرأ مقالات المدونة من قاعدة البيانات ديناميكياً (مع حماية من الفشل).
- الخطوط الثلاثة أصبحت مستضافة ذاتياً عبر `next/font` بنفس الأوزان — تحميل أسرع، وأصلح لتطبيق الجوال، وبطاقة الإنجازات (canvas) عُدلت لتوافقها.

---

## 🔧 ملاحظات تقنية

- لم نعمل أي `git commit` — كل الإصلاحات تعديلات عمل جاهزة لمراجعتك ثم الـ commit (مع ملاحظة تنظيف التاريخ أعلاه).
- فهرس GitNexus **أُعيد بناؤه في نهاية الجلسة** (10,551 رمزًا · 19,894 علاقة · 300 مسار) — أثناء الجلسة استُعيض عنه بتحليل أثر يدوي قبل كل تعديل. ملاحظة: إضافة البحث النصي الكامل (FTS) لم تُحمَّل لأن خادمها كان غير متاح؛ عند توفر الاتصال شغّل مرة واحدة: `npx gitnexus analyze --repair-fts`.
- التحقق النهائي: `tsc --noEmit` = 0 أخطاء · `next build` = نجاح · معاينة حية: الرئيسية والمكتبة وصفحة النظام تعمل بدون انهيار.
