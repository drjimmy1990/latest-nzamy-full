# دليل المالك — تحديث محتوى المكتبة القانونية والمدونة

**التاريخ:** ٢٥ سبتمبر ٢٠٢٦ · **إلى:** د. محمد

هذا الدليل يشرح **من أين يأتي المحتوى وكيف يُحدَّث**: المكتبة القانونية (الأنظمة والمواد والمبادئ والفقه)
والمدونة (المقالات وصور الأغلفة). كل الأرقام هنا قُرئت من قاعدة البيانات الجديدة يوم كتابته.

> **أين نحن اليوم:** المنصة تنتقل إلى خادم قاعدة بيانات خاص بنا (`auth.nezamy.sa`) بدل خدمة Supabase السحابية.
> المحتوى كله موجود على الخادم الجديد الآن. الموقع `nezamy.sa` يُحوَّل إليه عند الإطلاق (يتولّاه المطوّر).
> **البداية نظيفة:** الحسابات القديمة لا تنتقل — ستحتاج أنت حساباً جديداً على المنصة بعد الإطلاق.

---

## ١ · ما الموجود الآن على الخادم الجديد

| المحتوى | العدد | مصدره |
|---|---:|---|
| الأنظمة واللوائح والقرارات | ٥٬٩٠١ | حزمتك (٢٠ سبتمبر) |
| الأبواب والفصول | ١٢٬٦٢٩ | حزمتك |
| المواد | ١١٠٬٧٩٤ | حزمتك |
| تعديلات المواد | ٦٬٣٣٤ | حزمتك |
| اللوائح المرتبطة بالمواد | ١٥٬٣٣٦ | حزمتك |
| المراسيم والتعاميم / صفحاتها | ٣٬٣١٨ / ٧٬٧١١ | حزمتك |
| مجموعات الأحكام / المبادئ / فقرات المبادئ | ٢٠٩ / ١٨٬٩٨٣ / ١٬٤٤٣ | حزمتك |
| كتب الفقه / أبوابها / فصولها / نصوصها | ١٨٥ / ٩٥٬٦٦٨ / ١٣٩٬٤٠٤ / ٢٠٨٬٧٧٥ | حزمتك |
| مقالات المدونة | ٦١٦ (منها ٦١٤ بصورة غلاف) | مجلد المدونة |
| أقسام المدونة | ٣١ | نُقلت من القاعدة القديمة |

**كل جدول من جداول المكتبة الأربعة عشر يطابق ملفات حزمتك سطراً بسطر** — لم يسقط صف واحد.
(للمقارنة: القاعدة القديمة كان فيها ٣٨٦ نظاماً و١٣٬٤٣٦ مادة.)

**مقالتان بلا صورة غلاف** — تحتاجان منك صورة:
- `company-incorporation-saudi-arabia-2026` (مجلد `sec_04_commercial`) — توجد صورة باسم قريب
  `company-incorporation-saudi-arabia.webp` لمقالة أخرى بنفس الموضوع في `sec_05_commercial`. إما تنسخها
  بالاسم الجديد، أو ترسل صورة مختلفة.
- `saudization-administrative-support-professions-2026` (مجلد `sec_06_labor`) — لا صورة لها أصلاً.

---

## ٢ · قبل أي أمر: ملف المفاتيح

كل أوامر التحديث تحتاج ملفاً اسمه `.env.local` في مجلد المشروع، فيه عنوان الخادم ومفتاح الخدمة
(`SUPABASE_SERVICE_ROLE_KEY`). **هذا المفتاح يفتح قاعدة البيانات كلها بلا قيود:**

- يسلّمه لك المطوّر مباشرة — لا يُرسل في واتساب ولا إيميل ولا يُلصق في محادثة.
- لا يُرفع إلى git أبداً (الملف مستثنى تلقائياً، فلا تغيّر ذلك).
- إن شككت أنه انكشف، أخبر المطوّر فوراً ليغيّره.

إن لم يكن المفتاح عندك، أرسل المحتوى الجديد للمطوّر ويشغّل هو نفس الأوامر.

---

## ٣ · المكتبة القانونية

### من أين يقرأ الأمر
من مجلد حزمتك (فيه `evidence/full/rows/*.jsonl` — ١٤ ملفاً). **احفظ الحزمة خارج مجلد المشروع**
(مثلاً `D:\nzamy-packages\2026-09-20`) وأخبر الأمر بمكانها:

```bash
# ١) تجربة بلا كتابة: يقرأ كل الملفات ويعدّ الصفوف ويبلّغ عن أي سطر تالف
npm run library:seed:owner -- --dry --rows "D:\nzamy-packages\2026-09-20\evidence\full\rows"

# ٢) التحميل الفعلي (آمن لإعادة التشغيل: يحدّث الموجود ويضيف الجديد، لا يكرّر)
npm run library:seed:owner -- --rows "D:\nzamy-packages\2026-09-20\evidence\full\rows"

# ٣) مقارنة القاعدة بملفاتك جدولاً جدولاً
npm run library:counts
```

- جدول واحد فقط: أضف `--table laws` (أو `articles`، `principles`، …).
- الاستئناف من جدول معيّن بعد انقطاع: أضف `--from articles`.
- في النهاية يطبع الأمر جدولاً بعدد المقروء والمكتوب والمتجاوز لكل جدول. **إن تجاوز صفاً واحداً ينتهي
  بخطأ ويذكر السبب** — لا يقول «اكتمل بنجاح» وهو ناقص.
- لا تستخدم أبداً `library:reseed:wipe` — يمسح معه علامات المستخدمين المحفوظة.

**ملاحظة لاحقة:** ثلاثة حقول في حزمتك (مراجعة بشرية مطلوبة وسببها، وعلامة الصفحة المصطنعة في الفقه)
لا تُحمَّل اليوم لأن أعمدتها لم تُضف بعد في القاعدة. عندما يضيفها المطوّر يكفي إعادة تشغيل أمر التحميل مرة.

---

## ٤ · المدونة — طريقتان

### الطريقة (أ): دفعة كاملة من الملفات — للمقالات الكثيرة
المحتوى في مجلد `blog-toolkit/blog_final/` داخل المشروع:

```
blog_final/
├── sec_00_procedural/ … sec_30_culture/   ← كل مقالة ملف .md (العنوان والبيانات في أعلاه، ثم النص)
├── images_delivered/                       ← صور الأغلفة .webp — اسم الصورة = اسم المقالة (slug)
└── blog_images_registry.json               ← ربط كل مقالة بصورتها
```

```bash
# ١) الصور: تجربة ثم رفع (يرفع إلى الخادم، والتكرار آمن)
npm run blog:images -- --dry
npm run blog:images

# ٢) المقالات: تجربة ثم تحميل (يحدّث المقالة إن كانت موجودة بنفس الاسم، ويضيفها إن كانت جديدة)
npm run blog:seed -- --dry
npm run blog:seed

# قسم واحد فقط بعد تعديله
npm run blog:images -- --sections sec_07_labor
npm run blog:seed   -- --sections sec_07_labor
```

- **لا تستخدم `blog:reseed` ولا `blog:clear` لتحديث عادي** — الاثنان **يمسحان كل المقالات أولاً**.
  `blog:seed` وحده يكفي دائماً للتحديث.
- رابط الغلاف يُبنى تلقائياً من اسم المقالة، فإن لم تُرفع صورتها تظهر المقالة بلا غلاف.
- **أمر رفع الصور يقرأ من السجل `blog_images_registry.json` لا من المجلد.** مقالة جديدة لا تُرفع صورتها
  حتى يُضاف لها سطر في السجل (الاسم `slug`، ومسار الصورة `images_delivered/<slug>.webp`) — هذا بالضبط
  سبب المقالتين بلا غلاف في القسم ١. `npm run blog:images -- --dry` يعرض ما سيُرفع فعلاً، فراجعه قبل الرفع.

### الطريقة (ب): مقالة واحدة من لوحة الأدمن
`لوحة الأدمن ← المحتوى ← المقالات` (`/dashboard/admin/content/articles`): إنشاء، تعديل، نشر، أرشفة، حذف.
حقل الغلاف هنا **رابط** وليس رفع ملف: ارفع الصورة بالطريقة (أ) أو ضع رابطاً لصورة موجودة.

> **تنبيه مهم:** إن عدّلت مقالة من اللوحة ثم شُغّل `blog:seed` لاحقاً وفي الملفات مقالة **بنفس الاسم**،
> فنسخة الملفات تكتب فوق تعديلك. اختر لكل مقالة مكاناً واحداً تعدّلها فيه.

---

## ٥ · الصور: المكتبة غير المدونة

| النوع | صور؟ | لماذا |
|---|:-:|---|
| مواد الأنظمة (`library.articles`) | لا | نصوص رسمية خالصة |
| مقالات المدونة (`public.articles`) | نعم | لكل مقالة غلاف `.webp` في مخزن `blog-covers` |
| المراسيم والتعاميم | لا | نص الصفحات وبياناتها فقط |

(الجدولان يحملان الاسم نفسه `articles` لكنهما في مكانين منفصلين تماماً — مواد الأنظمة غير مقالات المدونة.)

---

## ٦ · git: لا تحتاجه لتحديث المحتوى

مجلد المدونة `blog_final` ومجلد حزمة المكتبة **مستثنيان من git عمداً** (حجمهما مئات الميغابايت).
تحديث المحتوى = الأوامر أعلاه فقط، بلا git.

إن عدّلت شيئاً في الكود أو الوثائق على فرعك `owner-edits`:
- **لا تستخدم أبداً `git add .`** — أضف الملفات التي عدّلتها بأسمائها:
  `git add "اسم_الملف.md"`.
- لا تضع `.env.local` ولا أي ملف فيه مفتاح أو كلمة مرور في git — المستودع عام.
