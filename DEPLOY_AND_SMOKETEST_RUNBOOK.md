# NZAMY — Deploy & Smoke-Test Runbook / دليل النشر والاختبار

> **الهدف / Goal:** نشر عمل هذه الجلسة (بوابات النزاهة Phase 0 + دفع n8n) والتحقق أن الحلقة تعمل حيّاً قبل بناء المزيد.
> **التاريخ / Date:** 2026-07-06 · **الخادم / Server:** PM2 app `nzamy`, port 3055 · **n8n:** `https://n8n.asra3.com`
> رتّب التنفيذ: **A ثم B ثم C ثم D ثم E**. كل خطوة فيها «المتوقّع / Expected» — لو خالفها أوقف وابحث.

---

## A — نشر التطبيق / Deploy the app

على الخادم داخل مجلد التطبيق:

```bash
cd /www/wwwroot/nzamy/latest-nzamy-full
git pull origin main

# 1) طبّق أي migration معلّق (منها الجديد 20260706_reminder_flags.sql)
npx supabase db push        # أو: npx supabase migration up --linked

# 2) تأكّد من متغيّرات البيئة في .env.local
#    - CRITICAL: يجب أن يكون backend = supabase
grep NEXT_PUBLIC_NZAMY_WORKFLOW_BACKEND .env.local     # ⇒ =supabase
#    - فعّل الدفع إلى n8n (اختياري الآن — يلزم لاختبار الحلقة من التطبيق)
#      أضِف هذين السطرين:
#      N8N_WEBHOOK_BASE_URL=https://n8n.asra3.com/webhook
#      N8N_WEBHOOK_SECRET=<اختر سرّاً قوياً>   (اختياري)

# 3) ابنِ وأعد التحميل
npm run build
pm2 reload nzamy

# 4) تحقّق من الإقلاع
pm2 logs nzamy --lines 20    # ⇒ يجب أن ترى: ✓ Ready in ...
```

**المتوقّع / Expected:** `✓ Ready`، ولا أخطاء `Could not find a production build` أو انهيار بدء التشغيل (لو `NEXT_PUBLIC_NZAMY_WORKFLOW_BACKEND` خاطئ، `instrumentation.ts` يوقف الإقلاع عمداً في الإنتاج).

---

## B — تحقّق بوابات النزاهة (Phase 0) حيّاً / Verify Phase 0 gates

افتح الموقع (كمستخدم عادي) وتحقّق:

| افحص / Check | المتوقّع / Expected |
|--------------|---------------------|
| `/ai/report-generator` → ولّد تقريراً | **بطاقة مراجعة** «تم الإعداد… إرسال للمراجعة الذكية» — **لا** تقرير مزيّف قابل للتنزيل |
| `/ai/smart-inspector` و `/ai/legal-opinion` (cross-exam/letter) | نفس بطاقة المراجعة على المخرج |
| `/marketplace` | **يُعاد توجيهه إلى `/services`** |
| `/services` | قسم «نخبة المستشارين» + رابط `/lawyers` **مختفيان** |
| `/dashboard/lawyer/cases/<id>/sharing` | صفحة **«قريباً»** (لا واجهة مشاركة وهمية) |
| `/dashboard/lawyer/activity` (محامٍ جديد) | **حالة فارغة** + إحصائيات «—» (لا تاريخ ملفّق) |
| `/dashboard/lawyer/profile` | **لا** تبويبَي «إنجازاتي» و«التقييمات» |

---

## C — تجهيز n8n / Set up n8n

**لا حاجة لأي متغيّر بيئة في n8n — أزلنا `$env` نهائياً.** عُقد الإرسال تحمل رابطاً نصّياً واضحاً `https://REPLACE-WITH-EVOLUTION-OR-EMAIL.example/send` — افتح العقدة وعدّل الحقل مباشرةً عند جاهزية Evolution/البريد. وسرّ 1.2b قيمة نصّية `CHANGE-ME-APPROVAL-SECRET` داخل عقدة `1.2b Check token` (عدّلها إن أردت تفعيل رابط الاعتماد).

1. **فعّل الحاويات / Activate** (زر Active): ابدأ بـ **NZAMY · Service Requests** (`YkvR5SI8ljcSOfuC`).
   - Communication/Admin فيها cron — تفعيلها يبدأ المؤقّتات (غير ضارّ مع رابط الإرسال الوهمي).

---

## D — اختبار الحلقة / Smoke-test the loop

### D1) اختبار مباشر للـ webhook (الأسرع — بلا تطبيق)
```bash
curl -X POST https://n8n.asra3.com/webhook/new-request \
  -H "Content-Type: application/json" \
  -d '{ "event":"service_request.created",
        "entity":{"id":"req_test","type":"service","status":"pending_assignment"},
        "actor":{"id":"u1","name":"عميل تجريبي","role":"individual"},
        "recipient":{"role":"lawyer"}, "payment":{"amount":0,"status":"not_required"},
        "timestamp":"2026-07-06T12:00:00.000Z",
        "data":{"title":"استشارة تجريبية","description":"اختبار","receiver":"lawyer","requesterUserId":"u1"} }'
```
**المتوقّع:** يرجع `200` فوراً. في n8n → **Executions** تشغيل ناجح: `2.1 Compose new` يُخرج `subject_ar`/`body_ar` عربي؛ `2.1 Deliver DUMMY` قد يظهر خطأ (رابط وهمي) لكنه لا يوقف التشغيل (onError continue) — **طبيعي**.

### D2) اختبار كامل من التطبيق (يلزم `N8N_WEBHOOK_BASE_URL` + Service Requests فعّالة)
1. سجّل دخول **كعميل** → أنشئ طلب خدمة ⇒ في n8n Executions يظهر `/new-request`.
2. سجّل دخول **كمحامٍ/أدمن** → أسند الطلب ⇒ `/request-assigned` يعمل، وعقدة **`2.2 Resolve client`** تُرجع ملف العميل (email/phone حقيقي).
3. علّم الطلب **مكتملاً** ⇒ `/request-completed` يعمل.
- لو لم يظهر شيء: تأكّد `N8N_WEBHOOK_BASE_URL` مضبوط + الحاوية Active + راجع `pm2 logs nzamy` عن `n8n dispatch failed`.

### D3) التذكيرات/SLA (اختياري)
- 2.4 SLA (cron كل ساعة) و4.2 reminder (كل 30 د) تعمل تلقائياً بعد التفعيل + تطبيق `20260706_reminder_flags.sql`. راقب Executions لاحقاً.

---

## E — تحقّق أمني سريع / Quick security check (تسريب PII)

```bash
curl -s "https://<موقعك>/api/v1/lawyers" | head -c 600
```
**المتوقّع:** لا يحتوي الرد على `phone` أو `email`، و`license_number` يظهر فقط لمن `show_contact=true`.

---

## قائمة نهائية / Final checklist

- [ ] A: `git pull` + `db push` + env + `build` + `pm2 reload` ⇒ `✓ Ready`
- [ ] B: بوابات Phase 0 كلها تعمل كما هو متوقّع
- [ ] C: n8n env + تفعيل Service Requests
- [ ] D1: webhook مباشر ⇒ execution ناجح
- [ ] D2: حلقة العميل↔المحامي كاملة ⇒ 3 executions + resolve يرجع جهة الاتصال
- [ ] E: `/api/v1/lawyers` بلا PII

**بعد نجاح كل ذلك:** الإرسال ما زال وهمياً — الخطوة التالية الحقيقية هي ربط Evolution/البريد (عدّل رابط عُقد الإرسال مباشرةً في n8n) + التحصين الأمني (rate-limiting على مسارات AI العامة). راجع [`PROJECT_STATUS_REVIEW_2026-07-06.md`](./PROJECT_STATUS_REVIEW_2026-07-06.md) و [`n8n_BUILD_LOG_AND_TEST_GUIDE.md`](./n8n_BUILD_LOG_AND_TEST_GUIDE.md).

---

## طلبات الخدمات اليدوية — فحص يدوي

- [ ] عميل: `/ai/draft` → ٣ خطوات فقط (التحديد · القضية · الإرسال)
- [ ] وقائع أقل من ٣٠ حرفاً → رسالة خطأ، ولا يُنشأ صف
- [ ] إرسال ناجح → تحويل إلى `/ai/orders/<id>` بحالة "بانتظار الاستلام"
- [ ] أدمن: الطلب يظهر في `/dashboard/admin/service-orders` مع اسم وجوال العميل
- [ ] فتح "التفاصيل" → قسم "مرفقات العميل" يعرض ملفات الطلب، وزر التحميل يفتح كل ملف
- [ ] "استلام" → الحالة "قيد التنفيذ"، و`assigned_to` = الأدمن
- [ ] رفع ملف → الحالة "مُسلّمة"
- [ ] عميل: `/ai/orders/<id>` → "جاهز" وزر التحميل يفتح الملف
- [ ] `request_events` يعرض: created → status_changed → completed
- [ ] الجرس يعرض إشعار "طلبك جاهز"
- [ ] سجلّات الخادم تُظهر حمولة `request-completed` و`recipient.id` = **العميل** لا الأدمن
- [ ] محاولة تسليم مرتين → `409`
- [ ] غير أدمن على `/api/v1/admin/service-orders` → `403`

---

## اختبار الخدمات الثلاث المحوّلة يدوياً — محترف العقود · المحاكي الشامل · الرأي الفصل

هذه الخدمات الثلاث حوّلت هذه الجلسة من مخرجات ذكاء اصطناعي وهمية إلى **طلبات حقيقية ينفّذها أدمن بشري يدوياً** ويسلّمها كملف، بنفس خط الأنابيب الذي يستخدمه الصائغ القانوني أعلاه. `tsc` و٧٣ اختباراً و`next build` كلها تنجح دون أن تفتح متصفحاً أو تتصل بقاعدة بيانات حقيقية — **هذا الجدول هو الدليل الوحيد على أن الخدمة تعمل فعلياً**، بنفس السبب المذكور أعلاه بالضبط: خطأ سابق ترك الأدمن بلا أي مرفقات عميل، بصمت تام، بينما كل الفحوصات الآلية بقيت خضراء.

جرّب الدورة كاملة بحسابين: حساب عميل وحساب أدمن.

**الجرس والواتساب:** فحص إشعار الجرس ("طلبك جاهز") ورسالة n8n على واتساب موجود في قسم "طلبات الخدمات اليدوية" أعلاه لخدمة الصائغ القانوني — نفس خط الأنابيب بالضبط يشغّل التسليم في الخدمات الثلاث هنا، فلا داعٍ لتكراره في كل جدول أدناه.

**ملاحظة مهمة عن بطاقات الأدمن:** بطاقة الطلب في `/dashboard/admin/service-orders` **لا تعرض نص حالة على الإطلاق** — لا في البطاقة نفسها ولا داخل "التفاصيل". الدليل المرئي على أن التسليم تمّ هو: (أ) لوحة "التفاصيل" تُغلق نفسها تلقائياً وتختفي أزرار الاستلام/الرفع/الإلغاء، و(ب) الطلب يظهر عند الضغط على فلتر "مُسلّمة" أعلى الصفحة. الدليل الأقوى دائماً هو صفحة العميل `/ai/orders/<رقم>` التي تعرض "الحالة: جاهز" صراحة.

**تنبيه: كل إجراء ناجح يُغلق لوحة "التفاصيل"، وليس فقط عند التسليم.** الضغط على "استلام" نفسه يُغلق اللوحة تلقائياً — لأن حقل رفع الملف يظهر فقط داخل لوحة "التفاصيل" وللطلبات "قيد التنفيذ"، يجب الضغط على "التفاصيل" مرة أخرى على نفس الصف بعد الاستلام قبل أن يظهر حقل الرفع. الجداول أدناه تذكر هذه الخطوة صراحة في كل مرة.

### أ) محترف العقود — `/ai/contracts`

**وضع الصياغة والمراجعة مساران منفصلان تماماً في الكود، فلكل منهما دورة كاملة أدناه.**

| # | أين | ماذا تفعل | ✅ المتوقع |
|---|---|---|---|
| ١ | `/ai/contracts` | اختر بطاقة "صياغة عقد" ثم بطاقة "عقد بسيط" | تنتقل لخطوة "الأطراف" (شريط تقدّم من ٣ خطوات فقط) |
| ٢ | خطوة "الأطراف" | اضغط "التالي" دون تعبئة أي حقل | لا يوجد مانع — تنتقل لخطوة "الفكرة والسياق" (هذه الخطوة غير مُفعَّلة بالتحقق حالياً) |
| ٣ | خطوة "الفكرة والسياق" | اكتب وصف فكرة العقد بأقل من ٢٠ حرفاً | زر "التالي" يبقى معطّلاً (رمادي) |
| ٤ | نفس الخطوة | أكمل الوصف إلى ٢٠ حرفاً فأكثر واضغط "التالي" | تنتقل لخطوة "المراجعة والإرسال" |
| ٥ | خطوة "المراجعة والإرسال" | فعّل مربع الإقرار ("أقر بأن البيانات المدخلة صحيحة...") ثم اضغط "إرسال الطلب" | تحويل تلقائي إلى `/ai/orders/<رقم>` بحالة **"بانتظار الاستلام"** |
| ٦ | `/dashboard/admin/service-orders` (كأدمن) | افتح اللوحة | الطلب ظاهر مع **اسم العميل وجواله**، والعنوان يبدأ بـ"محترف العقود —" |
| ٧ | نفس الصف | اضغط "التفاصيل" | صندوق JSON يعرض `"mode": "draft"` ووصف الفكرة؛ **لا** يظهر قسم "مرفقات العميل" (وضع الصياغة لا يرفع ملفات إطلاقاً) |
| ٨ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً على نفس الصف ← اختر ملفاً من جهازك من حقل الرفع الذي ظهر الآن (لا يوجد زر "تسليم" منفصل — اختيار الملف يُسلّم الطلب فوراً) | لوحة "التفاصيل" تُغلق تلقائياً مجدداً فور اكتمال الرفع؛ فلتر "مُسلّمة" أعلى الصفحة يعرض الطلب الآن |
| ٩ | `/ai/orders/<رقم>` (كعميل) | افتح الطلب | **"الحالة: جاهز"** وزر "تحميل" يفتح الملف الذي رفعه الأدمن |
| ١٠ | `/ai/contracts` (كعميل، طلب جديد) | اختر "مراجعة عقد" | تنتقل مباشرة لخطوة "الهوية" (لا يوجد اختيار مستوى تفصيل في هذا الوضع) |
| ١١ | خطوة "الأنواع والرفع" — **الفحص الأهم لهذه الخدمة** | لا ترفع أي ملف، واضغط "التالي" | زر "التالي" يبقى معطّلاً — **الخطوة ترفض التقدّم بلا ملف**. (قبل هذه الجلسة كان هذا الصندوق صورة بلا أي حقل رفع حقيقي رغم وسمه "متطلب" — لو تقدّمت بلا ملف فالعطل عاد) |
| ١٢ | نفس الخطوة | ارفع ملف PDF أو Word حقيقياً | اسم الملف يظهر في صندوق أخضر، وزر "التالي" يُفعَّل |
| ١٣ | خطوة "الهوية" ثم "المراجعة والإرسال" | اكتب في "أنت تمثل أي طرف في العقد؟"، أكمل حتى شاشة المراجعة، فعّل الإقرار واضغط "إرسال الطلب" | تحويل إلى `/ai/orders/<رقم>` بحالة "بانتظار الاستلام"؛ شاشة المراجعة نفسها أظهرت "المرفقات (١)" قبل الإرسال |
| ١٤ | `/dashboard/admin/service-orders` (كأدمن) → "التفاصيل" | افتح الطلب الجديد | `"mode": "review"` في صندوق JSON، وقسم **"مرفقات العميل"** يعرض اسم الملف الذي رفعه العميل في الخطوة ١٢، وزر التحميل يفتحه فعلياً |
| ١٥ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً ← ارفع ملف تقرير المراجعة من الحقل الذي ظهر الآن | لوحة "التفاصيل" تُغلق تلقائياً مجدداً؛ فلتر "مُسلّمة" أعلى الصفحة يعرض الطلب الآن |
| ١٦ | `/ai/orders/<رقم>` (كعميل) | افتح الطلب | "جاهز" + تحميل تقرير المراجعة |

### ب) المحاكي الشامل — `/ai/wargaming`

| # | أين | ماذا تفعل | ✅ المتوقع |
|---|---|---|---|
| ١ | `/ai/wargaming` | اختر صفة الموكل وتخصص القضية، اكتب ملخصاً ٢٠ حرفاً فأكثر (يمكنك رفع ملف اختياري)، ثم اضغط "التالي: اختر أهداف المحاكاة" | تنتقل لخطوة "أهداف المحاكاة" |
| ٢ | خطوة الأهداف — **الفحص الأهم لهذه الخدمة** | فعّل هدف "نقض المذكرة" فقط، اترك مربع "نص المذكرة المراد نقضها" فارغاً، واضغط "مراجعة الطلب وإرساله" | تسمح بالانتقال (الزر لا يتحقق من نص المذكرة)، لكن شاشة المراجعة تُظهر السطر بلون كهرماني: "لم تُدرِج نص المذكرة بعد — عد للخطوة السابقة لإضافته قبل الإرسال" |
| ٣ | شاشة "مراجعة الطلب وإرساله" | فعّل الإقرار واضغط "إرسال الطلب" مع بقاء نص المذكرة فارغاً | **الطلب لا يُنشأ** — صندوق أحمر يظهر: "نص المذكرة مطلوب عند اختيار نقض المذكرة" |
| ٤ | نفس الشاشتين (الأهداف ← المراجعة) | ارجع خطوة، الصق نص مذكرة حقيقياً (أو ألغِ تحديد "نقض المذكرة")، ثم أرسل مجدداً | الطلب يُنشأ فعلياً، تحويل إلى `/ai/orders/<رقم>` بحالة "بانتظار الاستلام" |
| ٥ | `/dashboard/admin/service-orders` (كأدمن) → "التفاصيل" | افتح الطلب | صندوق JSON يعرض `"targets"` دائماً؛ إن أبقيت "نقض المذكرة" محددة وكتبت نصاً، يظهر أيضاً حقل `"memoText"` بنص مذكرتك كاملاً (وإن ألغيت تحديدها في الخطوة ٤ فلن يظهر هذا الحقل إطلاقاً)؛ وقسم "مرفقات العميل" إن رفعت ملفاً في الخطوة ١ |
| ٦ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً ← ارفع ملف التحليل النهائي من الحقل الذي ظهر الآن | لوحة "التفاصيل" تُغلق تلقائياً مجدداً؛ فلتر "مُسلّمة" أعلى الصفحة يعرض الطلب الآن |
| ٧ | `/ai/orders/<رقم>` (كعميل) | افتح الطلب | "جاهز" + تحميل الملف |

### ج) الرأي الفصل — `/ai/legal-opinion`

سبعة مسارات فرعية بنوع طلب واحد (`ai_legal_opinion`) يميّزها حقل `outputType`. جرّب ثلاثة مسارات مختلفة على الأقل — الجدول التالي يغطي **استشارة قانونية** (الأبسط)، **تقرير العناية الواجبة** (يتضمّن رفع ملف)، و**خطاب رسمي** (معالج مستقل تماماً بأربع خطواته الخاصة).

| # | أين | ماذا تفعل | ✅ المتوقع |
|---|---|---|---|
| ١ | `/ai/legal-opinion` | اختر بطاقة "استشارة قانونية" ثم اضغط "التالي" | تنتقل لخطوة السياق |
| ٢ | خطوة السياق | اختر مجالاً قانونياً واحداً من القائمة، واكتب استفسارك (٢٠ حرفاً فأكثر) في "استفسارك القانوني" | زر "مراجعة الطلب وإرساله" يُفعَّل |
| ٣ | شاشة المراجعة | فعّل الإقرار واضغط "إرسال الطلب" | تحويل إلى `/ai/orders/<رقم>` بحالة "بانتظار الاستلام" |
| ٤ | `/dashboard/admin/service-orders` (كأدمن) → "التفاصيل" | افتح الطلب | العنوان يبدأ بـ"استشارة قانونية —" واسم المجال؛ صندوق JSON يظهر `"outputType": "consult"` |
| ٥ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً ← ارفع ملفاً من الحقل الذي ظهر الآن | لوحة "التفاصيل" تُغلق تلقائياً مجدداً (فلتر "مُسلّمة" يعرضه الآن)؛ عميل: `/ai/orders/<رقم>` يعرض "الحالة: جاهز" ويحمّل الملف |
| ٦ | `/ai/legal-opinion` (طلب جديد) | اختر بطاقة "تقرير العناية الواجبة" ثم "التالي" | تنتقل لخطوة السياق (بلا خطوة إعدادات إضافية) |
| ٧ | خطوة السياق | اختر نوع الكيان، اكتب اسمه، ارفع ملفاً حقيقياً في "ارفع مستندات الكيان"، واكتب في "تفاصيل الفحص المطلوب" (٢٠ حرفاً فأكثر) | اسم الملف يظهر بعد اكتمال الرفع، وزر "مراجعة الطلب وإرساله" يُفعَّل |
| ٨ | شاشة المراجعة | أرسل الطلب | تحويل إلى `/ai/orders/<رقم>`؛ الشاشة نفسها أظهرت "المرفقات (١)" قبل الإرسال |
| ٩ | `/dashboard/admin/service-orders` (كأدمن) → "التفاصيل" | افتح الطلب | `"outputType": "due_diligence"` في صندوق JSON؛ قسم "مرفقات العميل" يعرض الملف وزر التحميل يفتحه فعلياً |
| ١٠ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً ← ارفع ملفاً من الحقل الذي ظهر الآن | لوحة "التفاصيل" تُغلق تلقائياً مجدداً (فلتر "مُسلّمة" يعرضه الآن)؛ عميل: "الحالة: جاهز" + تحميل |
| ١١ | `/ai/legal-opinion` (طلب جديد) | اختر بطاقة "خطاب رسمي" ثم اضغط زر "التالي" أسفل الصفحة (اختيار البطاقة وحدها لا يفتح المعالج — يجب الضغط على "التالي" بعدها) | **معالج منفصل تماماً** بأربع خطوات خاصة به: "نوع الخطاب · الأطراف · الموضوع · مراجعة وإرسال" (شريط تقدّم مختلف عن بقية المسارات) |
| ١٢ | الخطوة ١ (نوع الخطاب) | اختر نوعاً (مثلاً "إنذار قانوني") واضغط "التالي" | تنتقل لخطوة "الأطراف" |
| ١٣ | الخطوة ٢ (الأطراف) | اكتب اسم المرسِل واسم المستلِم واضغط "التالي" | تنتقل لخطوة "الموضوع" |
| ١٤ | الخطوة ٣ (الموضوع) | اكتب موضوع الخطاب (١٠ أحرف فأكثر) واضغط "التالي: مراجعة وإرسال" | تظهر معاينة كاملة لنص الخطاب، مبنية فعلياً من بياناتك لا نصاً جاهزاً |
| ١٥ | الخطوة ٤ (مراجعة وإرسال) | اضغط "إرسال الطلب" مباشرة (لا يوجد مربع إقرار في هذه الشاشة تحديداً) | تحويل إلى `/ai/orders/<رقم>` بحالة "بانتظار الاستلام" |
| ١٦ | `/dashboard/admin/service-orders` (كأدمن) → "التفاصيل" | افتح الطلب | العنوان "خطاب رسمي — <نوع الخطاب>"؛ صندوق JSON يظهر `"outputType": "letter"` وحقل `letter.fullLetterText` بنص الخطاب الكامل كمسودة انطلاق للأدمن |
| ١٧ | نفس الصف | اضغط "استلام" (تُغلق لوحة "التفاصيل" تلقائياً) ← اضغط "التفاصيل" مجدداً ← ارفع ملفاً من الحقل الذي ظهر الآن | لوحة "التفاصيل" تُغلق تلقائياً مجدداً (فلتر "مُسلّمة" يعرضه الآن)؛ عميل: "الحالة: جاهز" + تحميل |

### د) فحص البوابة الصادقة — أداة بلا حمولة طلب حقيقية

معظم أدوات نظامي (نحو ٥٠ أداة) ما زالت خلف بوابة المراجعة **بلا** ربط بطلب حقيقي بعد — هذا هو المتوقع في هذه المرحلة، بشرط ألا تدّعي البوابة استلام طلب لم يُسجَّل. الأداة المختارة هنا: **مراقب الامتثال** على `/ai/corp/compliance` — اختيرت لأنها الأبسط في هذا الجدول للمالك بلا خبرة برمجية: خطوة واحدة، بلا أي نموذج يُعبَّأ.

| # | أين | ماذا تفعل | ✅ المتوقع |
|---|---|---|---|
| ١ | `/ai/corp/compliance` (كعميل) | اضغط الزر "ابدأ الفحص" وانتظر ثوانٍ | تظهر ٣ بطاقات إحصائية أعلى الصفحة (ملتزم / يحتاج مراجعة / منتهك) بلا أي تفاصيل تفسيرية تحتها؛ **لا** يظهر أي زر "إرسال"، و**لا** وعد "خلال ٤–٢٤ ساعة" — بدل التفاصيل يظهر: "تفاصيل امتثال الشركة: المخرج التلقائي غير متاح خلال البيتا" ثم "هذا المخرج لا يُعرض تلقائيًا خلال الفترة الحالية من البيتا، ولم يُسجَّل أي طلب بشأنه." مع رابط واحد إلى "الصائغ القانوني" |
| ٢ | `/dashboard/admin/service-orders` (كأدمن) | تحقق من قائمة الطلبات | **لا** يظهر أي طلب جديد بسبب الخطوة ١ — لأنه فعلاً لم يُسجَّل شيء |

**ملاحظة:** صفّ `/ai/report-generator` في القسم "B — تحقّق بوابات النزاهة" أعلاه من هذا المستند وُثِّق قبل هذا العمل، ويصف بطاقة قديمة («تم الإعداد… إرسال للمراجعة الذكية»). هذه الصياغة ما زالت موجودة فعلياً في كود `BetaReviewGate.tsx` (فرع وجود حمولة طلب حقيقية)، لكن `/ai/report-generator` لا يمرّر أي حمولة فلا يصل إليها إطلاقاً — أي أن هذا المسار تحديداً **لا يعرضها بعد الآن**. ما يظهر اليوم فعلياً على `/ai/report-generator` (وعلى أي أداة أخرى بلا حمولة) هو نفس البطاقة الصادقة الموصوفة في الصف أعلاه.

### ما لا تثبته هذه الخدمات الثلاث بعد

لا ذكاء اصطناعي يولّد أي شيء في محترف العقود أو المحاكي الشامل أو الرأي الفصل — **الأدمن يكتب المُخرج (المذكرة، تقرير المراجعة، الرأي، الخطاب) يدوياً** ويرفعه كملف. واجهات "النتيجة" و"التحليل" القديمة (مثل `ResultView`، `PolishPanel`، `StudyDocumentEditor`) لا تزال موجودة في الكود لكنها **مخفية لا محذوفة**، تحسّباً لوصول ذكاء اصطناعي حقيقي لاحقاً — لا تتوقع رؤيتها أثناء هذا الاختبار، وهذا سلوك متعمَّد لا عطل.
