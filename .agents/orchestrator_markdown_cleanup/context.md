# Orchestrator Context

## Task
Organize, clean, and update all Markdown files in the root directory of the `nzamy-website` repository.

## Instructions
1. Follow the requirements in `.agents/ORIGINAL_REQUEST.md`.
2. Do NOT write code directly if you need to perform edits; instead, you can spawn worker agents to perform specific file operations and updates, or perform them if permitted. (Note: Since this is purely a documentation organization and update task, you can plan and coordinate it).
3. Update status tracking files with the latest milestones.
4. Keep active files in the root, move historical files to `OLD/`.
5. Update `DOCUMENTATION_INDEX.md` with the new paths.
