# 📚 دليل الفحص الشامل للمكتبة القانونية — منصة نظامي (Nzamy)
## دليل خطوات الفحص (QA Testing Guide) المخصص للمكتبة القانونية وأنظمة الذكاء الاصطناعي

هذا الدليل مصمم ليمكّن المالك (أو فاحص جودة البرمجيات QA) من اختبار **كل** شاشات وأدوات المكتبة القانونية خطوة بخطوة، بحيث يتم كشف الأخطاء البرمجية وملاحظات التعديل المطلوبة قبل الإطلاق.

> 💡 **نصيحة للفاحص**: اختبر المكتبة بحسابين مختلفين:
> - **حساب باقة مجانية (Free)** — لاختبار القيود وبوابات الدفع (Paywall).
> - **حساب باقة احترافية (Pro) أو ذكاء اصطناعي (AI)** — لاختبار البحث المتقدم وأدوات الذكاء الاصطناعي المفتوحة.
>
> ويفضّل الاختبار على متصفحين: واحد بوضع عادي (RTL عربي) وآخر بوضع خفي (Incognito) باللغة الإنجليزية (LTR) للتحقق من ثنائية اللغة والاتجاه.

> 🧪 **قاعدة ذهبية**: كل خانة `[ ]` في الدليل يجب تعبئتها بعد التحقق. إذا ظهر خطأ، سجّله في **نموذج الإبلاغ عن الأخطاء** في نهاية الملف مع رقم الخطوة ورابط الصفحة ولقطة شاشة.

---

## ⚙️ التجهيز قبل البدء (للمطور)

1. تأكد من تطبيق مخطط قاعدة بيانات المكتبة + البذور (Seeding):
   ```bash
   npx supabase db push                 # لتطبيق مخطط المكتبة 20260626_legal_library_schema.sql
   npx tsx scripts/seed-library.ts --dir "./output" --dry-run   # تحقق فقط
   npx tsx scripts/seed-library.ts --dir "./output"             # بذر حقيقي
   ```
2. تأكد من وجود بيانات في الجداول: `library.laws`، `library.articles`، `library.principles`، `library.decrees_circulars`، `library.books`. (بدون بيانات بذر سترى الحالة التجريبية/dev-fallback — وهذا متوقع في وضع التطوير فقط).
3. متغيرات البيئة في `.env.local`:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   SUPABASE_SERVICE_ROLE_KEY=...            # ضروري للبذر فقط
   N8N_EXPLAIN_ARTICLE_WEBHOOK_URL=...       # لأداة "شرح المادة" (إن وُصلت)
   N8N_LIBRARY_CHAT_WEBHOOK_URL=...          # لمحادثة المكتبة (إن وُصلت)
   ```
4. شغّل الموقع: `npm run dev` وافتح `http://localhost:3000/laws`.

---

## 🗂️ أولاً: الصفحة الرئيسية للمكتبة والتصفح — `/laws`

التحقق من تحميل الصفحة الرئيسية وتبويباتها الأربعة والفلاتر.

### 1. تحميل الصفحة الرئيسية وتبويبات المحتوى
* **الخطوات**:
  1. افتح `/laws` دون تسجيل دخول.
  2. انتقل بين التبويبات الأربعة: **أنظمة** · **مبادئ قضائية** · **تعاميم ومراسم** · **فقه وشريعة**.
* **التحقق**:
  * [ ] تظهر شاشات التحميل (Skeletons) ثم تُحمّل البيانات من `/api/library/init` (وليس مصفوفات ثابتة).
  * [ ] كل تبويب يعرض عدد العناصر الفعلي من قاعدة البيانات (`catTotalCount`).
  * [ ] تبويب «فقه وشريعة» يعرض الكتب مع تصنيفات المذاهب والأنواع بشكل صحيح.
  * [ ] لا تظهر عناصر تجريبية (Demo) في وضع الإنتاج — يجب أن تظهر حالة فارغة منظمة إذا خلت قاعدة البيانات.

### 2. الفلاتر والتصنيفات
* **الخطوات**:
  1. داخل تبويب «أنظمة»، اضغط على تصنيف فرعي (مثل: أنظمة عمل، أنظمة تجارية، أنظمة جنائية).
  2. جرّب قائمة «تصنيفات أخرى» (Other) واختر تصنيفاً غير رئيسي.
  3. بدّل وضع العرض بين **شبكة (Grid)** و**قائمة (List)**.
  4. اضغط على أحد الوسوم (Hashtags) لفلترة المحتوى.
* **التحقق**:
  * [ ] تتغير القائمة فوراً عند اختيار تصنيف وتعود النتائج صحيحة من قاعدة البيانات.
  * [ ] يظهر عدد النتائج المتطابقة مع التصنيف المختار.
  * [ ] التبديل بين الشبكة والقائمة سلس ويحافظ على الفلتر الحالي.
  * [ ] إلغاء الوسوم (Hashtag) يرجع القائمة لحالتها الكاملة.

### 3. البطاقات الجانبية (Widgets)
* **التحقق** بزيارة الصفحة الرئيسية:
  * [ ] **المجلدات الذكية (Smart Folders)** تظهر في الجانب وتعرض المجلدات الافتراضية.
  * [ ] **ملاحظاتي (My Notes)** تظهر وتُحمّل من قاعدة البيانات للمستخدم الحالي.
  * [ ] **بطاقة التحفيز (Gamification)** تعرض تقدم المستخدم (إن وُجدت بيانات).
  * [ ] **التحديثات التشريعية (Legislative Updates)** تظهر أحدث الأنظمة/المراسيم.
  * [ ] **الجلسات الأخيرة (Recent Sessions)** تعرض آخر جلسات بحث/قراءة.

---

## 🔍 ثانياً: البحث والاقتراحات (Search & Autocomplete)

### 4. شريط البحث والاقتراحات الفورية (Autocomplete)
* **الخطوات**:
  1. اكتب في شريط البحث كلمة لا تقل عن حرفين، مثلاً: `إثبات`.
  2. راقب قائمة الاقتراحات المنسدلة (تظهر بعد ~300ms).
  3. اضغط على أحد الاقتراحات للانتقال لمحتواه.
* **التحقق**:
  * [ ] الاقتراحات تأتي من `/api/library/autocomplete?q=...` وتعرض عدداً لكل قسم (أنظمة/مبادئ/مراسم/فقه).
  * [ ] **تطبيع الأحرف العربية**: البحث بـ `الإثبات` يطابق المحتوى المخزّن بـ `الاثبات` (همزة مقطوعة/موصلة).
  * [ ] **الأرقام الهندية**: البحث بسنة `١٤٤٤` يطابق `1444` المخزّنة.
  * [ ] اختيار اقتراح ينقل المستخدم لصفحة التفاصيل الصحيحة.

### 5. البحث الأساسي عبر الأقسام
* **الخطوات**:
  1. اكتب استعلاماً مثل `عقد إيجار` واضغط Enter (أو زر البحث).
  2. بدّل القسم (Section) بين: الكل / أنظمة / مبادئ / مراسم / فقه.
  3. جرّب الفرز (Sort): الصلة · الأحدث · الأقدم · أبجدي.
* **التحقق**:
  * [ ] يُستدعى `POST /api/library/search` ويعرض نتائج من الأقسام المختارة فقط.
  * [ ] الفرز يغيّر ترتيب النتائج فعلياً.
  * [ ] إذا لم توجد نتائج، تظهر حالة فارغة واضحة (وليس شاشة بيضاء/تعطل).
  * [ ] التنقل بين صفحات النتائج (Pagination) يعمل إن وُجدت نتائج كثيرة.

### 6. البحث القانوني المتقدم (Advanced Search) — 🔒 ميزة مدفوعة
* **الخطوات**:
  1. بحساب **Free**: اضغط زر «البحث المتقدم» → يجب أن يظهر **PaywallModal** يطلب الترقية لباقة AI.
  2. بحساب **AI/Pro**: افتح البحث المتقدم واملأ حقلاً واحداً على الأقل:
     - عنوان/رقم النظام، نوع النظام (نظام/لائحة)، المصدر، الجهة المُصدرة، السنة الهجرية، الحالة (ساري/معدّل/ملغى)، الفرع القانوني، تاريخ من/إلى.
  3. اضغط **تطبيق**.
* **التحقق**:
  * [ ] حساب Free يُحجب بوابة الدفع بشكل صحيح (لا يمكن فتح النموذج).
  * [ ] حساب AI يفتح النموذج ويرسل الفلاتر إلى `/api/library/search` ويرجع نتائج مطابقة.
  * [ ] زر الإلغاء/الإغلاق يعمل دون ترك الصفحة في حالة معلقة.

---

## 📄 ثالثاً: صفحة تفاصيل النظام — `/laws/[slug]`

أهم صفحة في المكتبة — تحتوي على أدوات متعددة يجب فحص كل منها.

### 7. تحميل النظام والتنقل بين مواده
* **الخطوات**:
  1. افتح نظامٌ حقيقي مثل `/laws/labor-law` (أو أي slug موجود في البذور).
  2. تصفّح فصول النظام ومواده في القائمة الجانبية (`_sidebar`).
  3. اضغط على مادة للانتقال إليها.
* **التحقق**:
  * [ ] يُجلب النظام من `/api/library/laws/[slug]` مع مواده وفصوله ولائحته التنفيذية.
  * [ ] التنقل بين المواد سلس ويُحدّث المؤشر النشط في الشريط الجانبي.
  * [ ] ترقيم المواد (`number` / `number_text`) يظهر بدقة.

### 8. البحث داخل النظام
* **الخطوات**:
  1. استخدم حقل البحث داخل صفحة النظام.
  2. ابحث برقم مادة، أو عنوان، أو نص، أو مرجع اللائحة التنفيذية.
* **التحقق**:
  * [ ] يفلتر البحث المواد (يشمل رقم المادة + عنوانها + نصها + اللائحة التنفيذية).
  * [ ] تُبرز (Highlight) الكلمة المطابقة داخل النتائج بشكل صحيح ولا يكسر تطبيع الأحرف موضع الإبراز.

### 9. اللائحة التنفيذية وتبويب «اللائحة فقط»
* **الخطوات**:
  1. افتح نظاماً ذا لائحة تنفيذية (`document_type = نظام_ولائحة`).
  2. بدّل بين تبويب **النظام + اللائحة** وتبويب **اللائحة فقط**.
* **التحقق**:
  * [ ] يظهر التبويب فقط للأنظمة التي تملك لائحة تنفيذية فعلية.
  * [ ] عرض اللائحة وحدها يخفي مواد النظام الأصلية ولا يدمجها.
  * [ ] التسمية الديناميكية (`executive_label_override`) تظهر عند وجودها.

### 10. أدوات المادة: نسخ / حفظ / طباعة
* **الخطوات**:
  1. اضغط زر **نسخ** على إحدى المواد.
  2. اضغط زر **حفظ/إشارة مرجعية (Bookmark)**.
  3. اضغط زر **طباعة** (`Printer`) في رأس الصفحة.
* **التحقق**:
  * [ ] النسخ يضع نص المادة في الحافظة بنص عربي سليم (يتضمن مرجع اللائحة عند وجوده).
  * [ ] الإشارة المرجعية تُحفظ وتظهر في قسم الملاحظات/المحفوظات.
  * [ ] الطباعة تفتح معاينة طباعة مع **علامة مائية (PrintWatermark)** ولا تكسر التنسيق العربي (RTL).

### 11. أدوات الذكاء الاصطناعي في الشريط الجانبي — 🔒 ميزة مدفوعة
* **الخطوات**:
  1. بحساب **Free**: تحقق من ظهور قفل/بوابة على بطاقة `LibraryAI`.
  2. بحساب **AI/Pro**: اضغط **شرح المادة** على إحدى المواد، ثم جرّب **محادثة المكتبة**.
* **التحقق**:
  * [ ] حساب Free يُحجب عن أدوات AI مع رسالة ترقية واضحة.
  * [ ] حساب AI: إن وُصل webhook الخاص بـ n8n (`N8N_EXPLAIN_ARTICLE_WEBHOOK_URL`)، يعود شرح المادة. **إن لم يُوصل بعد**، يجب أن تظهر رسالة صريحة «الذكاء الاصطناعي قيد التفعيل» بدلاً من رد وهمي بـ `setTimeout`. (✅ **تم الإصلاح 2026-06-29**: `LibraryAI` يستدعي `/api/ai/library-chat` حقيقياً؛ `ArticleExplainModal` يعرض «قريباً» بدل `setTimeout` وهمي — راجع العنصر #1 في «ملاحظات معروفة»).
  * [ ] لا تظهر إجابة AI ملفّقة/ثابتة — إن لم يكن هناك اتصال حقيقي، تُعرض حالة «قريباً». (✅ محقّق الآن)

### 12. المستندات المرتبطة
* **التحقق**:
  * [ ] الشريط الجانبي يعرض المستندات المرتبطة (مراسيم/لائحة/سوابق) للنظام الحالي وروابطها تعمل.

---

## 📁 رابعاً: المجلدات الذكية (Smart Folders) — `/api/library/folders`

### 13. إنشاء مجلد ذكي وإدارة عناصره
* **الخطوات**:
  1. سجّل دخولك (المجلدات مرتبطة بالمستخدم).
  2. اضغط **مجلد جديد** (`CreateFolderInline`).
  3. أدخل اسماً، اختر لوناً من 8 ألوان، وأيقونة (افتراضي/نجمة/تثبيت/كتاب).
  4. أضف نظاماً/مبدأً/مرسوماً إلى المجلد (من بطاقة العنصر أو من داخل التفاصيل).
  5. ثبّت (Pin) المجلد ليظهر في الأعلى.
* **التحقق**:
  * [ ] يُحفظ المجلد عبر `POST /api/library/folders` ويظهر فوراً.
  * [ ] العناصر المُضافة تُحفظ وتبقى بعد تحديث الصفحة (F5).
  * [ ] التثبيت (Pin) يعيد ترتيب المجلدات.
  * [ ] تعديل المجلد (`PATCH`) يحدّث الاسم/اللون/الأيقونة.

### 14. حذف المجلد والملكية
* **الخطوات**:
  1. احذف مجلداً أنشأته.
  2. (للمطور) تأكد أن مسار `DELETE /api/library/folders` يتحقق من **ملكية** المجلد قبل الحذف.
* **التحقق**:
  * [ ] يُحذف المجلد وعناصره دون التأثير على مجلدات مستخدم آخر.
  * [ ] ✅ **تم الإصلاح (2026-06-29)**: مسار `DELETE /api/library/folders?itemId=` يتحقق الآن من ملكية المجلد الأب (`smart_folders.user_id = auth.uid()`) قبل الحذف، ويرفض بـ403 إن لم يكن المالك. اختبر بحسابين للتأكيد: مستخدم (أ) لا يستطيع حذف عنصر مجلد مستخدم (ب) عبر تلاعب بالمعرف.

---

## 📝 خامساً: الملاحظات (Notes) — `MyNotesSection`

### 15. إنشاء/تعديل/حذف الملاحظات
* **الخطوات**:
  1. بحساب مسجّل، افتح قسم «ملاحظاتي».
  2. أضف ملاحظة مرتبطة بمادة/نظام، ثم عدّلها، ثم احذفها.
* **التحقق**:
  * [ ] تُحفظ الملاحظات لكل مستخدم على حدة وتظهر بعد تحديث الصفحة.
  * [ ] لا تظهر ملاحظات مستخدم آخر (عزل البيانات عبر RLS).

---

## ⚖️ سادساً: المحتوى القانوني الآخر (مبادئ/سوابق/مراسم/فقه/كتب)

### 16. صفحة تفاصيل المبدأ القضائي — `/precedents/[slug]`
* **التحقق**:
  * [ ] تُحمّل هوية المبدأ (`_identity-panel`)، نص المبدأ (`_principle-block`)، والمحتوى المرتبط (`_related`) ولوحة الجانب (`_sidebar`).
  * [ ] البيانات تأتي من `/api/library/precedents/[slug]`.

### 17. صفحة تفاصيل الحكم — `/precedents/judgment/[slug]`
* **التحقق**:
  * [ ] ✅ **تم الإصلاح (2026-06-29)**: الصفحة الآن تعرض بطاقة «قريباً» (`DashboardComingSoon`) بدل بيانات `DEMO_PRECEDENTS` التجريبية — لا مسار API حقيقياً بعد، لذا لا تظهر بيانات ثابتة ملفّقة.

### 18. صفحة تفاصيل المرسوم/التعاميم — `/laws/orders/[slug]`
* **التحقق**:
  * [ ] يُجلب المرسوم من `/api/library/decrees/[id]` مع الجهة المُصدِرة وتاريخ الإصدار.
  * [ ] تبويب «تعاميم ومراسم» في الرئيسية يفلترها بشكل صحيح.

### 19. صفحة تفاصيل الكتاب الفقهي — `/book/[slug]`
* **التحقق**:
  * [ ] يُجلب الكتاب من `/api/library/books/[slug]` مع هوية الكتاب ولوحته الجانبية.
  * [ ] صفحة `/book` (قائمة الكتب) و`/book/consultation` تفتحان دون أخطاء.

### 20. معاينة الفقه — `/laws/feqh-preview`
* **التحقق**:
  * [ ] تفتح المعاينة وتعرض بنية الكتب الفقهية بشكل سليم.

---

## 🔐 سابعاً: بوابات الدفع وقيود الباقات (Paywall & Tier Gating)

المكتبة مقيدة حسب الباقة. استخدم حسابات بباقات مختلفة للتحقق.

| الميزة | الباقة المطلوبة | المرجع البرمجي |
|--------|----------------|----------------|
| وصول كامل للمكتبة | **Pro** (`library-full-access`) | `access-control.ts` |
| البحث المتقدم | **AI** (`library-advanced-search`) | `access-control.ts` |
| شرح المادة / محادثة المكتبة | **AI** | `LibraryAI` |
| العناصر المجانية المفتوحة دائماً | Free | `library_free_items` (إعداد إداري) |

### 21. قيود المستخدم المجاني (Free Tier)
* **الخطوات**:
  1. سجّل دخولك بحساب Free.
  2. تصفّح الأنظمة وتجاوز حد المواد المجانية (`library_free_article_limit`).
  3. حاول فتح نظام غير مُدرج في القائمة البيضاء (`library_free_items` / `library_whitelisted_laws`).
* **التحقق**:
  * [ ] يظهر **PaywallModal** بعد تجاوز الحد المجاني يدعو للترقية.
  * [ ] العناصر المُدرجة في القائمة البيضاء تفتح مجاناً دون حجب.
  * [ ] زر «ترقية الاشتراك» في البوابة ينقل لصفحة الاشتراك `/laws/subscribe`.

### 22. صفحة الاشتراك — `/laws/subscribe`
* **التحقق**:
  * [ ] تعرض الباقات والأسعار بشكل صحيح.
  * [ ] ⚠️ **ملاحظة معروفة**: بوابة الدفع الحقيقية **غير مفعّلة** (محجوبة بعلم `payments_gateway = disabled` عبر لوحة الإدارة). زر الاشتراك يجب أن يعرض رسالة «الدفع غير متاح حالياً» بدلاً من إنشاء طلب. (هذا متوقع — راجع `payments-gateway-admin-gate.md`).

---

## 🤖 ثامناً: أدوات الذكاء الاصطناعي — `/ai/*`

> ملاحظة: معظم أدوات AI تعتمد على ربط n8n webhooks. إن لم تُربط بعد، يجب أن تظهر حالة «قريباً/قيد التفعيل» بدلاً من ردود وهمية. سجّل أي أداة تعرض رداً ثابتاً بـ `setTimeout` كخطأ.

### 23. المحلل الذكي — `/ai/analyze`
* **الخطوات**:
  1. ارفع مرفقاً/وثيقة أو أدخل نصاً قانونياً.
  2. جرّب تصدير المرفق إلى صندوق المسودات (Draft Inbox) عبر `AttachmentSqueezer`.
  3. شغّل التحليل.
* **التحقق**:
  * [ ] رفع المرفقات يعمل ويُصدّر إلى `draftInboxStore` عبر `researchService.addToDesktop`.
  * [ ] التحليل يعرض تقريراً (`evalReport`) أو حالة «قيد التفعيل» إن لم يُربط n8n.

### 24. المساعد القانوني — `/ai/assistant`
* **التحقق**:
  * [ ] يفتح واجهة المحادثة؛ الردود تأتي من n8n (`N8N_LIBRARY_CHAT_WEBHOOK_URL`) أو تظهر «قريباً».

### 25. أدوات AI الإضافية
جرّب كل صفحة وتحقق أنها تفتح دون تعطل وأن أي رد AI حقيقي/أو حالة قريباً واضحة:
- [ ] `/ai/compare` — مقارنة المستندات (مع لوحة الإصلاح التلقائي `_auto-fix-panel`).
- [ ] `/ai/case-brief` — ملخص القضية.
- [ ] `/ai/brief-check` — تدقيق الملخص.
- [ ] `/ai/analyze-strength` — قوة الدعوى (مع `ScoreGauge`).
- [ ] `/ai/collector` — جامع الأدلة (`SessionsPanel` + `DesktopPanel`).
- [ ] `/ai/communicate` — التواصل.

---

## 🛠️ تاسعاً: لوحة إدارة المكتبة (Admin) — `/dashboard/admin`

### 26. إدارة محتوى المكتبة
* **الخطوات**:
  1. سجّل دخولك بحساب مدير.
  2. افتح تبويب المكتبة (`LibraryTab`) في لوحة الإدارة.
  3. جرّب فلترة المحتوى بالتصنيفات، وتعديل «العناصر المجانية» (`library_free_items`).
* **التحقق**:
  * [ ] تصنيفات الفلتر موحّدة بين الواجهة والمسار (`/api/v1/admin/library`) وكل تصنيف يرجع نتائج.
  * [ ] تبديل «العناصر المجانية» فعّال فعلاً ويُفتح المحتوى للمستخدمين المجانيين بعد الحفظ (راجع `nzamy-audit-fix-status.md` — البند P0 #4).
  * [ ] تبويب الإعدادات (`/dashboard/admin/settings`) يتيح التحكم في بوابة الدفع `payments_gateway`.

### 27. التقارير — `/api/library/reports`
* **التحقق**:
  * [ ] مسار التقارير يرجع بيانات صحيحة للمدير (إحصاءات استخدام المكتبة) أو يُحجب عن غير المدير.

---

## 📱 عاشراً: التوافق والتجربة العامة (UX / RTL / الطباعة)

### 28. ثنائية اللغة واتجاه RTL/LTR
* **التحقق**:
  * [ ] الواجهة العربية (RTL) متسقة: الأيقونات، التبويبات، الجداول، الفلاتر.
  * [ ] التبديل للإنجليزية (LTR) يصحّح الاتجاه ولا يكسر التخطيط.
  * [ ] النصوص المختلطة (عربي + أرقام هندية/إنجليزية) تظهر بشكل صحيح.

### 29. الجوال والاستجابة (Responsive)
* **التحقق**:
  * [ ] على شاشة جوال (≤768px): الشريط الجانبي يطوى، الفلاتر في قائمة منسدلة، البطاقات بعمود واحد.
  * [ ] أزرار النسخ/الطباعة/الحفظ تعمل باللمس.

### 30. الطباعة
* **التحقق**:
  * [ ] طباعة صفحة نظام تطبع المواد مع العلامة المائية ولا تقطع النصوص.
  * [ ] معاينة الطباعة تحافظ على RTL.

---

## ⚠️ ملاحظات معروفة (متوقعة — ليست أخطاء برمجية)

هذه عناصر **مؤجلة عن قصد** موثّقة في `NEXT_STEPS.md` و`nzamy-audit-fix-status.md`. عند اختبارها، إن ظهرت بالشكل الموصوف فهي متوقعة؛ إن ظهرت بشكل مختلف فسجّلها كخطأ:

> 📌 **تحديث 2026-06-29**: العناصر 1 و2 و3 و5 أُصلحت في جولة الإصلاحات؛ العنصر 4 يحتاج تحذير تراجع (انظر أدناه).

| # | العنصر | السلوك المتوقع حالياً |
|---|--------|----------------------|
| 1 | ردود AI الوهمية (`setTimeout`) | ✅ **أُصلحت**: `LibraryAI` يستدعي `/api/ai/library-chat` حقيقياً (وإلا رسالة «قريباً»)؛ `ArticleExplainModal` و`CommunityQuestionModal` يعرضان «قريباً» — لا ردود وهمية. سجّل أي رد وهمي متبقٍ. |
| 2 | مسار `/precedents/judgment/[slug]` | ✅ **أُصلحت**: الصفحة الآن تعرض بطاقة «قريباً» (DashboardComingSoon) بدل بيانات `DEMO_PRECEDENTS` التجريبية. |
| 3 | بيانات `demo-data*.ts` و`law-metadata-map.ts` | ✅ **أُصلحت (التطبيق)**: مُسيَّجة عبر `src/app/laws/demo-data-access.ts` — تُرجع `[]` في الإنتاج ما لم يُضبط `NEXT_PUBLIC_LIB_DEMO_FALLBACK=1`. `law-metadata-map.ts` تُرك كما هو (lookup، ليس محتوى وهمي). |
| 4 | البحث وتطبيع الأحرف | ⚠️ **تراجع معروف بعد إعادة كتابة FTS**: مسارا `search` و`autocomplete` يستخدمان الآن `textSearch('fts', ...)` بإعداد `library.arabic` (`copy=simple` لا يُطبّع). لذا البحث بـ`الإثبات` قد **لا** يطابق `الاثبات` المخزّن، و`١٤٤٤` قد لا يطابق `1444`. إن لم يعمل التطبيع فسجّله (كان يعمل سابقاً عبر `normalizeSearch`+`ilike`). |
| 5 | `seed-library.ts --clean` | ✅ **أُصلحت**: العلم يُمرَّر الآن لكل بذّار (seedLaws/Decrees/Precedents/Feqh) ويحذف قبل الإدراج (children-first) عند `--clean` وغير `--dry-run`. |
| 6 | ترقيم `parse-feqh.ts` | ⬜ **غير مُصلح (معلّق بصدق)**: ما زال `volume: 1` كقيمة افتراضية مع TODO صريح — لا يوجد كشف حقيقي للحجم. |
| 7 | بوابة الدفع | معطّلة بعلم `payments_gateway` — رسالة «الدفع غير متاح حالياً» متوقعة |
| 8 | تقسيم صفحات `/api/library/init` | ✅ **أُصلحت**: يقبل `?limit` (1-200، افتراضي 100) و`?page` ويستخدم `.range()` بدل `select('*')` غير المحدود. |
| 9 | فهرسة FTS/GIN | ✅ **أُصلحت (استخدام الفهرس)**: المسارات تستخدم `fts @@` بدل `.ilike` (توقف فحص الجدول الكامل) — لكن انتبه لتراجع التطبيع في العنصر #4. |

---

## 🐞 نموذج الإبلاغ عن الأخطاء / ملاحظات التعديل

انسخ هذا النموذج لكل خطأ/ملاحظة وأرسله للمطور:

```
رقم الخطأ: L-###
التاريخ: 2026-__-__
الفاحص: __________
الباحة المختبرة: [Free / Pro / AI / Admin]

القسم في الدليل: (مثال: ثانياً - البحث والاقتراحات)
رقم الخطوة: (مثال: 5)

رابط الصفحة: (مثال: /laws/labor-law)
الخطوات المتبعة:
1. ...
2. ...

السلوك المتوقع:
...

السلوك الفعلي (الخطأ):
...

لقطة شاشة: (مرفقة)
رسالة الخطأ من الطرفية/المتصفح (Console):
...

الشدة: [حرج 🔴 / متوسط 🟡 / بسيط 🟢]
هل هو من «الملاحظات المعروفة»؟ [نعم/لا]
```

---

## ✅ قائمة التحقق النهائية قبل الإطلاق

- [ ] جميع تبويبات المكتبة الأربعة تُحمّل من قاعدة البيانات.
- [ ] البحث + الاقتراحات يعمل. ⚠️ **تطبيع الأحرف (الإثبات/الاثبات، ١٤٤٤/1444) قد يكون متراجعاً** بعد إعادة كتابة FTS (إعداد `library.arabic` هو `copy=simple` لا يُطبّع) — اختبره وسجّله إن فشل (انظر ملاحظة #4).
- [ ] البحث المتقدم محجوب عن Free ومفتوح لـ AI.
- [ ] صفحة تفاصيل النظام: تنقل المواد + اللائحة التنفيذية + نسخ + طباعة + علامة مائية.
- [ ] المجلدات الذكية: إنشاء/إضافة/تثبيت/حذف مع عزل الملكية.
- [ ] الملاحظات معزولة لكل مستخدم.
- [ ] المبادئ/المراسم/الفقه/الكتب تُحمّل من مساراتها.
- [ ] بوابات الدفع تظهر عند تجاوز حد الباقة المجانية.
- [ ] بوابة الدفع معطّلة برسالة واضحة (متوقع).
- [ ] أدوات AI تُظهر «قريباً» بدل ردود وهمية عند عدم ربط n8n.
- [ ] لوحة الإدارة: تصنيفات موحّدة + «العناصر المجانية» فعّالة.
- [ ] RTL/LTR والجوال والطباعة سليمة.
- [ ] لا توجد بيانات تجريبية في وضع الإنتاج.
- [ ] `npx tsc --noEmit` و`npx next build` خالية من الأخطاء.