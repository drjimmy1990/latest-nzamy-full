# تقرير للمالك — ملاحظات UAT-20260915 على البروفايلات (العميل · الشركة · المحامي)

**التاريخ:** 2026-09-21 · **المرجع:** حزمة الاختبار `nzamy-developer-test-نهائي-2026-09-20` + `evidence/uat-20260915` · **الخطة:** `docs/plans/PROFILES_COMPLETION_PLAN_2026-09-20.md` · **التقرير الفني:** `docs/audits/2026-09-20-profiles-uat/00-IMPLEMENTATION-REPORT.md`

## 1. الخلاصة في سطور

- كل ملاحظات P0 وP1 اللي في حزمتك اتصلّحت في الكود **وفي قاعدة البيانات الحية** (المشروع `gdqfqfcxnwrwgaphtfhu`) اليوم 2026-09-21، بعد نسخة احتياطية كاملة قبل أي تعديل ونسخة ثانية بعده.
- الكود اترفع على GitHub (`main` → `8727d2d`) والنشر على الـ VPS جارٍ وقت كتابة هذا التقرير.
- **حسب معيارك (متصفح + API/DB + إعادة اختبار)** لا يوجد بند «مغلق» رسمياً بعد: التحقق حتى الآن آلي (1431 اختبار وحدة، 8 سلاسل اختبار RLS، تنفيذ على نسخة طبق الأصل من الـ schema الحية، ثم التنفيذ الحي نفسه بنجاح). الإغلاق يحتاج **جولة المتصفح معك** (القسم 5).
- اكتشفنا أثناء الشغل ثلاث حاجات مهمة ما كانتش في الحزمة (القسم 6) — أخطرها ثغرة في ملفات `documents` كانت شغالة فعلاً واتقفلت اليوم.

## 2. ما طلبته وما تم

| المعرّف / البند | طلبك | ما تم | حالة الإغلاق |
|---|---|---|---|
| **UAT-SEC-001** (P0) | مستخدم يقرأ بروفايل مستخدم آخر | migration `20260921_01`: policies `profiles` صارت 3 فقط (قراءة الذات، تعديل الذات، الأدمن) لدور `authenticated` بس، وسحب صلاحيات `anon`. مطبّق حيّاً ✔ | ينتظر سكربت `verify-auth-and-profile-rls` (المتوقع 47/47 مخفي) + جولة المتصفح |
| **UAT-SUB-001** (P0) | مستخدم يرقّي اشتراكه بنفسه | `20260906_fix_subscriptions` (كان مكتوباً ولم يُطبَّق) + `20260921_02`: لا policies ولا grants كتابة على `subscriptions` و`credit_transactions` لـ `authenticated`/`anon`. مطبّق حيّاً ✔ | ينتظر `verify-subscription-rls` + المتصفح |
| **UAT-STORAGE-001** (P0) | مستخدم B ينزّل ويمسح ملفات A في bucket `documents` | الـ 6 policies اللي كانت على `storage.objects` (معمولة من الداشبورد) **اتنين منهم كانا يسمحان لأي مستخدم مسجّل بقراءة ومسح أي ملف**. اتصلّحت كلها لتقييد الـ bucket + مجلد المالك (`auth.uid()`). مطبّق حيّاً ومُثبت من `pg_policies` ✔ | ينتظر `verify-document-storage-isolation` + المتصفح |
| **UAT-TEAM-001** (P1) | 500 على أعضاء المكتب/الشركة (`42P17` recursion) | `20260921_03`: إعادة بناء policies الكيانات الأربعة (firm/business/government/ngo) على دوال `security definer` (8 دوال)، بدون أي قراءة داخلية للجداول. الملفان `20260916_*` من الحزمة **لم يُطبَّقا** (مكسوران: `42P13` + أسماء policies خاطئة) وأعيدت تسميتهما `_superseded_`. مطبّق حيّاً ✔ | ينتظر `verify-entity-membership-rls` (11/11 بدون 500) + المتصفح |
| **UAT-BIZ-001 / TENANT-003 / COST-001** (P1) | `business_id` على الطلبات، جداول تكاليف المحكمة، `cr_number` | `20260914` + `20260906_court_costs` (كانا مكتوبين ولم يُطبَّقا). مطبّق حيّاً ✔ (+ backfill عضوية مالك الشركة: `business_members` 11 → 20) | ينتظر `verify-core-tenant-isolation` + `audit-deployed-migration-objects` |
| **UAT-LIVE-SESSION-001** (P0) | الجلسة تضيع بين المتصفح والـ SSR/API | فشل الاتصال بـ Supabase صار **503** واضحاً بدل 401/تحويل للدخول (58 نقطة)، وضع demo مستحيل في production، بوابة جلسة على `/dashboard` و`/settings`، مصافحة الدخول عبر `GET /api/v1/auth/session` | ينتظر الجولة الحية (دخول → تحديث → روابط مباشرة → POST محمي → قطع الشبكة ⇒ 503) |
| **UAT-ENV-001** | مسار الاتصال يفشل (TLS) | فحص صحة Auth عند الإقلاع + قسم `NODE_EXTRA_CA_CERTS` في `.env.example`، بدون تعطيل TLS | ينتظر تأكيد على السيرفر |
| **UAT-REG-001 / REG-002** (P1) | تحقق البريد/الاسم/الجوال في التسجيل | تحقق بريد واسم في `/register/client`، رسائل عربية، و`20260921_04`: قيد DB على الجوال بصيغة `+9665XXXXXXXX` (3 أرقام قديمة غير صالحة اتطبّعت أو اتحفظت في `metadata` بدون حذف)، و`handle_new_user` يطبّع الجوال عند التسجيل | ينتظر `verify-profile-write-guards` (جوال مشوّه ⇒ 23514) + المتصفح |
| **UAT-CONTACT-001** | API التواصل بلا تحقق | تحقق + حدود طول في `/api/v1/contact` | ينتظر جولة API |
| **خطوات المحامي ي١–ي٤، ك١** | رابط الملف العام، الجنسية، «قبول عملاء جدد»، الرابط بالـ slug لا UUID، الصياغة | كلها في صفحة الملف/التعديل؛ إخفاء هوية المراجعات مصلّح في المسارين | ينتظر المتصفح + لقطات «بعد» |
| **UAT-LIVE-CASE-001** | نموذج قضية جديدة يفشل بصمت | تعطيل حقيقي، تحقق على السيرفر قبل الإدراج، بطاقة العميل مربوطة دائماً | ينتظر المتصفح + صف `service_requests` |
| **UAT-LIVE-AI-001 / GHOST-002** | `/ai/direction-support` وهمي | صفحة «قريباً» صادقة؛ الروابط والشارة اتشالت (القرار ٥: الميزة الحقيقية تحتاج spec خاصة) | ينتظر المتصفح |
| **العميل الفرد (ك١، القرار ٤، ز٢)** | لوحة العميل تبتلع الفشل في بيانات demo | ملخص اللوحة ثلاثي الحالة، مدخل «الملف الشخصي»، `/settings?tab=` يعمل | ينتظر المتصفح |
| **الشركة (ك٢)** | لا يمكن تعديل عنوان/هاتف/بريد الشركة؛ الأعضاء وهميون | العنوان والمدينة والهاتف والبريد والموقع عبر نفس حقيبة `metadata.settings` (بدون migration)؛ الأعضاء: API حقيقي (قائمة/دعوة/دور/إزالة، 9 أدوار، الكتابة للمالك فقط)؛ العضو يرى بيانات الشركة للقراءة فقط برسالة واضحة؛ 6 صفحات mock صارت «قيد الإعداد» | ينتظر المتصفح بحسابين (مالك + عضو) |
| **UAT-GHOST-001** | بحث اللوحة يعرض محتوى ليس للمستخدم | البحث «من محتواك» مربوط بمستندات وطلبات الحساب نفسه فقط | ينتظر اختبار بحسابين |

## 3. ما لم يُنفَّذ (عمداً أو خارج النطاق)

- **بروفايل المكتب (firm) ومزوّد الخدمة (provider):** صفحاتهما ما زالت mock (0 استدعاء API) — خارج نطاق هذه الدفعة، ولا يُحسبان مكتملين.
- **6 صفحات أخرى في لوحة الشركة** (`circuits-emails`, `employee-contracts`, `hearings`, `reports`, `seconded-counsel`, `wallet`) ما زالت تعرض بيانات ثابتة — commit متابعة واحد يكملها إن أردت (القرار ٤).
- **ميزة «داعم الاتجاه + المدقق التشريعي»** الحقيقية: تحتاج spec مستقلة (القرار ٥).
- **ملف الدفع التجريبي** `20260916_enable_test_payment_gateway.sql`: **لم يُطبَّق** ولن يُطبَّق على الإنتاج (كان سيحوّل بوابة الدفع الحية إلى stub). `payments_gateway` = `disabled` كما كان.
- **4 migrations في الريبو لا يستخدمها الكود الحالي** (`academy`, `feqh is_synthetic_page`, `needs_human_review`, `private precedent details`): لم تُطبَّق حتى تقرر.

## 4. أسئلة تحتاج قرارك حتى نكمل

| # | السؤال | الافتراض الحالي إن سكتّ |
|---|---|---|
| 1 | **بريد البروفايل:** `profiles.email` ما زال قابلاً للتعديل مباشرة عبر PostgREST (منفصل عن `auth.users`). نسحب صلاحية تعديل `email`/`user_type`/`verification_status` من `authenticated` في migration جديدة؟ (Q8) | نعم — نسحبها |
| 2 | **القضايا:** جدول `public.cases` لا يُكتب فيه أبداً؛ جداول تكاليف المحكمة تشير إليه، فإدخال التكاليف غير قابل للوصول فعلياً رغم زوال الخطأ. من أين تُنشأ القضية في منتجك؟ (Q9) | خارج النطاق حتى تحدد |
| 3 | **القسم القانوني في الشركة:** حقل `has_legal_dept` صار قابلاً للتعديل، لكن الصلاحيات ما زالت تُقرَّر من فئة الاشتراك. أيهما الحَكَم؟ | فئة الاشتراك (كما هو) |
| 4 | **أرقام 920 الموحدة:** تُقبل كهاتف كيان (شركة/مكتب) أم يُشترط جوال؟ قيد الجوال الجديد على `profiles` يخص الأفراد فقط. | مسموحة للكيانات، ممنوعة كجوال شخصي |
| 5 | **6 صفحات الشركة المتبقية:** نكمل تحويلها إلى «قيد الإعداد» الآن أم تبقى؟ | نكملها |
| 6 | **بحث المكتبة:** الكود يستدعي RPC `library_search` غير موجود لا في الريبو ولا في المشروع (يعمل عبر fallback أبطأ). نبنيه؟ | لاحقاً |
| 7 | **جولة المتصفح:** نحتاج تسليم دخول يدوي لحسابات اختبار (محامٍ، عميل فرد، مالك شركة + عضو). متى؟ | — |
| 8 | **الانتقال إلى self-host:** النسخة بعد الإصلاحات جاهزة ومجرّبة الاسترجاع. نحدد موعداً بعد إغلاق هذه الجولة؟ وهل نُبقي نفس مفاتيح JWT (الجلسات تستمر) أم نولّد جديدة (الجميع يعيد الدخول — الأأمن)؟ | بعد الإغلاق؛ مفاتيح جديدة |

## 5. الخطوات القادمة بالترتيب

1. انتهاء النشر على الـ VPS والتأكد من `✓ Ready` (جارٍ).
2. تشغيل سكربتات التحقق `scripts/uat/verify-*.ps1` بممثلين تجريبيين جدد (ليس `actors.json` القديم) وحفظ المخرجات.
3. جولة المتصفح معك: كل بند في القسم 2 له عناصر إثبات محددة في تقارير `WP1…WP7`، مع لقطة «بعد» لكل لقطة «قبل» عندك.
4. مصفوفة جديدة `evidence/uat-<date>/uat-matrix.csv` (بدون الكتابة فوق ملف 2026-09-15) + `docs/audits/<date>-profiles-closure.md` لكل معرّف.
5. بعد الإغلاق: قرارات القسم 4، ثم موعد الـ self-host.

## 6. ثلاث حقائق اكتشفناها أثناء العمل

1. **ثغرة الملفات كانت حية:** policies bucket `documents` (المعمولة من الداشبورد بلاحقة `flreew_`) كانت تسمح لأي مستخدم مسجّل بقراءة ومسح أي ملف. اتقفلت اليوم — وهذا يفسّر نتيجة UAT-STORAGE-001 عندك.
2. **28 من 71 migration في الريبو لم تُطبَّق قط على المشروع** (ليس 3 فقط). طبّقنا منها ما يعتمد عليه الكود الحالي (8 ملفات — أهمها RPC أفعال العميل على طلبات الخدمة: «طلب تعديل» و«تعديل الوصف» كانا يفشلان لأن الدالتين غير موجودتين). سبب التراكم: `supabase db push` لم يُستخدم على المشروع قط، وأسماء الملفات تتكرر فيها البادئة الرقمية بما يمنع الـ CLI من استخدامه أصلاً. النشر الآن يتم **بدون** `db push`، والـ migrations يدوياً بترتيب موثّق (`_fix-delivery-2026-09-20/sql-apply-order/README-ORDER.md`).
3. **نسخ احتياطية:** `run1` (قبل أي تعديل) و`run2` (بعده) محفوظتان عند المطوّر خارج الريبو، مع نسخة جاهزة للاسترجاع على self-host اتجرّبت على PostgreSQL مستقل وطابقت النسخة الحية عنصراً بعنصر (3038 عنصر).

---

## 7. دليل الاختبار اليدوي — خطوة بخطوة لكل إصلاح

**الهدف:** تعيد أنت نفس اختباراتك في `UAT-20260915` على النسخة الجديدة، وتسجّل «بعد» مقابل كل «قبل».

### 7.0 التجهيز

| البند | التفاصيل |
|---|---|
| الحسابات المطلوبة | محاميان (أ، ب) · عميلان فردان (أ، ب) · مالك شركة + عضو `legal_manager` في نفس الشركة · مالك شركة ثانية (ب) · مالك مكتب (firm) + عضو. استخدم حسابات اختبار جديدة (`scripts/uat/seed-actors.ps1` يولّدها)، لا حسابات حقيقية |
| الـ token لاختبارات الـ API | بعد الدخول: DevTools → Network → أي طلب إلى `/rest/v1/` → انسخ قيمة `Authorization: Bearer …` — هذا هو token المستخدم الحالي. **لا تستخدم service_role في أي اختبار** |
| متغيرات الـ API | `SUPABASE_URL = https://gdqfqfcxnwrwgaphtfhu.supabase.co` · `ANON_KEY` من `.env.local` (`NEXT_PUBLIC_SUPABASE_ANON_KEY`). كل طلب REST يحمل `apikey: ANON_KEY` و`Authorization: Bearer <token>` |
| الدليل لكل اختبار | لقطة شاشة «بعد» + نص استجابة الـ API (الحالة + الجسم) + ناتج استعلام DB من SQL Editor، في `evidence/uat-<التاريخ>/` (لا تكتب فوق ملفات 2026-09-15) |
| الاختبارات الآلية الموازية | `scripts/uat/verify-*.ps1` — المطوّر يشغّلها بممثلين جدد ويرفق مخرجاتها بجانب دليلك |

### T-01 · UAT-SEC-001 — لا أحد يقرأ بروفايل غيره
1. ادخل بحساب العميل أ، خذ الـ token.
2. نفّذ: `GET {SUPABASE_URL}/rest/v1/profiles?select=id,email,display_name` مع token أ.
3. **المتوقع:** صف واحد فقط (أ نفسه). في اختبارك السابق رجعت عشرات الصفوف.
4. نفّذ: `GET …/rest/v1/profiles?id=eq.<id العميل ب>&select=id` مع token أ → **المتوقع:** `[]`.
5. بدون token (anon فقط): نفس الطلب → **المتوقع:** `[]` أو 401.
6. **DB:** `select policyname, roles from pg_policies where tablename = 'profiles';` → 3 صفوف بالضبط، كلها `{authenticated}`.
7. **السكربت:** `verify-auth-and-profile-rls.ps1` → `foreignProfileHidden 47/47`.

### T-02 · UAT-SUB-001 — لا ترقية ذاتية للاشتراك
1. بحساب أ: `GET …/rest/v1/subscriptions?select=*` → **المتوقع:** 200 واشتراكه فقط.
2. `PATCH …/rest/v1/subscriptions?user_id=eq.<id أ>` بجسم `{"plan_id":"enterprise"}` (أي حقل) → **المتوقع:** 401/403 ورسالة `permission denied for table subscriptions`.
3. `POST …/rest/v1/subscriptions` بأي جسم → **المتوقع:** نفس الرفض. نفس الشيء على `credit_transactions`.
4. في الموقع: `/settings` → «الخطة والحدود» تعرض الخطة الحقيقية بلا تغيير.
5. **السكربت:** `verify-subscription-rls.ps1` → كلا الكتابتين ترفضان.

### T-03 · UAT-STORAGE-001 — ملفات `documents` للمالك فقط
1. بحساب المحامي أ: ارفع مستنداً (تبويب المستندات في قضية). سجّل مساره: `documents/<uid أ>/<اسم الملف>`.
2. بحساب ب (token ب): أعد **نفس** الطلبين اللذين في دليلك السابق (`document-storage-isolation.json`):
   - `GET {SUPABASE_URL}/storage/v1/object/documents/<uid أ>/<الملف>` → **المتوقع:** 400 أو 404 (`Object not found`) — كان 200.
   - `DELETE` على نفس المسار → **المتوقع:** 400 أو 404 — كان 200.
3. بحساب أ: الملف ما زال يُفتح ويُنزَّل.
4. **DB:** `select policyname, cmd, qual from pg_policies where schemaname='storage' and tablename='objects';` → كل الصفوف تحتوي `bucket_id = 'documents'` **و** `auth.uid()… = (storage.foldername(name))[1]`.
5. **السكربت:** `verify-document-storage-isolation.ps1` → التنزيل والمسح الأجنبيان مرفوضان.

### T-04 · UAT-TEAM-001 — أعضاء المكتب/الشركة بلا 500
1. مالك المكتب: `/settings` → تبويب الفريق يعرض الأعضاء (كان 500).
2. مالك الشركة: `/dashboard/business/team` يعرض الأعضاء. افتحه في **نافذة خاصة جديدة** (تبويب بارد) → يعمل بلا 500.
3. عضو (لا مالك): `GET …/rest/v1/firm_members?select=*` أو `business_members` مع token العضو → **المتوقع:** 200 وقائمة أعضاء كيانه فقط. كان: 500 `infinite recursion detected in policy for relation "firm_members"` (42P17).
4. **DB:** `select proname from pg_proc where proname in ('is_firm_owner','is_business_owner','is_government_owner','is_ngo_owner','is_active_firm_member','is_active_business_member','is_active_government_member','is_active_ngo_member');` → 8 صفوف.
5. **السكربت:** `verify-entity-membership-rls.ps1` → 11/11 بدون 500.

### T-05 · UAT-BIZ-001 / TENANT-003 — طلبات الشركة معزولة
1. عضو الشركة أ ينشئ طلب خدمة من أي مسار AI.
2. **DB:** `select id, business_id from public.service_requests order by created_at desc limit 1;` → `business_id` = معرّف الشركة أ (كان `null`/عمود غير موجود).
3. مالك الشركة أ يرى الطلب في `/ai/orders`.
4. عضو الشركة ب: `/ai/orders` لا يعرضه، و`GET …/rest/v1/service_requests?id=eq.<الطلب>` مع token ب → `[]`.
5. **السكربت:** `verify-core-tenant-isolation.ps1` (فحوصات الشركة لم تعد محجوبة).

### T-06 · UAT-COST-001 — جداول تكاليف المحكمة موجودة
1. **DB:** `select to_regclass('public.court_cost_notices'), to_regclass('public.case_disbursements');` → الاثنان غير `null`.
2. `select column_name from information_schema.columns where table_name='firm_profiles' and column_name='cr_number';` → صف واحد.
3. مالك المكتب: `/settings` → «إعدادات الكيان» تعرض حقل السجل التجاري ويُحفظ.
4. ملاحظة: إدخال التكاليف نفسه غير قابل للوصول حتى تجيب على السؤال 2 في القسم 4 (`public.cases` لا يُكتب فيه).

### T-07 · UAT-LIVE-SESSION-001 — الجلسة لا تضيع
1. محامٍ: دخول → اللوحة تظهر → **F5** → ما زال داخلاً.
2. اكتب مباشرة في شريط العنوان: `/dashboard/lawyer` ثم `/ai/draft` ثم `/settings` → تُفتح بلا تحويل إلى `/login`.
3. `GET {الموقع}/api/v1/profile` (من DevTools أو curl بالكوكيز) → 200 ببياناتك.
4. أنشئ طلب خدمة صالحاً → 201 وصف في `service_requests`.
5. **(مع المطوّر، على السيرفر)** اقطع الاتصال بـ Supabase 30 ثانية (`iptables … REJECT` إلى `gdqfqfcxnwrwgaphtfhu.supabase.co` منفذ 443) → حدّث الصفحة/نادِ `/api/v1/profile` → **المتوقع:** **503** برسالة «تعذّر التحقق من الجلسة حالياً، حاول بعد قليل» **وبدون** تحويل إلى `/login`. أعد الاتصال → يعمل بلا إعادة دخول.
6. الدليل: `live-browser-lawyer.json` جديد + HAR للخطوة 3–4.

### T-08 · UAT-ENV-001 — فحص الإقلاع والاتصال
1. على السيرفر بعد `pm2 reload nzamy`: `pm2 logs nzamy --lines 50` → سطر فحص صحة Auth عند الإقلاع (نجاح، أو تحذير واضح لو الشهادة/الشبكة فيها مشكلة — بدون تعطيل TLS).
2. نموذج التواصل من الموقع → 200 وصف في `contact_messages`.

### T-09 · UAT-REG-001 / REG-002 — التسجيل يرفض القمامة
1. `/register/client` الخطوة 2: بريد `not-an-email` → لا يمكن المتابعة وتظهر رسالة عربية تحت الحقل.
2. جوال `abc@example.com` → رسالة «أدخل رقم جوال سعودي يبدأ بـ 05 — مثال: 0512345678» ولا يمكن المتابعة.
3. سجّل حسابات بجوال `0512345678` ثم `+966512345678` ثم `00966512345678` ثم `٠٥١٢٣٤٥٦٧٨` (أرقام عربية) → **DB:** `select phone from profiles where id = '<id>'` → دائماً `+966512345678`.
4. جرّب `+96605…`، 9 أرقام، 11 رقماً، أحرفاً → مرفوضة في المتصفح.
5. **API مباشر (كما في سكربتك السابق):** `PATCH …/rest/v1/profiles?id=eq.<id أنت>` بجسم `{"phone":"letters-and-email@example.test"}` مع token أنت → **المتوقع:** 400 وكود `23514` واسم القيد `profiles_phone_e164_saudi_mobile`. كان: 200 والقيمة تُخزَّن.
6. أعد تحميل `/settings` → الجوال معبّأ مسبقاً؛ `/onboarding` لا يُطلب من جديد.
7. **السكربت:** `verify-profile-write-guards.ps1` → `23514` و`invalidPhonePersisted: false`.

### T-10 · UAT-CONTACT-001 — API التواصل يتحقق
1. `POST {الموقع}/api/v1/contact` بجوال غير صالح أو رسالة أطول من الحد → 400 برسالة عربية.
2. بيانات صالحة → 200 وصف في `contact_messages`.

### T-11 · المحامي — خطواتك ي١–ي٤ وك١
1. **ي١.1** حرّر العنوان المهني والـ slug في `/dashboard/lawyer/profile/edit` → احفظ → أعد التحميل → صفحة الملف تعرض «رابط ملفك العام» بالـ slug المحفوظ (مع جملة البيتا).
2. **ي١.4** المحامي ب يحاول نفس الـ slug → «هذا الرابط مستخدم من محامٍ آخر» تحت الحقل.
3. **ك١.1** `/settings` → «الملف الشخصي» يعرض «الجنسية» → احفظ → أعد التحميل ونافذة خاصة → القيمة باقية. **DB:** `select nationality from profiles where id='<id>'`.
4. **ك١.5** في «المهنة» بدّل «أستقبل موكلين جدد» → أعد تحميل `/dashboard/lawyer/profile` → البلاطة الرابعة تنعكس. **DB:** `select is_accepting_clients from lawyer_profiles where user_id='<id>'`.
5. **G** «مشاركة ملفي المهني» في `/dashboard/lawyer` ينسخ رابطاً فيه الـ slug لا الـ UUID.
6. **ي٤** عميل يقيّم طلباً مكتملاً **بشكل مجهول** → تبويب «التقييمات» عند المحامي: لا اسم **ولا سطر «الخدمة: …»** (كان يكشف الهوية عبر اسم الخدمة)، والرد يعمل. الصفحة العامة `/lawyers/<slug>` (أو `GET /api/v1/lawyers/<id>`) لا تعرض سطر «عن: …».
7. **ي٣** معاينة الطباعة/PDF: صف الرابط الجديد **لا** يظهر.

### T-12 · UAT-LIVE-CASE-001 — قضية جديدة لا تفشل بصمت
1. `/dashboard/lawyer` → قضية جديدة → اترك العميل أو العنوان فارغاً → زر الإرسال معطّل فعلاً مع سبب.
2. أدخل بيانات صالحة (عميل مكتوب يدوياً) → تظهر القضية بعد التحديث. **DB:** `select lawyer_client_id from service_requests order by created_at desc limit 1` → غير `null`، وبطاقة جديدة في `lawyer_clients`.
3. **API:** `POST {الموقع}/api/v1/service-requests` بجسم `{"title":"   "}` → 400 برسالة عربية؛ عنوان 201 حرفاً → 400 كذلك.

### T-13 · UAT-LIVE-AI-001 / GHOST-002 — «داعم الاتجاه» صادق
1. `/ai/direction-support` → صفحة «قريباً» (لا نموذج وهمي).
2. القائمة الجانبية للمحامي/المكتب ومركز الذكاء الاصطناعي: لا صف ولا بطاقة تحمل شارة «جديد» للميزة.

### T-14 · العميل الفرد — ك١، القرار ٤، ز٢
1. سجّل فرداً → `/settings` → «الملف الشخصي»: الاسم، الجوال، البريد (للقراءة)، المدينة، الجنسية معبّأة. **لا حقل عنوان** (قرار Q4 — هذا متوقع).
2. عدّل → «تم الحفظ» → أعد التحميل ونافذة خاصة → القيم باقية.
3. `/dashboard/client` يعرض الاسم الحقيقي والخطة (لا بيانات تجريبية).
4. في DevTools احجب `/api/v1/dashboard/summary` (أو اجعله يعيد 500) → **المتوقع:** شريط كهرماني «تعذّرت قراءة ملخص لوحتك» مع زر «إعادة المحاولة» يعمل — **لا** لوحة فارغة ولا بيانات demo. هذا هو الاختبار الوحيد الذي يُظهر الإصلاح من المتصفح.
5. زر «الملف الشخصي» في `/dashboard/client` يفتح `/settings` على تبويب الملف الشخصي؛ `/settings?tab=security` يفتح الأمان؛ `/settings?tab=entity` على حساب فرد يفتح أول تبويب متاح (لا صفحة فارغة).
6. **ز٢** «الخطة والحدود» للفرد — إعادة اختبار فقط.

### T-15 · الشركة — ك٢ (مالك + عضو)
1. **المالك:** سجّل شركة (الاسم، السجل التجاري، صفة الممثل، نموذج الخدمة) → `/settings` → «إعدادات الكيان» تعرض بيانات الهوية **و** العنوان/المدينة/الهاتف/البريد/الموقع + نموذج الخدمة + مفتاح «قسم قانوني».
2. عدّل العنوان والهاتف → «تم الحفظ» → أعد التحميل ونافذة خاصة → باقية. **DB:** `select metadata->'settings'->>'phone', service_model, has_legal_dept from business_profiles where owner_user_id='<id>'` → الهاتف بصيغة `+9665…`.
3. **الفريق:** `/dashboard/business/team` (حقيقي الآن): ادعُ عضواً بالبريد بدور `legal_manager` → **DB:** صف في `business_members` بحالة `invited` أو `active`. غيّر دوره؛ أزله → الحالة `removed`.
4. **العضو:** يدخل → «إعدادات الكيان» تعرض بيانات الشركة **للقراءة فقط** برسالة «هذه البيانات يعدّلها مالك الحساب فقط» وبلا زر حفظ.
5. **API:** `PATCH {الموقع}/api/v1/profile` من حساب العضو بجسم يحتوي `businessProfile` → **403** «تعديل بيانات الشركة متاح لمالك الحساب فقط.» (كان 500).
6. `/dashboard/business` في نافذة خاصة جديدة (مالك ثم عضو) → بلا 500.
7. الصفحات الست `/dashboard/business/{requests,consultations,governance,health-check,kanban,reviews}` (كمدير أيضاً) → «قيد الإعداد»، لا بيانات ثابتة.
8. **ك٢ العزل:** العضو ينشئ طلباً → `business_id` مضبوط → عضو الشركة ب لا يراه (T-05).

### T-16 · UAT-GHOST-001 — البحث «من محتواك» لا يعرض محتوى غيرك
1. بحساب أ: ارفع مستنداً باسم مميّز وأنشئ طلباً. افتح البحث (⌘K / Ctrl+K) واكتب جزءاً من كل اسم → يظهران؛ صف المستند يفتح مستندات أ، وصف الطلب يفتح `/ai/orders/<id أ>`.
2. بحساب ب في نافذة خاصة: ابحث باسم مستند أ وعنوان طلبه → «لا توجد نتائج في محتواك» وصفر صفوف. لوحة Network: `/api/v1/documents` و`/api/v1/service-requests` تعيدان صفوف ب فقط.
3. بحساب ب الصق `/ai/orders/<id أ>` مباشرة → «الطلب غير موجود».
4. احجب `/api/v1/service-requests` في DevTools وافتح البحث → «تعذّرت قراءة جزء من محتواك» بجانب المستندات التي حُمّلت (لا «لا توجد نتائج»). احجب الاثنين → «تعذّرت القراءة» + «إعادة المحاولة» يعمل.
5. حساب جديد بلا محتوى → «لا توجد نتائج في محتواك»؛ بعد رفع ملف واحد → يظهر.

### T-17 · أفعال العميل على الطلب (كانت تفشل لأن الدوال لم تكن موجودة)
1. عميل لديه طلب بحالة `pending_assignment`: في `/ai/orders/<id>` عدّل الوصف → «حفظ التعديل» → يُحفظ ويظهر (كان يرجع خطأ). **DB:** `select metadata->'editHistory' from service_requests where id='<id>'` → عنصر جديد.
2. طلب **مكتمل ومسلَّم**: «إرسال طلب التعديل» مع ملاحظات → الحالة تصير `in_review`. **DB:** `select status, metadata->'revisions' from service_requests where id='<id>'`.
3. طلب لم يُسلَّم بعد: «طلب تعديل» يُرفض برسالة «لا يمكن طلب تعديل قبل تسليم المستند من فريق نظامي.».
4. عميل آخر يحاول نفس الفعل على طلب ليس له (API مباشر) → رفض `not_owner`.

### T-18 · تكامل النشر
1. المطوّر: `npm run build` نجح على السيرفر، و`grep -r "MOCK_" .next/server/app/dashboard/business` فارغ، و`grep -r "المتحدة ضد محمد العمري" .next` فارغ.
2. `_verify.sql` على قاعدة الإنتاج: 0 أخطاء (تم 2026-09-21 — أعده بعد أي migration جديدة).

## 8. ماذا تفعل لو فشل بند
اكتب في المصفوفة: المعرّف · الخطوة · المتوقع · الفعلي · لقطة · استجابة الـ API. لا تُغلق البند، وأرسله للمطوّر؛ بعد الإصلاح يُعاد نفس الاختبار من أوله (قاعدتك: «إعادة اختبار بعد إصلاح المبرمج»).
