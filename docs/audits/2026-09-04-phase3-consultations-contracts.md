# المرحلة ٣ — الاستشارات والعقود: سجل التنفيذ

**المرجع:** [`خطة_البناء_الكاملة_٢٠٢٦-٠٩-٠٢.md`](../../خطة_البناء_الكاملة_٢٠٢٦-٠٩-٠٢.md) §٧ · البنود ١٠٨ · ١١٠ · ١١١ · ١١٣ · ١١٤ · ١١٥ · ١١٦ · ١١٧ (والبند ١٠٩ — الكوكبيت المرئي — لا بنية له، خارج هذه المرحلة)
**الترحيل:** [`supabase/migrations/20260905_phase3_consultations_and_contracts.sql`](../../supabase/migrations/20260905_phase3_consultations_and_contracts.sql) — **مكتوب ومُختبَر محلياً، لم يُشغَّل على الإنتاج بعد** (المالك يشغّله). **يشترط** أن يكون ترحيلا المرحلة ٢ والمرحلة ٥ قد شُغِّلا — وقد شُغِّلا (٤ سبتمبر).

**التسلسل:**

1. الترحيل + اختبار العزل محلياً ← **منجَز** (§١–§٥)
2. العقود المشتركة ثم الشاشات ← §٦ (يُكتب عند إنجازه)
3. `tsc` + الاختبارات + بناء نظيف، ثم `main` ثم `owner-edits`

---

## ١ · ما وجدناه قبل الكتابة (قراءة فقط من الإنتاج ومن الكود)

| الحقيقة | الدليل | الأثر على التصميم |
|---|---|---|
| جدولا `consultations` و`contracts` موجودان منذ ٢٠٢٦٠٥١٨ **بصفر صف**، ولا يكتب فيهما شيء: كاتب الاستشارات الوحيد (`casesService.createConsultation`) لا يستدعيه أحد، ولا مسار API للعقود أصلاً | REST مباشر: `*/0` للجدولين · `grep` على `from("contracts")` = لا شيء | نتبنّى الجدولين ونكملهما بدل إسقاطهما (§١ من الخطة: لا `DROP`) |
| مسار `PATCH /api/v1/consultations/[id]` يسمح بعمود `notes` **غير موجود** في الجدول | `consultations/[id]/route.ts:79` | ملاحظات المحامي تُنقل إلى جدول مستقل بعزل خاص (القرار ٣) |
| كل شاشات الاستشارات والعقود تقرأ `service_requests` بمفتاح `type` — **وثلاث شاشات تختلف على معنى «عقد»**: مدير عقود المحامي يكتب `business_case`، صفحة «عقودي» للعميل تقرأ `ai_draft`، ومدخل `/ai/contracts` يكتب `ai_contracts`. عقد المحامي لا يراه عميله أبداً | `lawyer/contracts/page.tsx:360` · `client/contracts/page.tsx:230` · `orderIntake.ts:15` | جدول `contracts` واحد يقرؤه الطرفان؛ لا قيمة `type` رابعة |
| الاستشارة عند العميل حجز حقيقي (`service_requests.type='consultation'`) بلا دورة حياة عند المحامي: لا جدولة ولا إتمام ولا رأي مكتوب ولا تحويل إلى قضية؛ صفحة تفاصيل الاستشارة عند المحامي «قريباً» صراحةً | `lawyer/consultations/page.tsx` (لا تغيير حالة) · `lawyer/consultations/[id]/page.tsx:13-21` | دورة الحياة على `consultations` نفسها، والتحويل مرة واحدة بقيد فريد (القرار ٤) |
| ثلاث مفردات مختلفة لحالة الاستشارة في الكود، ولا قيد `CHECK` في الجدول | `casesService.ts:53` · `lawyer/consultations/page.tsx:34` · `client/consultation/page.tsx:43` | مفردات واحدة مقيَّدة: `requested / scheduled / completed / cancelled / no_show` |
| شاشات المكتب (الاستشارات والعقود، القائمة والتفاصيل) **وهمية بالكامل**: مصفوفات ثابتة، وأزرار «استشارة جديدة»/«عقد جديد» بلا معالج | `firm/consultations/page.tsx:7-13,40-43` · `firm/contracts/page.tsx:30-37,99-102` · `firm/contracts/[id]/page.tsx:45,160` | تُبنى على المسارات نفسها بعزل المكتب عبر `can_access_case_row` |
| «فحص العقد» في `/ai/contracts` ليس فحصاً آلياً: شاشات النتائج مُعطَّلة عمداً منذ المهمة C3 لأنها كانت مسرحاً، والطلب يذهب لفريق التنفيذ | `ai/contracts/page.tsx:20-23` | البند ١١٥ = رابط يحمل هوية العقد إلى المدخل، لا «فحص بنقرة» يُدّعى |
| المرحلة ٥ تركت مكان الرابط جاهزاً: `deadlines.kind` يقبل `contract`، وملف الترحيل نفسه أرجأ البند ١١٦ إلى جداول هذه المرحلة | `20260904_phase5:10,167` | `contract_obligations.due_on` ← صفّ `deadlines` يكتبه الـAPI (القرار ٥) |
| الإنتاج اليوم: `service_requests` **صفر صف** (حُذفت صفوف الاختبار الـ٢٩) | REST مباشر | لا backfill له أثر؛ الترحيل يتضمّنه للاكتمال فقط |
| **مصدر البيانات الحقيقي للاستشارات هو `service_requests`**، وله ثلاثة كتّاب (معالج العميل، نافذة حجز المحامي، الإدارة) | `client/consultation/new:316` · `lawyer/consultations:262-315` | صفّ `consultations` يُولد بـtrigger مع طلبه، أيّاً كان الكاتب (القرار ٢) |

## ٢ · ما في الترحيل

| القسم | المحتوى |
|---|---|
| `consultations` (يُكمَّل) | افتراضي خادمي للمعرّف · `firm_id` · `lawyer_client_id` · `duration_minutes` (٥–٤٨٠) · `ended_at` · `outcome` (`advice_given / opinion_delivered / converted_to_case / no_action / referred`) · **الرأي المُسلَّم** `opinion_text` + `opinion_delivered_at` + `opinion_attachment_path` بقيد: لا رأي بلا وقت تسليم · `converted_case_request_id` · `fee_sar` + `fee_paid` · `cancelled_reason` · قيد الحالة الخماسي · فهارس المحامي/المكتب/العميل/الموعد · `updated_at` تلقائي |
| `consultation_notes` (جديد) | `visibility` `private` (الكاتب فقط) / `firm` (زملاؤه النشطون)؛ **العميل لا يقرؤها أبداً** ولا يكتبها |
| trigger على `service_requests` | عند إدراج طلب `type='consultation'`: صفّ `consultations` من `metadata` الموجودة أصلاً (`mode` بعد التحقق من مفرداته، `day`+`time` بتوقيت الرياض من نافذة المحامي، `duration` إن كانت في المدى، `specialty`) — الحالة `scheduled` إن كان مُسنَداً وله موعد، وإلا `requested`. وعند تحديث الإسناد/الحالة/المكتب على الطلب تتبعه السجلّة (إلغاء الطلب = إلغاء الاستشارة إن لم تكتمل). `security definer` لأن الكاتب قد يكون العميل |
| التحويل مرة واحدة | `service_requests.source_consultation_id` + فهرس فريد جزئي → المحاولة الثانية 23505 |
| `contracts` (يُكمَّل) | إعادة تسمية `assigned_user_id` → `owner_user_id` (النمط الموحَّد؛ لا كود يشير إليه) · `firm_id` · `lawyer_client_id` · `title` · `counterparty_name` · `value_sar` · `currency` · `starts_on`/`ends_on` (قيد الترتيب) · `auto_renew` · `renewal_notice_days` (٠–٣٦٥) · `signed_on` · `current_version_id` · `notes` · قيد النوع (تسعة أنواع) · قيد الحالة (`draft / under_review / pending_signature / active / expired / terminated / cancelled`) |
| `contract_versions` (جديد) | نسخة لكل ملف: `version_no` فريد داخل العقد · `label` (`draft / revised / final / signed`) · `storage_path` في حاوية `documents` الموجودة · الرافع · القياس |
| `contract_parties` (جديد) | الدور/النوع/الاسم/نوع الكيان · ربط اختياري ببطاقة `lawyer_clients` · سجل تجاري (١٠ أرقام) · **لا رقم هوية هنا** (قاعدة المرحلة ٢: البصمة فقط، وعلى بطاقة الموكّل وحدها) |
| `contract_obligations` (جديد) | `kind` (`delivery / payment / notice / renewal / termination / other`) · `due_on` · الطرف المسؤول · الحالة · **`deadline_id`** → صفّ في الرادار |
| `contract_payments` (جديد) | جدول مستحقّات لا شحن: `stage` (`advance / milestone / final / other`) · مبلغ موجب · `due_on` · الحالة (`paid` تستلزم `paid_on`) |
| الرادار | `deadlines.contract_id` (محروس بـ`to_regclass`) + FK من الالتزام إلى المهلة |
| RLS | الاستشارات: يقرؤها طالبها أو جانب المحامي (`can_access_case_row(lawyer_user_id, firm_id)`)؛ **يحدّثها جانب المحامي وحده** · الملاحظات: الكاتب/المكتب · العقود: جانب المحامي أو `client_user_id`؛ الكتابة لجانب المحامي · الجداول الأربعة الفرعية عبر دالتين `security definer` (`can_access_contract` / `can_manage_contract`) على نمط إصلاح التكرار |

**لا `DROP` ولا `TRUNCATE` ولا `DELETE`. لا نقل بيانات.**

## ٣ · الاختبار — بعد ترحيلي المرحلتين ٢ و٥ في القاعدة نفسها

`supabase/tests/rls/run.sh` صار يقبل عدّة ترحيلات بالترتيب قبل ملف الاختبار (المرحلة ٣ تعتمد على `lawyer_clients` و`deadlines`). واستُكملت `stubs.sql` بأعمدة ٢٠٢٦٠٥١٨ الحقيقية للجدولين (`scheduled_at`، `metadata`، …) — أول تشغيل توقّف عندها، فأُضيفت إلى الـstubs **وأُعيد ذكرها في الترحيل بـ`if not exists`** حتى لا يتوقّف على قاعدة ناقصة. الاختبار: [`phase3_consultations_and_contracts.test.sql`](../../supabase/tests/rls/phase3_consultations_and_contracts.test.sql) — خمسة مستخدمين: A وB محاميان مستقلّان، O صاحب مكتب F، C عضو نشط في F، K عميل.

| # | السيناريو | النتيجة |
|:--:|---|---|
| T0 | ثلاثة طلبات استشارة بأشكال الكتّاب الثلاثة → ثلاثة صفوف؛ حجز المحامي `scheduled` بموعد `2026-09-10 10:30` ومدّة ٤٥؛ حجز العميل `requested` بلا محامٍ؛ مدّة ٩٠٠ تُهمَل لا تُخزَّن؛ طلب غير استشارة لا يُنشئ صفاً؛ الإسناد اللاحق ينتقل إلى السجلّة | ✓ |
| T1 | K يرى استشارته وحدها؛ A يرى الاثنتين المُسنَدتين إليه؛ B لا شيء؛ O يرى استشارة عضوه C | ✓ |
| T2 | K لا يغيّر الحالة (٠ صف)؛ A يجدولها؛ حالة مجهولة 23514؛ رأي بلا وقت تسليم 23514؛ الرأي المُسلَّم يقرؤه K | ✓ |
| T3 | ملاحظة A الخاصة لا يراها K ولا B؛ K لا يكتب ملاحظة (42501)؛ ملاحظة C «للمكتب» يراها O وخاصته لا | ✓ |
| T4 | التحويل الأول يُخزَّن؛ **الثاني 23505**؛ الرابط العكسي يُخزَّن | ✓ |
| T5 | عقد A يراه A لا B؛ B لا يُدرج باسم A (42501)؛ عقد C بمكتب F يراه O؛ حالة مجهولة 23514؛ `ends_on < starts_on` 23514 | ✓ |
| T6 | نسخة v1؛ **v1 مرّة ثانية 23505**؛ الغريب لا يرفع نسخة؛ العميل K (بعد ربطه) يقرأ العقد ونسخه ولا يرفع ولا يعدّل (٠ صف) | ✓ |
| T7 | طرف بسجل تجاري صحيح؛ `123` مرفوض؛ التزام بحالة مجهولة مرفوض؛ دفعة صفر مرفوضة؛ `paid` بلا `paid_on` مرفوضة؛ B لا يرى شيئاً من الفرعيات؛ O يدير التزامات عقد عضوه | ✓ |
| T8 | مهلة رادار بـ`contract_id` وربط الالتزام بها | ✓ |
| footer | السياسات: consultations=3 · notes=4 · contracts=4 · versions=3 · parties=2 · obligations=2 · payments=2؛ الأعمدة الأربعة المتوقَّعة | ✓ |

**٤٦ تحققاً (٣٢ قيمة متوقَّعة + ١٤ مساراً استثنائياً)، كلها كما هو متوقّع.** وأُعيد تشغيل اختبارَي المرحلتين ٢ و٥ على المشغّل المعدَّل والـstubs المستكملة — كلاهما أخضر بلا خطأ.

## ٤ · قرارات مسجَّلة

- **المعرّفات تبقى `text`** في `consultations` و`contracts` لأنها مربوطة بـ`service_requests.id` النصّي؛ الجداول الفرعية `uuid`. لا خياطة أنواع بين الاستشارة وطلبها.
- **الرأي على الصفّ هو الرأي المُسلَّم بحكم القيد**؛ المسوّدات ملاحظات. السبب: RLS على مستوى الصفّ، والعميل يقرأ صفّه بكل أعمدته عبر PostgREST.
- **`contract_payments` جدول مستحقّات يتابعه المحامي بيده** — لا فاتورة ولا تحصيل (المرحلة ٤، محجوبة بالسؤال ٣).
- **لا حساب تاريخ في SQL**: مهلة الالتزام/التجديد يكتبها الـAPI في `deadlines` بالمحرك والتذكيرات نفسها.
- **البند ١٠٩ (المقابلة المرئية والتدوين اللحظي)** لا يُبنى: لا بنية اجتماعات على المنصّة، والادّعاء أسوأ من الغياب.

## ٥ · ما لم يُفعل هنا عمداً

- لا كود شاشات في هذا الالتزام (الخطوة ٢).
- ثلاث قيم `type` القديمة للعقد على `service_requests` تبقى كما هي؛ الشاشات المعاد بناؤها تقرأ `contracts` فقط.
- سياسات حاوية `documents` في `storage.objects` كما هي (مسار `{uid}/…`)؛ نسخ العقود تُرفع تحت مجلد الرافع نفسه، وقراءة الزملاء/العميل عبر روابط موقَّعة من الخادم بعد فحص RLS.
