# تدقيق حساب المحامي — ٢٧ أغسطس ٢٠٢٦

**النطاق:** ٢٤ مسار تحت `/dashboard/lawyer` وما تستورده، ~١١٬٤٠٠ سطر.
**الطريقة:** سبعة قرّاء مستقلين، ثم لكل منطقة **وكيل مهمته الوحيدة تكذيب ما وجده**.
**السياق:** ستة حسابات محامين حقيقية في الإنتاج. حساب الشركة: صفر.

> نتيجة التكذيب: لم يسقط شيء يُذكر. أحد المراجعين افتتح تقريره بـ«لم أستطع قتل
> واحدة منها»، وثلاثة أبلغوا أن الحال **أسوأ** مما وُصف. الأرقام أدناه هي ما نجا.

| الخطورة | العدد | الحالة |
|---|:-:|---|
| حرجة | 22 | ✅ أُصلحت |
| مرتفعة | 53 | ✅ أُصلحت |
| متوسطة | 54 | ⏳ مسجَّلة للجولة القادمة |
| منخفضة | 15 | ⏳ مسجَّلة للجولة القادمة |
| **الإجمالي** | **144** | |

---

## Lawyer dashboard — «جميع القضايا» list, the case file (/dashboard/lawyer/cases/[id] and all seven tabs), the sharing page, AddCaseModal, and LawyerCaseSubViews

This area is not healthy. The catastrophic finding the brief described as "already fixed" is NOT fixed: only the AI-analysis overlay was repaired; the MOCK_NODES seed at src/app/dashboard/business/kanban/_use-case-graph-state.ts:22 is untouched, and cases/[id]/page.tsx still mounts `<CaseGraphView isDark={isDark} isGlobal={false} />` at lines 1037 and 1054 with no `initialNodes`, so an invented contractor dispute still renders under the lawyer's real case title beside a «نظامي AI» badge — and three more fabrication surfaces hang off it (a canned "AI analysis", a fake voice transcription that writes a legal instruction into the board, and an export headed «تحليل آلي بواسطة نظامي AI»). Separately, the case list is unconditionally empty for every lawyer: `filtered` is a useMemo whose dependency array omits `cases` (cases/page.tsx:181), so after the fetch resolves the header prints «N قضية» while the list beneath it prints «لا توجد قضايا مطابقة». Almost every derived field the list shows is a hardcoded default rather than a query — degree, collab, team, nextDateSort, hasDeadline — which makes four filter groups dead and turns the entire appeal-deadline apparatus (banner, «طعون» footer counter, «طعون قادمة» filter) into a permanent zero. The sharing page is honestly gated and correct; LawyerCaseSubViews.tsx is genuinely unreferenced.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | بيانات مخترعة | `_use-case-graph-state.ts:22` | The case-file Graph tab still seeds a real lawyer's case with MOCK_NODES — an invented contractor dispute — under the real case title and a «نظامي AI» badge. This was NOT fixed; only the AI-analysis overlay was. |
| حرجة | عمل وهمي | `_use-case-graph-state.ts:264` | The graph's «تحليل AI» button is a two-second setTimeout that analyses nothing, and it runs over the fabricated board |
| حرجة | بيانات مخترعة | `_use-case-graph-state.ts:272` | The graph node mic button («تفريغ صوتي») fakes a 2.5-second voice transcription and writes a hardcoded legal instruction into the lawyer's own case board |
| حرجة | بيانات مخترعة | `_use-case-graph-state.ts:135` | «تصدير مستند» exports the fabricated graph as a document headed «تحليل آلي بواسطة نظامي AI» with three canned «التوصيات» |
| حرجة | فشل صامت | `page.tsx:181` | The case list is unconditionally empty: `filtered` is memoised without `cases`, so «لا توجد قضايا مطابقة» renders under a header that counts the real cases |
| مرتفعة | وعد لا يُنفَّذ | `CaseGraphView.tsx:97` | The graph canvas is labelled «مساحة محفوظة» ("saved workspace") but nothing is ever persisted, and switching tabs discards the lawyer's whole board back to the mock |
| مرتفعة | وعد لا يُنفَّذ | `AddCaseModal.tsx:107` | AddCaseModal's success screen states the case was filed as ACTIVE with a team assigned; both halves are false |
| مرتفعة | مُدخلات تُرمى | `AddCaseModal.tsx:69` | AddCaseModal's «مستوى الأهمية» and «إسناد إلى المحامي» are collected, stored under keys nothing reads, and silently overridden — a case marked «حرجة» displays as «عادي» |
| مرتفعة | بيانات مخترعة | `page.tsx:122` | Every hearing the lawyer schedules is rendered in «جميع القضايا» as a case, and inflates the case counts |
| مرتفعة | فشل صامت | `page.tsx:113` | A failed case query is served as a 200 with an empty list, so a server error is displayed to the lawyer as «لا توجد قضايا» |
| مرتفعة | بيانات مخترعة | `lawyerCasesData.ts:97` | Nothing writes `metadata.deadline`, so the entire appeal-deadline apparatus is a permanent zero: the red «طعون» banner never fires, the footer counter always renders 0, and the «طعون قادمة» filter always returns nothing |
| مرتفعة | فشل صامت | `workflowService.ts:50` | If the server rejects the case creation, the modal still says «تم إضافة القضية بنجاح!» and the case is written only to this browser's localStorage |
| متوسطة | بيانات مخترعة | `page.tsx:350` | The case file's «الفريق» tab prints the assigned lawyer's raw UUID as their name, and the header prints it as the assignee |
| متوسطة | مُدخلات تُرمى | `page.tsx:442` | The Notes tab runs a 600ms fake save spinner and then erases the lawyer's typed note |
| متوسطة | زر ميت | `page.tsx:473` | Document view/download buttons silently do nothing when the signed URL cannot be produced |
| متوسطة | فشل صامت | `page.tsx:375` | A failed task query on the case file renders as «لا توجد مهام لهذه القضية بعد» |
| متوسطة | بيانات مخترعة | `page.tsx:140` | A cancelled case is presented as «مغلقة» with its progress bar filled through to «إغلاق», indistinguishable from a completed one |
| متوسطة | فشل صامت | `page.tsx:94` | Dragging a case between Kanban columns never reverts when the server refuses the status change — the card stays moved and no error is shown |
| متوسطة | زر ميت | `page.tsx:181` | The «المحكمة» filter does nothing: `courtFilter` is applied inside the memo but is missing from its dependency array |
| متوسطة | زر ميت | `page.tsx:167` | The «اليوم» / «هذا الأسبوع» / «هذا الشهر» time filters always return zero because `nextDateSort` is never set on a real case |
| متوسطة | بيانات مخترعة | `lawyerCasesData.ts:93` | Every real case is badged «ابتدائي» (first-instance) regardless of its actual court degree, and the degree filter and Kanban grouping are dead |
| متوسطة | بيانات مخترعة | `lawyerCasesData.ts:101` | The collaboration rail hardcodes every case as «بمفردي», so the «مشتركة» and «فريقي» tabs are permanently empty and permanently show 0 |
| متوسطة | فشل صامت | `casesService.ts:144` | A server error loading a case is displayed to the lawyer as «القضية غير موجودة … قد يكون الرابط غير صحيح أو أن القضية محذوفة» |
| متوسطة | بيانات مخترعة | `workflowService.ts:41` | The case list silently stops at 20 rows — the API's default limit is never overridden and the `total` it returns is never read |
| منخفضة | بيانات مخترعة | `LawyerCaseSubViews.tsx:128` | LawyerCaseSubViews.tsx is entirely unreferenced dead code, and contains a fabricated AI case-strength percentage plus three dead controls |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — src/app/dashboard/lawyer/cases/[id]/sharing/page.tsx is exemplary and should be left alone. It is a bare DashboardComingSoon with an accurate reason («لا تُحفظ إعدادات المشاركة») and a working backHref, and its header comment records exactly which controls were removed and why. This is the correct fix pattern for the defect class.
- HEALTHY — the Tasks tab's write paths on cases/[id]/page.tsx are correct and are the model the rest of the file should follow: `addCaseTask` surfaces a real `taskError` (page.tsx:400-407), and both `cycleTaskStatus` (:409-419) and `toggleCaseSubtask` (:424-433) do optimistic updates with genuine reverts on a falsy server response. The «إضافة جلسة» button on the Hearings tab is correctly `disabled` with «قريباً» (page.tsx:~893). The Overview tab's AI card is correctly neutralised to a «قريباً» state with no fabricated percentage (page.tsx:653-663).
- HEALTHY — document upload is real: `uploadDocumentFile(file, { requestId: id })` followed by a refetch (page.tsx:459-471), with a visible `uploadError` banner. Only the download half is silent (reported).
- HEALTHY — no IDOR. service_requests_select_policy (supabase/migrations/20260815_marketplace_excludes_ai_workspace.sql:28-54) scopes a lawyer to `requester_user_id = auth.uid() OR assigned_to = auth.uid()`; the marketplace browse clause requires `lawyer_profiles.verification_status = 'verified'` and all six production lawyer_profiles are `pending`, so it is currently inert. The GET route's lack of an assigned_to filter (api/v1/service-requests/route.ts:59-61) is therefore not exploitable — RLS carries it. Worth noting the flip side: because that clause is inert, none of the 11 `pending_assignment` rows is visible to any lawyer, so the case list can only ever contain rows the lawyer created themselves.
- TWO FINDINGS MASK EACH OTHER on the case list — the `filtered` memo bug (cases/page.tsx:181) empties the list unconditionally, so the `degraded`-flag silent failure sits behind it. A fixer who patches only the `degraded` check will believe the screen works; a fixer who patches only the dependency array will re-expose the silent failure. Both need to land.
- THE BRIEF'S PREMISE WAS WRONG ON ONE POINT, and I have checked it twice. The instruction said the CaseGraphView/MOCK_NODES finding was already fixed and should not be re-reported. It is not fixed. What was fixed is a different file — CaseGraphOverlays.tsx, whose AI-analysis panel no longer cites the invented «سابقة قضائية (رقم ٣٤٢ لعام ١٤٤٤هـ)». The seed at _use-case-graph-state.ts:22 is byte-for-byte the pattern described, and cases/[id]/page.tsx still mounts the component unseeded at lines 1037 and 1054. That overlay's own comment (CaseGraphOverlays.tsx:223-236) explicitly lists two claims it could not reach, and the seed is not among them, so it was not deferred — it was missed. I have reported it as finding 1.
- COULD NOT DETERMINE — whether the six production lawyers have created any cases at all through AddCaseModal, and therefore how many rows are affected today. I read no production data; the counts in the brief (29 service_requests) are not broken down by creator. This changes nothing about the findings' validity but does affect how urgent the 20-row cap is in practice.
- NOT REPORTED (out of scope by instruction) — /marketplace and /lawyers are unreachable under BETA_MONOPOLY_MODE and were not examined. The lawyer routes using DashboardComingSoon correctly were left alone. No `!isSupabaseMode` or `isDemoUiEnabled` branch is reported: I confirmed the two BACKEND_MODE constants are module-level (src/lib/runtimeMode.ts, src/lib/services/api.ts:20) and every finding above is on the supabase side of the branch — the workflowService demo fallbacks I do report (findings 12 and 13) run in the `catch` of the supabase path, not in the dead-code-eliminated demo path.

---

## Lawyer «الملف المهني» (/dashboard/lawyer/profile), its editor (/profile/edit), and «طلباتي في السوق» (/dashboard/lawyer/marketplace → MyMarketplaceDashboard)

This area is not healthy. The profile page renders the lawyer's REAL identity (name, licence number, city, bio, specialties, verified seal — all fetched from GET /api/v1/profile) and then surrounds it with roughly forty fabricated numbers pulled from two module-constant files (src/app/dashboard/lawyer/_data/analytics.ts and performance.ts): a 74% win rate over 151 named case outcomes, 123,450 ر.س of annual revenue on a platform where no money has ever moved, a client-satisfaction tier placing the lawyer "in the top 50% of his city", and a national percentile. The «مشاركة» modal then offers to publish the win rate and the profile on a card that travels off-platform, via a share URL that 404s. /dashboard/lawyer/marketplace is one click from the lawyer's landing page («نشر في السوق», src/app/dashboard/lawyer/page.tsx:460 — the sidebar filter does not cover it) and is 100% constants: named service providers, a named prison, a hearing date, and two collaboration invitations carrying 15,000 and 22,000 ر.س fee splits, with «قبول الدعوة» / «رفض» / «اقترح هذا التوزيع» all handler-less. The one genuinely important control on this surface — the `marketplace_visible` checkbox, which profile/edit is the app's only writer of — does persist, but it lists the lawyer nowhere and says nothing about why.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | بيانات مخترعة | `page.tsx:790` | «نتائج القضايا» shows a fabricated 74% win rate over 151 invented case outcomes, and the share modal offers to publish it on a professional card. |
| حرجة | بيانات مخترعة | `page.tsx:878` | «ملخص الإيرادات» reports 123,450 ر.س of annual revenue to a lawyer on a platform through which no money has ever moved. |
| حرجة | بيانات مخترعة | `MyMarketplaceDashboardData.ts:44` | «طلباتي في سوق المهنيين» is entirely fabricated: named service providers, a named prison, a hearing date, and an offer marked «مقبول» implying the lawyer already retained someone. |
| حرجة | بيانات مخترعة | `MyMarketplaceDashboardData.ts:134` | «دعوات التعاون» invents two named lawyers inviting this lawyer onto cases, with 15,000 and 22,000 ر.س fee splits and a live-looking «قبول الدعوة». |
| مرتفعة | بيانات مخترعة | `performance.ts:199` | The «الأداء» tab tells the lawyer his national percentile against «محامو المملكة», computed entirely from hardcoded averages. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:139` | The «الظهور في دليل المحامين» checkbox — the app's only writer of `marketplace_visible` — persists but lists the lawyer nowhere, and says nothing about the two conditions that stop it. |
| مرتفعة | فشل صامت | `page.tsx:71` | If the profile GET fails, the editor keeps the Save button live over an empty form — pressing it wipes the lawyer's bio, specialties, licence number and bar association, and forces `marketplace_visible` back to false. |
| مرتفعة | بيانات مخترعة | `page.tsx:116` | «مستوى رضا الموكلين» tells the lawyer 61% of his clients are promoters and places him in the top 50% of his city — with zero reviews and zero consultations in the database. |
| مرتفعة | بيانات مخترعة | `page.tsx:97` | The profile header shows «١٤٣ قضايا / ٤٫٨★ / ٧+ س خبرة / ٨٩ تقييمات» as hardcoded literals, sitting beside the lawyer's real name and licence number — and the page discards the real `years_experience` it fetched. |
| مرتفعة | بيانات مخترعة | `page.tsx:214` | Every number on the «الأداء» tab is a module constant — hours, tasks, active cases, pomodoros, productivity %, streak, the week bar chart, the year activity chart, the metal tier, and the four «مؤشرات التطوير المهني» scores. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:291` | The profile page never shows the lawyer his verification status or his directory visibility — the two facts that decide whether anything on the page is public. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:253` | «بعض الإحصائيات تجريبية … ستُحدَّث تلقائياً مع الاستخدام» promises an automatic update that no code performs. |
| متوسطة | زر ميت | `LawyerProfileForms.tsx:235` | «مشاركة» hands the lawyer a share link for his professional profile that 404s, and copies it to his clipboard. |
| متوسطة | زر ميت | `LawyerProfileForms.tsx:248` | The share modal's two action buttons — «مشاركة عبر واتسآب» and «مشاركة البطاقة» — have no handlers, so the six privacy toggles above them are collected and discarded. |
| متوسطة | وعد لا يُنفَّذ | `LawyerProfileForms.tsx:320` | «المؤهلات», «المحاكم» and «التواصل الاجتماعي» render permanently empty — the API returns none of these fields and the editor exposes no input for them. |
| متوسطة | مُدخلات تُرمى | `FeeSplitModal.tsx:127` | The fee-split calculator's «اقترح هذا التوزيع» has no handler, so the split the lawyer drags on the slider is discarded on close. |
| متوسطة | زر ميت | `MyMarketplaceDashboard.tsx:427` | «قبول الدعوة» and «رفض» on collaboration invitations have no handlers — the lawyer can neither accept nor decline. |
| متوسطة | زر ميت | `MyMarketplaceDashboard.tsx:124` | Every outbound link on «طلباتي في السوق» lands on /services instead of where its label says. |
| منخفضة | فشل صامت | `page.tsx:183` | A failed profile fetch is swallowed into a blank profile with no message. |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — `marketplace_visible` genuinely round-trips. I traced the full chain rather than assuming: PATCH /api/v1/profile allowlists it (route.ts:194), all three toggle columns exist in the schema (`marketplace_visible` 20260603_phase1_001_profiles.sql:106, `is_accepting_clients` 20260616_production_readiness_fixes.sql:13, `show_contact` 20260705_lawyer_show_contact.sql:12), the route correctly routes lawyer-only keys to `lawyer_profiles` and refuses non-lawyers BEFORE any write (route.ts:255-260), and the RLS policy "lawyers update own profile" (20260603_phase1_001_profiles.sql:144-147) admits the self-write. The defect is what the flag then achieves, not the plumbing.
- HEALTHY — the achievements and reviews tabs are correctly withheld. `ACHIEVEMENTS` (page.tsx:74-83, eight fabricated badges with dates like «مارس ٢٠٢٤»), `REVIEWS` (85-89, three named five-star clients) and `MILESTONES` (91-95, «قضايا مكسوبة 143/200») all still exist in the file and their components are still imported, but `PROFILE_TABS` is `["overview", "performance"]` (line 107), `isProfileTab` gates the `?tab=` reader (lines 109-111, 236-239), and the `tabs` array (lines 224-228) renders only two buttons. So `?tab=achievements` and `?tab=reviews` both fall back to overview and none of that content is reachable. I did not report them.
- HEALTHY — «تصدير PDF» (page.tsx:297-304) is honestly disabled: `disabled`, `opacity-50 cursor-not-allowed`, and both `title` and `aria-label` read «تصدير PDF — قريباً». That is the correct shape for an unbuilt control.
- HEALTHY — the editor's load-failure copy «تعذّر تحميل بيانات الملف» (edit/page.tsx:66) correctly says the read failed rather than showing an empty state as fact; and the error path renders the API's Arabic message verbatim (line 92), which the API is written to guarantee (route.ts:4-18). The defect I filed is that Save stays enabled underneath it, not the message.
- HEALTHY — the profile page's identity mapping is careful and I want that on the record: the comment at lines 46-48 and the code at 171-181 map real `profiles` + `lawyer_profiles` columns, fall back to honest empties, and drive the verified seal off `verification_status === "verified"` rather than a hardcoded true. Someone fixed a real bug here (the `res.data` guard noted at lines 163-165). The fabrication is entirely in the statistics layered on top.
- REACHABILITY, stated once because it decides four blocking findings: /dashboard/lawyer/marketplace is NOT redirected. BETA_MONOPOLY_MODE redirects /marketplace (src/app/marketplace/layout.tsx:19) and /lawyers (src/app/lawyers/layout.tsx:27) via layout `redirect()`, and it strips /dashboard/lawyer/marketplace from the SIDEBAR (src/constants/navigation.sidebars.ts:462 inside `hiddenHrefs`, applied at lines 470-473 by filtering sidebar group items). It does nothing to the route itself, and the lawyer dashboard landing page links to it unconditionally at src/app/dashboard/lawyer/page.tsx:460 («نشر في السوق», gold-bordered, in the same header toolbar as «الصائغ القانوني» and «قضية جديدة»). src/lib/notificationsStore.ts:109 also carries a notification pointing there.
- NOT REPORTED as findings, for the record: the `AI_TOOLS` progress bars divide by a hardcoded `47` (page.tsx:838) rather than the array max — cosmetic, and the underlying numbers are already covered. The «اللغات» card (LawyerProfileForms.tsx:361-375) hardcodes «العربية» at a permanent 5/5 because the fill condition is `s <= 5`, always true — cosmetic dishonesty on a single row. The `SHARE_TOGGLE_DEFAULTS` privacy defaults (analytics.ts:123-130) are sensible (financial off by default); the problem is that the submit button is dead, which I did report.
- COULD NOT DETERMINE — whether migrations 20260616 and 20260705 have actually been applied to the production database. My verification that the three toggle columns exist is against supabase/migrations/*.sql in the repo. If 20260616 is unapplied in prod, PATCH would fail on `is_accepting_clients` and the profile editor would 500 on every save, which would make the visibility finding blocking rather than high. Worth one query against prod before the fix pass.

---

## Lawyer dashboard — «مهامي» (tasks), «الجلسات القادمة» (hearings), «سجل النشاط» (activity), AddHearingModal, AddTaskModal

This area is not healthy. The hearings page is the worst surface I found: it renders every one of the lawyer's own tasks as a «جلسة قضائية» happening TODAY (counted in red as «X موعد اليوم»), it reads at most 20 rows so a busy lawyer's real hearings fall off the calendar entirely, and a hearing whose POST fails is written to localStorage while the modal reports «تم إضافة الموعد بنجاح!» — three independent paths to a missed or invented hearing. Separately, the HijriDateWidget rendered in the activity page header ships a hardcoded `MOCK_TASKS` map keyed to today, tomorrow and +2/+4 days containing invented hearings and an invented appeal-filing deadline, on a real lawyer's calendar with no demo gate. The tasks page is in better shape — its optimistic handlers were clearly repaired in a prior pass and mostly do the right thing — but it renders an entirely fabricated performance panel (75% productivity, 4.7 hours, «أنت في أعلى ٥٤% ضمن محامو المملكة») above the real task list, and its inline add-task modal collects a priority it never sends. The activity page is the most honest of the three; its route and coverage banner are genuinely well built.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | فشل صامت | `AddHearingModal.tsx:82` | A hearing whose save fails is written to browser localStorage while the modal reports «تم إضافة الموعد بنجاح!» — it never reaches the database and never appears again. |
| حرجة | بيانات مخترعة | `page.tsx:510` | Every task the lawyer creates is rendered on the hearings calendar as a «جلسة قضائية» taking place today, and counted in the red «X موعد اليوم» header. |
| حرجة | فشل صامت | `page.tsx:504` | The hearings page reads only the 20 most recent rows, so a lawyer with more than 20 requests or tasks sees an empty or truncated hearing calendar with no indication anything was dropped. |
| حرجة | بيانات مخترعة | `HijriDateWidget.tsx:21` | The Hijri calendar widget in the activity page header ships hardcoded fake hearings and a fake appeal deadline, keyed to today and the next four days. |
| مرتفعة | فشل صامت | `page.tsx:291` | A failed activity feed is displayed as «لا يوجد نشاط مسجَّل بعد» — an assertion that the lawyer has no history. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:697` | After saving a hearing the list behind the modal never refetches, so the lawyer reads «تم إضافة الموعد بنجاح» over a calendar that still shows «لا توجد جلسات قادمة». |
| مرتفعة | بيانات مخترعة | `page.tsx:15` | The hearings calendar's Hijri day number disagrees with the app's own Hijri widget on 674 of 730 days. |
| مرتفعة | بيانات مخترعة | `page.tsx:596` | The tasks page renders a fabricated productivity panel — 4.7 hours, 3 tasks done, 2 active cases, 75%, and «أنت في أعلى ٥٤% ضمن محامو المملكة» — with no query behind any of it. |
| مرتفعة | فشل صامت | `page.tsx:446` | A failed task query is displayed as «بيانات تجريبية — لا توجد مهام بعد»; the failure is swallowed three times over. |
| مرتفعة | فشل صامت | `page.tsx:394` | The inline «تأكيد الإضافة» button closes the modal without awaiting the save, and a failed POST is logged to the console only — the task is lost silently. |
| مرتفعة | فشل صامت | `page.tsx:422` | Dragging a task between kanban columns can never roll back or report a failure — the rollback is attached to a promise that never rejects. |
| مرتفعة | زر ميت | `page.tsx:160` | «مهام الفريق» is permanently empty — the API returns no owner field — and the tab is decorated with two invented team-member avatars. |
| متوسطة | بيانات مخترعة | `route.ts:201` | A failed stat count is rendered as «٠», which the page deliberately reserved a «—» state to avoid. |
| متوسطة | وعد لا يُنفَّذ | `AddHearingModal.tsx:119` | The hearing success screen claims the appointment was synced to a calendar; no calendar sync exists anywhere in the codebase. |
| متوسطة | زر ميت | `page.tsx:475` | «تصدير PDF» in the activity page header does nothing. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:470` | The activity page header promises to track AI consultations, which the page's own code comment states cannot be recorded. |
| متوسطة | مُدخلات تُرمى | `page.tsx:123` | «أضف لـ Google» exports the wrong date and silently drops the time the lawyer entered, creating an all-day event. |
| متوسطة | بيانات مخترعة | `page.tsx:663` | When a filter matches nothing, the calendar view silently discards the filter and displays every event instead. |
| متوسطة | بيانات مخترعة | `page.tsx:82` | A hearing's «case» chip carries an id invented from the free-text case name, and opens the hearing's own row rather than a case. |
| متوسطة | مُدخلات تُرمى | `page.tsx:882` | The priority selector in the inline add-task modal has no click handler, and the task is hardcoded to «عادية» before it is sent. |
| متوسطة | مُدخلات تُرمى | `page.tsx:874` | The case name chosen in the inline add-task modal is thrown away, then looked up against the wrong table, so a case-linked task is always saved with no case reference. |
| منخفضة | بيانات مخترعة | `AddHearingModal.tsx:67` | An appeal deadline saved through the modal is titled «جلسة — …» whenever a case name is entered. |
| منخفضة | وعد لا يُنفَّذ | `page.tsx:139` | Everything more than five days out is grouped under «هذا الشهر», including hearings months or years away. |
| منخفضة | فشل صامت | `page.tsx:530` | The hearings search lowercases the query but not the text it searches, so a capitalised Latin search term never matches. |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — src/components/ui/CasePicker.tsx is the best-behaved component I read in this area. It queries real `service_requests` with an explicit `limit: 100` (deliberately overriding the shared helper's 20-row default, with a comment explaining why), filters out task rows via `metadata.task`, distinguishes three different empty states (loading / load failed / genuinely no cases), and refuses to substitute demo cases — 'a lawyer with no cases must not be shown invented ones, or they would link a task to a case id that does not exist'. Its ids are real and resolvable by /dashboard/lawyer/cases/[id].
- HEALTHY — src/app/dashboard/lawyer/_components/AddTaskModal.tsx does not have the defect its inline twin on the tasks page has. `done` is set only after `createLawyerTask` resolves, `createLawyerTask` genuinely throws on failure (unlike the workflow create path), the error is surfaced in Arabic in the modal, the form is disabled while saving, and the backdrop click is blocked mid-save. Its CasePicker call captures both the id and the title.
- HEALTHY — every optimistic handler on the tasks page except `onDrop` is correct: onToggle, onDelete, onArchive, onRestore, onStatusChange, onSubtaskToggle and onEditSave each capture the previous value *inside* the state updater and check the resolved boolean rather than attaching a `.catch()`, then restore and set an Arabic `saveError`. `onDelete` even restores the removed row at its original index. `onEditSave` handles the server's `titleSkipped` case by reverting only the title and explaining why. This looks like a prior audit pass that fixed six of seven call sites.
- HEALTHY — GET/POST/PATCH /api/v1/lawyer/tasks is careful where it matters: PATCH is a scoped read-modify-write that merges metadata instead of replacing it (so a subtask tick cannot silently drop caseId/dueDate/notes), it whitelists patchable keys rather than spreading the body, it scopes both read and write by `assigned_to = user.id`, and it refuses to rename a client's own request while still saving the rest of the edit. GET never spreads `meta`, so `metadata.internalNotes` cannot leak to the lawyer.
- HEALTHY — GET /api/v1/lawyer/activity is the most honest data path in this area. It scopes by request participation rather than actor (with a comment explaining the bug that caused), it uses one query rather than two merged limited streams so cursor pagination cannot drop rows, its four stat cards are exact server-side head-counts rather than anything derived, and it drops `metadata` after reading `serviceTitleAr`. The client's coverage banner (activity/page.tsx:575-581) explaining that the card counts orders while the feed lists events on loaded pages only is exactly the right way to handle a number that cannot be made to agree.
- HEALTHY — the activity page's stat cards go inert (unclickable, `filterKey: null`) rather than filtering to a wrong subset whenever the order index is incomplete, checking both the route's `degraded` flag and `total > rows.length`. Its `dayGroupOf` compares at local midnight so a DST shift cannot misbucket a row, and `relativeTimeAr` uses `Intl.RelativeTimeFormat`.
- HEALTHY — the EventCard workflow-steps panel carries an explicit unavailability notice («ملاحظة: تعديل الخطوات مرئي فقط هذه الجلسة وغير محفوظ بعد»), which is the correct handling of a not-yet-wired control. In practice the panel never renders at all, because `workflowToHearing` sets `workflow: undefined` — it renders nothing rather than something false. Same for `LINKED_TASKS = {}` at hearings/page.tsx:111: the linked-tasks section and its «عرض كل المهام» link never appear.
- NOT A FINDING, checked and dismissed — `MOCK_ACTIVITIES` (activity/page.tsx:56-78) and `STATS` (lines 97-102) hold invented data including named people and case numbers, but both are reachable only under `!isSupabaseMode`, a module-level constant from src/lib/runtimeMode.ts, and the page explicitly initialises `useState(isSupabaseMode ? [] : MOCK_ACTIVITIES)` and never calls the API in demo mode. Dead-code-eliminated from a supabase build. Likewise `INIT_TASKS` in tasks/_data.ts:62 is defined but never imported by the tasks page, and `SHARED_CASES` is imported there only to compute an id set for filtering (ids '1'–'6' cannot collide with real UUIDs) — nothing from it is rendered.

---

## Lawyer dashboard overview (/dashboard/lawyer) and analytics (/dashboard/lawyer/analytics)

The overview is not a healthy screen. Its header calendar ships a hardcoded schedule of court hearings keyed to `new Date()`, so every one of the six real lawyers sees an invented «جلسة: المحكمة التجارية - ٩ ص» today and an invented «موعد تقديم مذكرة الطعن» tomorrow. Its «الجلسات القادمة» / «مواعيد حرجة» cards read the `consultations` table (0 rows in production) while real hearings live in `service_requests.metadata.date`, so the overview prints «لا توجد جلسات قادمة» over hearings the /hearings page lists; and every read on both the route and the client service degrades a failure into zeros, so "could not read" is rendered as "you have none". «قضية جديدة» on this page mounts AddCaseModal without the `user` prop the cases page passes, so the case is written with `assigned_to: null` and disappears behind a success screen. The analytics page is 100% literals — NPS 46, «رضا الموكلين ٩٢», 183 AI-tool uses, a year of monthly bars — with no query behind four of its six cards; it carries a «بيانات تجريبية» banner today only because its one real-data path (the `cases` table) has no writer anywhere in the repo, and that banner is wired to invert the moment the path works.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | وعد لا يُنفَّذ | `page.tsx:329` | «الجلسات القادمة» and «مواعيد حرجة» read the `consultations` table, but hearings are stored in `service_requests.metadata.date` — so the overview shows no hearings while /dashboard/lawyer/hearings lists them. |
| حرجة | مُدخلات تُرمى | `page.tsx:1008` | «قضية جديدة» on the overview saves the case with `assigned_to: null`, so it never appears on this dashboard again — after a «تم» success screen. |
| حرجة | بيانات مخترعة | `HijriDateWidget.tsx:21` | The lawyer's header calendar ships a hardcoded court schedule — a named court and a time today, an appeal-brief deadline tomorrow — rendered as this lawyer's own agenda. |
| حرجة | فشل صامت | `lawyerDashboardService.ts:44` | Every read on this dashboard turns a failed query into a confident empty state — a DB error, an expired session or a 500 all render «لا توجد جلسات قادمة» and «لا توجد قضايا نشطة». |
| مرتفعة | بيانات مخترعة | `route.ts:71` | The «الإيرادات» KPI sums the `payments` table with no lawyer predicate at all, so it counts money the lawyer *paid* as money the lawyer *earned*. |
| مرتفعة | بيانات مخترعة | `page.tsx:264` | «المهام العاجلة» invents each task's priority from its array position and prints the case's last-update time as the due date, ignoring the priority and due date the lawyer actually chose. |
| مرتفعة | عمل وهمي | `page.tsx:993` | The overview asserts that an AI secretary prepared reply drafts for a crowded schedule tomorrow — a hardcoded sentence with no data behind it, over a link to a page that contains no such report. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:250` | «الاستشارات المعلقة» counts the `consultations` table, which the lawyer's own consultations page does not use — so the tile is permanently ٠ while that page lists real requests. |
| متوسطة | بيانات مخترعة | `page.tsx:22` | The analytics screen is a wall of literals — NPS 46, «رضا الموكلين ٩٢», 183 AI-tool uses, a year of monthly activity — with no query behind four of its six cards. |
| متوسطة | فشل صامت | `page.tsx:187` | The analytics page's «خطأ في تحميل البيانات» banner can never appear — both services swallow their errors before the page's catch can see them. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:531` | The upgrade banner and the «محجوب» lock claim الصائغ القانوني is unavailable on the lawyer's plan, while the same page links to it twice unlocked and the tool has no gate. |
| متوسطة | بيانات مخترعة | `page.tsx:575` | The red banner tells the lawyer he has an appeal deadline in two days, with hardcoded wording, over a row that is a consultation of unknown kind. |
| متوسطة | بيانات مخترعة | `page.tsx:51` | The «القضايا النشطة» table labels completed and in-review cases «انتظار», and its status mapper tests a status value that does not exist in the schema. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:460` | «نشر في السوق» in the header sends the lawyer to a marketplace screen built from fabricated requests and offers, whose every onward action redirects out of the marketplace. |
| منخفضة | زر ميت | `page.tsx:320` | The «نشاط AI» tab in سجل النشاط is structurally incapable of ever showing a row. |
| منخفضة | بيانات مخترعة | `page.tsx:249` | Each KPI tile carries a coloured trend badge with an up-arrow and no number — a growth signal with no data behind it. |
| منخفضة | زر ميت | `page.tsx:608` | The «استشارة جديدة» quick action carries a `?book=1` parameter the destination page never reads. |
| منخفضة | وعد لا يُنفَّذ | `page.tsx:278` | The «الخطوة القادمة» column reads a metadata key that nothing in the codebase ever writes, so it is permanently a dash. |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY: the activity timeline's translation layer. `badgeToActivityType` maps only `delivery`→success and `cancelled`→warning and lets everything else fall to `info` (page.tsx:81-89), and `describeRequestEvent` returns real Arabic sentences per event token (src/lib/events.ts:190-219) — no row claims AI work, and the file's comments document the earlier bug where `notification.email_sent` matched "ai". The rows are genuine `request_events` for requests this lawyer participates in.
- HEALTHY: the «مشاركة ملفي المهني» button. It is correctly disabled under BETA_MONOPOLY_MODE with an accurate reason string, the title sits on the wrapper so the tooltip survives the disabled state, `copyToClipboard` returns a real boolean and the tick only shows on `true`, with a manual-copy fallback field (page.tsx:109-136, 199-222, 484-516). This is the pattern the rest of the page should follow.
- HEALTHY: the «جراف القضايا» control degrades honestly — with no cases it renders a disabled button titled «لا توجد قضايا بعد لعرض الجراف» instead of linking nowhere (page.tsx:449-459).
- HEALTHY: OnboardingBanner's three lawyer actions all point at routes that exist and work — /dashboard/lawyer/cases, /ai/draft, /dashboard/lawyer/hearings (OnboardingBanner.tsx:39-45).
- HEALTHY: the demo-mode banner at page.tsx:550 is behind `!isSupabaseMode`, a module-level constant (api.ts:20), so it is eliminated from the production build. Not reported, per the brief.
- NOT REPORTED, worth a look by whoever owns copy: the activity feed is written in the client's voice on the lawyer's own dashboard. `describeRequestEvent` produces «تم قيد طلبكم», «تم إنجاز معاملتكم», «بدأ العمل على طلبكم» — so a hearing the lawyer created for himself appears as «تم قيد طلبكم: طلب خدمة». It is accurate, just addressed to the wrong person, and the route hands back neither the status nor the Arabic service name so every row degrades to the generic «طلب خدمة».
- OUT OF AREA but directly adjacent: /dashboard/lawyer/profile imports ACTIVITY_DATA from ../_data/analytics and renders the same fabricated year-of-activity chart (profile/page.tsx:44, 717). That file also carries WORK_HOURS `{ total: 36.5, thisWeek: 8.5, thisMonth: 31.2 }` and a 10-level metal-tier system keyed to those hours. Unlike /dashboard/lawyer/analytics, the profile page IS in the sidebar.
- OUT OF AREA: /ai/secretary (linked from the overview's bottom card) is entirely literals — automation rules with `lastFired: "منذ ساعتين"`, a decision log with dates and an extracted invoice amount, and a greeting hardcoded to «أ. فهد» regardless of who is signed in (ai/secretary/page.tsx:36-85).

---

## Lawyer dashboard — «مدير العقود» (contracts), «المستندات» (documents), «الأرشيف الموحّد» (archive)

The archive route is honest and the documents UPLOAD path is genuinely good — real Supabase Storage, real `attachments` row, size/extension gate, per-file error reporting, timeouts, demo-mode lockout. Everything around those two points is not. «مدير العقود» is not a contracts store at all: it lists `service_requests` rows where `type = "service"`, which is the exact shape the app writes for the lawyer's HEARINGS, CASES, TASKS, CLIENTS and INVOICES — so those rows are rendered as contracts with an invented type, an invented status and an invented signing date, and the destructive buttons on them rewrite the real row's status column, which silently drops a scheduled hearing off the lawyer's calendar and a task off the Kanban. «المستندات» wraps a real upload in an entire taxonomy (7 categories, 3-level appeals, branch, party, subtype, archive) that no column backs, so every filter but «الكل» empties the list, and the view/download buttons on a document the lawyer just uploaded have no handler at all. The template tab runs a `setTimeout(1600)` behind a spinner and signs the canned output «تم توليده بواسطة نظامي AI», with no BetaReviewGate anywhere in the subtree.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | فشل صامت | `page.tsx:137` | Pressing «حذف»/«أرشفة»/«إنهاء العقد» in the contracts manager cancels the underlying service_requests row, which removes a scheduled court hearing from the lawyer's calendar. |
| حرجة | بيانات مخترعة | `page.tsx:41` | An invoice row is rendered as a signed, in-force contract («ساري») with the invoice amount as the contract value and the row's creation date as the signing date. |
| حرجة | عمل وهمي | `SmartTemplateModal.tsx:23` | «توليد بالذكاء الاصطناعي» is a 1600 ms setTimeout behind a spinner; the canned four-line output is signed «— تم توليده بواسطة نظامي AI —» and is not covered by BetaReviewGate. |
| مرتفعة | فشل صامت | `page.tsx:159` | «استعادة» on an archived contract sets status to draft, which deletes the underlying task from the lawyer's Kanban entirely. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:143` | «إرسال للتوقيع» and «تم إرسال العقد للتوقيع ✓» transmit nothing to any counterparty — the handler only changes a status column. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:384` | «مشاركة مع الموكل» and «رابط التوقيع» claim a link was copied to the clipboard; the file contains no clipboard call. |
| مرتفعة | زر ميت | `page.tsx:390` | «تذكير الطرف الثاني» reports «تم إرسال التذكير» without sending anything. |
| مرتفعة | زر ميت | `page.tsx:385` | «PDF» reports «جارٍ تحميل PDF...» and downloads nothing. |
| مرتفعة | فشل صامت | `page.tsx:107` | A failed contracts query is displayed as «0 عقد نشط» — the API returns HTTP 200 with an explicit degraded flag that this page never reads. |
| مرتفعة | مُدخلات تُرمى | `page.tsx:183` | The contract type and the fee the lawyer types in the new-contract wizard are written to metadata that nothing ever reads back, so both are lost on reload. |
| مرتفعة | بيانات مخترعة | `SmartTemplateModal.tsx:50` | The smart-template modal stamps «★ معتمد» (certified) and «تاريخ الإنشاء: اليوم / آخر تحديث: اليوم» as hardcoded literals on a legal template. |
| مرتفعة | بيانات مخترعة | `page.tsx:42` | Every uploaded document is hardcoded to category «المذكرات», and branch/party/subtype/archived are never set — so the entire filter taxonomy empties the list on any selection. |
| مرتفعة | زر ميت | `page.tsx:509` | A lawyer cannot view or download any document they uploaded — the Eye, Download and menu buttons on every row have no handler, though getDocumentFileUrl exists and the client page uses it. |
| مرتفعة | فشل صامت | `page.tsx:78` | A failed or timed-out document read is displayed as «0 مستند» and «لا توجد مستندات مطابقة», and the loading flag that would prevent it is never rendered. |
| مرتفعة | فشل صامت | `workflowService.ts:56` | A contract save that fails server-side reports «تم حفظ المسودة» — the row is written to localStorage and disappears on the next successful load. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:135` | «تم حذف العقد» does not delete anything — the row returns under the الأرشيف tab as «منتهي». |
| متوسطة | زر ميت | `page.tsx:255` | The «منتهي» filter chip is permanently 0 and always returns an empty list, because its count predicate contradicts the archive predicate. |
| متوسطة | مُدخلات تُرمى | `SmartTemplateModal.tsx:77` | A template advertised as «15 حقل» presents four generic fields, only two of which reach the output; the AI description box is discarded entirely. |
| متوسطة | بيانات مخترعة | `page.tsx:619` | Template usage counts (up to «1,203 استخدام») are literals in a source file, shown as platform statistics. |
| متوسطة | زر ميت | `page.tsx:243` | Eight buttons across the documents page and the template modal have no onClick handler at all. |
| متوسطة | وعد لا يُنفَّذ | `navigation.sidebars.legal.ts:67` | The sidebar sells «الأرشيف الموحّد» with a «جديد» badge; the route is a twelve-line unavailability notice. |
| منخفضة | بيانات مخترعة | `page.tsx:102` | The contracts list and every count on the page are silently capped at the 20 most recent rows. |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — src/app/dashboard/lawyer/archive/page.tsx is exactly right. Twelve lines, DashboardComingSoon, «غير متاح حالياً», an honest reason («فور توفّر مخزن أرشفة مشترك»), and a working backHref. No invented rows, no dead controls. The defect is the sidebar badge pointing at it, not the page.
- HEALTHY — the documents UPLOAD path is real and unusually careful, and I want that on the record because it is the counter-example to everything else in this area. src/app/dashboard/lawyer/documents/page.tsx:96-157 sends each file through partitionUploadFiles (size + extension gate), calls uploadDocumentFile (real Supabase Storage object + real `attachments` row, with a 60 s whole-operation deadline and a rollback), gives each file its own try so one failure does not abandon the batch, ends the batch only on a timeout, names every failed file in a visible banner (251-256), clears the input so the same file can be retried, is disabled under !isSupabaseMode with an honest message (98-101), and distinguishes «uploaded but the list refresh failed» from «upload failed» (152-156). Nothing here overstates.
- HEALTHY — the «صياغة بـ AI» link at contracts/page.tsx:275 points at /ai/contracts, which is a real, reachable page (src/app/ai/contracts/page.tsx exists; src/lib/auth/routeAccess.ts has no rule restricting the prefix, and src/proxy.ts only requires a session for it). Not a dead link.
- HEALTHY — MOCK_DOCS in src/app/dashboard/lawyer/documents/_taxonomy.ts:126-135 is eight fabricated case documents («صك ملكية العقار ٢١٣», «حكم ابتدائي — السبيعي») but it is a dead export: documents/page.tsx imports fourteen names from that module and MOCK_DOCS is not among them, and a repo-wide grep finds no other importer. Nothing renders it. Not reported as a finding for that reason — but it is one edit away from being one.
- VERIFIED NOT A FALSE POSITIVE — the PATCH that carries findings 1 and 4 is not refused. src/app/api/v1/service-requests/[id]/route.ts:751-752 resolves `permitted = isAssignee || isAdmin || (isRequester && targetStatus === "cancelled")`, and every feeder (AddHearingModal, AddCaseModal, api/v1/lawyer/tasks, clients, finance) sets assigned_to to the lawyer's own id, so isAssignee is true and any target status is written. I read to the end of the branch specifically to close this.
- VERIFIED — no BetaReviewGate anywhere in the three subtrees: `grep -rn "BetaReviewGate|betaConfig" src/app/dashboard/lawyer/{contracts,documents,archive}/` returns nothing. The SmartTemplateModal output is presented ungated.
- VERIFIED — none of the code I reported sits behind `!isSupabaseMode` or `isDemoUiEnabled`. The one isSupabaseMode branch in this area (documents/page.tsx:98) is the honest demo-mode upload refusal, i.e. the correct side.
- COULD NOT DETERMINE — how many of the 29 production service_requests rows actually carry receiver:"lawyer" and type:"service", and therefore how many phantom contracts each of the six lawyers currently sees. I anchored findings 1 and 2 on workflowToContract and on the five writers rather than on row counts, so both hold regardless. Someone with the service key should run `select type, receiver, status, count(*) from service_requests group by 1,2,3` before deciding how urgent the contracts page is — if any lawyer already has a hearing row in that list, finding 1 is live today.

---

## Lawyer dashboard — «الإيرادات والفواتير» (src/app/dashboard/lawyer/finance/page.tsx, 1,431 lines; src/components/dashboard/lawyer/FinanceCharts.tsx; API src/app/api/v1/lawyer/finance/route.ts). Repo root: D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website

The invoice half of this page is honest at its core — every invoice figure traces to a real `payments` row scoped through `service_requests.assigned_to`, and the manual-collection modal says out loud that it is a bookkeeping entry and not an electronic payment. The dishonesty is everywhere around that core: the expense half books wallet CREDITS as office expenses (net profit wrong by 2× the amount, inside a document titled «بيان الدخل»); the only control that can add an expense is a fake AI microphone that fabricates a 1,200 ﷼ line and a 180 ﷼ VAT reclaim; the default revenue chart still carries the four month labels from the deleted mock data, so in August 2026 every real invoice is invisible on it; and a failed read renders as «لا توجد فواتير» with no error anywhere. Thirteen findings, one blocking. No invented lawyer names, courts, case numbers or precedents were found on this surface — the mock `INVOICES`/`EXPENSES` arrays still exist in src/constants/lawyerFinanceData.ts but the page imports only the types and the status/category config, not the data.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | بيانات مخترعة | `page.tsx:152` | Wallet credits, pending rewards and reversals are all booked as office expenses, so net profit in the income statement is wrong by twice the amount of any credit. |
| مرتفعة | وعد لا يُنفَّذ | `route.ts:143` | «مسترجع ضريبة VAT (15%)» is structurally always ٠ for real data — the server hardcodes `vatIncluded: false` on every row — and when it is non-zero the inclusive-VAT formula overstates it by 15%. |
| مرتفعة | عمل وهمي | `page.tsx:190` | The only control that can add an expense is a fake AI microphone that fabricates a 1,200 ﷼ software-licence line and an 180 ﷼ VAT reclaim into the income statement. |
| مرتفعة | بيانات مخترعة | `page.tsx:108` | The default revenue chart still carries the four month labels from the deleted mock data, so in August 2026 every real invoice is invisible on it. |
| مرتفعة | فشل صامت | `page.tsx:68` | A failed financial read is rendered as «لا توجد فواتير» with all totals at ٠ — three separate layers each swallow the error, and the declared loading state is never displayed. |
| مرتفعة | بيانات مخترعة | `page.tsx:514` | «موقف الفواتير العام» reports a collection percentage that is wrong, because partial invoices enter it as their UNPAID remainder while every other segment enters as its full fee. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:986` | The income statement is headed «الربع الثاني ٢٠٢٦» — a hardcoded literal — while the figures under it are all-time and unfiltered. |
| مرتفعة | زر ميت | `page.tsx:685` | Two «إرسال تذكير» buttons tell the lawyer that payment reminders were sent to named clients; both are an alert() and confetti with no network call. |
| متوسطة | بيانات مخترعة | `route.ts:98` | Every financial total on the page is computed from at most the 50 most recent rows, with no indication that anything was left out. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:1061` | The net-margin card states a formula the code does not use, and renders figures like «-120000%» when nothing has been collected. |
| متوسطة | عمل وهمي | `page.tsx:1134` | The invoice-creation spinner claims an AI compliance check and a tax computation; the POST performs neither, and does not even reject a negative fee. |
| متوسطة | زر ميت | `page.tsx:914` | Five of the six expense-category chips can never match a row, and clicking one renders a completely blank area with no message. |
| متوسطة | بيانات مخترعة | `page.tsx:121` | Chart buckets ignore the year, so invoices from different years merge into the same quarter and all-time figures are labelled «عام ٢٠٢٦». |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — the manual-collection modal is a model of the honesty this audit is looking for. page.tsx (collect modal) prints «هذا قيد محاسبي يدوي لمبلغ استلمته خارج المنصة، وليس عملية دفع إلكتروني. لا يمكن التراجع عنه من هذه الصفحة بعد التأكيد.» before the confirm button, offers only «نقداً» and «تحويل بنكي» (COLLECT_METHODS, page.tsx:23-26), and the file-header comment explains why. No screen on this page claims an electronic payment was received. Leave it alone.
- HEALTHY — «تم التحصيل» reaches a real server and the totals are re-read from the DB. handleCollect (page.tsx:296-357) PATCHes /api/v1/lawyer/finance and replaces the local row with the server's response (`if (d) setInvoices(prev => prev.map(x => (x.id === d.id ? d : x)))`, line 337) rather than mutating optimistically. Server-side error strings are translated to Arabic via COLLECT_ERROR_AR (page.tsx:38-46) and surfaced in the modal — this is the ONLY read/write path on the page with real error display.
- HEALTHY — PATCH ownership is properly enforced despite `payments` having no lawyer column: route.ts:355-363 reads `service_requests.assigned_to` for the payment's `request_id` and 403s if it is not the caller. No IDOR here (contrast the client-workflow IDOR from the 2026-07-01 pass).
- HEALTHY — invoice figures are real. Every number in «إدارة الفواتير» and the invoice KPIs traces to a `payments` row scoped through `service_requests.assigned_to = user.id` (route.ts:82-99). No hardcoded invoice, client name, court, case number or amount is rendered anywhere on this page. The mock arrays INVOICES/EXPENSES still exist in src/constants/lawyerFinanceData.ts (lines 22-42, with names like «شركة الأفق» and «خالد القحطاني») but grep confirms page.tsx:14 imports only the types plus STATUS_CFG and EXP_CFG — the data is not imported by this or any other file.
- HEALTHY — the gateway banner (page.tsx:363-380) is accurate: it reads the live admin flag through usePaymentsStatus, says «البيانات المالية تعرض من قاعدة البيانات — الدفع والسحب غير متاحين حتى تفعيل البوابة», and does not claim a payment capability the platform lacks.
- LATENT, NOT REPORTED — route.ts:11-12 maps `payments.status = 'not_required'` to invoice status "paid", and line 40 then sets `paidAmount = amount` for it, so such a row would render «مسدّدة كاملاً» and feed «المبالغ المحصّلة فعلياً» and the P&L cash-in line for money nobody paid. I could not produce that row with current code: POST /api/v1/service-requests only inserts a `payments` row when `Number(payment.amount) > 0`, and that path is 402'd while the gateway flag is disabled (route.ts:153-176 of service-requests); the finance POST writes `requires_payment`; /api/v1/admin/payments is read-only; PATCH writes only `paid`/`requires_payment`. Same for `failed`/`refunded` → "overdue" (route.ts:13-15), which would show a refund as a client debt and feed the red «مستحقات متأخرة» banner. Both go live the moment the payments gateway is enabled or anyone backfills the table — worth fixing before that switch is flipped, but not a defect a lawyer can see today.
- OBSERVED, NOT A DEFECT — GET returns `totalRevenue`, `monthlyRevenue`, `totalInvoices`, `pendingInvoices` and `subscription` (route.ts:159-167) and the page discards all five, recomputing its own totals client-side with different definitions (e.g. the server's totalRevenue sums paidAmount of paid invoices only; the page's totalCollected sums paidAmount across all). Not user-visible today, but the two definitions will drift.
- NOT A DEFECT PER THE BRIEF — the `else` branches at page.tsx:253-282 and 348-356 (local-only invoice creation and collection) sit behind `isSupabaseMode`, a module-level constant in src/lib/services/api.ts:20, so they are eliminated from the production build. Reported nothing from them.

---

## Lawyer dashboard — «دليل العملاء» (clients list + client detail + AddClientModal + ClientDrawer) and «الاستشارات» (consultations list + booking modal + consultation detail)

Both screens are wired to real endpoints, but the clients list throws away everything the lawyer types about money, entity type, flags and rating (the POST writes those keys into service_requests.metadata; nothing in the repo ever reads them back), and then fills the gaps with literals — every client renders a green «✓» under «متبقي» (nothing outstanding) and a 3-of-5 star rating the lawyer never gave. The client drawer, which is the de-facto detail surface since nothing links to the /clients/[id] route, reads a hardcoded seven-name case/task map, so it reports «لا توجد قضايا مرتبطة» over the same client whose card says «2 نشطة». The consultations page reads service_requests (not the empty public.consultations table) but asks for only the 20 newest receiver=lawyer rows before filtering to type=consultation, ignores the API's own `degraded: true` failure marker, and its booking modal reports success even when the POST fails — writing the appointment to localStorage where nothing will ever read it. Four video-call buttons («بدء», «دخول», «انضم للجلسة») have no handlers at all.

| الخطورة | النوع | الموضع | الملاحظة |
|---|---|---|---|
| حرجة | بيانات مخترعة | `page.tsx:86` | Every client's «متبقي» (outstanding fees) cell renders a green «✓» because totalFees/paidFees are hardcoded to 0 — including for a client whose fees the lawyer typed in minutes earlier. |
| مرتفعة | فشل صامت | `route.ts:28` | GET /api/v1/lawyer/clients answers a database error with HTTP 200 and an empty array, so a failed query is displayed to the lawyer as «لا توجد موكلون بعد». |
| مرتفعة | بيانات مخترعة | `page.tsx:85` | Every client is shown with a 3-of-5 gold star rating that no one ever assigned; the API returns no rating field. |
| مرتفعة | زر ميت | `page.tsx:401` | All four video-call buttons on the consultations screen — «بدء», «دخول», «انضم للجلسة» — have no click handler at all. |
| مرتفعة | وعد لا يُنفَّذ | `page.tsx:124` | «جدولة استشارة» files a client-style order against the lawyer's own account: it lands in the admin «جديدة» queue unassigned, and the lawyer is notified «تم استلام طلبك … قيد المعالجة وسنعلمك بأي تحديث». |
| مرتفعة | مُدخلات تُرمى | `AddClientModal.tsx:54` | AddClientModal collects entity type, flags, rating and fees, POSTs them into service_requests.metadata, and nothing ever reads them back — after one reload a «شركة» is shown as an individual and all classifications are gone. |
| مرتفعة | بيانات مخترعة | `ClientDrawer.tsx:10` | The client drawer's cases and tasks come from a hardcoded seven-name map, so it reports «لا توجد قضايا مرتبطة» for every real client — directly contradicting the «نشطة» count on the card that opened it. |
| مرتفعة | فشل صامت | `workflowService.ts:40` | The consultations list asks for only the 20 newest receiver=lawyer rows BEFORE filtering to type=consultation, so a lawyer who has added 20 clients sees zero consultations under «لا توجد استشارات مطابقة». |
| مرتفعة | فشل صامت | `workflowService.ts:41` | The consultations page ignores the API's explicit `degraded: true` failure marker and renders a failed query as «لا توجد استشارات مطابقة». |
| مرتفعة | فشل صامت | `workflowService.ts:54` | «تأكيد الجدولة» reports success when the server rejects the booking — the consultation is written to localStorage instead, and the modal's Arabic error banner can never fire on a server failure. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:192` | The archive note promises manual client classification and one-click restore; neither control exists anywhere in the app. |
| متوسطة | زر ميت | `page.tsx:59` | Three of the five sort options do nothing, including «آخر تواصل», which is the default. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:115` | When the clients list is empty the lawyer is shown a banner headed «بيانات تجريبية» (demo data) and told to «اربط حسابك بقاعدة البيانات» — there is no demo data and no such linking feature. |
| متوسطة | مُدخلات تُرمى | `page.tsx:83` | The consultation duration the lawyer picks (30/45/60/90/120) is stored and then ignored — every card displays 60 د (or 30 د for chat). |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:80` | The «جارية الآن» status can never occur — the KPI, the amber live-session panel and the filter tab are permanently empty by construction. |
| متوسطة | وعد لا يُنفَّذ | `page.tsx:661` | Two places promise an automatic AI session summary sent to the client; no such code path exists and the summary field is never populated. |
| متوسطة | بيانات مخترعة | `page.tsx:502` | The «إجمالي الأتعاب» total on the consultations screen sums quoted amounts including cancelled consultations, on a platform where no payment has ever been taken. |
| متوسطة | فشل صامت | `page.tsx:319` | A consultation can be booked with no type, no mode, no date and no time; the card then renders a blank date and blank time rather than the intended «بانتظار التأكيد». |
| منخفضة | بيانات مخترعة | `page.tsx:257` | The (orphaned) client detail page tells the lawyer every client is «100% مسدّد» / «مسدّدة بالكامل» with a full green bar, from hardcoded zeros. |
| منخفضة | بيانات مخترعة | `page.tsx:165` | The same orphaned page presents invented per-client assessments: a 3-star rating, «مخاطر السداد: منخفضة ✓», «مستوى الأولوية: عادي» and a six-month revenue axis labelled نوف…أبريل. |
| منخفضة | فشل صامت | `page.tsx:220` | On the same orphaned page, client notes are kept in React state only and vanish on navigation, under a «Ctrl+Enter للحفظ» hint; the two hero buttons have no handlers. |
| منخفضة | بيانات مخترعة | `page.tsx:90` | «آخر تواصل» on the client card renders a raw ISO timestamp, and it is the creation date of the newest request, not a contact date. |
| منخفضة | زر ميت | `page.tsx:412` | Dead controls inside the consultation card: the expand caret does nothing on a completed consultation with no description, and the AI-summary block's two buttons have no handlers. |

**فُحص ووُجد سليماً / لم يُحسم:**

- HEALTHY — consultations/[id]/page.tsx is exactly right: 21 lines, no mock data, renders DashboardComingSoon with «هذه الميزة غير متاحة حالياً … لا توجد بيانات حقيقية لعرضها بعد» and a back link. Its docblock records that it previously held a hardcoded MOCK map and a setTimeout saveNotes. Leave it alone.
- HEALTHY — no cross-lawyer leak on service_requests. There is exactly ONE SELECT policy in the whole migration set (supabase/migrations/20260518_client_workflow_backend_ready.sql:148-150, `using (requester_user_id = auth.uid() or assigned_to = auth.uid())`), `grep -rn "on public.service_requests" supabase/migrations/*.sql` finds no broader policy, and 20260701_client_workflow_rls_assert.sql fails the deploy if RLS or the policies go missing. Both /api/v1/lawyer/clients and /api/v1/service-requests use the RLS-scoped client, never the service key.
- HEALTHY — the GET /api/v1/lawyer/clients docblock matches its behaviour: manually-added clients really are materialised as service_requests rows with `receiver: "lawyer"`, `assigned_to` = the lawyer, `metadata.client = true`, `status: "assigned"` (route.ts:161-182), and the GET picks them up via the `!req.requester_user_id && meta.client === true` branch (:49-68). The displayed name/phone/email come from the row's `requester` jsonb — real data. What is invented is everything the endpoint does NOT return: fees, flags, rating, entity type, closed-case count.
- NOT A BUG — the consultations page does not read public.consultations (0 rows in production) at all. It reads service_requests filtered `receiver=lawyer` then `type === "consultation"` in the browser. So the empty table is not why the page can be empty; the 20-row cap and the swallowed `degraded` flag are.
- NOT A BUG — `request.payment.amount` and `request.requester.name` cannot crash the mapper: both columns are `jsonb not null default` (supabase/migrations/20260518_client_workflow_backend_ready.sql:10 and :14), so a null-dereference cannot empty the list.
- COULD NOT DETERMINE — whether any of the six real lawyer accounts has a client whose profiles.display_name exactly equals one of the seven literals in src/app/dashboard/lawyer/_data/clientLinks.ts. If one does, that lawyer's drawer will show an invented case, court and hearing date for a real person. I did not query production, so I report the unconditional half (the map is always empty for real clients, contradicting the card's own count) as the finding.
- CONTEXT — /dashboard/lawyer/clients/[id] is orphaned. `grep -rn "lawyer/clients" src` returns only links to the LIST route (sidebar, navbar, two /ai pages, the [id] page's own back links) and `grep -rn "/clients/" src` returns nothing. The cards call `setDrawerClient(client)` (page.tsx:286), not a router push. Every defect on that page is therefore capped at low — but the fix for the drawer should probably be to wire the card to the detail route, which would promote all of them.
- CONTEXT — the «عرض الكل» link on the detail page passes `?client=${clientId}` to /dashboard/lawyer/cases ([id]/page.tsx:377), but that page never reads searchParams (`grep -n "searchParams|useSearchParams" src/app/dashboard/lawyer/cases/page.tsx` is empty), so the filter is ignored. Unreachable route, so not filed separately.

---
