# سجل المرحلة ٢ — حساب المحامي · ٢٨ أغسطس ٢٠٢٦

> هذا الملف هو **الدليل**، لا الملخّص. التدقيق الأصلي في
> [`2026-08-27-lawyer-audit.md`](2026-08-27-lawyer-audit.md)، والخطة في
> [`خطة_الإصلاح_الشاملة.md`](../../خطة_الإصلاح_الشاملة.md).

**النطاق:** الـ٦٩ ملاحظة المتوسطة والمنخفضة الباقية على `/dashboard/lawyer`.
**ستة محامين حقيقيين يفتحون هذه الشاشات كل يوم.**

## كيف جرت

```
  سبعة قرّاء               →  حكم على كل ملاحظة مقابل الشجرة الحالية
  وكيل مهاجم               →  حاول تكذيب الـ٤٥ حكماً بالإغلاق
  أربع جولات إصلاح         →  كل جولة يهاجمها وكيل لم يصنعها
  توقّفنا حين تقاربت        →  الجولة ٤: صفر رفض، صفر ارتداد
```

| | العدد |
|---|:-:|
| أُعيد التحقّق منها | ٦٩ |
| كانت مغلقة أصلاً بأثر المرحلة ١ | **٤٣** |
| كانت حيّة | ٢٤ |
| أُعلن إغلاقها ثم أعادها المهاجم | **٢** |
| **أُصلحت في المرحلة ٢** | **٢٦** |

> **٦٥٪ منها كانت مغلقة قبل أن نبدأ.** الخطة تنبّأت بذلك حرفياً: «نفَّذها بعد
> المرحلة ١، لا قبلها — المرحلة ١ ستغلق جزءاً منها تلقائياً، وحسابها قبل ذلك
> مضاعفة للعمل.» ولولا إعادة التحقّق لكان ٤٣ إصلاحاً مكرّراً على كود سليم.

---

## الحكم على كل ملاحظة من الـ٦٩

> **أرقام السطور هنا من ٢٨ أغسطس** — وستتقادم هي الأخرى. الوصف هو المرجع.

### القضايا وملف القضية

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| cases-1 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/cases/[id]/page.tsx:373` | The team memo still builds a member from the raw column: `name: String(caseData.assignedTo),` (:373) with `avatar: String(caseData.assignedTo).charAt(0)` (:375), and the header prints the same value — `const assigneeDisplay = caseData.assignedTo ?? "—";` (:63… |
| cases-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/cases/[id]/page.tsx:493` | `saveNote` is gone and replaced by a header comment at :493-516: «`saveNote` stood here. Its own comment said «do NOT pretend it saved» and then the body did exactly that: a 600 ms setTimeout behind a spinner, followed by `setNoteInput("")`.» The composer now… |
| cases-3 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/cases/[id]/page.tsx:551` | `handleDownload` is unchanged in the way that matters: `const url = await getDocumentFileUrl(doc.storage_path); if (url) { window.open(url, "_blank"); } } catch (e) { console.error("[lawyer case detail] download failed:", e); }` (:551-560). There is no `else`… |
| cases-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/cases/[id]/page.tsx:893` | The tasks tab now has a distinct unreadable branch ahead of the empty one: `) : tasksView === "unreadable" ? (` (:893) renders «تعذّرت قراءة المهام» with «هذه ليست لوحة فارغة — قد تكون لهذه القضية مهام لم تُقرأ.» and a working retry `<button onClick={loadCase… |
| cases-5 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/cases/[id]/page.tsx:156` | `mapStatus` still folds the two together — `case "completed": case "cancelled": return "closed";` (:155-157) — and `STATUS_CONFIG.closed` is `{ label: "مغلقة" … }` (:79), printed both as the status pill (:653) and as «الحالة الحالية» (:703). The stage bar the… |
| cases-6 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/cases/page.tsx:209` | `onKanbanDrop` now captures `originalCol`/`originalStatus` before the optimistic update, defines `const revert = () => { setCases(prev => prev.map(c => (c.id === caseId ? { ...c, kanbanCol: originalCol …, status: originalStatus ?? c.status } : c))); }` (:181-… |
| cases-7 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/cases/page.tsx:317` | The dependency array now carries it: `}, [activeCases, archivedCases, collabFilter, statusFilter, typeFilter, degreeFilter, courtFilter, teamFilter, timeFilter, priorityFilter, search, viewMode]);` (:317). The comment above it names exactly this defect — «`ac… |
| cases-8 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/cases/page.tsx:295` | The three clauses still gate on a field the mapper never assigns: `if (timeFilter === "today" && (c.nextDateSort === undefined \|\| c.nextDateSort > 0)) return false;` (:295) and the same for "week" (:296) and "month" (:297). `grep -rn nextDateSort src/` show… |
| cases-9 | متوسطة | 🔧 كانت حيّة — أُصلحت | `src/constants/lawyerCasesData.ts:57` | `workflowToCase` still returns the literal `degree: "primary",` (:57) for every row, while the line above it reads the real court out of metadata — `court: (request.metadata?.court as string) \|\| "بانتظار تحديد الجهة",` (:50). Every case therefore badges «اب… |
| cases-10 | متوسطة | 🔧 كانت حيّة — أُصلحت | `src/constants/lawyerCasesData.ts:65` | `workflowToCase` still ends `collab: "solo",` (:65) with no source read — nothing on the WorkflowRequest row feeds it. The filter it drives is live at cases/page.tsx:288 (`if (collabFilter !== "all" && c.collab !== collabFilter) return false; // S59`), so the… |
| cases-11 | متوسطة | ⚠️ أُعلن إغلاقها ثم أعادها التكذيب | `src/lib/services/casesService.ts:176` | The client half is genuinely fixed, but the guarantee it rests on is not produced anywhere. casesService.ts:176-177 reads `if (res.status === 404) return null; if (!res.ok) throw new Error("تعذّر تحميل الطلب");` and its comment above claims «404 → null; every… |
| cases-12 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/cases/page.tsx:69` | The page overrides the route default — `// How many rows to ask the list endpoint for. The route's own default is 20.` / `const LIST_LIMIT = 200;` (:68-69), passed as `{ receiver: "lawyer", limit: LIST_LIMIT }` (:103) — and it now reads `total`: `truncated: (… |
| cases-13 | منخفضة | ✅ أغلقتها المرحلة ١ | — | The file is gone. `git show --stat a5b69a1` lists `.../dashboard/lawyer/LawyerCaseSubViews.tsx \| 696 ---------`, `git ls-files \| grep -i SubViews` returns nothing, `test -f` on the path reports ABSENT, and the surviving contents of src/components/dashboard/… |

### الملف المهني والسوق

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| profile-1 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/profile/page.tsx:394` | The page now renders both facts as first-class tiles and then explains them. Line 340 renders the real status label from `VERIFICATION_CFG[profileData.verificationStatus]` under «حالة التوثيق», line 352 renders «مُفعَّل»/«غير مُفعَّل» under «طلب الظهور في الد… |
| profile-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/profile/page.tsx:56` | The banner is gone from the tree. `grep -rn "ستُحدَّث تلقائياً"` over src/ returns nothing, and the only remaining occurrence of the phrase in this file is the file-header note at line 56: «The «بعض الإحصائيات تجريبية» banner was removed WITH the statistics, … |
| profile-3 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/profile/page.tsx:15` | The 404 clipboard link is still literally present at LawyerProfileForms.tsx:238 (`navigator.clipboard?.writeText("https://nezamy.sa/share/lawyer/xK9mP3q")`) but nothing can render it any more. The only import of that module in the whole tree is line 15 of the… |
| profile-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/profile/page.tsx:15` | Same closure as profile-3, and by the same single line: `import { OverviewTab } from "@/components/dashboard/LawyerProfileForms";` is the tree's only reference to that module, so the handler-less «مشاركة عبر واتسآب 💬» (LawyerProfileForms.tsx:253) and «مشاركة … |
| profile-5 | متوسطة | 🔧 كانت حيّة — أُصلحت | `src/components/dashboard/LawyerProfileForms.tsx:336` | All three cards still render their headings over nothing, and OverviewTab is still mounted (profile/page.tsx:425). `<h3 …>المؤهلات</h3>` (336) sits above `{profile.education.map((ed, i) => (` (337); «المحاكم» (320) above `{profile.courts.map((c) => (` (323); … |
| profile-6 | متوسطة | ✅ أغلقتها المرحلة ١ | `src/components/marketplace/MyMarketplaceDashboard.tsx:62` | FeeSplitModal.tsx is unchanged (its «اقترح هذا التوزيع» button at line 127 still carries no onClick, unlike the «إلغاء» beside it at 130-131 which has `onClick={onClose}`), but it is now orphaned: `grep -rn "FeeSplitModal" src/` returns only self-references p… |
| profile-7 | متوسطة | ✅ أغلقتها المرحلة ١ | `src/components/marketplace/MyMarketplaceDashboard.tsx:62` | The whole component body is now `return (<DashboardComingSoon title="سوق المهنيين" description="سوق المهنيين — نشر الطلبات، استقبال العروض، ودعوات التعاون بين المحامين — غير متاح حالياً. لا توجد طلبات أو عروض أو دعوات حقيقية لعرضها، ولن يظهر هنا أي طلب حتى تُ… |
| profile-8 | متوسطة | ✅ أغلقتها المرحلة ١ | `src/components/marketplace/MyMarketplaceDashboard.tsx:62` | Every outbound link on «طلباتي في السوق» is gone with the screen. The file's only JSX is the `<DashboardComingSoon>` render at 62-66; `grep -n` for /services in this component returns nothing, and the single navigation it now offers is `backHref={BACK_HREF[us… |
| profile-9 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/profile/page.tsx:199` | I confirmed against the pre-fix blob that old line 183 was `.catch(() => { /* leave placeholders; never fall back to a mock identity */ });`. That silent catch is gone. The read is now a three-state machine (`type LoadState = "loading" \| "failed" \| "ready" … |

### المهام والجلسات والنشاط

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| tasks-1 | متوسطة | 🔧 كانت حيّة — أُصلحت | `src/app/api/v1/lawyer/activity/route.ts:216` | The four stat head-counts are still floored to zero with no error check: `ordersThisMonth: monthRes.count ?? 0,` (216) `ordersActive: activeRes.count ?? 0,` (217) `ordersCompleted: completedRes.count ?? 0,` (218) `ordersTotal: totalRes.count ?? 0,` (219). Onl… |
| tasks-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/_components/AddHearingModal.tsx:171` | The success screen no longer claims a sync. It now reads «تم حفظ الموعد في جدول أعمالك. يمكنك إضافته إلى تقويم Google يدوياً من بطاقة الموعد.» — it states the row was saved to the in-app diary and that adding it to Google is a manual step the lawyer takes fro… |
| tasks-3 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/activity/page.tsx:532` | The button element is gone; the only remaining match for «تصدير PDF» in the file is the comment that replaced it: `{/* A «تصدير PDF» button stood here with no onClick and no handler anywhere — no export route, no PDF generator, nothing bound to it. It was a p… |
| tasks-4 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/activity/page.tsx:529` | The header subtitle still promises AI-consultation tracking: `تتبع كافة أنشطتك — جلسات، مهام، استشارات AI، عقود، وموكلين` (529). The file's own comment above LIVE_STATS contradicts it: «الديمو pair «استخدامات AI» / «مواعيد اليوم» has no honest equivalent here… |
| tasks-5 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/hearings/page.tsx:191` | `const p = new URLSearchParams({action:"TEMPLATE",text:ev.title,dates:`${dateStr}/${dateStr}`,details,location:ev.location\|\|""});` — `dateStr` is `YYYYMMDD` only and `ev.time` is never referenced inside `googleCalUrl` (its only two uses in the whole file ar… |
| tasks-6 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/hearings/page.tsx:850` | `<CalendarView events={filtered.length>0?filtered:events} isDark={isDark}/>` — when the active time/type/urgency/search filters exclude every row, `filtered` is empty and the calendar silently renders `events`, i.e. the unfiltered diary. The list view directl… |
| tasks-7 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/hearings/page.tsx:135` | `caseId: caseName ? request.id : undefined,` (135), rendered at 300-303 as `<Link href={`/dashboard/lawyer/cases/${ev.caseId}`}>`. The code carries an explicit acknowledgement immediately above it: «NOTE: this makes the «القضية» chip link to /dashboard/lawyer… |
| tasks-8 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/tasks/page.tsx:396` | The inline modal that held the handler-less priority row was deleted. In its place: «Its «الأولوية» row was three buttons with no onClick at all: permanently styled as if «حرجة» and «عاجل» were both selected, and never sent» (396-398), and the render site now… |
| tasks-9 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/tasks/page.tsx:392` | «`addTask` and the inline «تفاصيل المهمة الجديدة» modal it served are gone.» (392). The replacement captures both halves of the case link and sends them straight through — `caseId: caseId \|\| undefined,` and `caseRef: caseRef \|\| undefined,` (AddTaskModal.t… |
| tasks-10 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/_components/AddHearingModal.tsx:91` | `title: caseName.trim() ? `جلسة — ${caseName.trim()}` : typeLabel,` — the case-name branch hardcodes «جلسة» regardless of the type the lawyer picked, so an appeal deadline («موعد طعن / نهائي») saved with a case name is titled «جلسة — قضية الأفق» and reads as … |
| tasks-11 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/hearings/page.tsx:200` | `const key = e.dateSort===0?"اليوم":e.dateSort===1?"غداً":e.dateSort<=5?"هذا الأسبوع":"هذا الشهر";` — the final branch is unbounded, so a hearing 90 days or three years out is grouped under «هذا الشهر». The bucket order at 204 (`const order = ["اليوم","غداً",… |
| tasks-12 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/hearings/page.tsx:646` | `if(search.trim()){const q=search.toLowerCase();evs=evs.filter(e=>e.title.includes(q)\|\|e.client?.includes(q)\|\|e.location?.includes(q));}` — the query is lowercased and none of the three haystacks are, so a lawyer searching a Latin term as typed («Aramco»,… |

### النظرة العامة والتحليلات

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| overview-1 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/analytics/page.tsx:45` | The whole page is now 51 lines whose only render is `<DashboardComingSoon title="إحصائيات الأداء المهني" description="لوحة الإحصائيات غير متاحة حالياً. المنصة لا تسجّل بعد تقييمات الموكلين ولا استخدام أدوات الذكاء الاصطناعي ولا نتائج القضايا…" backHref="/dash… |
| overview-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/analytics/page.tsx:43` | `export default function LawyerAnalyticsPage() { return ( <DashboardComingSoon … /> ); }` — the function body is one JSX element. There is no useEffect, no service call, no try/catch and no «خطأ في تحميل البيانات» string anywhere in the 51-line file, so the u… |
| overview-3 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/page.tsx:766` | The subscription banner still prints `باقة {TIER_CONFIG[lawyerTier].labelAr} — الصائغ القانوني ومفرغ الجلسات غير متاحَين` (766) and the AI grid still renders the «محجوب» pill for `item.href === "/ai/draft" && !TIER_CONFIG[lawyerTier].canDraft` (1258-1271), wh… |
| overview-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/page.tsx:538` | The banner is now gated on and describes the same row: `const soonestUrgent = criticalDeadlines.find((d) => d.severity === "urgent") ?? null;` (538), rendered as `soonestUrgent.daysLeft === 0 ? "موعد اليوم" : soonestUrgent.daysLeft === 1 ? "موعد غداً" : \`موع… |
| overview-5 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/page.tsx:428` | `status: CASE_STATUS_LABEL[c.status] ?? { label: c.status, tone: "waiting" as const }` (428) against the map at 61-66, which carries the four real statuses — `assigned` → «نشطة», `in_review` → «قيد المراجعة», `pending_assignment` → «بانتظار الإسناد», `complet… |
| overview-6 | متوسطة | ✅ أغلقتها المرحلة ١ | `src/components/marketplace/MyMarketplaceDashboard.tsx:62` | The header link at page.tsx:661 still points at /dashboard/lawyer/marketplace, but that route is now `return <MyMarketplaceDashboard userType="lawyer" />` over a 68-line component whose entire render is `<DashboardComingSoon title="سوق المهنيين" description="… |
| overview-7 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/page.tsx:471` | Every timeline row is built with `category: "manual" as "ai" \| "manual" \| "system"` (471) — a literal, with no branch that can ever produce "ai" — and the tab filters on `item => activityTab === "all" \|\| item.category === "ai"` (586), so the «نشاط AI» tog… |
| overview-8 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/page.tsx:378` | The `stats` array (374-388) now yields four objects whose only keys are `label / value / icon / color / bg / sub` — e.g. `{ label: "القضايا النشطة", value: dashboardData.activeCases === null ? null : String(dashboardData.activeCases), icon: Scales, color: "te… |
| overview-9 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/page.tsx:851` | The quick action is still `{ label: "استشارة جديدة", icon: CalendarCheck, href: "/dashboard/lawyer/consultations?book=1", shortcut: "C", accent: false }` (851), and the destination never reads it: grep for `useSearchParams`/`searchParams` across src/app/dashb… |
| overview-10 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/page.tsx:1188` | The table header list is now `{["اسم القضية", "النوع", "الحالة", "آخر تحديث", ""].map((h, i) => (` (1188) — five columns, no «الخطوة القادمة» — and the body rows render title / type / status / date / a caret link, with no `metadata.next_step` read anywhere in… |

### العقود والمستندات

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| contracts-1 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/contracts/page.tsx:321` | The old `deleteContract` (confirmed at a5b69a1^:contracts/page.tsx:132-135, `showToast("تم حذف العقد")` over a soft-delete to `status:"cancelled"`) no longer exists. The only destructive control is «إلغاء العقد» → `cancelContract`, whose toast is `done: "تم إ… |
| contracts-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/contracts/page.tsx:555` | The «منتهي» chip is gone: the chip set is now `{(["all", "draft", "pending_sign", "active"] as const).map(s => (` (555), and `type ContractStatus = "active" \| "pending_sign" \| "draft" \| "cancelled"` (line 21) no longer declares `"expired"` at all — grep fo… |
| contracts-3 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/documents/page.tsx:502` | src/app/dashboard/lawyer/documents/SmartTemplateModal.tsx was deleted in commit a5b69a1 (`git show --stat` shows `.../lawyer/documents/SmartTemplateModal.tsx \| 144 --`); a repo-wide grep for "SmartTemplate" now returns zero hits in src/. The templates tab ca… |
| contracts-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/documents/page.tsx:587` | The strip that rendered `{tmpl.uses.toLocaleString()} استخدام` (confirmed at a5b69a1^:documents/page.tsx:619) is gone, replaced by the comment at 583-590: «the «847 استخدام / ٢–٣ صفحات / 12 حقل» strip — a usage counter, a page count and a field count for a do… |
| contracts-5 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/documents/page.tsx:348` | Every `<button>` in documents/page.tsx now carries a handler — grep lists 297, 304, 377, 403, 427, 528, 537, 547, 552 and each has an `onClick`. The two the audit named are live: `onClick={() => handleView(doc)}` (298) and `onClick={() => handleDownload(doc)}… |
| contracts-6 | متوسطة | 🔧 كانت حيّة — أُصلحت | `src/constants/navigation.sidebars.legal.ts:67` | Unchanged: `{ label: "الأرشيف الموحّد", labelEn: "Unified Archive", href: "/dashboard/lawyer/archive", icon: "Archive", badge: "جديد" },`. The destination is still the twelve-line notice — archive/page.tsx returns `<DashboardComingSoon title="الأرشيف" descrip… |
| contracts-7 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/contracts/page.tsx:540` | The cap is no longer silent and no longer 20. `const WORKSPACE_PAGE_LIMIT = 200;` (152) is passed explicitly as `{ receiver: "lawyer", limit: WORKSPACE_PAGE_LIMIT }` (216), truncation is derived from the server's exact count — `cut = typeof response.total ===… |

### الماليات

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| finance-1 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/finance/page.tsx:553` | The 50-row cap is now `const MAX_ROWS = 500;` (route.ts:104) and the route returns an exact `invoicesTotal` / `walletTransactionsTotal` beside each list. The page turns that into a page-level banner ABOVE the tab bar: «هذه الشاشة تقرأ أحدث {invoices.length} ف… |
| finance-2 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/finance/page.tsx:1085` | The net-margin card no longer exists. The «pl» tab is retitled «ملخص الأتعاب والتحصيل» and ends in an explicit disclaimer instead of a margin figure: «هذا ملخص تحصيل وليس قائمة أرباح وخسائر: المنصة لا تسجّل مصروفات المكتب ولا ضريبة القيمة المضافة، فلا يمكن اح… |
| finance-3 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/finance/page.tsx:1160` | The submit spinner now says only «جارٍ حفظ الفاتورة...» / «يتم تسجيل الفاتورة في سجلّك المالي على المنصة.» (page.tsx:1160-1163), with the replacement recorded in place: «النص السابق كان يعد بـ«فحص المطابقة وحساب الضريبة بالذكاء الاصطناعي»؛ الطلب يكتب صف فاتور… |
| finance-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `src/constants/lawyerFinanceData.ts:12` | The category chips and everything behind them were deleted: «`EXPENSES`, `Expense`, `ExpenseCategory`, `EXP_CFG` — the whole expenses feature, deleted as one unit. Nothing outside this file referenced any of the four.» (lawyerFinanceData.ts:12-15). A grep for… |
| finance-5 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/finance/page.tsx:244` | Every bucket key and label now carries the Gregorian year: monthly `key: `${y}-${m}`, sort: y * 12 + m, label: `${GREGORIAN_MONTHS_AR[m - 1]} ${y}`` (page.tsx:232), quarterly `key: `${y}-Q${q}`, sort: y * 4 + q, label: `الربع ${q} — ${y}`` (page.tsx:238), ann… |

### العملاء والاستشارات

| # | الخطورة | الحكم | الموضع اليوم | الدليل |
|---|---|---|---|---|
| clients-1 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/clients/page.tsx:236` | The archive note is unchanged: «الموكلون هنا ليس لديهم طلبات نشطة حالياً أو تم تصنيفهم يدوياً كغير نشطين. لم يتم حذفهم — تستطيع استعادتهم في أي وقت.» Neither promise is backed. Archiving is computed, not chosen — `isArchived = (c) => c.activeRequests === 0 \|… |
| clients-2 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/clients/page.tsx:67` | Narrowed but live. `unpaid` and `rating` are now real comparators (lines 65-66, `outstandingOf(b) - outstandingOf(a)` and `(b.rating ?? -1) - (b.rating…)`), so two of the three no-ops were fixed — but the comparator still ends `return 0;` at line 67, and the … |
| clients-3 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/clients/page.tsx:156` | The «بيانات تجريبية» / «اربط حسابك بقاعدة البيانات» banner is gone — `grep -rn "بيانات تجريبية\|اربط حسابك" src` no longer matches this file at all. The empty-directory banner now reads «لا يوجد موكّلون بعد» with «أضف موكّلاً جديداً، أو انتظر أول طلب يُوجَّه … |
| clients-4 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/consultations/page.tsx:101` | `workflowToConsultation` now reads the stored value: `duration: typeof rawDuration === "number" && Number.isFinite(rawDuration) && rawDuration > 0 ? rawDuration : null` (:101-103) off `request.metadata?.duration` (:85), with the comment «This used to be `meta… |
| clients-5 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/consultations/page.tsx:32` | The status itself is gone: `type ConsultStatus = "upcoming" \| "completed" \| "cancelled"` (:32) under «No "inProgress": nothing sets it … so a «جارية الآن» bucket could only ever read 0» (:29-31). The KPI grid is now قادمة / مكتملة / ملغية (:632-635) with ««… |
| clients-6 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/consultations/page.tsx:359` | Both promises are intact. Booking step 4 still tells the lawyer «بعد انتهاء الجلسة يمكنك توليد ملخص AI وإرساله للعميل تلقائياً» (:359), and the footer tip still says «يمكن للمستشار AI توليد ملخص كامل للجلسة وإرساله للعميل مباشرةً بعد الانتهاء.» (:765). Phase … |
| clients-7 | متوسطة | ✅ أغلقتها المرحلة ١ | `~/consultations/page.tsx:602` | Both fee totals are deleted. Where the header total stood there is now only a comment: «The «إجمالي: X ﷼» that used to sit here summed `payment.amount`, which the booking form never collects and always stores as 0 — a money figure with no source, on a platfor… |
| clients-8 | متوسطة | 🔧 كانت حيّة — أُصلحت | `~/consultations/page.tsx:180` | The output half was fixed (`date: metaString(request.metadata?.day)` at :94 and the card's `c.time ? … : «بانتظار التأكيد»` at :450-452), but the input half is untouched and now fabricates instead of blanking. `handleConfirm` validates nothing: the footer but… |
| clients-9 | منخفضة | ⚠️ أُعلن إغلاقها ثم أعادها التكذيب | `~/clients/[id]/page.tsx:329` | OVERTURNED IN PART. The guard `const hasFees = client.totalFees !== null && client.paidFees !== null;` (:329) is defeated by a persisted literal 0, and 0 is exactly what the live add path writes for a SKIPPED fee field. AddClientModal's fee inputs are optiona… |
| clients-10 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/clients/[id]/page.tsx:671` | All four invented assessments are gone. «مستوى الأولوية» now reads «لم يُصنَّف» unless a flag was actually ticked (:671), «مخاطر السداد» reads «لم تُصنَّف» unless `hasLatePay` (:672) — the «منخفضة ✓» default is gone — and the rating row is spread in only when… |
| clients-11 | منخفضة | ✅ أغلقتها المرحلة ١ | `~/clients/[id]/page.tsx:719` | The notepad is still React state, but it no longer claims to save: the hint is «Ctrl+Enter للإضافة» (:732), not «للحفظ», and a permanent amber strip above the box states «ملاحظات مؤقتة داخل هذه الجلسة فقط — لا تُحفظ في النظام وتُفقد عند تحديث الصفحة.» (:719),… |
| clients-12 | منخفضة | ✅ أغلقتها المرحلة ١ | `src/components/dashboard/lawyer/ClientDrawer.tsx:144` | The raw ISO is gone: `toLawyerClientView` maps `lastContact: formatContactDate(d.lastActivity)` (:100) and `formatContactDate` returns `d.toLocaleDateString("ar-SA", { year: "numeric", month: "short", day: "numeric" })`, or "" when there is nothing (:139-148)… |
| clients-13 | منخفضة | 🔧 كانت حيّة — أُصلحت | `~/consultations/page.tsx:461` | The AI-summary half is closed («A «ملخص AI للاستشارة» panel used to live here too, with «إرسال للعميل» and «تصدير PDF» buttons under it … Removed.», :470-474), but the dead caret is exactly as described. The button renders on `{(c.status === "completed" \|\| … |

---

## ما فعلته كل جولة

### الجولة ١ — `5457eac`

**٢٦ بنداً · رفض التكذيب ١ · ارتدادات ٢**

| البند | النتيجة | ماذا تغيّر |
|---|---|---|
| `cases-1` | MADE_HONEST | Stopped rendering `assigned_to` (a UUID) as a lawyer's name and as an avatar initial. cases/[id]/page.tsx:396 — the team member built from `caseData.assignedTo` no longer carries the id: `name: "الاسم غير متاح"`, `avatar: ""`, and a new `nameKnown: false` flag; at :1214 the avatar circle renders a `<User />` icon inst… |
| `cases-3` | FIXED_REAL | A failed signed-URL left both document buttons doing literally nothing. Added `downloadError` state at cases/[id]/page.tsx:282 beside the existing `uploadError`, and rewrote `handleDownload` (:589-607): it clears the banner first, and the two failure branches that were already in hand now speak — falsy url → `تعذّر فت… |
| `cases-5` | FIXED_REAL | A cancelled case read «مغلقة» with a fully-filled stage bar. The page-local `CaseStatus` union (NOT the shared one in _types.ts) gained "cancelled" at cases/[id]/page.tsx:57; `STATUS_CONFIG` gained the exhaustive key at :87 — `cancelled: { label: "ملغاة", color: "text-rose-500 bg-rose-500/10 border-rose-500/20", dot: … |
| `cases-8` | MADE_HONEST | Removed the whole «نطاق زمني» filter group rather than only its three dead buttons. اليوم/هذا الأسبوع/هذا الشهر filtered on `Case.nextDateSort`, which `workflowToCase` never assigns (only businessCasesData.ts writes it), and «طعون قادمة» filters on `metadata.deadline`, which nothing in the repo writes — all five retur… |
| `cases-9` | FIXED_REAL | `workflowToCase` hardcoded `degree: "primary"` for every row. New `courtToDegree()` at constants/lawyerCasesData.ts:46 does `COURTS_LIST.find(c => c.id === court)?.degree ?? "primary"`, and :75 is now `degree: courtToDegree(court)` (the court string is lifted to a local const at :65 and shared with the `court` field).… |
| `cases-10` | MADE_HONEST | Removed the S59 collaboration rail outright (cases/page.tsx:860 now carries the why in its place), not just its two empty tabs. `workflowToCase` returns a hardcoded `collab: "solo"` with no source on the row, so «مشتركة» and «فريقي» returned nothing for every lawyer always — reading as «you collaborate on nothing» — w… |
| `cases-11` | DEFERRED | Not fixable inside my three files — the client half is already correct and needs no change. |
| `tasks-1` | MADE_HONEST | src/app/api/v1/lawyer/activity/route.ts:149-171 + :236-249 — the four head-count results now have their `.error` read and logged, matching the existing eventRes/auditRes pattern (`[lawyer/activity GET] ordersActive count failed: …`), and `stats` is returned as `null` when ANY of the four errored instead of `monthRes.c… |
| `tasks-4` | MADE_HONEST | src/app/dashboard/lawyer/activity/page.tsx:538 — subtitle was «تتبع كافة أنشطتك — جلسات، مهام، استشارات AI، عقود، وموكلين», now «تتبّع حركة طلباتك — جلسات، مهام، عقود، وموكلين». «استشارات AI» is removed because describeRequestEvent (src/lib/events.ts) has no AI badge at all — the same reason the «استخدامات AI» stat ca… |
| `tasks-5` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:190-243 — `googleCalUrl` now sends the hour. New `parseTimeOfDay()` reads `ev.time` (deliberately `/^\s*(\d{1,2}):(\d{2})/` with range checks, not `^\d{2}:\d{2}$` — `<input type="time"` emits HH:MM:SS at sub-minute step and older `metadata.time` is free text, and a regex miss… |
| `tasks-6` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:942 — `<CalendarView events={filtered.length>0?filtered:events}/>` is now `events={filtered}`, so the calendar can no longer silently redraw the UNFILTERED diary when the active period/type/priority/search filters exclude every row. Guarded above it (:928) `{events.length>0&&… |
| `tasks-7` | MADE_HONEST | src/app/dashboard/lawyer/hearings/page.tsx:131-141 and :372-383 — the `caseId: caseName ? request.id : undefined` assignment is deleted from `workflowToHearing` and the «القضية» chip is now a `<span>` instead of `<Link href={`/dashboard/lawyer/cases/${ev.caseId}`}>`, with the trailing ArrowSquareOut (which promised it… |
| `tasks-10` | FIXED_REAL | src/app/dashboard/lawyer/_components/AddHearingModal.tsx:99 — `title: caseName.trim() ? `جلسة — ${caseName.trim()}` : typeLabel` is now `` `${typeLabel} — ${caseName.trim()}` ``, so «موعد طعن / نهائي» saved with a case name is titled «موعد طعن / نهائي — قضية الأفق» instead of «جلسة — قضية الأفق», and the title stops c… |
| `tasks-11` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:249-268 — `groupByDate` had BOTH ends open. «هذا الشهر» was the unbounded final branch (a hearing 90 days or 3 years out sat under it), and a NEGATIVE `dateSort` fell through `<=5` into «هذا الأسبوع», which is how the الأرشيف view headed last year's sittings. Now `dateSort<0 … |
| `tasks-12` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:712-722 — the search filter lowercased only the query, so a Latin term typed as it appears («Aramco», «SABIC») matched nothing. Now a `hit()` helper lowercases each haystack too. It also searches `e.caseName`, which is near-noop rather than scope creep: the chip is on the car… |
| `clients-1` | MADE_HONEST | clients/page.tsx:265-283 — the archive note promised a restore that cannot exist: /api/v1/lawyer/clients exports GET and POST only, so the `inactive` flag AddClientModal writes at creation can never be un-ticked, and there is no restore control on the page. Removed «لم يتم حذفهم — تستطيع استعادتهم في أي وقت.» and rewr… |
| `clients-2` | FIXED_REAL | clients/page.tsx — the default sort fell through the comparator's `return 0` (was :67), and since GET /api/v1/lawyer/clients has no .order() the order it preserved was "profiles query order, then manual clients", not activity order. Two parts. (a) Label: option relabelled «آخر تواصل» → «آخر نشاط» at :360, matching wha… |
| `clients-6` | MADE_HONEST | consultations/page.tsx — both surviving AI-summary promises removed. (a) Booking step 4 (was :356-361): deleted the whole gold tip box whose text was «بعد انتهاء الجلسة يمكنك توليد ملخص AI وإرساله للعميل تلقائياً» — the last thing a lawyer read before confirming a booking, and false in both halves (no consultation-sum… |
| `clients-8` | FIXED_REAL | consultations/page.tsx — the booking form required nothing and the payload filled in whatever was skipped. handleConfirm (:198-224) no longer substitutes: `consultLabel` drops its `?? consultType` fallback, `title: consultLabel \|\| "استشارة قانونية"` → `title: consultLabel`, `name: clientName \|\| "عميل"` → `name: cl… |
| `clients-13` | FIXED_REAL | consultations/page.tsx:514-520 — the caret's condition `(c.status === "completed" \|\| c.notes)` did not match the panel's `expanded && c.notes`, so a completed consultation with an empty description (the booking modal writes `description: notes \|\| ""`) showed a caret that rotated and revealed nothing. Both are now … |
| `clients-9` | DEFERRED | The finding's file, src/app/dashboard/lawyer/clients/[id]/page.tsx, and both suggested fix sites are outside my file list, so I did not touch them. I DID close the same-cause defect on the surface I own: clients/page.tsx:401-411, where `outstanding` was gated on `totalFees !== null && paidFees !== null` — defeated by … |
| `overview-3` | MADE_HONEST | Confirmed the lock is fiction, then removed all of it. Verification first: src/app/ai/draft/page.tsx (152 lines) and src/app/ai/contracts/page.tsx (392 lines) contain no match for tier/plan/entitlement/canDraft/locked/subscription/upgrade; src/proxy.ts lists both paths only in PROTECTED, which is the auth gate, not a … |
| `overview-7` | MADE_HONEST | Confirmed and removed. `category` was pinned to the literal "manual" on every row (old page.tsx:471) and the filter tested `item.category === "ai"`, so the «نشاط AI» toggle was a live, undisabled, unwrapped button that could only ever empty the card — it failed all three زر-ميت exemptions. Deleted the two-button segme… |
| `overview-9` | MADE_HONEST | The suggested SMALL_REAL_FIX is not available to me: honouring the param means editing src/app/dashboard/lawyer/consultations/page.tsx, which is outside my file list. Re-verified the finding — that page calls neither `useSearchParams` nor a `searchParams` prop, so `?book=1` was inert, while its `showBooking` state (li… |
| `profile-5` | MADE_HONEST | src/components/dashboard/LawyerProfileForms.tsx — OverviewTab (was lines 266-398, now 264-345). Removed all FOUR permanently-empty cards: «المؤهلات» (mapped profile.education), «المحاكم» (mapped profile.courts), «اللغات» (mapped profile.languages, and drew a 5-segment proficiency bar whose fill condition was `s <= 5` … |
| `contracts-6` | MADE_HONEST | src/constants/navigation.sidebars.legal.ts:67 — `badge: "جديد"` → `badge: "قريباً"` on the الأرشيف الموحّد item. The destination /dashboard/lawyer/archive is the exemplary DashboardComingSoon notice («…غير متاح حالياً. ستظهر السجلات المؤرشفة هنا فور توفّر مخزن أرشفة مشترك…») and was deliberately NOT touched — only the… |

### الجولة ٢ — `6e39341`

**٢١ بنداً · رفض التكذيب ٢ · ارتدادات ٦**

| البند | النتيجة | ماذا تغيّر |
|---|---|---|
| `cases-11` | FIXED_REAL | route.ts:211 — the GET handler's `if (error \|\| !serviceRequest) → 404` is split. Added a module-level `PGRST_NO_ROWS = "PGRST116"` (route.ts:136) and `readFailure(where, error)` (route.ts:168), copying the shape at admin/library/route.ts:178 and lawyer/finance/route.ts:151: a non-PGRST116 `error` logs `message` + `c… |
| `cases-11-patch-status` | FIXED_REAL | route.ts:781 — the identical collapse in the PATCH status gate (was `if (existingError \|\| !existing) → 404 "Service request not found"`) is split the same way. This is the cancel path: OrderActions.tsx:93-98 keys its Arabic copy off the STATUS CODE, so a cancel during a database outage moves from «لم يعد هذا الطلب م… |
| `cases-11-patch-revision` | FIXED_REAL | route.ts:364 — third collapse, in the `request_revision` branch (`existingError \|\| !existing` → 404 «الطلب غير موجود»). Split; the 404 body is unchanged. RevisionPanel.tsx:120-147 switches on `reason` before status, and `read_failed` is not in that switch, so it correctly falls to the default 500 branch → «تعذّر تنف… |
| `cases-11-patch-edit-details` | FIXED_REAL | route.ts:621 — fourth collapse, in the `edit_details` branch. Split; 404 body unchanged. |
| `cases-11-post-support-ticket` | FIXED_REAL | route.ts:1057 — fifth collapse, in the POST `support_ticket` order lookup. Split; 404 body unchanged. This one mattered on its own: a support channel that answers «الطلب غير موجود» over a timeout refuses to hear from someone about an order that exists. |
| `edit-details-english-500` | FIXED_REAL | route.ts:689 — `edit_details` answered a failed write with `{ error: updateError.message }`, a raw PostgREST string in ENGLISH, and OrderEditPanel.tsx:64 ECHOES `body.error` onto the screen (its Arabic fallback fires only when the body is absent). That component's own comment reasons «this handler answers in Arabic on… |
| `get-sublist-false-empty` | DEFERRED | route.ts:237-271 — the GET's `events` and `attachments` reads both dropped `error`, so a failed sub-read renders «لا يوجد سجل» / «لا توجد مستندات» on a case that has them. Both now read and LOG the error with `message` + `code` (route.ts:246, :269), mirroring the pattern the duplicate-ticket check at :1109 already doc… |
| `clients-9` | FIXED_REAL | «مسدّدة بالكامل» over fees nobody entered is now unreachable for any non-positive total, and the rows already in the table are repaired without a migration.  READ SIDE FIRST (the part that repairs existing clients) — src/app/api/v1/lawyer/clients/route.ts:79-96, inside readClientClassification. The `money()` guard is … |
| `REGRESSION-A-route` | FIXED_REAL | route.ts:130-186 (GET) — the role-profile sub-query no longer discards its error. Switched `.single()` -> `.maybeSingle()` on all three branches (lawyer_profiles / provider_profiles / micro_profiles) and read the error, which separates the two states cleanly: `error != null` = read FAILED; `data == null, error == null… |
| `REGRESSION-A-client` | FIXED_REAL | The empty-bio sentence is now gated on whether the profile was actually read, so the screen can no longer contradict itself. page.tsx: added `roleProfileReadFailed` to EMPTY_PROFILE (:111) and to the local `ProfileApiResponse` (:182, optional so an older route reads as `undefined` -> not-failed), set from the response… |
| `REGRESSION-B` | FIXED_REAL | page.tsx:100-106 — the present-tense comment about the deleted languages card («renders a five-segment proficiency bar whose fill condition is `s <= 5` — always true») is gone with the key it annotated. Dropped five write-only keys (`languages`, `education`, `courts`, `linkedin`, `twitter`): verified read nowhere, the… |
| `CLEANUP-orphans` | FIXED_REAL | LawyerProfileForms.tsx cut from 534 to 151 lines; `OverviewTab` is now the only export and the only one that ever had a caller (page.tsx:15). Deleted `ShareModal` (+ ShareModalProps), `AchievementsTab` (+ props), `ReviewsTab` (+ props), `SpotlightCard` and `RingScore`, plus all three now-unused import statements (fram… |
| `EXTRA-profiles-404-conflation` | FIXED_REAL | Same defect class, same file, 80 lines above REGRESSION A and previously unreported: every `profiles` read failure answered 404 «لم نعثر على ملفك الشخصي.», and page.tsx:253 renders that string verbatim — so a transport failure told a lawyer his ACCOUNT did not exist. Now split on the code the repo already uses (admin/… |
| `DEFERRED-provision-missing-role-row` | DEFERRED | A lawyer with no `lawyer_profiles` row has no self-service way to get one, so the new absent-row banner deliberately states the fact and offers no next step — it does not link the editor, which would be a false promise. |
| `tasks-4` | MADE_HONEST | activity/page.tsx:564 — the reviewer was RIGHT and I verified every term independently before changing it. Three of the four kept terms named badges no event can carry. «جلسات»/«عقود»/«موكلين» are the hearing/contract/client badges, reachable ONLY from RequestEvent.HEARING_CREATED, CONTRACT_CREATED, CONTRACT_STATUS_CH… |
| `consultations-next-upcoming` | FIXED_REAL | consultations/page.tsx — «الاستشارة القادمة» was naming an arbitrary row. Three parts. (1) NEW scheduledOf() at :139 returns {at, hasTime} or null. Parse is strict (^\d{4}-\d{2}-\d{2}$ for the date, ^\d{1,2}:\d{2} unanchored for the time so a browser's "HH:MM:SS" is accepted and its seconds ignored) rather than new Da… |
| `overview-9` | FIXED_REAL | consultations/page.tsx now honours ?book=1 — but read the caveat, the user-visible path is NOT yet end-to-end. What I own is done: :14 imports useSearchParams from next/navigation, :639 const bookParam = searchParams?.get("book") ?? null, and :648 the existing showBooking state is SEEDED from it — useState(bookParam =… |
| `H1-week-bucket-vs-week-filter` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:216-217 — added `const WEEK_HORIZON_DAYS = 7` and `const MONTH_HORIZON_DAYS = 30` as the single definition, and made BOTH call sites read them: the headings in groupByDate (:317-318, was the hardcoded `<=5` / `<=30`) and the period filter in the page (:774-775, was `<=7` / `<… |
| `H2-archive-blank-calendar-grid` | FIXED_REAL | I picked the FIRST option the brief offered — the calendar now honours past/done rows — and it needed two halves, because the guard removal alone leaves the reported symptom on screen. (a) src/app/dashboard/lawyer/hearings/page.tsx:566-583: removed CalendarView's internal `if(ev.dateSort>=0 && !ev.done)` guard. `event… |
| `H3-linked-tasks-dead-code` | FIXED_REAL | Confirmed unreachable before removing: `LINKED_TASKS` was written by no code path in the repo (grep across src returned only its own declaration and the one lookup), so `linkedTasks` was `[]` for every event and every gate on it was false. Removed from src/app/dashboard/lawyer/hearings/page.tsx: the `LINKED_TASKS` Rec… |
| `H4-raw-iso-date-on-event-card` | FIXED_REAL | src/app/dashboard/lawyer/hearings/page.tsx:435 — the card printed `{ev.date}`, the raw "2026-09-15" from `<input type="date">`, inside an Arabic RTL line beside the Arabic-Indic Hijri strip. Now `{formatEventDateAr(ev.date)}`, a new helper at :255 producing «١٥ سبتمبر ٢٠٢٦» from the page's own AR_MONTHS plus `toArabic… |

### الجولة ٣ — `ab23a96`

**٢٦ بنداً · رفض التكذيب ٣ · ارتدادات ٧**

| البند | النتيجة | ماذا تغيّر |
|---|---|---|
| `overview-9` | FIXED_REAL | Restored `?book=1` on the «استشارة جديدة» quick action: href is now "/dashboard/lawyer/consultations?book=1". VERIFIED THE READER FIRST, as instructed, and it is real — consultations/page.tsx imports useSearchParams (:14), calls it (:625), derives `bookParam = searchParams?.get("book") ?? null` (:639), seeds `useState… |
| `overview-9-stale-comment` | MADE_HONEST | Rewrote the 8-line comment above that item, which asserted «Nothing read it: /dashboard/lawyer/consultations calls neither useSearchParams nor a searchParams prop» and called honouring the param a future change on another group's file. Round 2 landed that change, so the comment was actively false and was the mechanism… |
| `profile-mock-warning-stale` | MADE_HONEST | SECOND STALE COMMENT, not named in the brief and the most consequential one found. The ⚠️ paragraph in the share-profile block warned that «the profile page itself never reads its route segment; it renders a module-level mock, so every id shows the same fabricated lawyer», and that whoever flips BETA_MONOPOLY_MODE mus… |
| `secondment-comment-stale` | MADE_HONEST | THIRD STALE COMMENT. The one-liner above the KPI grid claimed «entry point is the sidebar link to /dashboard/lawyer/secondment (gated there)». Both halves false: `grep -rn "lawyer/secondment" src/` returned exactly one hit — the comment itself — so no sidebar entry, no link, nothing routes there; and the page is not g… |
| `kbd-shortcut-chips` | MADE_HONEST | MY OWN SCOPE CALL — not assigned, flagging it so the parent sees a decision rather than an unrequested deletion. Three items in the same quick-actions array rendered <kbd> chips promising keyboard shortcuts: Q on «قضية جديدة», C on «استشارة جديدة», D on «صيغ مستند». Nothing implements them. I searched src/ for `e.key`… |
| `ai-branch-dead-code` | MADE_HONEST | Last fragment of the «نشاط AI» toggle that round 1-2 removed: the activity timeline still tested `item.type === "ai"` twice — for a duotone icon weight and for gold #C8A762 text. `type` comes from badgeToActivityType(), whose switch returns only success \| warning \| info (ActivityBadge's other members all fall to the… |
| `hearing-type-label-mirror` | NOT_A_DEFECT | Checked, accurate, no change. The comment claims HEARING_TYPE_LABEL mirrors EVENT_CONFIG in hearings/page.tsx «so the overview and the schedule name the same thing identically». Compared key-by-key against hearings/page.tsx:168-179: all 10 keys present on both sides with identical Arabic labels (hearing/deadline/gov_r… |
| `task-category-label-mirror` | NOT_A_DEFECT | Checked, accurate, no change. TASK_CATEGORY_LABEL vs CATEGORY_CONFIG in tasks/_data.ts:28-34 — all 5 keys identical with identical labels (case قضية, document مستند, deadline ميعاد, admin إداري, client موكل). |
| `graph-deeplink-comment` | NOT_A_DEFECT | Checked, accurate, no change — and notable because it is the SAME defect class as overview-9, so I verified the reader rather than trusting it. The «جراف القضايا» button emits ?tab=graph, and cases/[id]/page.tsx does read it (:262-267: useSearchParams, `searchParams.get("tab")`, validated against TABS), with a "graph"… |
| `tier-removal-comment` | NOT_A_DEFECT | Checked, accurate, no change. The 30-line top-of-file note justifying the removed subscription-limit card rests on two checkable claims, both still true: (a) `grep -rn "entitlement\|tier\|useSubscription\|plan"` over src/app/ai/draft/page.tsx and src/app/ai/contracts/page.tsx returns ZERO hits — neither tool has a pla… |
| `notice-can-state-a-falsehood` | FIXED_REAL | Reproduced first, in node, exactly as described: a fetch at 22:00 on 29 Sept with a sitting on 30 Sept freezes dateSort=1; after midnight getEventDate(1) resolves to 1 October, inMonthCount for September drops to 0, and the page prints «لا يقع أيّ من المواعيد … في سبتمبر ٢٠٢٦» about a hearing happening that day. I als… |
| `datesort-frozen-at-fetch` | FIXED_REAL | The second half, option (b), because (a) alone leaves the list view able to head a card dated today with «غداً». dateSort is no longer frozen at fetch: the page keeps the local day as state (todayKey) and re-derives every row's dateSort from its absolute date against it, so the group headings, the period filters and t… |
| `google-calendar-wrong-day` | FIXED_REAL | Found while removing getEventDate: googleCalUrl built its start from getEventDate(Math.max(0, ev.dateSort)), so the same midnight drift sent Google the WRONG DAY — a 30 Sept sitting fetched the evening before was offered to Google as 1 October, silently, in the one place the lawyer would rely on a reminder. It now sta… |
| `stale-comment-drift-is-harmless` | MADE_HONEST | The comment on CalEvent.date claimed dateSort being a day out «only misplaces a row rather than misdating it on screen». Round 2's own notice made that false — a misplaced row became an assertion about a month — so the comment was actively reassuring the next reviewer about the exact defect this round exists to fix. R… |
| `own-comment-counterfactual` | MADE_HONEST | Self-inflicted, caught on review before reporting. In the new googleCalUrl comment I wrote that the removed `Math.max(0, …)` clamp «would silently have moved such a row onto today». That could not happen: the callsite gates on `ev.dateSort>=0`, so googleCalUrl was never reached with a negative offset and the clamp nev… |
| `regression-1-adjacent-screens-contradict` | FIXED_REAL | Verified first that GET /api/v1/profile ALWAYS emits `roleProfileReadFailed` (route.ts:205-212, including `false`), then branched the editor on it. Added `type BlockedReason = "read-failed" \| "no-row" \| "no-server"` plus a `BLOCKED_COPY` map, a `blocked` state set on every non-success exit of load(), and rewrote the… |
| `regression-1b-read-failed-copy-overclaimed` | MADE_HONEST | Not in the brief, found while writing it. The read-failed body asserted «الحفظ الآن كان سيمسح نبذتك وتخصصاتك ورقم ترخيصك» — a certainty about a row we by definition could not read, and which may not exist at all (a 404 on `profiles` reaches the same branch). Softened to «لا نعرف ما هو محفوظ فيه الآن، والحفظ بحقول فارغ… |
| `regression-2-stale-comment-dead-end-that-is-not-enforced` | MADE_HONEST | Narrowed the comment rather than gating the Link, with reasons recorded in it. The claim «Neither branch offers تعديل as the way out» described an enforcement that never existed — the Link renders unconditionally ten lines above in the same render. Its underlying fact (PATCH updates, does not insert) is still true and… |
| `scope-alignment-two-screens-implying-a-third-page` | MADE_HONEST | profile/page.tsx:358 said «لا يمكن إنشاء السجل المهني من هذه الصفحة». With the editor about to say the same thing about itself, a lawyer bouncing between the two would infer some third page can. Verified there is none: grep finds no `lawyer_profiles` INSERT/upsert anywhere in src/ except onboarding/account-type/route.… |
| `deferral-let-a-lawyer-create-his-own-role-row` | DEFERRED | Recorded the corrected deferral reason as a comment at the deferral site (the branch in edit/page.tsx that sets the "no-row" banner) and did NOT build the provisioning path, as instructed. The comment names the migration, the policy, the one production check, the service-role trap, and the out-of-scope file the build … |
| `1-rejected-paid-rendered-as-zero` | MADE_HONEST | readClientClassification now separates "no advance on record" from "an advance the guard refused". `paidFees` falls back to 0 ONLY when meta.paidFees is absent (undefined/null, which is what POST writes when the lawyer typed nothing) — round 2's actual stated rationale, preserved exactly. A value that IS present but u… |
| `2-advance-without-total-silently-discarded` | FIXED_REAL | CHOSE: BLOCK SUBMISSION, not accept-and-warn. New validateFees() in AddClientModal gates step 1 — «التالي» is disabled and an Arabic message renders inline under the fee fields the moment the entry is unusable. Three rules: (a) a non-numeric/negative total, (b) a non-numeric/negative advance, (c) any advance — 0 inclu… |
| `3-write-side-stored-what-the-read-refused` | FIXED_REAL | DECLARED EXTRA, same file, same defect as item 2 one layer up. route.ts persisted any `typeof v === "number"`, so totalFees 5000 / paidFees -100 was written, returned 200, and echoed a card with no fee panel — a number in the DB and on no screen, newly created rather than legacy. Hoisted a single isMoneyFigure() predi… |
| `4-rating-write-guard` | FIXED_REAL | DECLARED EXTRA, deliberate and reconsidered once. `if (typeof rating === "number") metadata.rating = rating` stored a rating of 7 that the read guard (integer 1-5) then dropped — identical shape to item 3. Added isRatingFigure(), used as both write guard and read guard, with a 400 in Arabic. I considered the narrower … |
| `5-english-and-raw-postgres-error-copy` | FIXED_REAL | DECLARED EXTRA, required by hard constraint 5. I verified apiMutate (src/lib/services/api.ts:56-58) throws `errorBody.error`, which AddClientModal puts straight into its red banner — so this route's error strings ARE screen copy. The insert failure path forwarded the raw Postgres `error.message`, putting English and s… |
| `6-stale-comments` | MADE_HONEST | Comments corrected so they describe the code that now exists. readClientClassification's fee paragraph now states the absent-vs-unreadable split and its cost; the POST header states that fee figures and rating are validated against the very predicates the GET reads them back with and that errors here are screen copy; … |

### الجولة ٤ — `(هذا الالتزام)`

**٥ بنداً · رفض التكذيب ٠ · ارتدادات ٠**

| البند | النتيجة | ماذا تغيّر |
|---|---|---|
| `ITEM 1` | FIXED_REAL | The invented three stars are gone from the rows already in the table, and a real rating is now recordable. READ SIDE FIRST, in readClientClassification (route.ts) — the single choke point: I verified all four render sites are fed only from it (clients/page.tsx card, [id] header, [id] «تقييم التعامل» row incl. "★".repe… |
| `ITEM-2` | FIXED_REAL | Confirmed the regression against HEAD~1 by `git show`: the button was `{isSupabaseMode && (` inside a `{!loaded && (` banner, so in a browser production build it rendered in BOTH non-loaded states; round 3 narrowed it to `{blocked === "read-failed" && (`, leaving the no-row lawyer with no in-page re-read. I did BOTH h… |
| `ITEM-3` | FIXED_REAL | Re-verified the reviewer's claim myself and it is right in substance but too strong in wording, which changed what the honest copy had to be. Verified: the tree's only insert into lawyer_profiles is onboarding/account-type/route.ts:222 via a DYNAMIC `spec.table` (I grepped `\.from\(<identifier>\)` across all of src — … |
| `ITEM-4` | FIXED_REAL | Both clauses were false and are gone. The bullet now reads: «`load()` in LawyerProfileEditPage (dashboard/lawyer/profile/edit) leaves the form unpopulated — and Save disabled — for ANY null `roleProfile`, which is what stops a blank form from overwriting a real licence number. The marker does not touch that gate; it o… |
| `ITEM 5` | MADE_HONEST | src/app/dashboard/lawyer/page.tsx ~:844-862 — the «Q / C / D» REMOVED note. Round 3's <kbd> deletion stands untouched; only the note's two false assertions were corrected. (1) «The only window-level listeners in the tree are Escape (modals) and Ctrl+K (GlobalSearch)» was replaced with the RULE rather than a file list,… |

---

## ما سُجِّل ولم يُصلَح

> **دَين المرحلة ١ عاش في سجلّات سير العمل ومات هناك.** هذا الجدول موجود لكي
> لا يتكرّر ذلك: كل ما رآه وكيل ولم يُصلحه، بالمصدر الذي رآه فيه.

**٩٠ بنداً.**

| المصدر | الملف | الملاحظة |
|---|---|---|
| مراجعة الـ٦٩ · cases | `src/constants/lawyerCasesData.ts` | The «مستوى الأهمية» override looks still live despite the مرتفعة row (AddCaseModal.tsx:69) being recorded as fixed. AddCaseModal collects a priority and writes it — `metadata: { court, priority, assignee }` (src/app/dashboard/lawyer/_components/AddCaseModal.tsx:88, with the «حرجة/عاجلة/طبيعية» buttons at :186-188) — but `workflowToCase` … |
| مراجعة الـ٦٩ · cases | `src/constants/lawyerCasesData.ts` | `metadata.assignee` has the same shape of loss: AddCaseModal writes it at :88, but `workflowToCase` returns `team: []` (:60) and reads no assignee, so the value is stored under a key nothing renders. This is the second half of the same مرتفعة row and is one line from the priority issue above. |
| مراجعة الـ٦٩ · cases | `src/app/dashboard/lawyer/cases/[id]/page.tsx` | `mapStatus` has no branch for the `suspended` CaseStatus (:146-159 returns only active/pending/closed), yet STATUS_CONFIG still carries a `suspended: { label: "معلقة" … }` entry (:78). Dead config rather than a defect, but it mirrors the reason the «معلقة» Kanban column was removed from cases/page.tsx (comment at :194-200: no backend sta… |
| مراجعة الـ٦٩ · cases | `src/app/dashboard/business/cases/page.tsx` | The business case list carries the identical `nextDateSort` time-filter clauses at :93-95 that make cases-8 dead on the lawyer side. Outside this audit's scope, but whoever removes the three dead controls on the lawyer page should know the same three exist here. |
| مراجعة الـ٦٩ · profile | `src/components/dashboard/LawyerProfileForms.tsx` | ShareModal (134-264) survives as an orphaned export nobody imports, and it still contains the hardcoded 404 share URL nezamy.sa/share/lawyer/xK9mP3q at 235/238 and the two handler-less share buttons at 248-257. AchievementsTab (425) and ReviewsTab (543) are orphaned the same way. Harmless today, but a single future `import { ShareModal }… |
| مراجعة الـ٦٩ · profile | `src/components/marketplace/FeeSplitModal.tsx` | The entire 142-line file is now unreachable — no importer anywhere in src/. It still imports the `CollabRequest` type from MyMarketplaceDashboardData.ts, which is the sole reason that types-only module explains at line 30 that it cannot delete the interface. Deleting FeeSplitModal.tsx would free both. |
| مراجعة الـ٦٩ · profile | `src/components/dashboard/LawyerProfileForms.tsx` | The اللغات card (358-377) changed shape rather than being fixed. The original auditor declined to report it as a cosmetic always-full bar on «العربية»; now EMPTY_PROFILE.languages is [] (page.tsx:106) and nothing ever sets it, so the card renders a heading over an empty list — a fourth permanently-empty card alongside the three in profil… |
| مراجعة الـ٦٩ · profile | `src/app/dashboard/lawyer/_data/analytics.ts` | SHARE_TOGGLE_DEFAULTS (line 123) is still exported although its only consumer, the profile page's shareToggles state, was removed. Dead constant in a file the pass deliberately left intact. |
| مراجعة الـ٦٩ · profile | `src/app/dashboard/lawyer/page.tsx` | The «نشر في السوق» button at line 666 still links a lawyer from the dashboard landing page to /dashboard/lawyer/marketplace. It now lands on the honest «سوق المهنيين — غير متاح حالياً» gate rather than fabricated data, so it is no longer harmful, but it is a prominent gold-bordered toolbar button whose only destination is a coming-soon p… |
| مراجعة الـ٦٩ · tasks | `src/app/dashboard/lawyer/hearings/page.tsx` | Line 305: the event card prints the raw ISO date string straight from the HTML date input — `<Clock size={11} />{ev.date}{ev.time?` — ${ev.time}`:""}` — so an Arabic RTL diary shows «2026-09-15» beside an Arabic-Indic Hijri strip. Every other date on the page is localized. |
| مراجعة الـ٦٩ · tasks | `src/app/dashboard/lawyer/_components/AddHearingModal.tsx` | The (now honest) success copy at line 171 points the lawyer to «بطاقة الموعد» to add the appointment to Google manually — and that is exactly the control that drops the entered time (tasks-5). The two compound: the modal directs the lawyer to a button that produces an all-day event. |
| مراجعة الـ٦٩ · tasks | `src/app/api/v1/lawyer/activity/route.ts` | Asymmetric error handling in the same Promise.all: eventRes.error and auditRes.error are both logged (142-148), but monthRes/activeRes/completedRes/totalRes errors are never read or logged at all, so a failed head-count is invisible to the operator as well as to the lawyer. |
| مراجعة الـ٦٩ · tasks | `src/app/api/v1/lawyer/tasks/route.ts` | The GET doc-comment at 52-64 self-reports an unfixed silent cap: `.limit(50)` with a bare-array body and no `total`, listing the three call sites that must change in the same commit. It is documented, not closed — outside my numbered findings but the same class Phase 1 was clearing. |
| مراجعة الـ٦٩ · tasks | `src/app/dashboard/lawyer/hearings/page.tsx` | `googleCalUrl` builds `dates=YYYYMMDD/YYYYMMDD` with identical start and end. Google's all-day TEMPLATE convention is end = start + 1 day; a same-day pair is accepted but is not the documented shape, so this is worth checking alongside the tasks-5 time fix rather than after it. |
| مراجعة الـ٦٩ · overview | `src/app/dashboard/lawyer/_data/mockData.ts` | Orphaned fabricated constants left behind by the fix wave: `UPCOMING_DEADLINES` (mockData.ts:111 — «موعد الطعن بالاستئناف» ٨ أبريل / daysLeft 2, etc.) is defined and imported by no file, and the whole of _data/analytics.ts (WIN, WIN_RATE_PCT, PRO_SCORES, AI_TOOLS, ACTIVITY_DATA, FINANCIAL_DATA, WORK_HOURS, METAL_TIERS) now has no importe… |
| مراجعة الـ٦٩ · contracts | `src/app/ai/templates/page.tsx` | Same invented-statistic defect as contracts-4, different area, still live: line 242 renders `{tmpl.uses.toLocaleString()}` and line 300 renders `{selected.uses.toLocaleString()} استخدام`, fed by literals in src/app/ai/templates/_data.tsx (uses: 1247, 2891, 1654, 987, 312, 543, 678, 1103, 2134). Outside my surface — flagging so it is not … |
| مراجعة الـ٦٩ · contracts | `src/app/dashboard/lawyer/contracts/page.tsx` | Line 540 renders the truncation cap as `{WORKSPACE_PAGE_LIMIT}` → Latin «200» inside an otherwise Arabic-Indic UI. src/lib/services/listRead.ts::truncationNoticeAr deliberately uses toLocaleString("ar-SA") for exactly this reason; this hand-rolled banner does not go through it. Cosmetic, not a false statement. |
| مراجعة الـ٦٩ · contracts | `src/app/dashboard/lawyer/documents/_taxonomy.ts` | `uses` and `fields` on the Template interface (lines 38-39) and their values on all eight TEMPLATES rows (150-157) are now dead fields — nothing renders them after the templates-tab rewrite. MOCK_DOCS (126-135) is still an unimported dead export, as the original audit noted; the MOCK_DOCS hits elsewhere in the repo are unrelated local co… |
| مراجعة الـ٦٩ · finance | `src/app/api/v1/lawyer/finance/route.ts` | NEW, narrower than finance-1: `invoicesTotal` is `null` whenever ANY payments chunk answers without a count (route.ts:242-244). In that branch `invoicesTruncated` is false, so the truncation banner does not render AND the P&L header falls back to its unconditional «جميع الفواتير المسجلة على حسابك — بدون تحديد فترة» (page.tsx:1060) — a cl… |
| مراجعة الـ٦٩ · finance | `src/app/api/v1/lawyer/finance/route.ts` | Documented and deliberately unfixed by Phase 1, worth carrying forward: «the most recent MAX_ROWS» is only exact while no SINGLE 150-request chunk exceeds its own 500 cap — a lawyer with >500 invoices inside one chunk loses that chunk's 501st row even if it is newer than a row another chunk kept (route.ts:207-217). Wrong-rows, not unrepo… |
| مراجعة الـ٦٩ · finance | `src/app/dashboard/lawyer/finance/page.tsx` | The wallet-tab truncation notice (page.tsx:984-990) and the wallet list render inside `activeTab === "expenses"` only, while the invoice banner is page-level. That is deliberate per the comment, and correct — noting only that the two notices use different wording («يُعرض أحدث» vs «هذه الشاشة تقرأ أحدث») for the same idea. |
| مراجعة الـ٦٩ · clients | `src/app/dashboard/lawyer/clients/page.tsx` | Line 40: `isArchived` treats `activeRequests === 0` as archived, so a brand-new manually-added client whose one row is not in ACTIVE_STATUSES lands straight in «غير نشطين / أرشيف» and disappears from the default «الموكلون النشطون» tab the lawyer is looking at after adding them. `onAdd` (:92) prepends to `clients`, but the archive filter … |
| مراجعة الـ٦٩ · clients | `src/app/dashboard/lawyer/clients/page.tsx` | Line 166: the red «أتعاب غير مسددة» banner is gated on `badClients > 0 && totalUnpaid > 0`, and `totalUnpaid` sums only clients with a fee agreement on record — correct — but the «عرض فقط» button next to it sets `activeFlags` to `late_pay` alone (:173), which hides the `bad`-flagged clients the same sentence just counted. |
| مراجعة الـ٦٩ · clients | `src/app/api/v1/lawyer/clients/route.ts` | Line 181/210-211: `lastActivity` is `created_at`, never `updated_at`, so a client with months of activity on an old request keeps the original creation date as their newest timestamp. Relevant if the fix stage decides to sort or relabel on it (clients-2). |
| مراجعة الـ٦٩ · clients | `src/app/dashboard/lawyer/consultations/page.tsx` | Line 173: the booking POST hardcodes `requester: { name: clientName \|\| "عميل", role: "individual", tier: "free" }`, so `workflowToConsultation`'s `clientType` (:89) can never be "company" for a lawyer-booked consultation — the Buildings badge on the card (:423) is unreachable from this screen. |
| جولة ١ · cases | `src/app/dashboard/lawyer/cases/[id]/page.tsx` | The assigned lawyer still has no NAME on this screen — the team tab shows «الاسم غير متاح» and the header shows «مُسندة إلى محامٍ». Making it real needs a profiles join for `assigned_to` in api/v1/service-requests/[id]/route.ts:126 (outside my files); once that route returns an assignee name, both sites here take it with the `nameKnown` … |
| جولة ١ · cases | `src/constants/lawyerCasesData.ts` | Rows carrying no `metadata.court` — every request that reaches a lawyer from a client through the platform — still fall back to `degree: "primary"` and badge «ابتدائي». `Case.degree` is a required `CourtDegree` in src/app/dashboard/lawyer/cases/_types.ts, which I do not own, so there is no "unknown" value to return. Fixing it needs eithe… |
| جولة ١ · cases | `src/app/dashboard/lawyer/cases/page.tsx` | Pre-existing lint warnings I deliberately left (verified against HEAD as not caused by my edit): ten unused @phosphor-icons imports (ChartBar, CalendarBlank, SquaresFour, Dot, CheckCircle, TrendUp, Star, Handshake, UsersThree, User) and `pConf` assigned-but-unused at :381. Also pre-existing: ListView/KanbanView/ArchiveView are declared i… |
| جولة ١ · tasks | `src/app/dashboard/lawyer/hearings/page.tsx` | One definition, two numbers: the «هذا الأسبوع» heading bucket is `dateSort<=5` (groupByDate, ~:259) while the `week` time-filter is `dateSort>=0&&dateSort<=7` (~:704). A hearing 6-7 days out is inside the «هذا الأسبوع» filter but printed under a «هذا الشهر» heading. Left alone deliberately — no finding asks for it and aligning them chang… |
| جولة ١ · tasks | `src/app/dashboard/lawyer/hearings/page.tsx` | `LINKED_TASKS` (~:230) is a `Record` that is never populated, so the whole linked-tasks UI is unreachable dead code: the «N مهام» Link, the toggle list, `toggleLinkedTask`, `pendingCount`, and the `CalEvent.caseId` field itself now that the mapper no longer sets it. Dead, not dishonest — nothing of it renders — so it was left for a scope… |
| جولة ١ · tasks | `src/app/dashboard/lawyer/hearings/page.tsx` | Pre-existing, not a regression: in calendar view with الأرشيف selected, `CalendarView`'s own internal `ev.dateSort>=0 && !ev.done` guard excludes every archived row, so the month grid draws blank with no explanation — and the new empty-state card does not fire, because `filtered.length>0`. The calendar cannot show the archive at all; eit… |
| جولة ١ · tasks | `src/app/dashboard/lawyer/activity/page.tsx` | `ChatCircle` is imported (line 7) and never used — the one eslint warning on my four files (0 errors). It predates this pass and is not on a line I touched, so removing it would be unexplained diff noise while other agents are editing in parallel. |
| جولة ١ · clients | `src/app/dashboard/lawyer/consultations/page.tsx` | Line 561 (`const upcoming = consults.filter(c => c.status === "upcoming")`) applies no sort, so the «الاستشارة القادمة» panel at :660-684 renders upcoming[0] — an arbitrary upcoming consultation, not the soonest one. Real, and in a file I own, but not in my assigned findings and not a one-liner: ordering needs date+time parsing where `me… |
| جولة ١ · clients | `src/app/dashboard/lawyer/clients/page.tsx` | Pre-existing, untouched: line 64 `s.has(f) ? s.delete(f) : s.add(f)` in toggleFlag trips @typescript-eslint/no-unused-expressions, and line 158's `useEffect(() => { loadClients(); }, [loadClients])` trips react-hooks/set-state-in-effect. Both are warnings, both predate this pass, and consultations/page.tsx has the same effect warning at … |
| جولة ١ · overview | `src/app/dashboard/lawyer/consultations/page.tsx` | Not my file — the other half of overview-9. To restore the «استشارة جديدة» deep link, read a `book` search param and seed `showBooking` from it: the state (line 500) and `<BookingModal … lawyerUserId={user.userId} />` (line 568) already exist, so it is `useSearchParams()` + `useState(sp.get("book") === "1")`. No column, endpoint or migra… |
| جولة ١ · overview | `src/app/dashboard/lawyer/page.tsx` | Left deliberately, not a defect: `item.type === "ai"` still guards an icon weight (line ~1147) and a gold text colour (line ~1150) in the activity row. Those branches are unreachable — `badgeToActivityType()` returns only "success", "warning" or "info" — but they are style branches, not claims the lawyer can read, and its declared return… |
| جولة ١ · overview | `src/lib/services/lawyerDashboardService.ts` | NEEDS_BACKEND, recorded because I deleted the surface that promised it: nothing anywhere records AI-tool usage as an event, so the lawyer's «سجل النشاط» can never show it. Reinstating an AI-activity view needs a real event source (an AI-usage row, or a `request_events` token the AI tools actually write) before any tab over it would be ho… |
| جولة ١ · overview | `src/app/ai/draft/page.tsx` | The decision my overview-3 fix leaves open, for whoever owns pricing: /ai/draft and /ai/contracts now have no plan gate on any surface, which matches reality — they have no gate in the route, the page or the proxy either. If a plan is ever meant to gate them, the gate belongs in those pages (or the proxy), not in a padlock drawn on one d… |
| جولة ١ · misc | `src/components/dashboard/LawyerProfileForms.tsx` | ShareModal, AchievementsTab, ReviewsTab, SpotlightCard and RingScore are exported but imported nowhere — OverviewTab is the file's only live export (profile/page.tsx:15 is its sole importer; the SpotlightCards in ClientMediaPanel.tsx, lawyers/browse/page.tsx and PricingCards.tsx are separate local definitions). ShareModal is the notable … |
| جولة ١ · مؤجَّل · cases | `cases-11` | The fix is in src/app/api/v1/service-requests/[id]/route.ts, which is outside my file list. Its GET handler at :156 collapses `if (error \|\| !serviceRequest)` into one HTTP 404, and supabase-js returns `{data: null, error}` rather than throwing, so PGRST116 (genuine absence), a statement timeout (57014), an RLS error and a gateway 503 a… |
| جولة ١ · مؤجَّل · clients | `clients-9` | Not a missing column — a file-ownership boundary. Three files outside my list still carry it: src/app/dashboard/lawyer/clients/[id]/page.tsx:329 (`hasFees` needs `(client.totalFees ?? 0) > 0`), src/app/api/v1/lawyer/clients/route.ts:46 (the read-side repair for already-persisted zero rows — `money()` should use `v > 0`, not `v >= 0`), an… |
| جولة ٢ · request-detail | `src/app/api/v1/service-requests/[id]/route.ts` | Line 899, the generic PATCH path: `return NextResponse.json({ error: error.message }, { status: 500 })` still ships a raw English PostgREST message. LEFT DELIBERATELY — I traced all three consumers of this branch (clientWorkflowRepository discards the body entirely; lawyer/cases/page.tsx and lawyer/contracts/page.tsx both render fixed Ar… |
| جولة ٢ · request-detail | `src/app/ai/orders/[id]/_components/OrderEditPanel.tsx` | The comment at :62-65 justifying `setErr(body.error)` says «this handler answers in Arabic on every refusal it owns», which was false for the 500 until this round. It is true now, but the comment cites the gate and the validator only. Worth one line naming the two 500s (`read_failed`, `write_failed`) so the next reader does not have to r… |
| جولة ٢ · request-detail | `src/app/ai/orders/[id]/_components/OrderActions.tsx` | The doc comment at :68-72 pins line numbers in the route that this change moved (`route.ts:138`, `:205-208`, `:299`) and states «a 500 returns the raw PostgREST message verbatim», which is no longer true of the edit path. The behaviour it describes (never echo the body, key on the status code) is still correct and needs no code change — … |
| جولة ٢ · client-fees | `src/app/api/v1/lawyer/clients/route.ts` | DEFERRED — a real zero-fee agreement (pro bono) cannot be represented. It needs a per-client fee record with an explicit zero-agreement flag: either a dedicated column/table or a distinct metadata marker written at insert AND a rewrite of the «مسدّدة بالكامل» subtitle at clients/[id]/page.tsx:477 (which is what a 0-total agreement would … |
| جولة ٢ · client-fees | `src/components/dashboard/lawyer/AddClientModal.tsx` | Pre-existing eslint warning at :58 — `s.has(f) ? s.delete(f) : s.add(f);` in toggleFlag trips @typescript-eslint/no-unused-expressions. Untouched by this pass and left as-is rather than cleaned up as unexplained diff noise. |
| جولة ٢ · client-fees | `src/components/dashboard/lawyer/AddClientModal.tsx` | Pre-existing eslint warning at :99 — `catch (e: any)` trips @typescript-eslint/no-explicit-any. Untouched by this pass. `npx eslint` on both files I own reports 0 errors and only these 2 pre-existing warnings; no new warnings introduced. |
| جولة ٢ · client-fees | `src/components/dashboard/lawyer/AddClientModal.tsx` | The new hint copy asserts «لا تظهر بطاقة الأتعاب … إلا إذا كان الإجمالي أكبر من صفر», which depends on three render sites outside this group's file boundary (clients/[id]/page.tsx:472,:627; ClientDrawer.tsx:269; clients/page.tsx:487). True at all three today. If any of them is later changed to render a 0-total fee surface, this modal cop… |
| جولة ٢ · profile | `src\app\dashboard\lawyer\profile\page.tsx` | PRE-EXISTING eslint warning at :227 (`react-hooks/set-state-in-effect` on `useEffect(() => { load(); }, [load])`, reported at :203 before my comment additions shifted it). Untouched line, left as-is rather than cleaned up as unexplained diff noise. It is the only warning on my three files; the two `@typescript-eslint/no-explicit-any` war… |
| جولة ٢ · profile | `src\app\dashboard\lawyer\profile\edit\page.tsx` | Can now consume the new `roleProfileReadFailed` marker. Its comment at :102 explicitly says «this also covers a read that failed rather than a row that is genuinely absent — and we cannot tell which from here»; that is no longer true. Its message «تعذّر قراءة بيانات ملفك المهني» is stated for both states, so a lawyer with no row is told … |
| جولة ٢ · profile | `src\app\api\v1\profile\route.ts` | The `subscription` read (now ~:190-198) still drops its `error` on a `.maybeSingle()`, so a failed read becomes "no active subscription". Left deliberately to keep the diff tight: no consumer reads the field — profile/page.tsx, edit/page.tsx and onboarding/page.tsx all type the envelope without it — so no false statement currently reache… |
| جولة ٢ · activity-consults | `src/app/dashboard/lawyer/page.tsx` | RESTORE the deep link, and update the comment that explains its removal. At ~:834 the «استشارة جديدة» quick action reads href: "/dashboard/lawyer/consultations" and carries a comment stating the param was removed because that page 'calls neither useSearchParams nor a searchParams prop' and that honouring it 'is a three-line change in tha… |
| جولة ٢ · activity-consults | `src/app/dashboard/lawyer/activity/page.tsx` | PRE-EXISTING eslint warning on a line I did not touch, left deliberately rather than cleaned up as unexplained diff noise: :7 'ChatCircle' is defined but never used (@typescript-eslint/no-unused-vars). It is an unused @phosphor-icons/react import; safe to drop in a tidy-up pass. |
| جولة ٢ · activity-consults | `src/app/dashboard/lawyer/consultations/page.tsx` | TWO PRE-EXISTING eslint warnings on lines I did not touch, left deliberately: :309 'Unexpected any' (@typescript-eslint/no-explicit-any) on the `catch (e: any)` in BookingModal's handleConfirm — narrowing to `unknown` with a typeof/instanceof guard before reading e?.message would clear it; and :697 react-hooks/set-state-in-effect on the … |
| جولة ٢ · hearings | `src/app/dashboard/lawyer/hearings/page.tsx` | `CalEvent.workflow` is the same class of dead code as LINKED_TASKS and I left it deliberately, since it was outside the brief and removing it would also delete an honest notice. `workflowToHearing` hardcodes `workflow: undefined` (:158) and CalEvent objects are created nowhere else, so `ev.workflow` is always undefined: the expander togg… |
| جولة ٢ · hearings | `src/app/dashboard/lawyer/hearings/page.tsx` | `CalEvent.deadlineDaysLeft` is never set by `workflowToHearing` either, so the days-left badge on deadline cards (:416-420, «⚠ اليوم» / «N يوم» with its red/amber escalation) never renders. It is derivable from `dateSort` with no schema change, so this one can be made real rather than removed — but it is a behaviour decision, not a rende… |
| جولة ٢ · hearings | `src/app/dashboard/lawyer/hearings/page.tsx` | The stale-`dateSort` problem I avoided on the card date line still applies to the two remaining `getEventDate` callers: the calendar grid's day placement (:563) and `googleCalUrl`'s start stamp (:271). Both re-read `new Date()` at render against an offset computed at fetch, so a tab open across local midnight places a dot on the wrong gr… |
| جولة ٢ · مؤجَّل · request-detail | `get-sublist-false-empty` | Escalating the whole GET to 500 because a sub-list failed would be a regression, not a fix: `request_events` and `attachments` carry their own RLS policies, so a caller who can read the request but not its timeline would lose a working screen entirely. The honest fix is an envelope field — e.g. `{ data: {...}, unreadable: ["events", "att… |
| جولة ٢ · مؤجَّل · profile | `DEFERRED-provision-missing-role-row` | PATCH /api/v1/profile uses `.update().eq(user_id)`, which cannot create a missing row. Making it real needs an upsert plus an INSERT path for the user's own RLS context — 20260603_phase1_001_profiles.sql:127-132 grants only SELECT policies, so there is no evidence an owner INSERT policy exists, and adding one is a migration (banned). The… |
| جولة ٣ · overview-link | `src/app/dashboard/lawyer/_data/mockData.ts + src/app/ai/secretary/page.tsx` | STRONGEST follow-up — this page still ships a link to a screen its own comment calls fiction. The «AI Secretary Notice — REMOVED» note in dashboard/lawyer/page.tsx says /ai/secretary «is built entirely on literals, down to a greeting hardcoded to a different person's name», yet AI_QUICK (mockData.ts:108) still renders a tile «السكرتير ال… |
| جولة ٣ · overview-link | `src/app/dashboard/lawyer/consultations/page.tsx` | NON-VERIFICATION, stated as such rather than as a defect. That page calls useSearchParams() bare, with no Suspense boundary. I could not empirically confirm the build accepts it or that the param resolves before useState initialises, because constraint 6 bars next build and a dev server would compile a tree other agents are actively writ… |
| جولة ٣ · overview-link | `src/app/dashboard/lawyer/secondment/page.tsx` | Unreachable route. After my comment rewrite, nothing in src/ references /dashboard/lawyer/secondment at all — the stale comment I fixed was its only mention, and the page renders DashboardComingSoon. Either link it from the lawyer sidebar or delete the route; a coming-soon page no navigation reaches is dead weight that reads as a shipped… |
| جولة ٣ · overview-link | `src/app/dashboard/lawyer/_data/mockData.ts` | Minor dead data, noted while fixing the unreachable "ai" branches. ACTIVITY_TYPE_CONFIG carries `ai` and `urgent` entries that badgeToActivityType() can never produce, so those two icon/colour configs are unreachable from the lawyer overview. Harmless (the map lookup falls back to "info"), but it is what made the dead branches in my file… |
| جولة ٣ · hearings-notice | `src/app/dashboard/lawyer/hearings/page.tsx` | Dead UI, left deliberately: the deadline countdown chip at ~:493 is gated on `ev.deadlineDaysLeft !== undefined`, and nothing in the repo ever sets deadlineDaysLeft (grep for `deadlineDaysLeft:` returns nothing). It therefore never renders and asserts nothing to anyone today, so it is not a lie — unlike LINKED_TASKS it is three lines, no… |
| جولة ٣ · hearings-notice | `src/app/dashboard/lawyer/page.tsx` | Not touched (outside my file list) and not a defect today, but the same class one layer down: daysUntilDate/dayLabel compute «اليوم»/«غداً» from `new Date()` at render, which is correct at each render but has no midnight anchor, so on a dashboard left open overnight those labels stay wrong until something else triggers a re-render. The h… |
| جولة ٣ · hearings-notice | `src/app/dashboard/lawyer/hearings/page.tsx` | Pre-existing, unreachable via the UI, and now at least self-consistent: an out-of-range stored day such as '2026-02-30' is rolled over by the JS Date constructor to 2 March. That was already true of the old daysFromToday and formatEventDateAr, and because the card and the grid now share the single eventDayDate parse they roll it over ide… |
| جولة ٣ · profile-edit | `src/app/api/v1/profile/route.ts` | MY CHANGE FALSIFIED A COMMENT HERE — same defect class as the one this round opened for, one layer up, and in the file the next reviewer of `roleProfileReadFailed` will read first. The GET docstring's "WHY A MARKER RATHER THAN A 500" section says: «src/app/dashboard/lawyer/profile/edit/page.tsx:102 already treats `roleProfile == null` as… |
| جولة ٣ · profile-edit | `src/app/dashboard/lawyer/profile/edit/page.tsx` | PRE-EXISTING eslint warning on a line I did not touch: `react-hooks/set-state-in-effect` at `useEffect(() => { load(); }, [load]);`. 0 errors. |
| جولة ٣ · profile-edit | `src/app/dashboard/lawyer/profile/page.tsx` | PRE-EXISTING eslint warning on a line I did not touch: `react-hooks/set-state-in-effect` at `useEffect(() => { load(); }, [load]);`. 0 errors. |
| جولة ٣ · client-fees-2 | `D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website\src\components\dashboard\lawyer\AddClientModal.tsx` | PRE-EXISTING eslint warning, line 104, untouched: @typescript-eslint/no-unused-expressions on toggleFlag's `s.has(f) ? s.delete(f) : s.add(f)` expression-statement ternary. Not fixed per instruction. |
| جولة ٣ · client-fees-2 | `D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website\src\components\dashboard\lawyer\AddClientModal.tsx` | PRE-EXISTING eslint warning, line 162, untouched: @typescript-eslint/no-explicit-any on `catch (e: any)`. Not fixed per instruction. These two are the only eslint output for my files; route.ts is clean and I introduced no new warnings. |
| جولة ٣ · client-fees-2 | `D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website\src\components\dashboard\lawyer\ClientDrawer.tsx` | NOT FIXED, out of scope (two of the three render sites are outside my file list). An advance GREATER than the total is accepted everywhere — both figures are the lawyer's own and are stored, so it is not a silent discard, but it renders wrong: ClientDrawer.tsx:289 sets the bar width to `(paid/total)*100`% with no clamp, giving a bar past… |
| جولة ٣ · client-fees-2 | `D:\DEV\projects\SITE MAPS NZAMY (1)\SITE MAPS NZAMY\nzamy-website\src\app\api\v1\lawyer\clients\route.ts` | KNOWN ASYMMETRY, left deliberately and documented in the POST guard comment. `flags` and `clientType` are still filter-on-read (KNOWN_FLAGS) rather than reject-on-write, unlike the fee figures and rating which now reject with a 400. Reasoning: the KNOWN_FLAGS filter is documented defence against arbitrary strings from any source, and dro… |
| جولة ٣ · مؤجَّل · profile-edit | `deferral-let-a-lawyer-create-his-own-role-row` | The old ground — «no owner INSERT policy exists, and adding one is a migration» — is FALSE: supabase/migrations/20260614_auto_create_role_profiles.sql:195-206 creates policy "users insert own lawyer profile" on public.lawyer_profiles for insert with check (user_id = auth.uid()). Verified by reading the file. The REAL ground: nobody in a … |
| جولة ٤ · clients-rating | `src/components/dashboard/lawyer/AddClientModal.tsx` | IDENTICAL DEFECT SHAPE to the rating, deliberately not fixed (out of scope). `const [type, setType] = useState<"individual"\|"company">("individual")` is sent unconditionally in the POST body, and the route stores it with `if (type) metadata.clientType = type`. So every manually-added client carries entity type «فرد» whether or not the l… |
| جولة ٤ · clients-rating | `src/app/api/v1/lawyer/clients/route.ts` | Pre-existing eslint warnings only, left alone as instructed: AddClientModal.tsx:124 `no-unused-expressions` (the `s.has(f) ? s.delete(f) : s.add(f)` ternary in toggleFlag) and :191 `no-explicit-any` (`catch (e: any)`). Both predate this pass; my two files report 0 errors. clients/page.tsx:158 and ClientDrawer.tsx:197 also carry pre-exist… |
| جولة ٤ · clients-rating | `src/app/api/v1/admin/erp/route.ts` | Noting so it is not rediscovered as new — NOT part of this pass and a different table and entity (law firms, not lawyer clients). Line 73: `const rating = ratingVal !== undefined ? Number(ratingVal) : 4.5;` invents a 4.5-star default for any firm whose metadata carries no rating, and line 96 returns it to the admin ERP screen. Same class… |
| جولة ٤ · profile-copy | `src/app/dashboard/lawyer/profile/edit/page.tsx` | The DEFERRED block inside load() still records the one unverified prerequisite for ever building row creation from this page: does production's pg_policies hold tablename='lawyer_profiles', policyname='users insert own lawyer profile' (created by 20260614_auto_create_role_profiles.sql)? Nobody in a coding session can check that, and acco… |
| جولة ٤ · profile-copy | `src/app/api/v1/profile/route.ts` | The GET docstring still carries a live cross-file line anchor, `src/app/onboarding/page.tsx:846`, in the bullet above the one I rewrote. It will rot the same way item 4's did. Same class: convert to a symbol reference. I left it because it is not in this round's list and the claim itself is still true — only the number is fragile. |
| جولة ٤ · profile-copy | `src/app/dashboard/lawyer/profile/edit/page.tsx` | Pre-existing eslint warning (not an error), untouched by me: react-hooks/set-state-in-effect at `useEffect(() => { load(); }, [load])`, line ~271. |
| جولة ٤ · profile-copy | `src/app/dashboard/lawyer/profile/page.tsx` | Pre-existing eslint warning (not an error), untouched by me: react-hooks/set-state-in-effect at `useEffect(() => { load(); }, [load])`, line ~224. |
| جولة ٤ · profile-copy | `src/app/dashboard/lawyer/profile/page.tsx` | Product gap, out of scope and NOT fixed: a lawyer in the no-row state has no way to create his professional record, and after this round the copy correctly stops promising one. The only path that creates the row is the signup trigger. If this state ever occurs for a real lawyer, the remedy is an operator running a manual INSERT with the … |
| جولة ٤ (تكذيب) · clients-rating | `src/app/api/v1/lawyer/clients/route.ts` | An uncorroborated `rating: 3` is now accepted with a 200 and silently not stored, while every other unstorable figure in this handler gets an Arabic 400 ("Rejecting rather than quietly dropping is the whole point" — its own comment). The departure is deliberate and defensible: a 400 would hard-fail every pre-deploy tab, and the response … |
| جولة ٤ (تكذيب) · clients-rating | `src/components/dashboard/lawyer/AddClientModal.tsx` | Deploy-window cost, disclosed by the fixer and confirmed by me: a lawyer working in a tab still running the pre-deploy bundle who deliberately clicks the third star sends `rating: 3` with no `ratingChosen`, and that rating is dropped with nothing on screen saying so. Bounded to tabs open across the deploy — the new modal derives `ratingC… |
| جولة ٤ (تكذيب) · clients-rating | `src/app/api/v1/lawyer/clients/route.ts` | Cosmetic bookkeeping in the fixer's report, not in the code: followUps[1] files the two pre-existing eslint warnings under route.ts, but both are in AddClientModal.tsx (:124 no-unused-expressions, :191 no-explicit-any). I reproduced them there; route.ts itself reports clean. |
| جولة ٤ (تكذيب) · profile-copy | `src/app/api/v1/profile/route.ts` | ITEM-4's verification claim is over-stated, though the fix itself is real. It asserts "every one of those anchors still lands on its intended code"; two do not. `route.ts:230`, cited from edit/page.tsx:143 for "the API applies exactly what it is given (no merge, no partial semantics)", is a BLANK line — 231-232 is a comment about rejecti… |
| جولة ٤ (تكذيب) · profile-copy | `src/app/dashboard/lawyer/profile/page.tsx` | Consistency call for the owner, not a defect: «يرجى التواصل مع الدعم» remains this codebase's house phrase for exactly this class of operator-only dead end — src/app/api/v1/onboarding/account-type/route.ts:144-147 ships it three times (profile_missing, type_already_set, onboarding_already_completed). These two screens now describe the me… |
| جولة ٤ (تكذيب) · profile-copy | `src/app/dashboard/lawyer/profile/edit/page.tsx` | One state constructed and disposed of clean, recorded so it is visibly considered: `roleProfileReadFailed?: boolean` is optional, so an older route deploy would read `undefined !== true` → "no-row", printing «قرأنا حسابك بنجاح» over an actual read failure, now alongside this round's new manual-DB-intervention clause. Not a finding — rout… |
| جولة ٤ (تكذيب) · profile-copy | `src/app/dashboard/lawyer/profile/page.tsx` | The brief's rating instruction has no target in this group: `grep -n "★\|rating\|repeat(\|sort("` across all three in-scope files returns exactly one hit, a COMMENT at profile/page.tsx:39 describing the four hardcoded hero-stat literals («١٤٣ قضايا / ٤٫٨★ / ٧+ س خبرة / ٨٩ تقييمات») that an earlier round already deleted. No `"★".repeat(..… |
| جولة ٤ (تكذيب) · profile-copy | `src/app/dashboard/lawyer/profile/edit/page.tsx` | Scope answered explicitly, since it was asked. The fixer stayed in its three files: `git status` shows four other modified files (lawyer/clients/route.ts, dashboard/lawyer/page.tsx, AddClientModal.tsx, docs/audits/2026-08-27-lawyer-audit.md) but the audit-doc diff contains no profile-copy content, so those are parallel agents' work. No .… |

